# MEASUREMENTS — замеры проекта: число = команда + дата + результат

**Зачем.** Требование **ПР4** мастер-плана. Из ~650 утверждений прошлого аудита 40 опровергнуты,
треть подтвердилась «частично» — потому что число приходило **без способа перепроверки**.
Пример цены: «подпись-подпись 0/161 при кегле 12» жило в решениях, пока кто-то не пересчитал
вручную и не получил **14/161**.

**Правило.** Любое число, попадающее в план, отчёт или решение, обязано иметь здесь строку:
**команда** (воспроизводимая) · **дата** · **результат** · **где машина**. Число без команды —
не число, а мнение. Строки только на дозапись; устаревшую строку не стирают, а дополняют новой
датой.

**Среда замеров, если не указано иное:** Windows 10, системный `python` 3.11.9,
`C:\project\pid_pipeline_deploy`, ветка `dev`. ⚠ Часть чисел зависит от локального состояния
(20 uid в `storage/diagrams`, веса в `models/`) и **на чистом клоне не воспроизведётся** — такие
строки помечены 🖥.

---

## 1. Набор тестов и окружение

| # | что | команда | дата | результат |
|---|---|---|---|---|
| 1.1 | Сбор тестов | `python -m pytest --collect-only -q` | 2026-08-14 | **690 собрано, 3 файла-ошибки, сбор прерван → исполнено 0** |
| 1.2 | Кто именно не собирается | то же | 2026-08-14 | `tests/observability/test_stage_durations.py` (нет `fastapi`); `tests/ui/test_bundled_fonts.py`, `tests/ui/test_frame_crop_preview.py` (`ImportError: QKeyEvent … unknown location`) |
| 1.3 | Причина двух последних — **не** сломанный PySide6 | `python -m pytest tests/ui/test_bundled_fonts.py --collect-only -q` | 2026-08-14 | **14 собрано, зелено.** В одиночку файл исправен |
| 1.4 | Кто травит | `python -m pytest tests/test_stage7_graph_flow.py tests/ui/test_bundled_fonts.py --collect-only -q` | 2026-08-14 | **ImportError.** `test_stage7_graph_flow.py:163 _stub_qt()` подменяет `sys.modules["PySide6*"]` на уровне модуля |
| 1.5 | Полный прогон | `python -m pytest --continue-on-collection-errors -q` | 2026-08-14 | ⛔ **Интерпретатор умирает на ~60 %:** `Windows fatal exception: access violation`, `tests/test_seat_contract.py:251 _editor` (`img.save`). До падения ~20 `F`, ~11 `E` |
| 1.6 | Платформа Qt ни при чём | `QT_QPA_PLATFORM=offscreen python -m pytest --continue-on-collection-errors -q` | 2026-08-14 | **Падает там же.** Причина — межмодульное загрязнение процесса |
| 1.7 | Тот же файл в одиночку | `python -m pytest tests/test_seat_contract.py -q` | 2026-08-14 | **37 passed за 0.83 с** |
| 1.8 | Он же после соседа | `python -m pytest tests/test_contours_merge.py tests/test_seat_contract.py -q` | 2026-08-14 | 46 passed, 1 skipped, 1.66 с |
| 1.9 | Что стоит в системном питоне | `python -m pip list` | 2026-08-14 | pytest 9.1.1 ✓ · PySide6 6.11.1 ✓ · **fastapi ✗** · torch 2.10.0+cu128 · opencv-python 4.11.0.86 |
| 1.10 | Что стоит в `.venv311` | `.\.venv311\Scripts\python.exe -c "import PySide6"` / `-m pytest` | 2026-08-14 | PySide6 6.11.1 ✓ · **pytest ✗** · **fastapi ✗** |
| 1.11 | Конфигурация тестов | `Get-Content pytest.ini` | 2026-08-14 | **5 строк**, только `testpaths = tests`. Маркеров нет, `asyncio_mode` не задан → 4 `@pytest.mark.asyncio` в `test_refactoring.py:574-607` дают `PytestUnknownMarkWarning` |
| 1.12 | Конфиги линтеров | наличие `pyproject.toml`, `ruff.toml`, `mypy.ini`, `setup.cfg`, `.pre-commit-config.yaml` | 2026-08-14 | **ни одного**, при `ruff` и `mypy` в `requirements/dev.txt` |
| 1.13 | CI | наличие `.github/` | 2026-08-14 | **каталога нет** |

### 1а. После пункта 0.0 — среда собрана (2026-08-14, тот же день, дозапись к 1.1–1.10)

| # | что | команда | дата | результат |
|---|---|---|---|---|
| 1.14 | Что доставила установка | `python -m pip install -r requirements/api.txt -r requirements/ui.txt -r requirements/dev.txt` | 2026-08-14 | **40 пакетов, всё аддитивно**: fastapi 0.141.1, uvicorn 0.52.3, celery 5.6.3, alembic 1.19.1, asyncpg, psycopg2-binary, scikit-image 0.26.0, pytest-asyncio 1.4.0, ruff 0.16.3, mypy 2.3.0. **Ничего не понижено и не повышено**: numpy остался 1.26.4, torch 2.10.0+cu128, PySide6 6.11.1, opencv 4.11.0 |
| 1.15 | Проверка среды | `python -c "import fastapi, pytest, PySide6"` | 2026-08-14 | **зелено** (fastapi 0.141.1 · pytest 9.1.1 · PySide6 6.11.1) |
| 1.16 | Сбор после починки среды | `python -m pytest --collect-only -q` | 2026-08-14 | **703 собрано** (было 690), **2 ошибки** вместо 3 — ушла `test_stage_durations.py`. Остались `tests/ui/test_bundled_fonts.py` и `test_frame_crop_preview.py`, обе — отравление `_stub_qt` (Н1). Сбор по-прежнему `Interrupted` |
| 1.17 | Полный прогон после починки среды | `python -m pytest --continue-on-collection-errors -q` | 2026-08-14 | **исполнено 437 из 703** (62 %), из них **414 ✓, 14 F, 9 E**; дальше **тот же `access violation`** — `tests/test_seat_contract.py:251 _editor`. ⟹ среда на падение **не влияет**, подтверждён Н1б (межмодульное загрязнение). Было «~20 F, ~11 E» — разница за счёт установленного `fastapi`. **266 тестов не исполнялись ни разу** |
| 1.18 | `worker.txt` для тестов не нужен | `python -m pytest tests/test_worker/ -q` | 2026-08-14 | **16 passed за 0.40 с** без `ultralytics`/`sahi`/`smp` — задачи воркера импортируются внутри тестов, не на уровне модуля |
| 1.20 | Покрытие плана картами ТЗ | `coverage.py` в скрэтчпаде: строки таблицы `EXECUTION.md` против заголовков карт в `TZ …§10` + четырёх файлах `TZ_2026-08-14_stage*_cards.md` | 2026-08-14 | **200 строк · 6 сделано/снято · 83 с картой · 111 без карты.** Покрыты целиком: вся ближняя очередь (волны 0–3) и Этапы 1, 2, 5, 6. Без единой карты: Этапы 3 (15), 4 (9), 4б (1), 7 (7), 8 (9), 9 (7), 10 (16), 11 (8), 12 (6), 13 (10) = **88 строк**; плюс стенды СТ3–СТ10 (8), сквозные Д0–Д6/ПР2/ПР3/ПР6 (10), вне этапов ВН2/ВН3/ДН3 (3), хвосты Этапа 0 (2). ⚠ Метод считает по заголовкам: `КД7` (внутри карты 0.8) и `2.10` (решено исполнять в 13.4\*) числятся непокрытыми, хотя содержание у них есть |
| 1.21 | То же **после карт Этапа 3** | `coverage.py` (тот же) | 2026-08-14 | **98 с картой · 96 без карты** (было 83/111). Этап 3 ушёл из непокрытых целиком. Осталось: Этапы 4 (9), 4б (1), 7 (7), 8 (9), 9 (7), 10 (16), 11 (8), 12 (6), 13 (10) = **73 строки**; стенды СТ3–СТ10 (8), сквозные Д0–Д6/ПР2/ПР3/ПР6 (10), вне этапов (3), хвосты (2) |
| 1.22 | То же **после карт Этапов 4/4б и стендов** | `coverage.py` (тот же) | 2026-08-14 | **113 с картой · 81 без карты** (было 98/96). Ушли целиком: Этапы 4 и 4б, а также все строки-предусловия Этапа 0 и стендов `СТ3`/`СТ4`/`СТ10`/`КД7`. Осталось: Этапы 7 (7), 8 (9), 9 (7), 10 (16), 11 (8), 12 (6), 13 (10) = **63 строки**; сквозные Д0–Д6/ПР2/ПР3/ПР6 (10); хвосты `2.10`, `НД1`, `НД2`, `НД6`, `ВН2`, `ВН3`, `ДН3`, `СТ9` (8) |
| 1.23 | То же **после карт Этапов 7, 8, 9** | `coverage.py` (тот же) | 2026-08-14 | **136 с картой · 58 без карты** (было 113/81). Ушли целиком Этапы 7, 8 и 9. Осталось: Этапы 10 (16), 11 (8), 12 (6), 13 (10) = **40 строк** + `СТ9`; сквозные Д0–Д6/ПР2/ПР3/ПР6 (10) — **одной памяткой, не картами**; хвосты `2.10`, `НД1`, `НД2`, `НД6`, `ВН2`, `ВН3`, `ДН3` (7) |
| 1.24 | То же **после карт Этапа 10** | `coverage.py` (тот же) | 2026-08-14 | **152 с картой · 42 без карты** (было 136/58). Осталось: Этапы 11 (8), 12 (6), 13 (10) = **24 строки** + `СТ9`; сквозные Д0–Д6/ПР2/ПР3/ПР6 (10) — **памяткой**; хвосты `2.10`, `НД1`, `НД2`, `НД6`, `ВН2`, `ВН3`, `ДН3` (7) |
| 1.25 | ⭐ **Покрытие плана — полное** | `coverage.py` (тот же) | 2026-08-15 | **193 с картой · 0 без карты** (было 83/111 в начале). Все 14 этапов, стенды, сквозные требования и хвосты имеют ТЗ. Прирост за сессию: **83 → 193** |
| 1.19 | ⚠ `.venv311` — **не тестовая среда** | `.\.venv311\Scripts\python.exe -m pip list` | 2026-08-14 | **сборочная среда клиента**: pyinstaller 6.21.0 + hooks, PySide6, opencv, PyMuPDF, qasync. Ни torch, ни pytest, ни fastapi. «Одна среда» из 0.0 = **системный `python` 3.11.9**; `.venv311` не сливать — он нужен СТ11 |

## 2. Незакоммиченная работа (пункт 0.1)

| # | что | команда | дата | результат |
|---|---|---|---|---|
| 2.1 | Объём | `git diff --stat` | 2026-08-14 | **11 файлов, +313 / −115** (плюс 4 untracked) |
| 2.2 | Untracked | `git status --porcelain` | 2026-08-14 | `modules/canvas_to_fxml.py`, `modules/graph/core/contours_merge.py`, `tests/test_canvas_to_fxml.py`, `tests/test_contours_merge.py` |
| 2.3 | Тесты фичи | `python -m pytest tests/test_contours_merge.py tests/test_canvas_to_fxml.py tests/test_canvas_pipeline_golden.py -q` | 2026-08-14 | **41 passed, 1 skipped, 2.29 с** — фича готова и покрыта |
| 2.4 | Почему порядок коммитов не свободен | чтение `app/services/layout_dispatch.py:41-43` | 2026-08-14 | `contours_merge` импортируется **на уровне модуля** → коммит гейта раньше ядра уронит FastAPI на старте |
| 2.5 | Размеры новых модулей | подсчёт строк | 2026-08-14 | `contours_merge.py` 136 · `canvas_to_fxml.py` 416 · `test_contours_merge.py` 168 · `test_canvas_to_fxml.py` 163 |
| 2.6 | Те же тесты **после сборки среды (0.0)** | та же команда, что 2.3 | 2026-08-14 | **42 passed** — было «41 passed, 1 skipped». Пропуск ожил сам: его причиной была неполная среда, а не логика теста. Побочный эффект 0.0, замеренный при приёмке 0.1 |
| 2.7 | Приёмка модульного импорта | `python -c "import app.main"` | 2026-08-14 | ОК — проверяет ровно тот импорт `contours_merge` на уровне модуля (`layout_dispatch.py:41-43`), ради которого задан порядок коммитов |
| 2.8 | Пункт 0.1 закрыт | `git log --oneline -5` | 2026-08-14 | 4 коммита фичи: `8f99544` ядро → `6bd3c87` производитель+гейт → `4039d1a` экспорт → `0e08e29` `.gitignore`; отдельно `2e52af5` — доки 0.0. Не запушено |

## 3. Ветки и путь на прод

| # | что | команда | дата | результат |
|---|---|---|---|---|
| 3.1 | Дистанция до боя | `git rev-list --count deploy..dev` / `dev..deploy` | 2026-08-14 | **110 / 0** |
| 3.2 | Чего нет на `deploy` | `git ls-tree -r dev --name-only` против `deploy` | 2026-08-14 | **27 файлов только на `dev`**: весь `modules/graph/core/layout/` (13), `edit_avoid/checks/engine/smooth`, `seating.py`, `ports.py`, `pretransform.py`, `canvas_state.py`, `text_import.py`, `graph_access.py`, `canvas_input.py`, `worker/tasks/layout.py`, `app/services/{layout_dispatch,layout_policy}.py` |

## 4. Конвейер: таймауты и брокер (пункт 1.2 · Б10)

| # | что | команда | дата | результат |
|---|---|---|---|---|
| 4.1 | Все `time_limit` | `Select-String -Path worker\tasks\*.py -Pattern "time_limit"` | 2026-08-14 | detection **5400/5340** · ocr 3600/3300 · segmentation 3600/3540 · graph 1800/1740 · skeleton 1800/1740 · layout 1800/1500 · junction 1200/1140 · contours 600/540 · direction 600/540 · FXML 300/270 · recognize_boxes 300/280 |
| 4.2 | Глобальные лимиты и подтверждение | чтение `worker/celery_app.py:49-54` | 2026-08-14 | `task_time_limit=3600`, `soft=3300`, `task_acks_late=True`, `task_reject_on_worker_lost=True` |
| 4.3 | Настройка брокера | grep `broker_transport_options` по репозиторию | 2026-08-14 | **0 вхождений** → Redis `visibility_timeout` = 3600 по умолчанию, то есть **меньше** 5400 у детекции |
| 4.4 | Очереди | чтение `worker/celery_app.py:58-63` | 2026-08-14 | **четыре**: `default`, `gpu`, `ocr`, `sam2` (в плане названы три — `default` пропущен; важно для 3.14) |

## 5. Диспетчеризация (6.9 и «тупики» 1.13/1.14/1.16)

| # | что | команда | дата | результат |
|---|---|---|---|---|
| 5.1 | Сырые `send_task` | grep `send_task\|apply_async` по `*.py` | 2026-08-14 | 12 сырых в `app/api/` + 2 враппера (`db_helpers.py:136`, `dispatch.py:38`) + `apply_async` (`segmentation.py:100`) + авто-цепочка (`skeleton.py:768`) |
| 5.2 | Все ли они в async-контексте | `scratchpad/asyncchk.py` (поиск ближайшего `def` вверх) | 2026-08-14 | **ASYNC 12, sync 0** — то есть все 12 блокируют единственный event loop |
| 5.3 | Политик отказа | чтение `db_helpers.py:111-148`, `app/services/dispatch.py:19-48`, `app/api/graph.py:106-121` | 2026-08-14 | **три и они не взаимозаменяемы**: ERROR+commit · warning+None · откат в литерал `VALIDATED_MASKS`+503 |
| 5.4 | Почему третья — тупик (1.14) | чтение `app/api/graph.py:54-58` | 2026-08-14 | `allowed_statuses = (VALIDATED_JUNCTIONS, BUILT, ERROR)` — `VALIDATED_MASKS` там **нет** → 400 навсегда |

## 6. Корпус, фикстуры, веса

| # | что | команда | дата | результат |
|---|---|---|---|---|
| 6.1 | Фикстуры в git | `Get-ChildItem tests\fixtures -Recurse -File` | 2026-08-14 | **один файл**: `layout/synth_med.json`, 72.9 КБ |
| 6.2 🖥 | Локальный корпус | `Get-ChildItem storage\diagrams -Directory` | 2026-08-14 | **20 uid** |
| 6.3 🖥 | Три самых мелких `graph_validated.json` (кандидаты 0.8) | размеры файлов | 2026-08-14 | `d74eb9f1…` 52.6 КБ · `089feca2…` 57.0 КБ · `6e7144d5…` 91.6 КБ = **201 КБ**. Оценка плана «53+57+92 ≈ 200 КБ» подтверждена |
| 6.4 🖥 | Веса | сумма по подкаталогам `models/` | 2026-08-14 | ≈**3.2 ГБ**: ocr 1527 МБ · segmentation 761.6 · `.hf_cache` 422 · yolo 347.8 · junction_seg 116.5 · text_detect 72.6 · direction 9.8 · sam2 5.4 · ⛔ **junction_cnn 0 МБ (пусто)** при монтировании `docker-compose.yml:145` |

## 7. Дубли и константы

| # | что | команда | дата | результат |
|---|---|---|---|---|
| 7.1 | Копии `ArtifactDownloader` | grep `class .*Downloader` по `ui/` | 2026-08-14 | **6**: `base_graph_tab:661`, `junction_tab:27`, `pipe_tab:27`, `ocr_binding_tab:59`, `mask_validation_window:29`, `graph_validation_window:31`. Интерфейс идентичен, различаются **только списками артефактов** |
| 7.2 | Мёртвые окна | grep импортов `MaskValidationWindow\|GraphValidationWindow\|CVATWindow` | 2026-08-14 | внешних импортёров **нет**: только `ui/windows/__init__.py:2-4` и `tests/test_stage7_graph_flow.py`. `MainWindow` (`ui/main.py:34`) остаётся |
| 7.3 | Допуски в геометрии (7.2/7.3) | grep `ORTHO_TOL\|SIDE_EPS\|PIN_TOL\|DIAG_TOL` по `modules/` | 2026-08-14 | **четыре разные величины, совпадает только число 1.5**: `ORTHO_TOL=1.5` (`avoid_router:53`, `edit_avoid:71`) · `DIAG_TOL=1.5` (`edit_checks:36`) · `SIDE_EPS=1.5` (`avoid_router:52`, `edit_avoid:70`, **`_gate:28`**) · `ORTHO_TOL=1.0` (`_axes:17`, другой смысл) · `DIAG_TOL_DEG=20.0` (`_gate:26`) · `PIN_TOL=0.5` (два места) |
| 7.4 | Защёлка ресайза (1.6) | чтение `simple_graph_editor.py:274-278`, `:423-434` + grep вызовов | 2026-08-14 | **одно определение, четыре двери**: `simple:480`, `advanced:5499`, `simple:207-209` (без `_stop_resize`) и `simple_handlers:169` (со `_stop_resize` — безопасна) |
| 7.5 | Стенды и пробы | листинг `tools/` | 2026-08-14 | 6 стендов + **7 «проб»** (`autofix_ab_probe` 112, `drag_stress_probe` 241, `reseat_preview_probe` 78, `reseat_render_probe` 145, `corner_probe` 87, `edit_render_probe` 188, `legal_audit_probe` 86) — читать до написания СТ5/СТ8 |

## 8. Инфраструктура (compose, `/health`)

| # | что | команда | дата | результат |
|---|---|---|---|---|
| 8.1 | ⛔ Адрес 2.12 | чтение `docker-compose.yml:157-164`, `:204-211` | 2026-08-14 | по этим адресам — **закомментированный блок резервирования GPU** (`reservations.devices: driver: nvidia`), а не лимиты памяти. Раскомментировать на CPU-only = контейнеры не стартуют. Лимитов памяти в файле **нет вовсе** |
| 8.2 | Дефолтные пароли (3.2) | чтение `docker-compose.yml` | 2026-08-14 | **четыре** места: `:36` `DB_PASSWORD:-changeme`, `:78` (api), `:83` `CVAT_SUPERUSER_PASSWORD:-admin123`, `:173` (worker_ocr) |
| 8.3 | Healthcheck | чтение `docker-compose.yml:41-46`, `:61-66` | 2026-08-14 | есть у `postgres` и `redis`, **нет у обоих воркеров** |
| 8.4 | Redis | чтение `docker-compose.yml:56` | 2026-08-14 | `redis-server --appendonly yes` — ни `maxmemory`, ни `maxmemory-policy` |
| 8.5 | `/health` | чтение `app/main.py:57-78` | 2026-08-14 | **три дефекта**: 200 при `degraded` · блокирующий `control.ping(timeout=2)` в async-эндпоинте · ключ `redis` меряет **воркеров**, не Redis |
| 8.6 | Клиентская половина | чтение `ui/services/api_client.py:212` | 2026-08-14 | `status in ("healthy","degraded")` → `degraded` считается живым |

## 9. Ограничения и что НЕ замерено

- **Боевой сервер не трогался ни разу.** Все числа — с dev-машины. Времена стадий, RSS воркеров,
  реальный конкурентный доступ, поведение брокера под нагрузкой — **не замерены** (это СТ2 и 0.12).
- **БД не запрашивалась:** цена схемы (ДН1), дубли строк `processing_stages`, сироты CVAT — нет
  доступа к данным с этой машины (см. карту §10.15 ТЗ: у ДН1 есть скрытое предусловие).
- **Диффы `graph_edited*` (ДН2) не считались** — только подтверждено, что 20 файлов лежат в корне.
- **Числа фронтов не перепроверялись:** 45-46→4, 148 наложений из 225, 3205/3245, ×6 медиана —
  наследуют доказательную базу `VERIFY_part_front*.md`. ⚠ **Частично закрыто §10** для фронта 1.
- **Адреса ~170 строк плана** из 193 не сверялись с кодом; сверены те, что перечислены в §9 ТЗ.

---

> ⚠ **Поправка даты (дозапись 2026-08-15).** Разделы **§10–§15** и находки **Л20–Л44** сняты
> **2026-08-15**, а в строках стоит `2026-08-14` — унаследованная шапка предыдущей сессии.
> Строки не переписываю (правило файла: «устаревшую строку не стирают, а дополняют новой датой»);
> читать все даты в §10–§15 как **2026-08-15**. Числа от этого не меняются: состояние кода то же,
> между 08-14 и 08-15 в репозиторий не коммитили (последний коммит — `2e52af5`).

## 10. Фронт 1: текст в FXML (Этапы 4 и 4б) — замерено при написании карт

🖥 **Все числа этого раздела зависят от локального `storage/diagrams` (19 uid) и на чистом клоне
не воспроизведутся.** Корпус: 32 файла (`graph_canvas.json` × 15 + `graph_validated.json` × 17),
**3971** текст-блок; классификация — по 15 холстам, **1916** непустых неслитых блоков.

| # | что | команда | дата | результат |
|---|---|---|---|---|
| 10.1 | ⛔ **Расхождение dup-1 (4.3)**: клампнутое правило FXML против сырого в редакторах | `python -c "…(max(1.0,h)>max(1.0,w)*1.3) != (h>w*1.3)…"` по всем `text_blocks` корпуса | 2026-08-14 | **0 из 3971.** Дефект **латентный**: боксов со стороной < 1 px в корпусе нет (Л20) |
| 10.2 | **Зона разногласия порогов (4.6)**: аспект в (1.3; 1.4] — OCR читает как горизонтальный, FXML печатает повёрнутым | тот же скрипт, счётчик `1.3 < h/w <= 1.4` | 2026-08-14 | **9 из 3971** (0.23 %): `13d1ef5f` 1, `8d517a35` 1, `d74eb9f1` 5 (Л21) |
| 10.3 | **Классификатор Т1.1** при `containment > 0.3` (решение заказчика) | классификация 15 холстов: `bindings` → `bound`; иначе containment > 0.3 → `in_node`; иначе `free` | 2026-08-14 | `bound` **208** · `in_node` **549** · `free` **1159**; **59** привязанных лежат внутри чужого узла ⟹ правило приоритета обязано быть явным |
| 10.4 | То же на боевом листе `620cc50d` | тот же скрипт, один uid | 2026-08-14 | **57 из 161 (35 %)** — тот же порядок, что «53 из 161» в постановке фронта 1 |
| 10.5 | **Диаметры** (`kind == 'edge'`) | подсчёт `bindings` по `kind` | 2026-08-14 | **2** привязки на весь корпус. Фильтр Т1.4 нужен как контракт, не как оптимизация |
| 10.6 | ⛔ **Ручной KKS** (`nodekks_*`, привязка **без** `text_block`) | подсчёт `block_id.startswith("nodekks_")` | 2026-08-14 | **0 на весь корпус** ⟹ **СТ7 эту регрессию Т1.4 не поймает**, нужен отдельный юнит-тест (Л24) |
| 10.7 | **Дрейф НД3** (`node.kks_full` против `graph.bindings`) | сравнение множеств «узлы с `kks_full`» и «узлы с привязкой» по 15 холстам | 2026-08-14 | согласны **201**; `kks_full` без привязки **4** (620cc50d); привязка без `kks_full` **6** (13d1ef5f 4, 751116c9 2). Всего узлов с `kks_full` — **416** из 10560 |
| 10.8 | `side` у привязок: где живёт | подсчёт `bindings` с непустым `side` | 2026-08-14 | **203** в холстах, **0** в `graph_validated` — подтверждает «`side` синтезирует `text_import.py:320-322`» |
| 10.9 | **Воронка 4.1** | grep `text_edited`, `canvas_state.stamp(` по репозиторию (без `_scratch`, `.claude`) | 2026-08-14 | `stamp()` в боевом коде — **2 вызова** (`base_graph_tab.py:90`, `worker/tasks/layout.py:216`), **оба без** `text_edited=` ⟹ флаг всегда `False`, обе защиты мертвы. Правок текста — **11** вызовов `_ocr_push_snapshot` (Л22) |
| 10.10 | `kksVisible="true"` — сколько мест | grep `kksVisible` по `modules/` | 2026-08-14 | **4**: `canvas_to_fxml.py:198`, `:297`, `graph_to_fxml.py:804`, `:1066`. Пятое место — ручной KKS `advanced_graph_editor.py:5734-5744` (привязка без блока) |
| 10.11 | Кегли на одном листе | чтение `graph_to_fxml.py:836`, `canvas_to_fxml.py:98` | 2026-08-14 | свободный текст **40.0**, KKS контрола **10.0** — **вчетверо**. `_TEXT_FONT_RATIO = 0.9` (`:832`) **мёртв** (1 вхождение), докстринг `:868` обещает несуществующее (Л26) |
| 10.12 | Размер файла-магнита (4.7) | `wc -l`, `grep -c "^def "`, `grep -c "^class "` | 2026-08-14 | `graph_to_fxml.py` — **2201** строки, **40** `def`, **2** класса; `canvas_to_fxml.py` — **484**, импортирует **33** символа. Импортёров — **8**, включая `pretransform.py:100,410` (ядро раскладки) (Л25) |
| 10.13 | Базовая линия тестов фронта 1 | `python -m pytest tests/test_canvas_to_fxml.py tests/test_text_import.py tests/test_canvas_staleness.py tests/test_canvas_pipeline_golden.py -q` | 2026-08-14 | **59 passed за 5.62 с.** Это все тесты, покрывающие фронт 1 |
| 10.14 | Мёртвый путь OCR | grep `run_paddle_recognize` | 2026-08-14 | определён `modules/ocr/paddle_recognize.py:103`, **вызовов ноль**. Живой источник угла — `recognize_surya.py:76-77` (порог **1.4**) |
## 11. Стенды: чем сегодня доказывается серверная и клиентская половины

| # | что | команда | дата | результат |
|---|---|---|---|---|
| 11.1 | ⛔ **Асинхронной сессии в тестах нет** | чтение `tests/conftest.py` целиком | 2026-08-14 | файл **51 строка**; `:34-35` подменяет `create_async_engine` на `MagicMock`, `:37-39` — `async_sessionmaker`, **до** импорта `app`. `app/api/` весь async ⟹ тестов уровня эндпоинта быть не может до починки харнесса (Л28) |
| 11.2 | Единственная фикстура БД | grep `create_all`, чтение `tests/test_refactoring.py:48-71` | 2026-08-14 | **одна**, и та **локальная в тест-модуле**; `Base.metadata.create_all` — `test_refactoring.py:55`, **не** `conftest.py:45` (уточнение к Н5, Л27) |
| 11.3 | Что на самом деле покрыто на сервере | grep импортов `app.`/`worker.`, `TestClient`/`httpx`, `worker.tasks` по `tests/` | 2026-08-14 | `app.`/`worker.` — **~30** файлов (**24** из них `tests/observability/`); `TestClient`/`httpx` — **5**, все в observability; `worker.tasks` — **6**. Тесты есть, но все через линзу наблюдаемости |
| 11.4 | `tests/ui/` — про что | листинг + докстринг каждого файла | 2026-08-14 | **14 файлов, все про редакторы** (T1–T12, drag, посадка, регуляторы, шрифты, обрезка рамки). Про вкладки / workspace / переходы статусов — **ни одного** |
| 11.5 | Фикстуры корпуса | листинг `tests/fixtures/` | 2026-08-14 | **один файл** — `layout/synth_med.json` (72.9 КБ) |
| 11.6 | Семя для СТ3 | чтение `_scratch/audit_2026-08-06/scripts/j4_lib.py` | 2026-08-14 | **10.9 КБ**, уже умеет offscreen-`QApplication`, генерацию `blank1920.png`, реальный граф `d74eb9f1`, синтез `QMouseEvent`/`QKeyEvent`. ⛔ Абсолютный путь `REPO` на `:6` — при переносе заменить |
| 11.7 | `_canvas_mode` — кто ставит | grep по `ui/` | 2026-08-14 | объявлен `base_graph_editor.py:136` (`False`), боевые установщики — `base_graph_tab.py:928`, `:956`; потребителей **8**. Стенд без флага меряет растровые координаты (ложный эталон) |

### 11а. Базовая линия СТ9 (метрики структуры кода) — замерена попутно, карты СТ9 ещё нет

Исключены `tests/`, `_scratch/`, `alembic/`, `dist/`, `vendor/`, `build/` — иначе `dist/` даёт
двойной счёт (первый прогон показал 70 `diagram.status =` вместо 50 именно из-за этого).

| # | что | команда | дата | результат |
|---|---|---|---|---|
| 11.8 | Боевых `.py` | скрипт `st9_baseline.py` (в скрэтчпаде сессии) | 2026-08-14 | **309** |
| 11.9 | Крупнейшие файлы | он же | 2026-08-14 | `advanced_graph_editor.py` **5833** · `ocr_binding_editor.py` **3319** · `diagram_workspace.py` **2325** · `graph_to_fxml.py` **2201** · `ocr_binding_tab.py` **1828** · `base_graph_editor.py` **1782** · `skeleton_extension/core.py` **1605** · `layout/spread.py` **1395** |
| 11.10 | `except Exception` | он же | 2026-08-14 | **238** в **83** файлах; из них `except Exception:` + `pass` — **35** в **23** файлах (`CLAUDE.md` называет 24 — расхождение на один файл) |
| 11.11 | Прямые присваивания статуса (5.2) | он же + разбор вручную | 2026-08-14 | `diagram.status =` — **50 в 18 файлах** (цифра плана **воспроизвелась точно**); `stage.status =` — **7**, но из них **3** внутри `app/models/stage.py:115,121,137` — это **законная инкапсуляция**, долгом считать нельзя. Долг — **4**: `app/api/cvat.py:440`, `layout_dispatch.py:240`, `:263`, `worker/tasks/layout.py:71` |

---


---

## 12. Единая геометрия (Этап 7)

| # | что | команда | дата | результат |
|---|---|---|---|---|
| 12.1 | ⭐ **Каноны посадки: сколько живо и сколько расходится** | `python -m pytest tests/test_seat_contract.py -q` + греп `seating.reseat_edge` по `ui/` | 2026-08-14 | **37 passed за 0.83 с.** Реализаций в матрице **7**; `seating.reseat_edge` зовут **4** (`advanced_graph_editor.py:1004`, `:793`, `autofix_chains.py:447`, `base_graph_editor.py:1191`); `_recalculate_edge_fast` — **0 вхождений (удалён)**; `get_line_endpoints` канон не зовёт, но расхождений на каноничном входе **0** |
| 12.2 | Сколько расхождений осталось и почём | чтение `tests/test_seat_contract.py:96-137` | 2026-08-14 | **10 записей ALLOWLIST**, все — решения заказчика: **20.00 px** («порт вместо канонного слака», 2 записи) и **8.50 px** («скин на рамке bbox vs letterbox», 8 записей). Записи Э1 (12 шт.) сожжены, замер по ним `0.00px` |
| 12.3 | Адреса §12.2 протухли | греп по именам функций | 2026-08-14 | `optimize_edge` 734 → **765** · `_recalculate_edge` 921 → **967** · `auto_fix_graph` 348 → **222** (файл 472 строки, а не 772+) · `get_connection_point` **1172** (совпал) · `get_line_endpoints` **614** (совпал) |
| 12.4 | ⛔ **Копий допуска ортогональности** | греп `ORTHO_TOL|DIAG_TOL|STRAIGHT_TOL` по `modules/`, `ui/` | 2026-08-14 | семейство 1.5 — **4 копии**: `avoid_router.py:53`, `edit_avoid.py:71`, `edit_checks.py:36`, **`_gate.py:27`** (последней нет ни в плане, ни в Н3). Потребителей `ec.DIAG_TOL` в `edit_smooth.py` — **18** |
| 12.5 | ⛔ Одно имя — две величины | тот же греп | 2026-08-14 | `STRAIGHT_TOL` = **1.5** в `_gate.py:27` и = **3.0** в `seating.py:35`, `ports.py:43`, `advanced_graph_editor.py:3875`, `autofix_chains.py:58` (+ре-экспорт `port_model.py:17`). Сводить по имени нельзя |
| 12.6 | `PIN_TOL` — обе копии | греп `PIN_TOL` | 2026-08-14 | **0.5** в `edit_avoid.py:72` (проверка `:556-559`) и `avoid_router.py:54` (браковка `:362-364`). Значения равны и согласны — заморожено решением |
| 12.7 | **Инвентарь IoU** | греп `def .*iou` по `modules/`, `ui/`, `app/`, `worker/` | 2026-08-14 | именованных **6** (`text_detect_yolo.py:41` ✅жив, `text_filter.py:53` ✅жив, `pipeline.py:310` ⚰, `clean_and_ocr.py:119` ⚰, `utils.py:38` ⚰ 0 потребителей, `metrics.py:56` — метрика обучения) + инлайновых **2** (`contours_merge.py:99-115`, `sam2_contour.py:352-357`). ⚠ **«11 реализаций» из плана не воспроизвелось** |
| 12.8 | Адреса 7.4 не совпали | `find`, чтение | 2026-08-14 | `modules/graph/core/sam2_contour.py` **не существует** (файл — `modules/sam2_contour.py`); `pretransform.py:580` — `_count_overlaps`, проверка пересечения AABB в формате «центр+w/h», **не IoU** и без порогов |
| 12.9 | Мёртвый путь OCR | греп импортов `modules.ocr.pipeline` | 2026-08-14 | боевой — `pipeline_clean.py` (`worker/tasks/ocr.py:107`); `modules/ocr/pipeline.py` упомянут только в двух докстрингах ⟹ он и `clean_and_ocr.py` мертвы вместе с их копиями IoU |
| 12.10 | ⛔ **7.5 воспроизводится** | `python -c "import mask_refinement"` · `python -c "import pipe_segmentation"` | 2026-08-14 | **оба `ModuleNotFoundError`.** В бою работают из-за `Dockerfile.worker:104` и `Dockerfile.worker_ocr:77`: `ENV PYTHONPATH="/app:/app/modules"`. Голых импортов — **6** (`skeleton.py:570`, `pipe_segmentation/__init__.py:21`, `inference/__init__.py:3,8,13,18`) |
| 12.11 | Последствие для 0.12 | чтение `worker/tasks/skeleton.py:566-620` | 2026-08-14 | импорт внутри `try` (`:569`), отказ — `except Exception` + `logger.warning(exc_info=True)` (`:615-620`) ⟹ **вне контейнера этап проходит «успешно», артефакт `PIPE_MASK_REFINED` не создаётся**. Приёмку 0.12 уточнить: проверять артефакты, не только статусы |

---

## 13. Фронт 7: посадка концов и дубли рёбер (Этап 8)

| # | что | команда | дата | результат |
|---|---|---|---|---|
| 13.1 | ⛔ **`_get_node_bbox`: сколько вызовов держит запрет 8.1** | греп `_get_node_bbox` по `ui/` | 2026-08-14 | **25 вызовов в 3 файлах**: `advanced_graph_editor.py` 22 · `contour_editor.py:308-309` · `ocr_layer_mixin.py:352`; плюс алиас `_get_node_bbox_for_routing:963-965`. План называет «8 мест» |
| 13.2 | Все девять адресов препятствий из плана | чтение | 2026-08-14 | **живы**: `advanced_graph_editor.py:1023`, `:1177`, `:1296`, `:1601`, `:1664`, `:1690`, `:1707-1708`, `:1719`, `:1812` |
| 13.3 | Механизм поломки Этапа 9 | чтение `modules/graph/core/edit_avoid.py:272-282` | 2026-08-14 | `if bb and len(bb)==4 and bb[2]>bb[0] and bb[3]>bb[1]` — вырожденный бокс **отбрасывается без `else` и без лога** ⟹ коннекторы исчезнут из препятствий молча |
| 13.4 | Диспетчер «Контуров» | чтение `ui/editors/contour_editor.py:331-351` | 2026-08-14 | **8 веток**; ветки «коннектор → bbox» — `:338-340` и `:341-343`; **ветки коннектор↔коннектор нет** ⟹ второй коннектор входит как виртуальный бокс r=8 (отсюда 8.00–11.31 px = r и r·√2) |
| 13.5 | Контракт, который это нарушает | чтение `modules/graph/core/seating.py:7-9` | 2026-08-14 | «connector-конец: sp/tp **ДОЛЖНЫ** равняться centroid коннектора (FXML игнорирует `source_point` для connector-концов)» |
| 13.6 | **Г5а** — адрес жив | чтение `ui/editors/graph_geometry.py:218-232` | 2026-08-14 | `:224-225` — `if overlap_x and overlap_y: return ((acx, acy), (bcx, bcy), "overlapping")` — при пересечении bbox возвращаются **центроиды** |
| 13.7 | ⚠ Два `_reseat_after_resize` | греп | 2026-08-14 | `simple_graph_editor.py:362` («Проверка схемы», переписывает **ОБА** конца — докстринг `:365-369`) и `advanced_graph_editor.py:2252`. Адреса `Г5` протухли: `add_edge` `:92-93` → **`:69`**, `_reseat_after_resize` `:375-380` → **`:362`**, `add_edge_with_waypoints` `:649-655` → **`:621`** |
| 13.8 | **Г7**: сколько триггеров пересадки | чтение | 2026-08-14 | «Контуры» — **6** точек вызова `_recalculate_edges_for_node` (`polygon_commands.py:154,195,207`; `contour_editor.py:224,248,521`); «Проверка схемы» — **1** (только ресайз, `simple_graph_editor.py:346`), после `add_edge` не пересаживается никогда |
| 13.9 | **8.5**: на чём стоит дёрти-флаг | чтение `undo_manager.py:112-171`, `base_graph_tab.py:789,1149` | 2026-08-14 | `has_unsaved_changes` = `undo_mgr.revision != _saved_revision`, `revision` растёт **только** на операциях стека (`+=1` в 4 местах) ⟹ пересадка мимо Command вкладку грязной не пометит |
| 13.10 | ⛔ **Дедупа пар не существует** | греп `seen|dedup|edge_key` по `modules/graph/core/tracing.py` | 2026-08-14 | **0 вхождений** на 987 строк. Цикл `:189` идёт по всем контактным пикселям, `:211-214` добавляет ребро **без проверки существующей пары** |
| 13.11 | Легаси-близнецы (8.7) | `grep "^def " tracing.py` + греп вызовов | 2026-08-14 | `trace_edges:635` и `trace_single_edge:711` импортированы `builder.py:13`, но зовётся **только** `trace_edges_v3` (`builder.py:311-312`). Живые: `:42 trace_edges_v3`, `:410 trace_single_edge_v3`, `:967 compute_edge_statistics` |
| 13.12 | Стенда качества сборки графа нет | листинг `tools/` | 2026-08-14 | есть `edit_bench.py`, `interactive_bench.py`, `layout_bench.py` + `tools/bench/`. **СТ6 писать с нуля**, образец структуры — `layout_bench.py` (корпус → метрики → список регрессий → `--uid/--json`) |

---

## 14. Фронт 6: роутинг (Этап 9)

| # | что | команда | дата | результат |
|---|---|---|---|---|
| 14.1 | ⭐ **`9.1`: кто на самом деле не пишет концы** | чтение `avoid_router.py:205,224,258-271,411-445` и `advanced_graph_editor.py:2004-2033` | 2026-08-14 | **сервер пишет:** `route_graph(..., ends_out)` → `ends_out[eid] = (new_sp, new_tp)` (`:263-267`), `apply_routing` передаёт `ends_new` (`:427-428`) и применяет (`:441-444`). **Редактор не пишет:** `_avoid_apply_route:2013-2018` берёт только `mid = pts[1:-1]`. ⟹ работа только клиентская |
| 14.2 | Единственный боевой вход роутинга | греп `apply_routing` | 2026-08-14 | `modules/graph/core/layout/__init__.py:172` — один вызов на весь репозиторий |
| 14.3 | **Причин полного отката** | чтение `avoid_router.py:474-482` | 2026-08-14 | **девять**, а не четыре: `new_diagonals`, `straight_broken`, `side_changed`, `overlaps`, `box_on_magistral`, `order_broken` (из `_gate.verify`) + `connectivity` + `defects` + `box_on_magi_drawn` |
| 14.4 | ⚠ Адресный откат уже есть | чтение `avoid_router.py:462-466` | 2026-08-14 | пер-ребёрный `_restore(e, snap)` + `stats["routed"] -= 1` при неортогональности или смене стороны — механизм, которого нет в плане |
| 14.5 | Что роутер вообще не видит | чтение `avoid_router.py:232-240` | 2026-08-14 | `routable` пропускает рёбра с `_manual_route` **и с любыми `waypoints`** ⟹ класс рёбер, на котором мерили §6.5, в роутер не попадает |
| 14.6 | **Два пути: чем различаются** | чтение обоих модулей | 2026-08-14 | слоты (нет/есть) · окно (всё / `MAX_SHAPES = 225`, `edit_avoid.py:60`) · буфер (`route_buffer = 6.0`, `params.py:100` / `CLEARANCE 6.0 + BUFFER_EPS 0.25`, `edit_avoid.py:49-58`) · пины (новый класс на конец `:336` / **переиспользование** `:421-426`) |
| 14.7 | ⚠ Расхождение буфера **обосновано** | чтение `edit_avoid.py:53-58` | 2026-08-14 | без добавки 0.25 судья браковал бы собственный маршрут сессии на float-границе (`gap = 5.9999999999999858`). **Сводить нельзя** без пересмотра судьи |
| 14.8 | ⛔ **`9.2`: ловушка чтения** | чтение `avoid_router.py:338-340`, `edit_avoid.py:436-438` | 2026-08-14 | пятый позиционный аргумент `ShapeConnectionPin(..., False, 0.0, ...)` — **`proportional`**, не `exclusive`. Механизм дефекта клиентский: `ckey = (round(off[0],4), round(off[1],4))` переиспользует класс пина (`edit_avoid.py:421-426`) |
| 14.9 | **`9.3`: сколько сторожей держат «конец в пине»** | греп `PIN_TOL|pinned_port|edge_pin` | 2026-08-14 | **шесть** — `edit_avoid.py:556-559` · `avoid_router.py:362-364` · `edit_engine.py:348,359` · `edit_smooth.py:38,179` · `autofix_chains.py:449-452` · `ports.py:22`. Цифра плана подтвердилась |
| 14.10 | **`9.5`: скоринг наложений уже есть** | чтение `ui/editors/edge_routing.py` | 2026-08-14 | `:328 _segments_collinear_overlap` · `:367` использование в скоринге · `:394 turns * 100  # R6: повороты`. Все три адреса живы |
| 14.11 | ⭐ **`O1–O11`: сколько закрыто** | чтение `docs/AUDIT_manual_edit_routing.md:117-127` | 2026-08-14 | **8 из 11 исправлены** (ветка `fix/routing-buttons`, 2026-07-02). Открыты: **O8** (наложения eq↔eq/conn↔conn), **O9** (развилки ≤3 px), **O11** (веса score, частично). ⛔ **O9 не покрыт ни одной строкой Этапа 9** |
| 14.12 | База приёмки роутинга | `python -m pytest tests/test_avoid_router.py tests/test_edit_avoid.py -q` | 2026-08-14 | **24 passed за 1.08 с** — оба пути, синтетика. ⚠ `tests/test_routing_metrics.py` (25 тестов), на который ссылается аудит, **не существует**. Без vendored-биндинга тесты честно скипаются |

---

## 15. Фронт 5 и декомпозиция UI (Этап 10)

### 15а. Размеры — все семь цифр плана подтвердились

| # | файл | команда | дата | результат |
|---|---|---|---|---|
| 15.1 | все файлы этапа | `wc -l` + `grep -c "^    def \|^def "` | 2026-08-14 | `advanced_graph_editor.py` **5833 / 209** (план 5834/202) · `ocr_binding_editor.py` **3319 / 124** · `diagram_workspace.py` **2325** · `ocr_binding_tab.py` **1828 / 69** (план ~75) · `base_graph_editor.py` **1782 / 94** · `graph_geometry.py` **1251 / 26 функций, 0 классов** · `api_client.py` **800 / 64** |
| 15.2 | god-методы `ocr_binding_tab.py` | скрипт по границам `def` | 2026-08-14 | **все четыре числа плана точны**: `_save_binding` `:1599` — **174** · `_auto_bind_diameters` `:1086` — **157** · `_on_download_finished` `:708` — **153** · `_setup_ui` `:317` — **148** |
| 15.3 | Хендлеры режимов | подсчёт `^class` в `mode_handlers/` + греп вне пакета | 2026-08-14 | **21 класс**: 18 в пакете (advanced 8, simple 7, contour 2, polygon 1, база 1) + **3 вне**: `advanced_graph_editor.py:119`, `ocr_layer_mixin.py:954`, `:999`. Цифра плана подтвердилась |
| 15.4 | Байт-в-байт копии AGE ↔ OBE | греп | 2026-08-14 | `_move_paste_ghost` `age:5004` / `obe:2633`; `_cancel_paste_ghost` `age:5025` / `obe:2650` — **все четыре адреса точны** |

### 15б. Что НЕ подтвердилось

| # | что | команда | дата | результат |
|---|---|---|---|---|
| 15.5 | ⛔ **`НД5`: дрейф `auto_fix_graph`** | чтение `autofix_chains.py:266-268`, `:320`, `:341`, `:358-359`, `:214-215` | 2026-08-14 | «±30 × 4 прохода ≈ 120 px» **неверно**: `orig_pos` снимается **один раз до цикла**, `_clamp` берёт координату оттуда ⟹ дрейф ≤ **±30** (`EQUIP_MAX_SHIFT`, `:54`) / **±80** (`CONN_MAX_SHIFT`, `:55`) **за весь прогон**. `MAX_PASSES = 4` (`:61`) на лимит не влияет |
| 15.6 | ⭐ **Где сдвиг всё-таки не ограничен** | чтение `autofix_chains.py:395-415` | 2026-08-14 | выталкивание коннектора из bbox оборудования стоит **после** цикла и пишет `n['centroid']` **мимо `_clamp`** (`:412`), инкрементя `stats['overlaps_fixed']` (`:413`). ⟹ проверка коллизий **есть** (вопреки доске), но **она же** и есть единственный неограниченный сдвиг. `box-on-pipe` нет вовсе |
| 15.7 | ⚠ `НД5`: адрес | `wc -l ui/editors/autofix_chains.py` | 2026-08-14 | **472 строки**; доска указывает `:348-772` и блок `:650-757` — таких строк в файле нет. `auto_fix_graph` на `:222` |
| 15.8 | ⚠ `10.6`: мутации модели | греп по шаблонам `node['…'] = `, `self.nodes[..][..] = `, `['centroid'/'bbox'/'segmentation'] = ` | 2026-08-14 | `node['x'] = ` — **0 вхождений** во всех трёх файлах. По шаблону `node['…'] = `: AGE **26** (план 40), `contour_editor.py` **8** (9), `simple_graph_editor.py` **3** (7). Шаблон подсчёта плана неизвестен |
| 15.9 | ⭐ `10.6`: главная цифра точна | `grep -c "\.execute()" ui/editors/base_graph_editor.py` | 2026-08-14 | **0** — база не зовёт команды ни разу, при 22 существующих классах команд |
| 15.10 | ⚠ `polygon_handlers.py:134` | чтение | 2026-08-14 | `editor.nodes[...].get("segmentation")` — **чтение**, не запись. Прямой доступ к модели хоста есть, мутации нет |
| 15.11 | ⛔ `10.7`: `api_client.py:213` | чтение | 2026-08-14 | **`except (APIError, Exception):`** — глотает **всё**, а не только `APIError`. Плюс `Dict[str, Any]` — **45** возвратов (план 44) |
| 15.12 | ⚠ `10.2`: объём | `grep -c CONNECTOR_DRAW_RADIUS` | 2026-08-14 | **11** вхождений (4 в AGE, 7 в базе), а не 19 из доски |
| 15.13 | `10.12`: боевых вызовов нет | греп `_breaks_anchor` | 2026-08-14 | определение `edit_smooth.py:463`, вызовы — **только** `tests/test_edge_pins.py:111,113,117`. В боевом коде **0** |
| 15.14 | `10.15`: предусловие проверено | чтение `ui/tabs/cvat_tab.py:96` | 2026-08-14 | `self.web_view = QWebEngineView()` — риск «ввод Chromium не доходит до `installEventFilter`» **реален**, пробу делать до кода |
| 15.15 | ⭐ **7.1: чей контракт прав по существу** | чтение `modules/canvas_to_fxml.py:169`, `:183-186` | 2026-08-15 | в 1:1-экспорте `prefWidth`/`prefHeight` контрола = **width/height bbox узла**, `layoutX/Y` = его угол ⟹ **в поставляемом FXML символ занимает ровно рамку bbox**. Значит «конец трубы на рамке» (редактор) — правда продукта, а letterbox (`seating._anchor_rect`) — внутреннее представление. ⚠ Второе расхождение (порт против слака, 20.00 px) — про качество роутинга, не про рамку |

## 16. Фронт 4: качество Э4 и ускорение (Этап 11)

| # | что | команда | дата | результат |
|---|---|---|---|---|
| 16.1 | ⛔ **Регрессию Э4 не ловит ничто** | `ls tools/smooth_bench.py` + `grep -c edit_smooth tools/edit_bench.py` | 2026-08-15 | `tools/smooth_bench.py` — **не существует**; `edit_bench.py` — **0 вхождений** `edit_smooth` ⟹ страховка С1 фиктивна, как и сказано в плане |
| 16.2 | База приёмки, которая есть | `python -m pytest tests/test_edit_smooth.py -q` | 2026-08-15 | **17 passed за 0.07 с** — модульная база 11.4/11.5 |
| 16.3 | Эталон редактора | `ls -l tools/bench/edit_baseline.json` | 2026-08-15 | **3442 байта**, в git — образец хранения эталона для 11.1 |
| 16.4 | Таблица метрик, из которой растёт СТ5 | чтение `tools/edit_bench.py:36-50` | 2026-08-15 | `COLS`: `diag`, `near_ortho`, `max_dev`, `along_own`, `along_foreign`, `through`, `corner`, **`:44 corner_near`**, `conn_off`, `poly_off`, `adrift`, `midpoint`, `side`, `manual`. ⛔ `С3` конфликтует именно с `:44` |
| 16.5 | ⭐ **Механизм 11.2** | чтение `_gate.py:28`, `:79`, `:184-187`; `avoid_router.py:464-468` | 2026-08-15 | `side_ok = all(post_sides.get(nid, s) & s …)` — **пересечение множеств сторон**; множества строит `_side_set(px, py, bb, eps=SIDE_EPS)` при `SIDE_EPS = 1.5`. Адрес плана «`:474-487`» уехал: живые — `:466-468` (адресный откат) и `:485-490` (полный) |
| 16.6 | ⭐ **11.4: «минус половина `deepcopy`» — буквально** | `grep -c deepcopy modules/graph/core/edit_smooth.py` + чтение | 2026-08-15 | **5 вхождений**, из них **4** — две дословно одинаковые пары: `:592-593` (`_try_remedies`) и `:619-620` (`_attempt`) |
| 16.7 | ⛔ **И2: вызовов `route_fn` четыре, а не три** | греп по `edit_smooth.py` | 2026-08-15 | `:682` («скольжение»), `:703` («коннектор»), `:729` («узел») — **это И2**; **`:641` («колено») — само средство**, трогать нельзя. `TZ …§5` называет только три |
| 16.8 | Вложенность снимков | чтение `edit_smooth.py:605` | 2026-08-15 | `_attempt` зовётся **из** `_try_remedies` ⟹ два снимка могут быть двумя уровнями отката, а не дублем. Проверить перед сведением |
| 16.9 | `11.5`: адреса dup-4 живы | `grep -n "def single_step\|def step_index\|def count_steps"` | 2026-08-15 | `single_step:113`, `step_index:278`, `count_steps:476` — совпадают с планом. ⚠ Дословность тел цикла **не проверялась** — сверить первым шагом |
| 16.10 | `11.7`: размер `_vpsc.py` | `wc -l modules/graph/core/layout/_vpsc.py` | 2026-08-15 | **511 строк** — цифра плана точна; вывод «не дубликат libvpsc» из неё не следует и требует своей проверки |

## 17. Ядро конвейера и офлайн-контур (Этапы 12 и 13)

### 17а. Этап 12 — впервые все цифры плана подтвердились без единого расхождения

| # | что | команда | дата | результат |
|---|---|---|---|---|
| 17.1 | Размеры шести файлов | `wc -l` | 2026-08-15 | `skeleton_extension/core.py` **1605** · `layout/spread.py` **1395** · `pipe_segmentation/cli.py` **1322** · `tracing.py` **987** · `nodes.py` **985** · `direction_nodes.py` **784** — все шесть точны |
| 17.2 | 13 god-функций | греп `def <имя>(` | 2026-08-15 | `connect_with_directed_lines:512` · `bfs_connect_endpoints:977` · `diagnose_unconnected_endpoints:1234` · `create_endpoint_protection_mask:151` · `spread:743` · `_sign:1193` · `get_or_create_node:86` · `try_rollback_to_fork:445` · `is_excluded_node:803` · `_pipe_dir:713` · `identify_node_by_point:195` · `prepare_tracing_data:640` · `infer:917`/`test:704`/`ensemble:1143` — **все адреса точны** |
| 17.3 | Четыре `try_*` внутри `spread()` | греп | 2026-08-15 | `try_shift:837` · `try_align:923` · `try_compound:961` · `try_band:1094` — все **вложенные** в `spread():743`, что и даёт «610 строк с 14 замыканиями» |
| 17.4 | Внутренние дубли `core.py` | чтение | 2026-08-15 | 3× удаление скелета: `:28` · `:46` · `:88`; 3× защитная маска: `:151` · `:273` · `:918` |
| 17.5 | ⛔ **Третий рецидив отравления сбора** | чтение `tests/observability/test_skeleton_extension_substeps.py:45-49` | 2026-08-15 | `sys.modules.setdefault("skeleton_extension.core", MagicMock())` **на уровне модуля**, без восстановления; импортируется `processing`, **не** `core`. Прогон файла — **25 passed за 3.93 с** (дефект латентный) |

### 17б. Этап 13 — размеры групп и конфликты

| # | что | команда | дата | результат |
|---|---|---|---|---|
| 17.6 | Легаси-OCR 3-pass | `wc -l` | 2026-08-15 | `pipeline.py` **681** · `clean_and_ocr.py` **608** · `evaluate.py` **435** · `paddle_recognize.py` **273** · `utils.py` **119** = **2116** («~2100» — точно) |
| 17.7 | Мёртвые OCR-модули | `wc -l` | 2026-08-15 | `recluster.py` **849** · `hooks.py` **465** · `classify_engine.py` **184** · `refine_pipe_mask.py` **154** · `noise_engine.py` **107** = **1759** (план — «~1900») |
| 17.8 | Профили | `wc -l` | 2026-08-15 | `kks_rosatom.py` **651** + `isa_s5.py` **103** = **754** — точно |
| 17.9 | ⛔ Конфликт opencv | чтение `requirements/` | 2026-08-15 | `ui.txt:23 opencv-python>=4.9.0` против `worker.txt:26 opencv-contrib-python-headless>=4.9.0` — **один пакет `cv2` из двух дистрибутивов**. `numpy>=1.26,<2.0` в `ui`/`worker`, **не в `base.txt`**; `fastapi>=0.109` дублирован (`api.txt:8`, `worker.txt:43`) |
| 17.10 | ⭐ Единственный точный пин — обоснован | чтение `worker.txt:35-38` | 2026-08-15 | `shapely==2.1.2`, и комментарий объясняет: в shapely 2.x менялся API `STRtree`, от которого зависит строгий арбитр наложений в гейте раскладки. **Снимать нельзя** |
| 17.11 | ⭐ **Дубль профиля — байтовый** | sha256 по трём файлам | 2026-08-15 | `domain_profile.yaml` и `domain_profile_kks.yaml` — **одинаковый sha256**, по 542 строки; `domain_profile_cyrillic.yaml` — 602 строки, отличается. Кодом читается **только** `domain_profile.yaml` (`project_loader.py:470`) |
| 17.12 | Проект без главного YAML | листинг `configs/projects/` | 2026-08-15 | `tec_boiler/` — **один** файл `domain_profile.yaml`; `thermohydraulics/` — **семь**, включая `thermohydraulics.yaml` |
| 17.13 | Сбой загрузки конфига молчит | чтение `project_loader.py:560,588` | 2026-08-15 | `for yaml_path in self.configs_dir.glob("*.yaml")` … `print(f"Warning: failed to load …")` + `break` — конфиг исчезает из списка **без исключения и без лога** |
| 17.14 | ⛔ Ложь про замену биндинга | чтение `modules/binding/__init__.py:4` + греп вызовов | 2026-08-15 | «Заменяет `modules/kks_binding/` и `modules/text_binding/`» — при этом оба живы: `advanced_graph_editor.py:5598,5788` и `ocr_binding_editor.py:834`, `ocr_binding_tab.py:1006`. `binding/engine.py` (**267**) вне пакета зовётся только из `tests/test_cyrillic_binding.py:206` |
| 17.15 | Подтверждение мёртвого пути | чтение `worker/tasks/ocr.py:104` | 2026-08-15 | дословно: «Старый путь (профили/конфиг/reclustering/маски/3 итерации) **отключён**» |

## 18. Закрытые пробелы проверки (дозапись 2026-08-15)

| # | что | команда | дата | результат |
|---|---|---|---|---|
| 18.1 | ✅ **`dup-4` (11.5): дословность тел цикла** | чтение `edit_smooth.py:113-139`, `:278-298`, `:476-499` целиком | 2026-08-15 | предикат `horiz`/`vert` — **четыре дословно одинаковые строки** во всех трёх. Единственное различие: `single_step` отбрасывает по `L > step_max or L < 1e-6` (⟹ **видит** ступеньку при `L == 1e-6`), две другие — по `1e-6 < L <= step_max` (⟹ **не видят**) |
| 18.2 | Агрегация различается намеренно | чтение `count_steps:479-482` | 2026-08-15 | докстринг объясняет: «Судья их НЕ считает дефектом и не может… иначе гейт не видит улучшения от их устранения и откатывает ход (репро `graph_edited_av`: 10 ступенек пережили сглаживание)» |
| 18.3 | ✅ **`13.8*`: какие ADR врут** | чтение шести ADR + сверка следствий с кодом | 2026-08-15 | **врут три**: 001 (одна строка `:33` про отдельный worker), 004 (целиком — `worker_sam2` нет), 006 (следствия — воркер строит одну модель). **Честны три**: 002 (`sam2_contour.py:145,151` — `lora_r` из конфига применяется), 003 (`:388-408` — `input_6ch` реально), 005 (smart snap в `sam2_contour.py`) |
| 18.4 | ⚠ **«Все шесть из GPU-посылок» — опровергнуто** | `grep -c "GPU\|VRAM\|RTX\|CUDA" docs/ADR/00*.md` | 2026-08-15 | 001 — **1**, 002 — **1**, 003 — **0**, 004 — **6**, 005 — **0**, 006 — **1**. На GPU по-настоящему построен только **004**; у трёх — одна строка; два не упоминают вовсе |

**Осталось непроверенным** (требуют стендов, которых нет): числа фронтов (45-46→4, 148/225,
3205/3245, ×6 медиана) · замеры Этапа 8 (3640/4763, 40.5 %, 354 px) · замеры 9.3/9.5
(55 %, 70/57 %, 1 из 151) · дрейф доков в шести группах (13.9\*).

## 19. Ревизия ТЗ: сплошная структурная проверка (2026-08-15)

Все числа воспроизводятся одной командой: `python tools/tz_lint.py` (или `--json`).
⚠ **ПР4-а: область грепа задана в самой команде** — иначе числа врут (18 строк ниже, 19.9).

| # | что | команда | дата | результат |
|---|---|---|---|---|
| 19.1 | Покрытие строк доски картами | `python tools/tz_lint.py` §1 | 2026-08-15 | **192 живых строки, 163 карты, без карты 0.** Строк таблицы 201, распознано 7-колоночных 200, **отброшено 0** — невидимых счётчику строк нет. (Было 193 живых; `ПР3` переведён в `снято`) |
| 19.2 | Ложные отметки покрытия | `python tools/tz_lint.py` §2 | 2026-08-15 | **0.** Обе прежние (`11.2`, `13.4*`) закрыты; каждый чужой ID в заголовке карты имеет собственную карту |
| 19.3 | Нумерация 163 карт | `python tools/tz_lint.py` §3 | 2026-08-15 | дублей **0**, пропусков **0**, нарушений порядка **0** |
| 19.4 | Адреса `file:line` в картах | `python tools/tz_lint.py` §4 | 2026-08-15 | проверено **784**; выдуманных путей **1** (`ui/tabs/autosave.py` → `ui/services/`, Л56, исправлено); за концом файла **3** (все — заведомо протухший `autofix_chains.py:772`, помечен находкой Л40); однофамильцев без пути **26** |
| 19.5 | Штампы происхождения | `python tools/tz_lint.py` §5 | 2026-08-15 | со штампом **106 из 122** карт с адресами; без штампа **6** содержательных (Л59), крупнейшие `4.7` (9 адресов) и `СТ4` (10) |
| 19.6 | Ссылки на документы | `python tools/tz_lint.py` §6 | 2026-08-15 | путей проверено **262**, ошибочных **1** (`docs/UI_TABS_BACKLOG.md` → `docs/planning/`, исправлено). Остальные отсутствующие — файлы, которые карта сама создаёт |
| 19.7 | Перекрёстные ссылки `Л`/`П`/`Х` | `python tools/tz_lint.py` (раздел 5 отчёта ревизии) | 2026-08-15 | `Л1`–`Л54` без пропусков; висячих `Л`/`Х` **0**; **2** висячих `П0` — оказались коллизией пространства (Л55), квалифицированы |
| 19.8 | Пять карт со сменённым вердиктом — сверка с кодом | прогон `pytest tests/test_seat_contract.py tests/test_edit_smooth.py tests/test_avoid_router.py tests/test_edit_avoid.py -q` | 2026-08-15 | **78 passed за 2.69 с** = 37 + 17 + 24, ровно как записано в `Л29`, `Л46`, `Л38`. Подтверждены все пять: `Л29` (7.1), `Л36` (9.1), `Л40` (НД5), `Л33` (8.8), `Л20` (4.3) |
| 19.9 | ⛔ **Фантомные копии дерева** | `find .claude/worktrees -name "*.py" \| wc -l` | 2026-08-15 | **564**. Проверка эффекта: `grep -rn "_recalculate_edge_fast" .` → **4** попадания (все фантомные) против **0** в рабочем дереве. Основание `ПР4-а` (Л57) |
| 19.10 | Л40 усилена: переклампа нет | чтение `ui/editors/autofix_chains.py:381-472` | 2026-08-15 | блок выталкивания `:395-415` стоит **вне** цикла `for pass_num in range(MAX_PASSES)` (`:271`); `n['centroid']` пишется на `:412` мимо `_clamp`; ниже `orig_pos` используется только для детекции сдвига (`:433`) и статистики (`:463`) — **переклампа нет вообще** |
| 19.11 | Дефолты автосейва (переснято, Л56) | чтение `ui/services/ui_settings.py:22-38` | 2026-08-15 | `autosave_enabled` → `QSettings("autosave/enabled", **True**)` на `:25`; `autosave_interval_sec` → `("autosave/interval_sec", **120**)` на `:33`. Числа плана верны, **адрес был неверен целиком** |
| 19.12 | Копии правила «вертикальный блок» (4.3, Л20) | `grep -rn "_TEXT_VERTICAL_RATIO" --include=*.py modules/ ui/` | 2026-08-15 | **три копии**: `modules/graph_to_fxml.py:882` (по клампнутым `h`/`w`), `ui/editors/ocr_binding_editor.py:79` и `ui/editors/ocr_layer_mixin.py:55` (по сырым `y2-y1`/`x2-x1`). Обе UI-копии несут комментарий «Продублировано из `graph_to_fxml.py`» |

## 20. Замеры под открытые решения (2026-08-15)

| # | что | команда | дата | результат |
|---|---|---|---|---|
| 20.1 | ⭐ **`10.12`: сколько узлов корпуса несут якорь оператора** | `m_10_12.py` (скрэтчпад): по 17 uid читает `graph_validated`, `graph_canvas`, `graph`; пин = `pin_source`/`pin_target` — dict `{dx,dy}` (`ports.py:252,269,279`); рёбра — каноном `graph_access.edges:20-21` | 2026-08-15 | **`graph_canvas`: 74 ребра с пином, 38 узлов, в 8 холстах из 15** (5160 узлов / 5427 рёбер). `graph_validated` и `graph` — **0 из 5677 и 5362** (пин ставит только оператор). ⟹ `_breaks_anchor` **сторожит живые данные, а не пустоту** |
| 20.2 | ⭐ **`10.12`: жива ли легаси-ветка `n['_ports']`** | тот же скрипт | 2026-08-15 | **0 узлов** на всём корпусе (15 460 узлов в трёх артефактах). ⟹ миграция Э5b завершена, ветка `if n.get("_ports")` (`edit_smooth.py:469`) — мёртвый код |
| 20.3 | ⛔ **Ловушка замера: рёбра лежат в `links`, не в `edges`** | `python -c "print(list(json.load(...).keys()))"` → `['directed','multigraph','graph','nodes','links','text_blocks','bindings']` | 2026-08-15 | Первый прогон 20.1 дал **честный ноль рёбер на всех 49 файлах**, потому что читал только `"edges"`. Канон — `graph_access.edges:20-21`: validated → `links`+`source`/`target`, raw → `edges`+`from`/`to`. ⚠ Тот же класс, что `ПР4-а`: ноль без проверки схемы неотличим от ноля по существу |
| 20.4 | **`Б-5`: `CONNECTOR_DRAW_RADIUS` — область решает всё** | `grep -rn "CONNECTOR_DRAW_RADIUS" --include=*.py <область>` | 2026-08-15 | `ui/` — **13** · `ui modules app worker` — **13** (вне `ui/` вхождений нет) · плюс `tools tests` — **38**. По файлам: `base_graph_editor.py` **7**, `advanced_graph_editor.py` **4**, `base_graph_tab.py` **2**. ⟹ `Л43` («11, а не 19») считала **два файла редакторов**; доска считала с тестами. Фантомов в `.claude/` для этого символа **0** |

---


---


---


---

**Куда это идёт дальше.** Пункт **0.10 (ПР4)** объявляет этот файл частью гейта: каждая задача
дописывает сюда свои числа, а «улучшение» без строки здесь улучшением не считается.
Первые строки уже здесь — заполнены при проверке связности плана 2026-08-14
(`docs/planning/TZ_2026-08-14_execution_order.md`).

## 21. Приёмка первой строки кода (0.5a), замерено ДО правки — 2026-08-15

| # | что | команда | дата | результат |
|---|---|---|---|---|
| 21.1 | Сбор набора **сегодня** | `python -m pytest --collect-only -q` | 2026-08-15 | **703 собрано, 2 ошибки → Interrupted.** Обе — отравление `sys.modules`: `ImportError: cannot import name 'QKeyEvent'` (`tests/ui/test_frame_crop_preview.py:19`) и `QTextLayout` (`test_bundled_fonts.py`) из фальшивого `PySide6` |
| 21.2 | ⭐ Сбор **без отравителя** — это и есть приёмка `0.5a` | `python -m pytest --collect-only -q --ignore=tests/test_stage7_graph_flow.py` | 2026-08-15 | **707 собрано, 0 ошибок, 0.91 с.** Снос одного файла и снимает отравление (Н1), и поднимает счёт |
| 21.3 | Сколько тестов уносит сам файл | `python -m pytest tests/test_stage7_graph_flow.py --collect-only -q` | 2026-08-15 | **24 теста.** Баланс: 703 − 24 своих + 28 оживших в `tests/ui/` = 707 |
| 21.4 | Разрыв дерева с `deploy` (контроль `Н9`) | `git rev-list --count deploy..dev` | 2026-08-15 | **115** (было **110** на 2026-08-14): +5 — четыре коммита фичи `0.1` и один по докам `0.0`. ⚠ Число `Н9` «110 коммитов» протухло, механизм тот же. Правки ревизии в счёт не идут: `docs/planning/` — в `.git/info/exclude`, `tools/tz_lint.py` — untracked |

## 22. Пересъёмка карт ближней очереди TZ §10 (2026-08-15)

| # | что | команда | дата | результат |
|---|---|---|---|---|
| 22.1 | Адреса всех 22 карт §10, переснято по именам символов | воркфлоу `tz10-harden`, 10 агентов | 2026-08-15 | адресов **205**: живых **172**, `УЕХАЛ` **18**, `НЕТ` **11**, `ЧУЖОЙ ФАЙЛ` **4**. Приёмок годных **21 из 21** |
| 22.2 | ⛔ **`1.10`: сколько файлов `ui/` реально логируют** | `find ui -name "*.py" \| wc -l`; `grep -rl "logger\|logging" --include=*.py ui/ \| wc -l` | 2026-08-15 | всего **69**, с `logger`/`logging` — **23**, с `print(` — **1**. ⟹ **без логирования 46 из 69**. План говорит «63 из 69 уже логируют» — неверно (Л69) |
| 22.3 | ⛔ **`Н5в`: чем стадия перекрёстков берёт модель** | `grep -n "junction" configs/projects/thermohydraulics/thermohydraulics.yaml`; `grep -n "junction" docker-compose.yml` | 2026-08-15 | конфиг: `junction_seg.weights: /models/junction_seg/best.pth`. `junction_cnn` — только монтирование `docker-compose.yml:145`, кодом **не читается**. ⟹ пустой каталог `0.12` не роняет (Л70) |
| 22.4 | ⛔ **ADR-004 «~4 ГБ» — чего именно** | `grep -n "4 GB\|VRAM\|GPU RAM" docs/ADR/004-separate-sam2-worker.md` | 2026-08-15 | `:15` «~4 GB **GPU RAM**», `:25` «RTX 4070 Laptop — 8 GB **VRAM**». ⟹ несопоставимо с RSS хоста, который меряет `СТ2`; порог для решения `2.1` A/C назначать от боевого железа (Л71) |
| 22.5 | Приёмка `1.6` — базовая линия «до» | однострочник Qt offscreen из карты `TZ …§10.4` | 2026-08-15 | **`before= resize_node current= resize_node`** — защёлка сработала на второй двери, выход мёртв. Проверено лично, ~2 с, exit 0 |
| 22.6 | Приёмка `1.4` — базовая линия «до» | однострочник Qt offscreen из карты `TZ …§10.5` | 2026-08-15 | **`dx 45.0 50.62 \| bbox_restored False \| rev 0`** — два одинаковых тика 90×90 копят пин (40.0 → 45.0 → 50.62), отказ bbox не восстанавливает. Проверено лично |
| 22.7 | Приёмка `0.0` — базовая линия «до» | `python -m pytest tests/test_stage7_graph_flow.py tests/ui/test_bundled_fonts.py tests/ui/test_frame_crop_preview.py -q` | 2026-08-15 | **2 errors in 0.21s → Interrupted**, exit 2; причина — `ImportError: cannot import name 'QKeyEvent'`, подмена `sys.modules["PySide6"]` на этапе коллекции (`test_stage7_graph_flow.py:39`) |
| 22.8 | Загрязнение корпуса (0.8, КД7) | `ls storage/diagrams/` | 2026-08-15 | **19 каталогов-uid + «Новая папка»** со **второй копией** `8d517a35-…` (`graph_validated.json` 370 523 Б). План говорит «20 uid» (Л73) |

## 23. Точки возврата: чего не было (2026-08-15)

| # | что | команда | дата | результат |
|---|---|---|---|---|
| 23.1 | ⛔ **Тегов в репозитории** | `git tag \| wc -l` | 2026-08-15 | **0.** Ни одной именованной точки возврата за всю историю проекта — подтверждает `Л75` |
| 23.2 | Веток | `git branch -a \| wc -l` | 2026-08-15 | **10**: `dev` (рабочая), `deploy` (бой), `main` (заброшена), `feat/observability`, `fix/routing-buttons`, `pr0/fix-test-infra` + **4 служебных `claude/*`** от worktree-сессий |
| 23.3 | Длина линии, по которой пришлось бы искать дефект | `git rev-list --count deploy..dev` | 2026-08-15 | **115** коммитов одной линией. Без тегов «вернуться к работавшему» = `git bisect` по перемешанной куче |

## 24. Пункт 0.0 дороги — сбор pytest чинится без сноса тестов (2026-08-17)

| # | что | команда | дата | результат |
|---|---|---|---|---|
| 24.1 | Сбор набора ДО правки | `python -m pytest --collect-only -q` | 2026-08-17 | **703 собрано, 2 ошибки → Interrupted**, exit 2: `QTextLayout` (`tests/ui/test_bundled_fonts.py:21`), `QKeyEvent` (`tests/ui/test_frame_crop_preview.py:19`) — оба «from PySide6.QtGui (unknown location)» |
| 24.2 | ⭐ PySide6 сам по себе ИСПРАВЕН — диагноз плана «битый PySide6 в 2 UI-файлах» неверен | `python -c "import PySide6.QtGui as g; print(g.__file__, hasattr(g,'QTextLayout'), hasattr(g,'QKeyEvent'))"` | 2026-08-17 | `…\site-packages\PySide6\QtGui.pyd True True` — оба имени на месте |
| 24.3 | ⭐ Виновник — отравление `sys.modules` на уровне модуля | `python -m pytest --collect-only -q tests/ui` против `… tests/test_stage7_graph_flow.py tests/ui/test_bundled_fonts.py` | 2026-08-17 | в одиночку `tests/ui` — **178 собрано, 0 ошибок**; в паре с отравителем — **1 ошибка**. `test_stage7_graph_flow.py:163` подменял `PySide6*` и 22 модуля `ui.*` НАВСЕГДА, а pytest импортирует все тест-модули на сборке |
| 24.4 | Приёмка (гейт QUEUE) — сбор ПОСЛЕ правки | `python -m pytest --collect-only -q` | 2026-08-17 | **732 собрано, 0 ошибок, 0.91 с**, exit 0. Прирост 703→732: +28 оживших в `tests/ui/` + 1 новый сторож |
| 24.5 | ⭐ Ни один тест не снесён (против §21.2, где приёмка достигалась удалением файла) | `python -m pytest tests/test_stage7_graph_flow.py -q` до и после правки | 2026-08-17 | до: **24 failed**; после: **24 failed**, подпись причин совпадает построчно (11 `ModuleNotFoundError: ui.windows.graph_validation_window` + 11 + 2 `FileNotFoundError`) — правка поведения модуля не меняет |
| 24.6 | ⛔ **Полный прогон ДО правки не доходил до конца** | `python -m pytest -q --continue-on-collection-errors` | 2026-08-17 | **`Windows fatal exception: access violation`** на ~61%, процесс умирал в `tests/test_seat_contract.py:251`: `_editor()` тянет `ui.editors.advanced_graph_editor` в рантайме и получал фальшивый класс из той же заглушки. Числа прогона получить было НЕЛЬЗЯ |
| 24.7 | ⭐ Полный прогон ПОСЛЕ правки доходит до конца | `python -m pytest -q` | 2026-08-17 | **38 failed, 661 passed, 13 skipped, 20 errors, 31.9 с**, аварийного сбоя нет. Это первая полная картина набора — материал для пункта 0.3 |
| 24.8 | Из них падают «сами по себе» (проверено прогоном модуля в одиночку) | `python -m pytest -q <модуль>` для каждого красного | 2026-08-17 | `test_stage7_graph_flow` **24 failed**, `test_refactoring` **12 failed**, `test_refactoring_v2` **1 failed + 9 errors**, `test_cyrillic_load` **1 failed** — итого **47**, к правке отношения не имеют |
| 24.9 | ⛔ **Остаток 11 — та же болезнь, другой файл (НЕ трогал, чужой пункт)** | `python -m pytest -q tests/ui/test_square_size_ops.py` против полного прогона | 2026-08-17 | в одиночку **12 passed**; в полном прогоне **11 errors**: `ValueError: not enough values to unpack` из `cv2.connectedComponentsWithStats`. Корень — `sys.modules.setdefault("cv2", MagicMock())` на уровне модуля в `tests/observability/` (`test_detection_errors.py:20`, `test_ocr_errors.py:26`, `test_postprocess_log_level.py:38`, `test_skeleton_extension_substeps.py:35`); соседи в тех же каталогах делают то же через `monkeypatch.setitem` — правильно и без утечки |
| 24.10 | `pytest-randomly` в окружении НЕТ — порядок сбора детерминирован (алфавитный) | `python -m pip list \| grep -i "randomly\|pytest"` | 2026-08-17 | `pytest 9.1.1`, `pytest-asyncio 1.4.0`. Прогоны с `-p no:randomly` и без него дали одно и то же (38/661/13/20) |

## 25. Пункт 0.3 дороги — базовая линия набора, маркеры, CI (2026-08-17)

| # | что | команда | дата | результат |
|---|---|---|---|---|
| 25.1 | Полный прогон воспроизведён (сверка с §24.7 перед фиксацией базы) | `python -m pytest -q --tb=no -rEf` | 2026-08-17 | **38 failed, 661 passed, 13 skipped, 20 errors** — счётчики §24.7 сошлись бит-в-бит, список красных снят (58 идентификаторов) |
| 25.2 | ⭐ **Чистое окружение воспроизводит базу точно** — состав зависимостей CI выведен замером, а не догадкой | venv + `pip install -r requirements/api.txt -r requirements/ui.txt -r requirements/dev.txt` + `shapely==2.1.2`, затем `pytest -q --tb=no -rEf` | 2026-08-17 | **38/661/13/20, множество красных ИДЕНТИЧНО** локальному (`diff` пуст). Торч, ultralytics, БД, Docker не нужны — набор действительно CPU-only, как и утверждал план |
| 25.3 | ⛔ **Без `shapely` 22 теста молча исчезают из сбора** (та же дыра, о которой предупреждал ревизор 0.0: «зелёный сбор, усохший набор») | `pytest --collect-only -q` в venv без shapely против venv с shapely | 2026-08-17 | **710 против 732** собранных; уходят целиком `test_avoid_router.py` (9), `test_layout_smoke.py` (9), `test_legal_pairs.py` (4) — `importorskip` в модулях раскладки. Плюс 3 ложных красных в `test_client_spec.py`. `shapely` живёт только в `requirements/worker.txt` |
| 25.4 | ⭐ **Один красный из 58 — артефакт кодировки, а не дефект** | `python -m pytest <тест>` против `python -X utf8 -m pytest <тест>` | 2026-08-17 | `test_refactoring.py::TestDeadCodeRemoved::test_graph_validation_window_imports_from_advanced`: под cp1251 **`UnicodeDecodeError`** (`test_refactoring.py:697` — `read_text()` без `encoding` на файле с кириллицей), под UTF-8 **зелёный**. Стенд принудительно ставит `-X utf8` дочернему pytest, поэтому база одинакова на любой консоли. Сам тест — ЧУЖОЕ, не правил |
| 25.5 | **База зафиксирована в git** | `python -X utf8 tools/suite_baseline.py --write-baseline` | 2026-08-17 | `tools/bench/suite_baseline.json`: **57 красных** (37 failed + 20 errors), собрано **738**, `min_collected` **706**. Разбивка по файлам: `test_stage7_graph_flow` 24, `test_refactoring` 11, `tests/ui/test_square_size_ops` 11, `test_refactoring_v2` 10, `test_cyrillic_load` 1 |
| 25.6 | Гейт зелёный на снятой базе | `python -X utf8 tools/suite_baseline.py --check` | 2026-08-17 | `[OK] новых красных нет`, exit **0**. В чистом venv (без `PYTHONUTF8` у родителя) — тот же результат, exit **0** |
| 25.7 | ⭐ **Гейт доказан инъекцией: новый красный ловится** | добавлен `tests/test_zz_injection_probe.py` с `assert False` → `--check` → файл удалён | 2026-08-17 | exit **1**, `[ПРОВАЛ] новые красные (1) + tests/test_zz_injection_probe.py::test_injected_regression`. Дерево восстановлено |
| 25.8 | ⭐ **Сторож усыхания доказан инъекцией** | во `collect_ignore` временно добавлен каталог `ui` → `pytest tests/test_collection_clean.py::test_suite_does_not_shrink` → conftest восстановлен | 2026-08-17 | **КРАСНЫЙ**: `assert 560 >= 706`. Без нижней границы этот сценарий проходил бы зелёным (exit-код сбора остаётся 0) |
| 25.9 | Маркер `ui` (авто, по импорту PySide6 в модуле) | `pytest --collect-only -q -m ui` / `-m "not ui"` | 2026-08-17 | **315** из 738 помечены `ui`, 423 без Qt. Файлов с импортом PySide6 — 22 (в `tests/ui/` 14, вне — 8), ни один не правился: маркер ставится в `conftest.py` |
| 25.10 | `--strict-markers` включён и сбор не сломал | `pytest --collect-only -q` | 2026-08-17 | **738 собрано, 0 ошибок, exit 0** — незарегистрированных маркеров в наборе нет (в ходу только builtin + `asyncio` от pytest-asyncio) |
| 25.11 | ⭐ **CI прогнался и зелёный с первого раза** | `git push origin road/0.3` → `gh run view 32055254327` | 2026-08-17 | windows-latest, **2 мин 24 с**: сбор **722 собрано, 0 ошибок**; `--check` → `[OK] новых красных нет`, exit 0. Красные на раннере совпали с базой идентификатор в идентификатор (37 failed + 20 errors, ни одного нового, ни одного позеленевшего) |
| 25.12 | Разница «раннер против машины разработки» объяснена полностью | `diff` списков `--collect-only` (лог CI против локального) | 2026-08-17 | **722 против 738 = −16**, и все 16 — одна параметризация: `test_canvas_pipeline_golden.py::test_t10_corpus_chain` (17 диаграмм корпуса `storage/` локально против одного пустого набора `[NOTSET]` в чистом клоне). Больше расхождений нет ни одного. `min_collected` 706 ниже обоих чисел |

## 26. Пункт 0.3 — доработка по ревизии: стенд перестал быть зелёным на убитом прогоне (2026-08-17)

| # | что | команда | дата | результат |
|---|---|---|---|---|
| 26.1 | ⛔ **Блокер ревизии воспроизведён на прежнем стенде** | зонд `tests/test_aaa_kill_probe.py` с `os._exit(77)` → `tools/suite_baseline.py --check` (версия 7904c18) | 2026-08-17 | «собрано 747, **{}**», «[позеленело] 57», **exit 0** — гейт зелёный при убитом прогоне |
| 26.2 | ⭐ **После доработки тот же зонд валит гейт** | тот же зонд → `python -X utf8 tools/suite_baseline.py --check` | 2026-08-17 | **exit 1**, сработали три независимые проверки: `pytest вернул 77` (штатные 0/1), «не разобрал итоговую строку», «позеленело сразу 57 (порог 10)». Зонд удалён, `git status` чист |
| 26.3 | ⭐ **Утечка `PYTEST_ADDOPTS` — реальный сценарий ложного зелёного, замерена** | `PYTEST_ADDOPTS="-x" tools/suite_baseline.py --check`, версия 7904c18 против доработанной | 2026-08-17 | 7904c18: прогон обрезан на первом падении (**1 failed, 239 passed**), «[позеленело] **56**», **exit 0**. Доработанная: окружение вычищено, прогон полный (**37F/676P/13S/20E**), exit 0 корректно; даже без чистки её остановил бы порог `MAX_FIXED` |
| 26.4 | Утечка `-k` на прежнем стенде давала не ложный зелёный, а ложный диагноз | `PYTEST_ADDOPTS="-x -k test_nothing_matches_here"`, версия 7904c18 | 2026-08-17 | «сбор pytest сломан (exit 5) — это пункт 0.0», exit 1: сбор с фильтром возвращает 5, и стенд обвинял чужой пункт |
| 26.5 | ⭐ **Самотест уровня дефекта падал бы ДО правки** (Д1) | `pytest tests/test_suite_baseline.py::test_killed_run_is_a_failure_not_a_green_gate` на версии 7904c18 стенда | 2026-08-17 | **КРАСНЫЙ**: `assert 0 == 1` — ровно симптом ревизии (`cmd_check` вернул 0). На доработанной версии зелёный |
| 26.6 | Нежадный парсер id: пробелы и кириллица в идентификаторе | `pytest tests/test_suite_baseline.py -q` (юнит на `FAILED ...::test_pair[Новая папка 2/graph.json] - AssertionError`) | 2026-08-17 | старый `\S+` резал id по первому пробелу; новый разбирает целиком. **15 тестов** файла зелёные (было 5, стало 13 + 2 в сторожe сбора) |
| 26.7 | Конверсия «красный → skip» больше не читается как починка | юниты `test_red_converted_to_skip_is_suspected` | 2026-08-17 | признаки порознь законны (тест починили; появился новый skip), вместе на одном прогоне — провал с объяснением |
| 26.8 | Гейт после доработки | `python -X utf8 tools/suite_baseline.py --check` | 2026-08-17 | `[OK] новых красных нет`, exit **0**; набор 746 собрано (было 738: +8 тестов доработки), красные не изменились — 57 |

## 27. Пункт 0.6 дороги — доки, портящие рассуждения: сверка с кодом (2026-08-18)

| # | что | команда | дата | результат |
|---|---|---|---|---|
| 27.1 | ⭐ **Сторож дрейфа доков КРАСНЫЙ до правки** (Д1) | `python -X utf8 -m pytest tests/test_docs_match_code.py -q` (новый файл, доки ещё не правлены) | 2026-08-18 | **4 failed, 9 passed**: `task_detect_yolo` (док обещает 1800, код ставит 5400) + три дока с формулой «OCR по умолчанию выключен» (`CLAUDE.md:21`, `DEPLOY_README.md:153`, `ROADMAP_techdebt_and_agent_team.md:12`) |
| 27.2 | Таймаут детекции в коде | `grep -rn "time_limit" worker/tasks/*.py worker/celery_app.py` | 2026-08-18 | `detection.py:56-57` — **5400 / 5340**; поднято коммитом `a1e0e14` (2026-06-22). Док `WORKER_TASKS.md:193-194` обещал 1800/1740 — занижение **втрое**, как и говорил план |
| 27.3 | ⭐ **Ловушка аудита подтверждена замером: остальные 1800/1740 — правда** | тот же сторож, 9 зелёных параметров | 2026-08-18 | `WORKER_TASKS.md:249-250` (skeleton) ↔ `skeleton.py:85-86` и `:322-323` (graph) ↔ `graph.py:130-131` — **совпадают**. Также сошлись segmentation 3600/3540, skeleton_simple 600/540, junction 1200/1140, ocr 3600/3300, contours 600/540, fxml 300/270, layout 1800/1500 (константы `layout.py:54-55`). Правился ТОЛЬКО блок detection |
| 27.4 | `ocr.enabled` в боевом конфиге | `grep -rn "enabled" configs/projects/thermohydraulics/*.yaml` + `git show deploy:configs/.../thermohydraulics.yaml` | 2026-08-18 | `thermohydraulics.yaml:276` → **`enabled: true`** (коммит `5c6d3f4`, 2026-07-01); на ветке `deploy` то же самое (там `:275`). Второго конфига проекта с секцией `ocr:` нет — у `tec_boiler` только `domain_profile.yaml` |
| 27.5 | Дефолт кода — тоже «включено» (fail-open) | чтение `app/api/validation.py:675`, `app/services/project_loader.py:245,475` | 2026-08-18 | `getattr(_pc_ocr.ocr, "enabled", True)`; `OcrConfig.enabled: bool = True` и `ocr_data.get("enabled", True)` — при отсутствии ключа OCR **включён** |
| 27.6 | ⭐ **Сторож зелёный после правки** (гейт пункта) | `python -X utf8 -m pytest tests/test_docs_match_code.py -q` | 2026-08-18 | **13 passed**, 0.10 с |
| 27.7 | Числа §4 TESTING.md пересняты | `wc -l` по девяти документированным файлам | 2026-08-18 | расходилась одна строка: `test_stage7_graph_flow.py` **514 → 543** (правка 0.0). Остальные восемь сошлись точно (714/382/171/308/370/330/196/110) |
| 27.8 | Указатель §6 TESTING.md протух | чтение `tests/test_stage7_graph_flow.py` | 2026-08-18 | `_stub_qt()` теперь `:35`, ставится фикстурой `scope="module", autouse=True` со снятием за собой (`:259-271`); в доке стояло `:163` от прежней, отравлявшей версии |
| 27.9 | ⛔ **Док §6 приводил в образец файл, нарушающий собственный запрет** | `grep -n "setdefault\|sys.modules" tests/observability/test_postprocess_log_level.py` | 2026-08-18 | `:38` — `sys.modules.setdefault("cv2", MagicMock())` на уровне модуля (очаг из §24.9). Из списка образцов убран, отклонение помечено с адресом пункта 0.3x; сам тест НЕ трогал — чужой пункт |
| 27.10 | Базовый набор не сдвинут | `python -X utf8 tools/suite_baseline.py --check` | 2026-08-18 | `[OK] новых красных нет`, exit **0**; собрано **759** (было 746: +13 тестов сторожа), красных по-прежнему **57** |
| 27.11 | Сбор чист | `python -m pytest --collect-only -q` | 2026-08-18 | **759 собрано, 0 ошибок**, exit 0 |
| 27.12 | ⛔ **`CLAUDE.md` и `docs/planning/` — ВНЕ git** (`.git/info/exclude:9-11`), значит правки 0.6 в них живут только на этой машине, а в CI этих файлов нет | `git ls-files --error-unmatch CLAUDE.md` / `git check-ignore -v CLAUDE.md` | 2026-08-18 | `did not match any file(s) known to git`; исключено локальным `exclude`. В коммит уходят только `DEPLOY_README.md`, `docs/TESTING.md`, `docs/WORKER_TASKS.md`, `tests/test_docs_match_code.py` |
| 27.13 | ⭐ **Сторож CI-безопасен: отсутствующий док пропускается, а не краснеет** (инъекция) | `mv CLAUDE.md CLAUDE.md.bak` → `pytest tests/test_docs_match_code.py -q -rs` → возврат | 2026-08-18 | **12 passed, 1 skipped** («CLAUDE.md нет в этом клоне»). Дерево восстановлено |
| 27.14 | ⭐ **CI зелёный на коммите пункта** | `git push origin dev` → `gh run view 32073477370` | 2026-08-18 | windows-latest, 2 мин: собрано **743** (было 730 = +13 тестов сторожа), `[OK] новых красных нет`, exit 0. Арифметика сходится: 13 = **11 passed + 2 skipped** (`CLAUDE.md` и `ROADMAP_*` в чистом клоне отсутствуют), passed 659→670, skipped 14→16, красные без изменений |

## 28. Пункт ВН1а дороги — `napravlenie`: состояние фичи сверено с кодом и артефактами (2026-08-18)

| # | что | команда | дата | результат |
|---|---|---|---|---|
| 28.1 | ⛔ **Главное утверждение ВН1 устарело: дефект уже починен** | `grep -n "napravlenie" worker/tasks/segmentation.py app/api/validation.py` | 2026-08-18 | `napravlenie` исключён из `node_mask` в **обоих** генераторах: `segmentation.py:42-47,173-191` и `validation.py:203-209`. Пришло коммитом `a1e0e14` (2026-06-22), на ветке `deploy` то же самое (`git show deploy:` обоих файлов) |
| 28.2 | Фича внедрена целиком, а не частично | чтение `worker/tasks/direction.py`, `modules/direction_classifier/classifier.py`, `app/api/segmentation.py:96-100`, `modules/graph/core/direction_nodes.py`, `alembic/versions/0006_*` | 2026-08-18 | есть инференс (`DirectionClassifier.predict_batch`), worker-шаг (600/540 с, `StageType.DIRECTION_CLASSIFICATION`), цепочка `chain(direction → segment)`, обработка узлов в графе, треугольник в FXML (`graph_to_fxml.py:1173,1257-1262`), миграция `0006`. План 2026-06 обещал «инференса НЕТ» — устарело |
| 28.3 | ⭐ **Фича отработала на реальных данных** (17 схем в `storage/`) | скрипт по `storage/diagrams/*/detection/coco_validated.json` и `*/graph/graph.json` | 2026-08-18 | схем с `napravlenie` — **16 из 17**; боксов **722**, из них с `attributes.direction` — **721 (99.9 %)**; direction-узлов в графах — **721**. Значит §3.1 чек-листа выполняется уже сегодня, прогон ВН1б на него тратить не нужно |
| 28.4 | ⛔ **Рёбра с `direction` — 0 из 16 схем** | тот же скрипт + `grep -rn "apply_direction_rules\|stitch_collinear_stubs" modules/ worker/ app/ ui/ tests/ tools/` | 2026-08-18 | ни одного ребра с полем `direction`. Причина найдена: `apply_direction_rules` и `stitch_collinear_stubs` (`direction_nodes.py:295,416`) **не вызываются из `builder.build`** (`builder.py:374-407` — их место занял разбор оси на трассировке), а это единственное место, где ставится `e["direction"]`/`flow_role`. Живы только в `tests/test_direction_nodes.py` (6 тестов из 13). Критерий приёмки «два ребра с direction» сегодня недостижим |
| 28.5 | Степени direction-узлов: ожидание плана данными не подтверждается | тот же скрипт | 2026-08-18 | из 721 узла `pass_through: true` — **63 (8.7 %)**, `degree == 1` — **525**, `degree == 0` — **133**. План ожидал «степень обычно 2». Норма это или обрыв трубы под боксом — различает только осмотр схемы (вход в ВН1б) |
| 28.6 | Модель узла отклонилась от плана | чтение `modules/graph/core/export_json.py:100-113`, замер типов | 2026-08-18 | тип узла остался **`equipment`** (у всех 721), направление — флаг `direction_node` + `flow_axis`/`flow_direction`/`pass_through`. Проверки чек-листа v1.0 по `type == "direction"` и id `dirnode_*` дали бы **ложный ноль** — в v2.0 исправлены |
| 28.7 | Поле `direction_classification.device` в конфиге мертво | чтение `worker/tasks/direction.py:188-194`, `worker/utils/device.py` | 2026-08-18 | устройство берётся из `resolve_device()` (переменная `PID_DEVICE`), `dc_cfg.device` не читается нигде. Для CPU-only сервера это хорошая новость: `device: "cuda"` в YAML ничего не ломает |
| 28.8 | Эндпоинт запуска в чек-листе был неверен | `grep -n "include_router" app/main.py:87`, `app/api/segmentation.py:39` | 2026-08-18 | реальный путь — `POST /api/segmentation/{uid}/segment`, в доке стоял `/api/diagrams/<UID>/segment` |
| 28.9 | Юнит-тесты графа зелёные | `python -X utf8 -m pytest tests/test_direction_nodes.py -q` | 2026-08-18 | **13 passed** за 0.13 с (чек-лист обещал «5/5» — устарело). Файл по-прежнему запускается и напрямую (`__main__`-блок) |
| 28.10 | ⭐ **Сторож дрейфа красный при возврате дефекта** (Д1) | инъекция: из `app/api/validation.py:209` убран `"napravlenie"` → `pytest tests/test_docs_match_code.py -q` → `git checkout --` | 2026-08-18 | **1 failed, 15 passed**: `test_node_mask_generators_exclude_napravlenie[app/api/validation.py]`. Дерево восстановлено (`git diff` по файлу пуст) |
| 28.11 | Сторож зелёный на HEAD (гейт пункта) | `python -X utf8 -m pytest tests/test_docs_match_code.py -q` | 2026-08-18 | **16 passed** (было 13: +2 параметра генераторов масок, +1 сверка списка классов доки с конфигом) |
| 28.12 | Базовый набор не сдвинут | `python -X utf8 tools/suite_baseline.py --check` | 2026-08-18 | `[OK] новых красных нет`, exit **0**; собрано **762** (было 759: +3 теста), красных по-прежнему **57** (37 failed + 20 errors) |
| 28.13 | Сбор чист | `python -X utf8 -m pytest --collect-only -q` | 2026-08-18 | **762 собрано**, 0 ошибок |

## 29. Пункт 0.9 дороги — ДН1 (цена схемы) и ДН2 (что оператор правит), 2026-08-18

**Среда ДН1:** локальная БД `pid_postgres` (docker, `postgres:15-alpine`, порт 5433) 🖥 —
это база **машины разработки**, а не боевого сервера; отделить работу оператора от
перезапусков во время разработки в ней нечем. Все запросы — только чтение,
файл `tools/dn1_stage_cost.sql`, запуск:
`docker exec -i pid_postgres sh -c 'psql -U "$POSTGRES_USER" -d "$POSTGRES_DB" -P pager=off -f -' < tools/dn1_stage_cost.sql`

| # | что | команда | дата | результат |
|---|---|---|---|---|
| 29.1 | Объём данных ДН1 | `select count(*) from diagrams` / `processing_stages` | 2026-08-18 | диаграмм **67**, строк стадий **966** по **63** uid, от 2026-04-07 до 2026-08-13; до `fxml_generation` дошли **20** |
| 29.2 | ⭐ **Машинная цена одной схемы** | `dn1_stage_cost.sql` §1–2 | 2026-08-18 | p50 машинного времени **484 с** (≈8 мин) по 20 схемам полного конвейера; по всем 51 схеме со стадиями — **140 с**. Это время машины разработки; боевой сервер CPU-only, там будет больше |
| 29.3 | ⛔ **Полное время («цена схемы» из плана) на этих данных не считается** | там же, колонка `idle_pct` | 2026-08-18 | простой = **99.9 %** времени жизни схемы, p50 простоя **325 084 с ≈ 3.8 суток**. Разрыв здесь — не «работал человек», а «вернулись к схеме через неделю». Посылка ДН1 «разрыв между стадиями = время оператора» на единственной доступной БД **не работает** |
| 29.4 | ⭐ **Стыки, между которыми стоит человек** (разрывы > 4 ч отброшены отдельной колонкой) | `dn1_stage_cost.sql` §5 | 2026-08-18 | правка масок/перекрёстков `junction_classification→final_skeletonization` p50 **4509 с** (n=10, ещё 10 за отсечкой) · ручная правка холста `layout→fxml_generation` p50 **745 с** (n=6, за отсечкой 0) · проверка графа `graph_building→contour_extraction` p50 **222 с** (n=43, за отсечкой 15) · боксы в CVAT `detection→cvat_validation` p50 **71 с** (n=29, за отсечкой 2) |
| 29.5 | Сшивать стадии через `max(A)/min(B)` нельзя | сравнение двух версий §5 | 2026-08-18 | при 27 попытках на стадию поздний перезапуск A уводит разрыв в минус: у пары `layout→fxml_generation` **0 из 5** наблюдений выходили положительными, у `junction→final_skeletonization` — **0 из 42**. Построчная сшивка («ближайшее предшествующее завершение A») даёт 6 и 20 наблюдений соответственно |
| 29.6 | ⭐ **`cvat_validation.duration_seconds` — не время оператора** (утверждение плана подтверждено) | `dn1_stage_cost.sql` §5a против §5 | 2026-08-18 | p50 длительности строки **2.3 с** против реального ожидания **71 с** — занижение в **31 раз**. Строка открывается, когда оператор уже нажал подтверждение |
| 29.7 | ⛔ **Три стадии не пишутся в таблицу вообще** | `dn1_stage_cost.sql` §6 | 2026-08-18 | `upload`, **`mask_validation`**, **`graph_validation`** — ноль строк за всю историю. То есть две из четырёх «ручных» стадий, которые `app/api/stats.py:40-45` исключает из p50, в данных не существуют; их человеческое время измеримо **только** разрывом |
| 29.8 | Половина машинного времени — перезапуски | `dn1_stage_cost.sql` §7 | 2026-08-18 | из 937 завершённых строк **491 — attempt > 1**; **14 358 с из 27 353 с (52 %)** машинного времени съедено повторами; максимум **27 попыток** на одну стадию |
| 29.9 | Самая дорогая стадия — OCR | `dn1_stage_cost.sql` §4 | 2026-08-18 | по всем попыткам **10 177 с из 27 353 (37 %)**, по последней попытке **5 191 из 14 031 (37 %)**, p50 **66.1 с**. Далее `graph_building` 6 057 с (p50 41.4), `contour_extraction` 3 029 с, `segmentation` 2 258 с, `detection` 2 045 с |

**ДН2.** Корпус — 20 файлов `graph_edited*.json` в корне репо (локальные сохранения вкладки
«Ручная правка», 2026-07-31…08-02) 🖥. Инструмент `tools/dn2_edit_diff.py`, запуск:
`python -X utf8 tools/dn2_edit_diff.py`

| # | что | команда | дата | результат |
|---|---|---|---|---|
| 29.10 | Состав корпуса — числа плана подтверждены | `dn2_edit_diff.py`, группировка по `canvas_transform.source_sha` | 2026-08-18 | **20 файлов, 2 группы**: `0149414beed7e4ca` — **17 файлов** (141 узел / 130 рёбер), `a2d8b989d1b159e6` — 3 файла (167/162). `operator_saved=true` ровно у **13**. План обещал «17 — один граф, у 13 operator_saved» — сошлось |
| 29.11 | ⭐ **Исходник группы из 17 на диске отсутствует** (оговорка плана подтверждена) | `graph_projection_sha` по `storage/diagrams/*/graph/graph_validated.json` и `graph_canvas.json` | 2026-08-18 | `a2d8b989…` найден — это **uid `13d1ef5f`**; `0149414b…` не встречается **ни в одном** из 17 `graph_validated` и 15 `graph_canvas`. Значит для 17 файлов дифф возможен только попарный, «от оригинала» — нет |
| 29.12 | Глобального сдвига холста в корпусе нет | `canvas_transform.s/offx/offy/canvas/orig_image_size` по всем 20 | 2026-08-18 | **один-единственный** набор `(0.30716723549488056, 196.99658703071668, 0.0, [1920,1080], [3516,4968])` — перемещения узлов в диффах не загрязнены панорамированием |
| 29.13 | ⭐ **ЧТО оператор правит: 2/3 — перемещение узла** | `python -X utf8 tools/dn2_edit_diff.py` | 2026-08-18 | 18 пар, 405 сырых изменений, из них **намерений оператора 144**: перемещение узла **96 (66.7 %)**, самостоятельная посадка конца ребра **37 (25.7 %)**, изменение размера **7 (4.9 %)**, добавление ребра **2 (1.4 %)**, удаление ребра **2 (1.4 %)**. **Ноль** добавлений/удалений узлов, **ноль** перецепок ребра, **ноль** смен типа |
| 29.14 | То же без пар через границу `layout_version` | там же, вторая сводка | 2026-08-18 | 16 пар, 374 изменения, **133 намерения**: перемещение **91 (68.4 %)**, конец ребра **31 (23.3 %)**, размер **7 (5.3 %)**, +ребро **2**, −ребро **2**. Вывод не меняется |
| 29.15 | ⛔ **Сырые доли врут втрое** — следствие считалось бы правкой | там же, колонки `e_end_flw` / `e_route` | 2026-08-18 | 136 переездов конца **вслед за своим узлом** и 125 пересчётов `waypoints` (кэш маршрута, `docs/DATA_FORMATS.md` §«Пины входа») — это следствия одного действия. Без их отделения «правки концов» дали бы 42.7 % и вышли бы на первое место вместо перемещения узлов |
| 29.16 | ⭐ **Независимая сверка на боевых холстах: оператор создаёт связность** | скрипт по `storage/diagrams/*/graph/graph_canvas.json` с `operator_saved=true` | 2026-08-18 | 10 холстов: узлов 2980, из них `manual` **152 (5.1 %)**; рёбер 3174, из них `manual` **403 (12.7 %)**; manual есть **во всех 10**. Классы созданных узлов: **connector 141**, `napravlenie` 9, оборудование **2**. То есть руками добавляют точки соединения и рёбра, а не оборудование |
| 29.17 | ⛔ **Корпус ДН2 — рабочий вход живого судьи** (вход в 0.2) | `tools/edit_bench.py:132` + ключи `tools/bench/edit_baseline.json` | 2026-08-18 | `edit_bench --all` берёт корпус глобом `REPO/graph_edited*.json`, в эталоне лежат **12** из этих 20 имён. `python -X utf8 tools/edit_bench.py --all --check` → «рост дефектов: 0», exit **0**. Убрать файлы из корня, не перенеся корпус и не пересняв эталон, — значит обнулить судью «Ручной правки» |
| 29.18 | Производитель файлов — тот, что назван планом | чтение `ui/editors/base_graph_editor.py:283-286` | 2026-08-18 | `QFileDialog.getSaveFileName(self, "Сохранить граф", "graph_edited.json", …)` без каталога → диалог открывается в CWD, у клиента это корень репо. План указывал `:282-289` — в допуске |
| 29.19 | Базовый набор не сдвинут (гейт) | `python -X utf8 tools/suite_baseline.py --check` | 2026-08-18 | `[OK] новых красных нет`, exit **0**; собрано **768** (было 762: +6 тестов классификатора), красных по-прежнему **57** (37 failed + 20 errors) |
| 29.20 | Сбор чист | `python -X utf8 -m pytest --collect-only -q` | 2026-08-18 | **768 собрано**, 0 ошибок |
| 29.21 | ⭐ **CI зелёный на коммите пункта** | `git push origin dev` → `gh run view 32080563001` | 2026-08-18 | windows-latest, 1м56с: собрано **752** (было 746 = +6 тестов классификатора), `[OK] новых красных нет`, exit 0. Арифметика сходится: passed 673→**679** (+6), skipped 16 без изменений, красные без изменений |

---

## 30. Пункт 0.2 дороги — корень репо очищен, производитель файлов починен (2026-08-18)

**Среда:** ветка `dev`, Windows 10, CPython 3.11.9, Qt offscreen. Гейт пункта —
`python -X utf8 tools/edit_bench.py --all --check` (судья «Ручной правки») плюс базовый
набор. Коммиты: `92d759e` (правка + перенос), `e0d7ec7` (пересъём эталона, Д6).

| # | что | команда | дата | результат |
|---|---|---|---|---|
| 30.1 | Состав уборки | `ls graph_edited*.json \| wc -l` в корне; сумма размеров | 2026-08-18 | **20 файлов**, 2.79 МБ; плюс 3 лог-файла (`wave14_smoke.log` 779 КБ, `wave2_dbg.log`, `wave2_dbg2.log`) и `.~lock.*#` |
| 30.2 | ⛔ **Расхождение с QUEUE/аудитом: `wave14_smoke.log` НЕ в индексе** | `git ls-files --error-unmatch wave14_smoke.log`; `git log --all -- wave14_smoke.log`; `git check-ignore -v` | 2026-08-18 | `error: pathspec … did not match any file(s) known to git`; история **пуста во всех ветках**; файл закрыт правилом `.gitignore:43 *.log`. `git rm` из подсказки пункта выполнить нечем — действие пустое. То же у `.~lock.…xlsx#` (закрыт `.git/info/exclude:23`) |
| 30.3 | Производитель подтверждён на живом пути | чтение `ui/editors/base_graph_editor.py:282-295`, `:1576`; греп `QShortcut\|Key_S` по `ui/tabs/base_graph_tab.py`, `ui/windows/graph_validation_window.py`, `ui/widgets/diagram_workspace.py` | 2026-08-18 | `save_graph()` без пути → `QFileDialog` с ОТНОСИТЕЛЬНЫМ `"graph_edited.json"` → диалог в CWD. Единственный обработчик Ctrl+S в клиенте — `keyPressEvent:1576` самого редактора: **QShortcut на Ctrl+S нигде нет** (0 попаданий), кнопка 💾 идёт другим методом (`_save_graph` → `editor.save_graph(<путь>)`). То есть Ctrl+S оператора — это ровно тот диалог |
| 30.4 | ⭐ **Судья пережил переезд на НЕТРОНУТОМ эталоне** | `python -X utf8 tools/edit_bench.py --all --check` после `mv` (эталон ещё старый) | 2026-08-18 | exit **0**, «рост дефектов: 0»; 20 замеров, 12 сверок с базой, 8 «в базе нет» — как и до переноса (§29.17). Сильнейшее из возможных доказательств: числа сравнивались с базой, замороженной ДО переезда |
| 30.5 | Перенос без потери данных | `sha256sum graph_edited*.json` до и после `mv` | 2026-08-18 | **20/20 совпали бит-в-бит**; корень пуст (`ls graph_edited*.json` → «No such file»), корпус — `tools/bench/edit_corpus/` |
| 30.6 | Потребители корпуса найдены грепом, а не памятью | греп `graph_edited` с фильтром на чтение файлов по `tests tools modules app ui worker` | 2026-08-18 | **три** глоба в корень: `tools/edit_bench.py:132`, `tools/edit_render_probe.py:198`, `tools/dn2_edit_diff.py:224` (дефолт `--dir`). План называл только первый. Плюс примеры запуска в `drag_stress_probe.py:8,16`. Боевой код (`app/`, `worker/`, `ui/`, `modules/`) корпус не читает — только упоминает в комментариях-репро |
| 30.7 | ДН2-инструмент 0.9 на новом месте даёт прежний состав | `python -X utf8 tools/dn2_edit_diff.py` | 2026-08-18 | «файлов 20, групп по `source_sha` 2», группа `0149414b…` — 17 файлов / 141 узел / 130 рёбер. Сошлось с §29.10 |
| 30.8 | Д1 — тест уровня дефекта КРАСЕН до правки | `python -X utf8 -m pytest tests/ui/test_save_dialog_dir.py -q` на коде `2b6302d` (тест поверх, правка не применена) | 2026-08-18 | **2 failed**: «диалогу отдан относительный путь `'graph_edited.json'` → он откроется в CWD»; второй — предложенный каталог `.` вместо запомненного. После правки — **2 passed** за 0.30 с |
| 30.9 | Пересъём эталона (Д6) — только добавление | `--all --write-baseline`, сверка с `HEAD:tools/bench/edit_baseline.json` по значениям | 2026-08-18 | ключей **12 → 20**, изменённых старых значений — **0** (128 вставок, 0 удалений). Добавлены `971`, `_222222`, `_av`, `_fix`, `_fix2`, `_fix3`, `_line`, `_size` — сохранения 2026-08-01/02, которые в эталон не попадали никогда. После пересъёма `--check` → exit **0** |
| 30.10 | Базовый набор не сдвинут (гейт) | `python -X utf8 tools/suite_baseline.py --check` | 2026-08-18 | `[OK] новых красных нет`, exit **0**; собрано **770** (было 768: +2 теста пункта), красных по-прежнему **57** (37 failed + 20 errors) |
| 30.11 | Сбор чист | `python -X utf8 -m pytest --collect-only -q` | 2026-08-18 | **770 собрано**, 0 ошибок |
| 30.12 | ⭐ **CI зелёный на финальном коммите** | `git push origin dev` → `gh run view 32083346003` | 2026-08-18 | windows-latest, **2м11с**, вывод success: сбор без ошибок + `[OK] новых красных нет`. Собрано **754** (было 752 на `2b6302d`: +2 теста пункта), passed 681 (было 679), skipped 16 и красные — без изменений |

---

## 31. Пункт 0.8 дороги — корпус-фикстура в git и загрузчик КД7 (2026-08-18)

**Среда:** ветка `dev`, Windows 10, CPython 3.11.9. «Чистый клон» ниже — `git worktree`
рабочей копии: в нём есть только отслеживаемые файлы, то есть ровно то, что видит раннер CI
(`storage/` под `.gitignore:32` туда не попадает).

| # | что | команда | дата | результат |
|---|---|---|---|---|
| 31.1 | Состав корпуса и выбор трёх | `for f in storage/diagrams/*/graph/graph_validated.json; do stat -c%s; done \| sort -n` | 2026-08-18 | **17 файлов**, от 53 902 до 713 272 байт. Три самых мелких — `d74eb9f1` 53 902, `089feca2` 58 374, `6e7144d5` 93 771, то есть **53 + 57 + 92 КБ**: сошлось с планом до килобайта |
| 31.2 | Адреса плана по коду | `python tools/layout_bench.py --uid d74eb9f1 --no-text`; разбор json | 2026-08-18 | `d74eb9f1` — **66 узлов**, 63 ребра, **0.1 с** (план: «66 узлов, пересобирается байт-в-байт за 0.1 с» — верно). `.gitignore:32 storage/diagrams/*`, единственная зафиксированная фикстура была `tests/fixtures/layout/synth_med.json` — тоже верно |
| 31.3 | Копия — байт-в-байт | `sha256sum` источника и фикстуры | 2026-08-18 | **3/3 совпали**: `988e673e…`, `7f20011f…`, `94953266…` (таблица — `tests/fixtures/graph/README.md`) |
| 31.4 | Сбор на машине разработки | `python -X utf8 -m pytest --collect-only -q` | 2026-08-18 | **770 → 775**: +5 тестов загрузчика. Корпусных параметров по-прежнему **17** (идентификаторы не изменились: фикстура и storage дают один и тот же `uid8`) |
| 31.5 | ⭐ **Сбор в чистом клоне (условия CI)** | `git worktree add … HEAD` → `pytest --collect-only -q` до и после переноса правок | 2026-08-18 | **754 → 761**. Главное не число: корпусных проверок в CI исполнялось **ноль** (`test_t10_corpus_chain` собирался ОДНИМ пустым параметром `[NOTSET]` и скипался), стало **три реально исполняемых** — `[089feca2]`, `[6e7144d5]`, `[d74eb9f1]` |
| 31.6 | Разрыв «локально ↔ чистый клон» | 775 − 761 против 770 − 754 | 2026-08-18 | **16 → 14**. Остаток — 14 графов корпуса, которых в git нет и не будет (данные заказчика, 4 МБ); он и задаёт нижнюю границу запаса пола |
| 31.7 | ⭐ **Стенд раскладки на чистом клоне: до и после** | в worktree: `python tools/_lb_head.py --no-text` (версия HEAD) против `python tools/layout_bench.py --no-text` (версия пункта) | 2026-08-18 | ДО — **8 из 8 «ПРОПУЩЕН — входа нет»**, стенд на чистом клоне бесполезен. ПОСЛЕ — `6e7144d5: 14 -> 0`, `d74eb9f1: 0 -> 0`, регрессий нет; остальные шесть по-прежнему пропущены. Числа **совпадают с машиной разработки** — стенд стал воспроизводимым вне этой машины |
| 31.8 | Поведение стенда на машине разработки не изменилось | `python -X utf8 tools/layout_bench.py --no-text` до правки (весь корпус) и `--uid d74eb9f1 --uid 6e7144d5 --uid 89ca7583 --uid 5137af27` после | 2026-08-18 | Совпало по всем колонкам: `d74eb9f1 0 -> 0`, `6e7144d5 14 -> 0`, `89ca7583 36 -> 2`, `5137af27` пропущен (сырой вход потерян, Э0). Загрузчик отдаёт фикстуру вместо storage — файлы бит-в-бит одинаковы (31.3), поэтому результат тот же |
| 31.9 | Пол базовой линии пересжат (зам. №3 ревизии 0.3) | правка `FLOOR_MARGIN` в `tools/suite_baseline.py` + пересъём базы | 2026-08-18 | Запас **32 → 18** = разрыв 14 (31.6) + 4. Теперь пол ниже 22 тестов раскладки, которые молча уходят без `shapely`: раньше эта потеря на машине разработки не ловилась вовсе (738 − 22 было выше пола 706), теперь валит пол на обеих машинах |
| 31.10 | Байты пережили git (ловушка `.gitattributes`) | `git show :tests/fixtures/graph/<uid8>.json \| sha256sum` + sha256 в свежем worktree | 2026-08-18 | Общее правило `*.json text eol=lf` переписывало CRLF исходников в LF — sha256 в индексе разошёлся с файлом (`4535ce59…` против `988e673e…`). Закрыто правилом `.gitattributes:25` `tests/fixtures/graph/*.json -text`: **3/3** в индексе и в свежем клоне совпали с таблицей README |
| 31.11 | Пересъём базы отдельным коммитом (Д6) | `python -X utf8 tools/suite_baseline.py --write-baseline`, сверка с `HEAD:tools/bench/suite_baseline.json` | 2026-08-18 | собрано 746 → **775**, passed 676 → 705, пол 714 → **757**. Множество красных **не изменилось**: 57 (37 failed + 20 errors), ушедших 0, новых 0 |
| 31.12 | ⭐ **Новый пол ловит потерю, которую старый не ловил** | зонд: `collect_ignore` на `tests/test_seating.py` (23 теста ≈ 22 без `shapely`) → `pytest tests/test_collection_clean.py` | 2026-08-18 | сбор 775 → 752, сторож **КРАСНЫЙ**: `assert 752 >= 757`. Со старым полом 714 тот же зонд был бы зелёным (752 > 714) — это и есть зам. №3 ревизии 0.3. Дерево после зонда восстановлено (`git checkout --`, сбор снова 775) |
| 31.13 | Пол не даёт ложного красного в чистом клоне | в worktree на коммите пункта: `pytest tests/test_collection_clean.py -q` | 2026-08-18 | **2 passed** (собрано 761 ≥ пола 757, запас 4) |
| 31.14 | ⭐ **CI зелёный на финальном коммите** | `git push origin dev` → `gh run view 32109234907` | 2026-08-18 | windows-latest, success. Собрано **761** — ровно то число, что предсказал замер в worktree (было 754); `[OK] новых красных нет`, красных 57 без изменений (37 failed + 20 errors). Арифметика сходится точно: passed 681 → **688** = +3 корпусные проверки +4 теста загрузчика; skipped 16 → 16 (ушёл пустой параметр `[NOTSET]`, пришёл скип сверки фикстуры с источником — storage на раннере нет). Пол 757 ложного красного не дал: 761 ≥ 757 |

## 32. Пункт 0.10 дороги — детерминизм раскладки (ПР1) и конфиг линтеров (ПР5), 2026-08-18

**Среда:** ветка `dev`, Windows 10, CPython 3.11.9, системный `python`. Зонды пункта —
разовые скрипты в скрэтчпаде сессии (не в репозитории); всё, что осталось гейтом, лежит
в `tools/layout_determinism.py` и `tools/lint_gate.py`.

### 32а. Проверка утверждений пункта по коду

| # | что | команда | дата | результат |
|---|---|---|---|---|
| 32.1 | Конфига линтеров нет ни одного (утверждение плана) | наличие `pyproject.toml`, `ruff.toml`, `mypy.ini`, `setup.cfg`, `.pre-commit-config.yaml` | 2026-08-18 | **ни одного**, при `ruff>=0.2.0` и `mypy>=1.8.0` в `requirements/dev.txt`. Установлены ruff **0.16.3**, mypy **2.3.0**. Строка 1.12 от 2026-08-14 подтверждена спустя четыре дня |
| 32.2 | Долг широких `except` | `python -m ruff check --isolated --select BLE001,E722 --statistics app modules worker ui tools tests alembic services client_main.py` | 2026-08-18 | **200 нарушений в 74 файлах**: 198 `BLE001` + 2 `E722`. План называл 215, аудит — «235 vs 215»: величина та же, счёт другой (план считал все `except Exception` грепом, ruff не считает те, что уже помечены `# noqa: BLE001` — таких 10) |
| 32.3 | Худшие файлы | тот же вывод, `--output-format concise`, группировка по файлу | 2026-08-18 | `ui/tabs/ocr_binding_tab.py` **15**, `ui/widgets/diagram_workspace.py` **12**, `modules/pipe_segmentation/utils/io.py` 8, `tools/check_profile_setup.py` 7. Хвост длинный: 74 файла на 200 нарушений |
| 32.4 | `MEASUREMENTS.md` уже есть и нетрекнут (ловушка аудита) | `git status --porcelain MEASUREMENTS.md`, `git check-ignore -v` | 2026-08-18 | `?? MEASUREMENTS.md`, **573 строки, 31 раздел**, правилами `.gitignore`/`.git/info/exclude` **не** закрыт. Заводить заново было бы потерей — файл закоммичен как есть |
| 32.5 | `cmp_bitexact` действительно бит-в-бит и красный при расхождении | чтение `tools/cmp_bitexact.py:105-137` | 2026-08-18 | `got == ref` по байтам `json.dumps`, `return 1 if failed` — утверждение аудита подтверждено. Работать сегодня он всё равно не может: эталон лежит в `_scratch/`, вне git |

### 32б. ⭐ ПР1: раскладка недетерминирована — где именно

| # | что | команда | дата | результат |
|---|---|---|---|---|
| 32.6 | ⭐ **Раскладка не воспроизводится** | `python -X utf8 tools/layout_determinism.py --uid 6e7144d5 --runs 30` | 2026-08-18 | **2 разных исхода из 30 прогонов** (`6474ccb5695dc1ed` и `484bb015b4e473b8`). Утверждение ПР1 «недетерминирована минимум на одном графе» — **подтверждено**, вердикт ❔ аудита закрыт замером |
| 32.7 | ⭐ **Локализация: виноват этап роутинга** | `python -X utf8 tools/layout_determinism.py --uid 6e7144d5 --runs 30 --no-routing` | 2026-08-18 | **1 исход из 30** (`17ec82e646426c9c`). Расстановка (`axial`) и раздвигание (`spread`) воспроизводимы; расходится только обход чужих форм |
| 32.8 | Хеш-сид ни при чём | `PYTHONHASHSEED=1`, 6 прогонов одного графа подряд | 2026-08-18 | исход **флипнул на шестом** при одном и том же сиде. То есть дело не в обходе множеств строк (первая гипотеза аудита, `spread.py:189`) |
| 32.9 | Наш вход в libavoid детерминирован | зонд: перехват `avoid_router._pin_port`, 20 прогонов `route_graph` в одном процессе | 2026-08-18 | **1 исход из 20** по всем пинам. Питоновская часть — порядок рёбер, порядок форм, выбор портов — повторяется точно |
| 32.10 | ⭐ **Сырой выход libavoid расходится** | зонд: перехват `avoid_router._simplify`, 30 прогонов `route_graph` на ОДНОМ и том же входе в ОДНОМ процессе | 2026-08-18 | сырых исходов **2** (15/15), принятых — тоже **2**. Источник недетерминизма — `vendor/adaptagrams`, а не наш код |
| 32.11 | Адрес объекта `Router` с исходом не коррелирует | зонд: печать `int(router.this)` рядом с исходом, 16 прогонов | 2026-08-18 | один и тот же адрес `0x258fdb6b220` даёт **оба** исхода. Простое «переиспользование адреса роутера» гипотезу не объясняет |
| 32.12 | Опции нуджинга libavoid не лечат | зонд: 5 вариантов (`idealNudgingDistance=0`, `performUnifyingNudgingPreprocessingStep`, `nudgeOrthogonalSegmentsConnectedToShapes`, `nudgeOrthogonalTouchingColinearSegments`, `nudgeSharedPathsWithCommonEndPoint` — каждая выключена), по 24 прогона | 2026-08-18 | **у всех пяти по 2 исхода**, и это те же два хеша. Ни один штатный ключ роутера недетерминизм не убирает |
| 32.13 | Что именно расходится | `diff` двух дампов `layout()` на `6e7144d5` | 2026-08-18 | **28 строк из ~3600**: обход достаётся либо `edge_78`, либо `edge_79` — двум равноправным рёбрам к `node_53`. Дефекты в обоих исходах одинаковы (**14 → 0**), то есть качество не меняется, меняется геометрия |

### 32в. ⭐ Масштаб: сколько корпуса невоспроизводимо

| # | что | команда | дата | результат |
|---|---|---|---|---|
| 32.14 | ⭐ **Полный корпус, первый замер** | `python -X utf8 tools/layout_determinism.py --runs 6` (17 графов, ~30 мин) | 2026-08-18 | **7 из 17 неповторимы**: `0aea61c0` (3 исхода из 6), `302171b3` (5), `54a60fd1` (2), `6e7144d5` (2), `751116c9` (**6 из 6 — каждый прогон свой**), `a5264536` (5), `a6d28736` (2) |
| 32.15 | ⭐ **Второй замер тем же ключом нашёл восьмой** | `python -X utf8 tools/layout_determinism.py --runs 6 --write-baseline` | 2026-08-18 | к семи добавился **`620cc50d`** — в первом замере он выглядел воспроизводимым. **Итого 8 из 17.** Ни один граф в обратную сторону не перешёл. Это прямое доказательство выборочности метода: «воспроизводим за 6 прогонов» ≠ «детерминирован» |
| 32.16 | Третий замер по восьми неповторимым | `python -X utf8 tools/layout_determinism.py --runs 6 --check --uid …` (8 графов, ~8 мин) | 2026-08-18 | семь из восьми снова неповторимы (`a5264536` — **6 исходов из 6**), а `0aea61c0` на этот раз дал один хеш. Эталон не переснимаю: неповторимость — наблюдение положительное, неудачная выборка его не отменяет |
| 32.17 | Что это значит для `cmp_bitexact` | следствие 32.14–32.16 | 2026-08-18 | Главный инструмент доказательства «рефакторинг ничего не изменил» ложно красен на **47 %** корпуса (8 из 17). ПР1 говорил «минимум на одном графе» — замер даёт восемь |
| 32.18 | ⭐ **Гуляет ли КАЧЕСТВО вместе с геометрией — НЕТ** | тот же прогон 32.16, вывод «качество» | 2026-08-18 | На **всех восьми** графах число дефектов одинаково во всех 6 прогонах: `0aea61c0` 156→11, `302171b3` 13→0, `54a60fd1` 70→4, `620cc50d` 51→0, `6e7144d5` 14→0, `751116c9` 44→0, `a5264536` 34→0, `a6d28736` 128→7. ⟹ шумит **только** побайтовое сравнение; `layout_bench` и любые проверки по счётчикам дефектов остаются рабочими гейтами |
| 32.19 | Фикстуры из git (то, что видит CI) | `python -X utf8 tools/layout_determinism.py --git-only --runs 30` | 2026-08-18 | `089feca2` и `d74eb9f1` воспроизводимы за **30** прогонов, `6e7144d5` — **2 исхода из 30**. Гейт CI на этих трёх надёжен: неповторимый записан неповторимым, ложного красного дать неоткуда |
| 32.20 | GPU/CPU как причина — исключено | `import modules.graph.core.layout` → `'torch' in sys.modules`; грепы по `layout/`, `seating.py`, `canvas_input.py` | 2026-08-18 | `torch` не загружается, `cuda`/`PID_DEVICE` в пути раскладки нет; из тяжёлого только `numpy` и `shapely`. Различие наборов инструкций CPU объяснить флип **внутри одного процесса** не может — путь выбирается один раз при загрузке бинаря |
| 32.21 | ⚠ Не замерено (вход в ВН3) | — | 2026-08-18 | Бой считает раскладку в Linux-контейнере со своим `_adaptagrams.so` (`vendor/adaptagrams/build/build_linux.sh`), замеры выше сняты на win-бинаре `_adaptagrams.pyd`. Совпадение байт-в-байт **между платформами** не проверялось вовсе — отдельная дыра, той же природы, что «Linux-прогона тестов нет» (§25) |

### 32г. Стенды и доказательства гейтов

| # | что | команда | дата | результат |
|---|---|---|---|---|
| 32.22 | Эталон линтеров снят | `python -X utf8 tools/lint_gate.py --write-baseline` | 2026-08-18 | `tools/bench/lint_baseline.json`: **199** нарушений, счётчик на каждый из **73** файлов (считаются только файлы под git — см. 32.26). Первичный снимок нового стенда — тем же коммитом (прецедент: ревизия 0.3, «первичный снимок базы тем же коммитом допустим, Д6 — про пересъём») |
| 32.23 | ⭐ **Гейт линтеров валится на новом широком `except`** | зонд: `except Exception: return {}` дописан в `tools/corpus.py` → `python -X utf8 tools/lint_gate.py --check` | 2026-08-18 | **exit 1**, `[ПРОВАЛ] tools/corpus.py: широких except 0 -> 1`, 201 в 75 файлах. Дерево восстановлено (`--check` снова exit 0, 200/74) |
| 32.24 | ⭐ **Вторая половина гейта: mypy** | зонд: `def graph_path(uid8: str)` → `uid8: int` в `tools/corpus.py` → тот же `--check` | 2026-08-18 | **exit 1**, `mypy: ошибок 2` (`call-overload` + `arg-type`) — ровно тот класс, ради которого ПР5 просил mypy (расхождение ключей `graph_val`/`val_graph`). Дерево восстановлено (`git checkout --`) |
| 32.25 | Что валит гейт стенда воспроизводимости | тесты `tests/test_layout_determinism.py` (9) и `tests/test_lint_gate.py` (9) | 2026-08-18 | вердикт вынесен в `verdict()` и проверяется тестом, а не глазами: граф был воспроизводим и перестал; новый граф корпуса неповторим; код возврата дочернего прогона вне `{0,1}`; неразбираемый вывод; молча потерянные графы; у линтеров — пустой результат при непустом эталоне. Оба стенда наследование `PYTEST_ADDOPTS` детям выпалывают (урок 0.3) |
| 32.26 | ⭐ **CI вскрыл дефект эталона линтеров** | `gh run view 32117802590 --log` против локального прогона | 2026-08-18 | локально **200 в 74 файлах**, на раннере **199 в 73**. Разница — нетрекнутый `tools/tz_lint.py` (чужой файл рабочего дерева, гейт отвергнутой системы `TZ_2026-08-14_*`). Его строка в эталоне заранее прощала бы долг файлу, которого в git ещё нет. Закрыто фильтром по `git ls-files` + тестом `test_baseline_holds_only_tracked_files`; эталон переснят: **199 в 73** |
| 32.27 | ⭐ **Раскладка воспроизводима МЕЖДУ машинами (в пределах Windows)** | тот же лог CI против локального прогона | 2026-08-18 | раннер GitHub дал те же самые хеши, что машина разработки: `089feca2` `f4510d0b989c015b`, `d74eb9f1` `51e20ed60bb97cdc`, и те же дефекты (6→0, 14→0, 0→0). `6e7144d5` там тоже неповторим — **2 исхода из 10**, независимое воспроизведение находки. Проверено только win↔win; про Linux — 32.21 |

---

## 33. Пункт 0.11 дороги — стенд диффов «до/после оператора» (ДН4), 2026-08-18

**Среда:** ветка `dev`, Windows 10, CPython 3.11.9, системный `python`, локальный
`storage/diagrams` (19 каталогов, из них 17 с артефактами). Всё, что стало гейтом, лежит
в `tools/pair_bench.py` и `tools/bench/pair_baseline.json`; разовые зонды — в скрэтчпаде
сессии. Полный прогон стенда — **10 с**.

### 33а. Проверка утверждений пункта по коду

| # | что | команда | дата | результат |
|---|---|---|---|---|
| 33.1 | Ловушка аудита: имена заняты живым судьёй | `grep -r "pair_bench\|pair_baseline"` по репо; `ls tools/bench` | 2026-08-18 | новых имён в репозитории **нет ни одного**; `tools/edit_bench.py` и `tools/bench/edit_baseline.json` (судья «Ручной правки», пункт 0.2) коммитом не тронуты — стенд ДН4 назван `pair_bench` и пишет свой эталон |
| 33.2 | Семь пар существуют на диске | `ls storage/diagrams/*/<пара>` по каждому имени | 2026-08-18 | detection **17/17**, segmentation **17/17**, junction **12/17**, graph **17/17**, contours **11/17**, ocr **17/17**, layout **15/17** — **106 пар** на 17 диаграммах. План (`PLAN_2026-08-13_stats_collection.md` §3) обещал «11 из 19» перекрёсткам и контурам: у контуров сошлось, у перекрёстков сегодня 12 |
| 33.3 | Кто пишет сторону «после» (иначе пара не про оператора) | чтение производителей | 2026-08-18 | `yolo_validated.txt` — `app/api/cvat.py:214` (конвертация выгрузки из CVAT); `points_validated.json` — `app/api/validation.py:51`; `contours_validated.json` — `app/api/contours.py:210` (загрузка после правки) и `:304` (авто-приёмка); `ocr_binding.json` — `app/api/ocr.py:272`; `graph_canvas.json` — `app/api/validation.py:974`. Все пять — ручные пути, а не воркер |
| 33.4 | Пара OCR «заработает только после `id` формата v3» (оговорка плана) | чтение `ocr_binding.json` v2 | 2026-08-18 | **частично опровергнуто**: `id` действительно нет, но в файле лежит `edited_blocks` — полный набор блоков после правки (2044 блока «до» по корпусу, 219 привязок). Этого хватает для геометрического диффа; блок опознаётся пересечением рамок, а не `id` |
| 33.5 | Маски пары сравнимы только после бинаризации | чтение PNG обеих сторон | 2026-08-18 | `pipe_mask.png` — режим `L` (значения 0/255), `pipe_mask_validated.png` — **RGBA**, альфа сплошь 255. Сравнение «как есть» дало бы 10.9 млн «изменённых» пикселей из 11.2 млн; после бинаризации по RGB — 55 тыс. Размеры листа у сторон совпадают у всех 17 пар (`shape_mismatch` 0/17) |

### 33б. ⭐ Главные числа: объём правок оператора по семи парам

Медиана — по диаграммам, где пара есть. Суммы приведены рядом, потому что почти во всех
парах их делает **одна схема `8d14cf73`** (у неё правился весь граф); медиана — мера,
которую это не перекашивает.

| # | пара | команда | дата | результат |
|---|---|---|---|---|
| 33.6 | ⭐ **Сегментация — единственная стадия, где правят ВСЕГДА** | `python -X utf8 tools/pair_bench.py --stage segmentation` | 2026-08-18 | правок **17 из 17**. Медиана: **+66 171 / −19 619 пикселей**, изменено 0.2–1.2 % листа (ppm 2154 … 11 532, медиана 6422). Дорисовано втрое больше, чем стёрто (1.31 млн против 0.35 млн); медиана прироста — **+26 % к площади машинной маски**. То есть модель труб систематически **недорисовывает** |
| 33.7 | ⭐ **Детекция: модель пропускает, а не выдумывает** | `--stage detection` | 2026-08-18 | без правок **6 из 17**. Сумма: **добавлено 77**, снято **5**, сдвинуто **11**, сменён класс **1**. Медиана добавленных — **1** на схему. Ложные срабатывания оператор снимает почти никогда — вся работа в пропусках |
| 33.8 | **Граф: правки редки, но одна схема правится целиком** | `--stage graph` | 2026-08-18 | без правок **5 из 17**. Медиана: добавлено рёбер **1**, всё остальное **0**. Суммы: узлов +141/−41, рёбер +475/−160, перемещений узлов 331, из них **310 — одна схема `8d14cf73`**. Следствия (`edge_end_follow` 349, `edge_rerouted` 0) в объём правок не входят — различение ДН2, пункт 0.9 |
| 33.9 | **Перекрёстки: почти всё принимается как есть** | `--stage junction` | 2026-08-18 | без единой правки **9 из 12**. Настоящих правок по корпусу — **3 сдвинутые точки** (`c2f79462`) и 1 снятая (`8d517a35`); «154 добавленных» у `8d14cf73` — не правки, а сдвиг листа (33.13) |
| 33.10 | ⭐ **Контуры SAM2: правок ноль, но и доходят до них редко** | `--stage contours` | 2026-08-18 | `was_edited` — **0 из 28** узлов во всех 11 парах. При этом узлов в `contours_auto` — **48**, в `contours_validated` — **28** (21 принят, 7 пропущен), до **20 узлов оператор не дошёл**. Вкладка контуров — не источник правок, а источник незавершённой работы |
| 33.11 | **OCR: правят рамку чаще, чем текст** | `--stage ocr` | 2026-08-18 | без правок **10 из 17**. Суммы: переделано рамок **168**, исправлено текстов **55**, добавлено 102, удалено 90. Медианы нулевые: правки OCR сосредоточены в трёх схемах из семнадцати |
| 33.12 | **Раскладка: состав холста оператор почти не меняет** | `--stage layout` | 2026-08-18 | состав не изменён на **12 из 15** холстов; `operator_saved` стоит у **10 из 15**. Ручных объектов на холстах — **186 узлов и 635 рёбер**, но они пришли ещё из графа (та же пара «граф»), а не из раскладки. Координаты стенд не сравнивает: у сторон пары они в разных системах |

### 33в. Что числа НЕ значат (замеренные оговорки, а не предположения)

| # | что | команда | дата | результат |
|---|---|---|---|---|
| 33.13 | ⭐ **Сдвиг листа читался бы как 154 правки** | зонд: модальное смещение «точка → ближайшая» на `8d14cf73` | 2026-08-18 | **+26/−21 px** у 66 точек из 154 при разбросе ±2 px — это разные обрезки листа, а не работа оператора. Стенд печатает `origin_shift 33` и мнимые правки за настоящие не выдаёт |
| 33.14 | ⭐ **Правка рамки OCR ломала счёт на порядок** | сравнение одного и двух проходов сопоставления на `620cc50d` | 2026-08-18 | один проход (IoU ≥ 0.5): «добавлено 170, удалено 165, исправлено текстов 2» — при том, что рамки те же, только растянутые (`орифер` → `калорифер 1Б`, IoU **0.18**). Два прохода: **138 переделанных рамок, 39 исправленных текстов**, добавлено 32, удалено 27 |
| 33.15 | **Порядок пары не гарантирован** | сверка `mtime` сторон у всех 106 пар | 2026-08-18 | у **6 пар** файл «до» новее файла «после» (4 × `layout`, 2 × `ocr`): стадию перезапустили ПОСЛЕ правки оператора. Такие пары стенд помечает `!`; их числа правками читать нельзя |
| 33.16 | **Стенд повторяем** | два прогона подряд, sha256 выгрузки `--json` | 2026-08-18 | `493902b9ae8be300` = `493902b9ae8be300`. Жадное сопоставление отсортировано с разбиением ничьих по индексам, поэтому от порядка перебора не зависит |

### 33г. Гейт и зонды

| # | что | команда | дата | результат |
|---|---|---|---|---|
| 33.17 | Эталон снят | `python -X utf8 tools/pair_bench.py --write-baseline` | 2026-08-18 | `tools/bench/pair_baseline.json` — 17 диаграмм, 106 пар. Первичный снимок нового стенда тем же коммитом (прецедент ревизии 0.3: Д6 — про пересъём, не про первый снимок) |
| 33.18 | Гейт зелёный | `python -X utf8 tools/pair_bench.py --check` | 2026-08-18 | **exit 0**, «рост правок: 0» |
| 33.19 | ⭐ **Гейт валится на росте правок** | зонд: в эталоне `54a60fd1/detection.added` 12 → 11, затем `--check` | 2026-08-18 | **exit 1**, `ХУЖЕ 54a60fd1/detection: added 11 -> 12`, «рост правок: 1». Эталон восстановлен, `--check` снова exit 0 |
| 33.20 | ⭐ **Пустой прогон не может быть зелёным** | `python -X utf8 tools/pair_bench.py --storage <нет такого> --check` | 2026-08-18 | **exit 2**, «замер пуст: ни одной пары не найдено — данных корпуса нет». Так стенд выглядит на чистом клоне и в CI — поэтому в CI он и не стоит (класс дефекта — блокер 0.3) |
| 33.21 | Д1: тесты бьют по существу (три инъекции) | `python -X utf8 -m pytest tests/test_pair_bench.py -q` после каждой правки кода | 2026-08-18 | шум округления, засчитанный как правка (`BOX_EPS = 0`) → красный `test_detection_counts_edits_and_ignores_rounding_noise`; один проход у OCR → красный `test_ocr_stretched_frame_is_an_edit_not_add_plus_remove`; «пустой замер = 0» → красный `test_empty_measurement_cannot_be_green`. В каждой инъекции **1 failed / 20 passed**, дерево восстановлено, итог — **21 passed** |
| 33.22 | Набор не пострадал | `python -X utf8 tools/suite_baseline.py --check`; `pytest --collect-only -q` | 2026-08-18 | **exit 0**, «новых красных нет»; собрано **815** (было 794 без файлов пункта, +21 тест), красных **57 без изменений** (37 failed + 20 errors). Расхождение с журналом 0.10 («793») объясняется его же фикс-коммитом `9652557`, добавившим один тест |

---

## 34. Пункт 0.12 дороги — конвейер прошёл end-to-end на локальном стеке (2026-08-18)

**Что проверялось.** Клейм плана «прогнать конвейер локально сегодня **нечем**»
(вердикт аудита ❔ — чтением кода не проверяется) и гейт пункта «один uid
проходит все этапы локально». Стенд — новый `tools/e2e_local.py`: ведёт одну
диаграмму по тем же REST-эндпоинтам, что клиент, авто-приёмкой закрывая ручные
этапы. Боевой код не тронут.

### 34а. Клейм плана: чего именно не хватало

| # | что | команда | дата | результат |
|---|---|---|---|---|
| 34.1 | ⭐ **«Нечем» опровергнуто: не хватало не компонентов, а способа** | `docker ps`; `curl /health`; `celery -A worker.celery_app inspect active_queues` | 2026-08-18 | стек **поднят и полон**: 15 сервисов из 15 в `Up`, `/health` = `{api, database, redis: healthy}`, воркеры слушают `default, gpu, sam2` (pid_worker) и `ocr` (pid_worker_ocr), веса в `models/` на месте. Не было **входа без клиента**: конвейер ведёт окно PySide6, и каждый ручной этап ждёт оператора |
| 34.2 | **«5 сервисов» плана — это P&ID-часть compose-файла из 15** | `docker compose config --services` | 2026-08-18 | **15** сервисов: P&ID — `postgres, redis, api, worker, worker_ocr` (5), остальные 10 — CVAT+traefik. Ловушка аудита подтверждена; при этом CVAT для прогона **обязателен** — приёмка bbox идёт только через него (`app/api/cvat.py:264`) |
| 34.3 | ⚠ **Локальный стек — на GPU, боевой — CPU-only** | `docker exec pid_worker python -c "import torch;print(torch.cuda.is_available())"` | 2026-08-18 | `True`; `.env` этой машины: `PID_DEVICE=cuda`, `YOLO_DEVICE=cuda` (файл в `.gitignore`, помечен «вернуть на cpu перед деплоем»). **Все секунды ниже к бою не переносятся** — это работа СТ2 (волна 2), здесь они только доказывают, что этап отработал |

### 34б. Прогон: один uid прошёл все этапы

| # | что | команда | дата | результат |
|---|---|---|---|---|
| 34.4 | ⭐ **Диаграмма дошла до `completed` за один запуск** | `python -X utf8 tools/e2e_local.py --check --run` | 2026-08-18 | uid `3263039b`, **14 шагов**, `uploaded → … → completed`, **39.4 с** стенных. Картинка — оригинал самой мелкой схемы корпуса 0.8 (`d74eb9f1`, 358 442 Б, 1978×1247) |
| 34.5 | **Отработали все 13 стадий, ни одной FAILED** | `select stage_type, status, duration_seconds from processing_stages where diagram_uid=…` | 2026-08-18 | `frame_removal 0.0 · detection 5.9 · cvat_validation 2.2 · direction_classification 0.3 · segmentation 2.4 · skeletonization 0.8 · final_skeletonization 0.6 · junction_classification 2.5 · graph_building 1.2 · ocr 15.9 · contour_extraction 9.4 · layout 0.1 · fxml_generation 0.1` — **13 из 13 completed** |
| 34.6 | **Второй прогон (uid `4c1c12fd`) — те же 13 стадий, дольше** | то же по первому uid | 2026-08-18 | `detection 11.6 · junction_classification 9.0 · ocr 47.8 · contour_extraction 17.7`, остальные — как у 34.5. Разница — прогрев весов в живых воркерах; **честного «холодного» замера здесь нет**, это предмет СТ2 (нужен рестарт воркеров + RSS) |
| 34.7 | ⭐ **След на диске: 24 артефакта, все 16 обязательных** | `--run` (проверка `GET /download/{type}`); `select artifact_type from artifacts` | 2026-08-18 | **16/16** обязательных, всего в БД **24** строки. Содержательно: граф **65 узлов / 62 ребра** (36 боксов детекции, 36 после приёмки, 36 перекрёстков, 0 мостов), контуры SAM2 — **32 узла из 32 eligible**, `mean_confidence 0.95`, FXML **17 250 Б**: 58 `Line`, 18 `ValveControl`, 9 `ButtonControl`, 4 `Polyline` |
| 34.8 | **Возобновление работает** | `tools/e2e_local.py --resume 4c1c12fd…` после обрыва на контурах | 2026-08-18 | подхватил со статуса `validated_graph`, доехал до `completed` за **19.6 с**; таблица `WAIT_STEPS` закрывает и промежуточные статусы (`detecting`, `building_graph`, `generating_fxml`), на которых стенд оставляет диаграмму при таймауте |

### 34в. Что прогон вскрыл (расхождения кода с доками)

| # | что | команда | дата | результат |
|---|---|---|---|---|
| 34.9 | ⭐ **SAM2 веером после перекрёстков НЕ запускается** | прогон встал на ожидании `contours_auto`; `docker logs pid_api` + `app/api/validation.py:666-669` | 2026-08-18 | в логе после `junctions/complete` диспатчатся только `task_build_graph` и `task_run_ocr`; в коде на месте авто-запуска SAM2 стоит комментарий «triggered on demand … via POST /api/contours/{uid}/extract» и `contour_task_id = None`. `STATUS_MACHINE.md:145` и `API.md §7` обещают авто-диспатч — **доки врут** (за правку отвечает не 0.12) |
| 34.10 | **Детекция стартует с `frame_cleaned`, а не с `uploaded`** | `app/api/detection.py:40`; прогон без шага `frame` → 400 | 2026-08-18 | между загрузкой и детекцией стоит фаза очистки рамки (`/api/frame/{uid}/skip` — «рамки нет»). `STATUS_MACHINE.md §2` рисует `uploaded → detecting` |
| 34.11 | **Молчаливой копии графа в validated больше нет** | `app/api/validation.py:728` | 2026-08-18 | `complete-simple` требует артефакт `GRAPH_VALIDATED` и отвечает 400 «Граф не сохранён», тогда как `STATUS_MACHINE.md §6` числит копию `graph.json → graph_validated.json` среди авто-приёмок. Стенд сохраняет граф явно |
| 34.12 | ⚠ **`GET /api/projects/` и `/summary` отдают пустоту на всех вызовах после первого** | 3 подряд `curl /api/projects/summary`; `app/services/project_loader.py:579` | 2026-08-18 | первый вызов — `[{"code":"thermohydraulics"…}]`, второй и далее — `[]` (и `{"items":[],"total":0}`). Причина: `load_all()` пропускает подпапку, чей код уже лежит в `self._cache`, а лоадер — процессный синглтон (`@lru_cache`). Задето прямо: клиент, открытый вторым, списка проектов не увидит. **Чужой дефект, не правился** — `--check` спрашивает `/api/projects/{code}` (стабилен) |

### 34г. Гейт и зонды

| # | что | команда | дата | результат |
|---|---|---|---|---|
| 34.13 | Гейт стенда зелёный | `python -X utf8 tools/e2e_local.py --check` | 2026-08-18 | **exit 0**: health зелёный, проект найден, очереди `default, gpu, ocr, sam2`, картинка на месте |
| 34.14 | ⭐ **Зонд: мёртвый API** | `--check --api http://localhost:9` | 2026-08-18 | **exit 1**, «ПЛОХО /health: ConnectError…», провалов 2 |
| 34.15 | ⭐ **Зонд: мёртвый брокер** | `--check --broker redis://localhost:6399/0` | 2026-08-18 | **exit 1**, «ПЛОХО брокер …: OperationalError», «никто не слушает: default, gpu, sam2, ocr» |
| 34.16 | **Зонд: чужой проект** | `--check --project net_takogo` | 2026-08-18 | **exit 1**, «проекта 'net_takogo' нет: /api/projects/net_takogo → 404» |
| 34.17 | ⭐ **Д1: тесты бьют по существу (три инъекции)** | `python -X utf8 -m pytest tests/test_e2e_local.py -q` после каждой правки кода | 2026-08-18 | шаг контуров выкинут из `STEPS` → **5 failed** (цепочка, достижимость, прогон, отчёт); статус `error` перестал останавливать прогон → красный `test_error_status_stops_run_with_stage`; `--check` перестал судить очереди → красный `test_check_red_on_broken_stack[stack1]`. Дерево восстановлено, итог — **16 passed** |
| 34.18 | Набор не пострадал | `python -X utf8 tools/suite_baseline.py --check`; `pytest --collect-only -q` | 2026-08-18 | **exit 0**, «новых красных нет»; собрано **833** = 815 локально до пункта (журнал 0.11) + 16 тестов пункта + **2** параметра корпусной цепочки (`test_t10_corpus_chain` даёт ровно ОДИН тест на схему в `storage/`); красных **57 без изменений** (37 failed + 20 errors); сбор 833/0 ошибок. ⚠ первая редакция этой строки объясняла дельту «42 параметрами от двух диаграмм» — атрибуция была неверной, проверено сносом одной схемы: 833 → **832**, ровно −1 |
| 34.19 | Соседние стенды не сломались | `python -X utf8 tools/pair_bench.py --check`; `python -X utf8 tools/lint_gate.py --check` | 2026-08-18 | `pair_bench` **exit 0**, «рост правок: 0», новые uid помечены «диаграммы нет в эталоне — пропуск»; `lint_gate` **exit 0**, 199 нарушений в 73 файлах (эталон 199), mypy 0 ошибок — `tools/e2e_local.py` добавлен в его список `files` |
| 34.20 | Данные первого прогона снесены решением Максима | `delete from diagrams where uid='4c1c12fd…'` (FK каскадные) + удаление каталога; затем гейты | 2026-08-18 | корпус этой машины **19 → 18** схем с `graph_validated.json` (21 каталог); остался чистый прогон `3263039b` (FXML на месте). `suite_baseline --check` **exit 0**, собрано **832**, красных 57 без изменений; `pair_bench --check` **exit 0**. Числа 34.6 сняты до сноса и остаются замером; воспроизвести их можно только новым прогоном |

---

## 35. Пункт ВН1б дороги — приёмка `napravlenie` и разбор боксов без рёбер (2026-08-18)

**Что проверялось.** Чек-лист `docs/NAPRAVLENIE_E2E_CHECKLIST.md` v2.0 в рабочей
среде (замок пункта — 0.12) и его ключевой вопрос: «133 бокса со степенью 0 —
где потерялась труба и что из этого восстановимо со 100 % уверенностью»
(решение Максима Р-ВН1-1). Инструмент — новый `tools/napravlenie_audit.py`;
боевой код не правился ни строкой.

### 35а. Прогон в рабочей среде (§3 чек-листа)

| # | что | команда | дата | результат |
|---|---|---|---|---|
| 35.1 | ⭐ **Схема со стрелками прошла конвейер целиком** | `python -X utf8 tools/e2e_local.py --run --image storage/diagrams/06b938ec…/original/image.png` | 2026-08-18 | новый uid `0fc9d04c`, **14 шагов**, `uploaded → … → completed`, **126.2 с**, артефактов **16/16**. Картинка выбрана по §2 чек-листа — оригинал схемы с 44 боксами `napravlenie`; прогон 0.12 шёл на `d74eb9f1`, где их **0**, поэтому §3–§4 им не закрывались |
| 35.2 | **13 стадий из 13, шаг направления отработал** | `select stage_type, status … from processing_stages where diagram_uid = uid прогона` | 2026-08-18 | `frame_removal 0.0 · detection 14.4 · cvat_validation 2.3 · direction_classification 0.8 · segmentation 9.4 · skeletonization 4.2 · final_skeletonization 3.6 · junction_classification 6.1 · graph_building 27.5 · ocr 56.5 · contour_extraction 47.1 · layout 3.5 · fxml_generation 0.2` — все `completed` |
| 35.3 | ⭐ **§4.1: направление у всех боксов** | `python -X utf8 tools/napravlenie_audit.py --uid 0fc9d04c` | 2026-08-18 | боксов `napravlenie` **44**, с `attributes.direction` — **44**; `direction_confidence` = 1.0 у всех, `direction_low_confidence` — **0**; распределение `down 24 · up 12 · right 7 · left 1` |
| 35.4 | ⭐ **§4.2: бокс не стал узлом, труба под ним цела** | там же | 2026-08-18 | в `node_mask` своей площадью — **0 из 44** (и ни одного накрытия соседом); медиана заливки бокса `pipe_mask` — **0.283**, боксов без трубы **0** |
| 35.5 | ⭐ **§4.3: 44 direction-узла, сирот НЕТ** | там же | 2026-08-18 | direction-узлов в `graph.json` **44** = числу боксов; степень **1 у всех 44** и по полю, и по факту рёбер; `pass_through` 0 (сквозных стрелок на этой схеме нет). Историческая раскладка того же чертежа (`06b938ec`, артефакты 08-05/08-12) числила 4 бокса «степени 0» — на сегодняшнем коде их **ноль** |
| 35.6 | **§4.4: перпендикуляр не подключён** | там же | 2026-08-18 | у каждого direction-узла ровно **1** инцидентное ребро, `connector`-ов `dirconn_*` — 0; рёбер с полем `direction` — **0** (норма по решению Р-ВН1-2) |
| 35.7 | **§1: предпосылки** | `ls models/direction/best.pt`; `pytest tests/test_direction_nodes.py -q` | 2026-08-18 | веса на месте (10 261 889 Б, 2026-06-17); юнит-тесты графа — **13 passed** (из них 6 бьют в мёртвые `apply_direction_rules`/`stitch_collinear_stubs`, см. `NAPRAVLENIE.md §5.3`) |

### 35б. Корпус: степень стрелки и протухшее поле `degree`

| # | что | команда | дата | результат |
|---|---|---|---|---|
| 35.8 | Состав корпуса | `python -X utf8 tools/napravlenie_audit.py` | 2026-08-18 | 19 диаграмм с графом, боксов `napravlenie` **766**, с направлением **765** (один бокс `13d1ef5f` без `direction` — так и лежит с 07-23), direction-узлов в графах **766** |
| 35.9 | ⛔ **Главная поправка: «133 сироты» — миф на треть** | там же | 2026-08-18 | по полю `degree` нулей **133**, по фактической инцидентности в `links` — **96**. 37 боксов «сироты» только на бумаге: ребро у них есть |
| 35.10 | ⭐ **Почему поле врёт: его не пересчитывает редактор** | `ui/editors/graph_data.py:155-186`; сверка поля с `links` по всем узлам корпуса | 2026-08-18 | `GraphData.save()` синхронизирует `num_edges`/`num_nodes`/`num_isolated_nodes` и ставит клеймо `_editor_commit`, но степени узлов не трогает; новые узлы рождаются с `degree: 0` (`:424`, `:450`) навсегда. Расхождение поле↔факт есть ровно в **7 файлах из 19 — и все семь с клеймом редактора** (`06b938ec` 82 узла из 432, `0aea61c0` 83/504, `302171b3` 35/196, `54a60fd1` 57/258, `620cc50d` 44/520, `8d517a35` 93/474, `c2f79462` 31/149; всего **425** узлов). В файлах без клейма расхождений **0** |
| 35.11 | **Следствие: `pass_through` тоже занижен** | там же | 2026-08-18 | флаг `pass_through: true` у **63** узлов, а степень ≥2 по факту рёбер — у **109**. Флаг считается из того же поля (`set_direction_pass_through`), поэтому на графах после редактора врёт вместе с ним |
| 35.12 | Распределение степеней по факту | там же | 2026-08-18 | `0: 96 · 1: 561 · 2: 106 · 3: 3`. По решению Максима законны степени 1 и 2, разбору подлежат только нули |

### 35в. Разбор 96 боксов без единого ребра

| # | что | команда | дата | результат |
|---|---|---|---|---|
| 35.13 | ⭐ **ОТВЕТ ПУНКТА: 85 восстановимо однозначно, 11 оператору** | `python -X utf8 tools/napravlenie_audit.py --orphans` | 2026-08-18 | `восстановимо: одна осевая сторона` **83** · `восстановимо: сквозная труба` **2** · `оператору: трубы по оси нет` **8** · `оператору: скелет не дотянулся` **3**. «Восстановимо» здесь значит **подключить существующую трубу**, а не дорисовать новую: труба лежит на `pipe_mask` и КАСАЕТСЯ осевой грани бокса |
| 35.14 | Сироты сидят на одной схеме | там же | 2026-08-18 | `51b339ab` **86** из 96 (при 253 боксах — 34 % её стрелок), дальше `a5264536` 4, `089feca2` 2, `c2f79462` 2, `13d1ef5f` 1, `89ca7583` 1. На 13 схемах корпуса сирот **нет вовсе** |
| 35.15 | ⭐ **Где именно теряется труба: между маской и скелетом** | зазор от грани бокса до первого пикселя маски, `51b339ab`, 86 сирот против 167 подключённых | 2026-08-18 | `pipe_mask`: сироты **79 из 86** касаются грани (зазор ≤2 px), подключённые — 167/167 → **сегментация не виновата**. `skeleton_final`: у подключённых **167/167** зазор ≤2 px, у сирот медиана **4 px**, 58 из 86 в диапазоне 3–10 px → скелет обрывается, не доходя до бокса, а контакт ищется на кольце в **1 px** (`prepare_tracing_data(dilation=1)`, `modules/graph/core/nodes.py:640-700`) |
| 35.16 | ⭐ **Что отличает сироту от подключённой стрелки** | заливка бокса `pipe_mask`, `51b339ab` | 2026-08-18 | у подключённых медиана **0.251** (83 % имеют >8 %), у сирот **0.022** (>8 % — ни одной). Подключаются те стрелки, чей треугольник сегментация нарисовала как трубу; где не нарисовала — труба кончается у рамки и скелет не дотягивается |
| 35.17 | **Постпроцесс `napravlenie` не виноват** | сверка `graph_raw_preprocess.json` ↔ `graph.json` по bbox, `51b339ab` | 2026-08-18 | все **86 из 86** сирот уже без рёбер в СЫРОМ графе; ни один бокс не потерял рёбра на постпроцессе (`drop_degenerate_stubs`/`cap_dangling_ends`/`collapse_straight_connectors`): пары «сырая степень → финальная» — `0→0: 86, 1→1: 157, 2→2: 4, 2→1: 2, 3→3: 2, 3→2: 2` |
| 35.18 | **Ложная тревога §4.2 снята замером** | доля бокса в `node_mask` до и после вычитания рамок соседей | 2026-08-18 | «наивно» ненулевая заливка — у **68** боксов корпуса, но **67 из них** объясняются накрытием чужим боксом оборудования (стрелка нарисована поверх насоса/задвижки). По СВОЕЙ площади (порог 5 %) в `node_mask` не попал **ни один бокс из 766** — дефект, починенный `a1e0e14`, нигде не воскрес |

### 35г. Гейт и зонды

| # | что | команда | дата | результат |
|---|---|---|---|---|
| 35.19 | Гейт приёмки на свежем прогоне | `python -X utf8 tools/napravlenie_audit.py --uid 0fc9d04c --check` | 2026-08-18 | **exit 0**, «нарушений критериев приёмки: 0» |
| 35.20 | ⭐ **Зонд: у бокса сняли `direction`** | копия артефактов прогона во временном каталоге, `--storage <копия> --check` | 2026-08-18 | **exit 1**, «направление проставлено 43 из 44». Боевые данные не тронуты — работа шла на копии |
| 35.21 | ⭐ **Зонд: бокс залит в `node_mask`** | там же, рамка стрелки закрашена в `node_mask.png` | 2026-08-18 | **exit 1**, «1 боксов napravlenie попали в node_mask — труба под ними вырезается» |
| 35.22 | **Зонд: судить нечем** | `--storage <пустой каталог> --check` | 2026-08-18 | **exit 2**, «судить нечем: диаграмм с графом нет» — тот же контракт, что у `pair_bench` |
| 35.23 | ⭐ **Д1: тесты бьют по существу (четыре инъекции)** | `pytest tests/test_napravlenie_audit.py -q` после каждой правки стенда | 2026-08-18 | «верить полю `degree`» → **2 failed** (`…stale_degree_field`, `…orphan_is_counted_when_no_links`); «не вычитать соседей из `node_mask`» → красный `test_neighbour_box_is_not_a_violation`; «не проверять скелет» → красный `test_bucket_skeleton_did_not_reach`; «пустой корпус = зелёный» → красный `test_empty_storage_is_not_a_green_check`. Дерево восстановлено (`diff --strip-trailing-cr` с копией пуст), итог — **14 passed** |

---

## 36. Пункт 0.4 дороги — характеризация диспетчера жестов редактора (2026-08-18)

**Что проверялось.** Утверждение плана «на диспетчер нажатия **ноль** тестов» и все адреса
пункта; затем — что именно делают с ДАННЫМИ настоящие `QMouseEvent`/`QKeyEvent`, поданные
в `AdvancedGraphEditor` на Qt offscreen. Боевой код не правился ни строкой: пункт целиком —
новый набор `tests/ui/test_gesture_dispatch.py` (19 тестов) плюс `docs/TESTING.md` §10.

### 36а. Проверка адресов пункта (до первой строки кода)

| # | что | команда | дата | результат |
|---|---|---|---|---|
| 36.1 | Утверждение «ноль тестов на диспетчер» | `grep -rn "mousePressEvent\|mouseMoveEvent\|mouseReleaseEvent" tests/` | 2026-08-18 | **подтверждено**: ни одного вызова. Соседние наборы бьют во внутренние методы жеста — `test_interactive_drag.py` зовёт `start_drag_node/drag_node_to/end_drag_node`, `test_mode_exit_finishes_gesture.py` — `on_exit` хендлера (ловушка аудита «не дублировать покрытие хендлеров» учтена) |
| 36.2 | Харнесс плана жив | `wc -l _scratch/audit_2026-08-06/scripts/j4_lib.py` | 2026-08-18 | **358 строк**, число сошлось; портирован (`_ev`/`press`/`move`/`release`), но корпус взят загрузчиком КД7 вместо `_scratch/raw/*.json` — иначе набор не пошёл бы в CI |
| 36.3 | Запрет гейта соблюдён | `grep -n "_current_mode\|_current_handler\|_ctrl_lmb" tests/ui/test_gesture_dispatch.py` | 2026-08-18 | 2 попадания, **оба — проза докстринга** («ни одного утверждения про…»); ни одного `assert` о внутреннем состоянии. `set_mode(...)` встречается дважды как ДЕЙСТВИЕ (кнопка тулбара), не как проверка |
| 36.4 | Данные для набора есть в git | `python -c "from tools import corpus; print(corpus.graph_path('d74eb9f1'))"` | 2026-08-18 | `tests/fixtures/graph/d74eb9f1.json` (пункт 0.8) — 66 узлов / 63 ребра, `image_size` 1247×1978; растр набор рисует сам |

### 36б. Замер поведения диспетчера (характеризация, «как есть»)

| # | что | команда | дата | результат |
|---|---|---|---|---|
| 36.5 | Жест мыши вообще доходит до данных | зонд `probe_dispatch.py` (scratchpad) | 2026-08-18 | Ctrl+ЛКМ + 4 кадра проводки: `centroid [17, 1502] → [17.0, 1541.37]`, глубина undo **0 → 1**, `undo()` возвращает `[17, 1502]` побайтово |
| 36.6 | ⛔ **Точность подачи события** | там же | 2026-08-18 | `mapFromScene` отдаёт ЦЕЛЫЙ пиксель вьюпорта при масштабе вида **0.3302** → обратный перевод врёт до **1.17 px** по сцене (замер на двух точках: +0.08/+1.17 и −0.08/+0.33). Отсюда допуск `DRAG_TOL = 4 px` на сдвиги; побайтово сравнивается только результат `undo`. `resize(1400, 900)` до/после `load_data` на масштаб НЕ влияет (0.3302 в обоих случаях) |
| 36.7 | Порог клик/протяжка | там же | 2026-08-18 | `DRAG_THRESHOLD = 5.0`; проводка 2 px — клик (данные и undo не изменились), 10 px — уже протяжка |
| 36.8 | Защёлка Ctrl снимается голым ЛКМ | там же | 2026-08-18 | при `ctrl_pressed = True` (эмуляция alt-tab) и нажатии ЛКМ **без** Ctrl: `ctrl_pressed → False`, узел не сдвинулся |
| 36.9 | «Залипший drag» обрывается | там же | 2026-08-18 | кадр `mouseMove` с отпущенной кнопкой: узел замер на `[17.0, 1541.37]` и не поехал ни на +300, ни на +600 px; жест лёг **одним** шагом undo |
| 36.10 | Потеря фокуса добивает жест | зонд `probe2.py` | 2026-08-18 | `focusOutEvent` посреди протяжки: геометрия замерла, undo **0 → 1**, `dragging_node = None`, последующие кадры узел не везут |
| 36.11 | Ctrl+ПКМ = удаление | там же | 2026-08-18 | узлов **66 → 65**, рёбер **63 → 62** (ушло инцидентное), undo **0 → 1**; `undo()` возвращает и узел, и ребро. Ctrl+ПКМ мимо всего: 66 → 66, undo 0 → 0 |
| 36.12 | Активный инструмент забирает клик себе | там же / `probe3.py` | 2026-08-18 | тот же жест по узлу: в `edit_waypoint` узел НЕ сдвинулся, а инструмент положил свой шаг undo («Цикл стороны», изменились `waypoints`); в `add_connector` узлов **66 → 67** при неподвижном узле под курсором |
| 36.13 | Видимые ручки размера | `probe3.py` | 2026-08-18 | Ctrl+2ЛКМ: ручек на сцене **0 → 4** (квадрат `HANDLE_SIZE` = 8 на слое `HANDLE_Z` = 20); Esc: **4 → 0**. Протяжка за правый-нижний угол: `bbox [1456, 0, 1549, 34] → [1456, 0, 1577.79, 54.51]`, undo **0 → 1**, `undo()` возвращает рамку побайтово. Двойной клик БЕЗ Ctrl: ручек 0, undo 0 |
| 36.14 | Групповая протяжка | там же | 2026-08-18 | два узла в выделении едут на ОДНУ дельту (`+60.65/+31.45` у обоих), шаг undo **один**, `undo()` возвращает обоих |
| 36.15 | ⛔ **Ветки расширения выделения читают КЛАВИАТУРУ** | `probe4.py` → `probe5` | 2026-08-18 | клик с модификатором Ctrl, но без предшествующего `KeyPress` Ctrl, в ветку `AdvancedGraphEditor.mousePressEvent` НЕ попадает (`_ext_click_target = None`, выделение не выросло): ветка читает защёлку `ctrl_pressed` ДО того, как базовый класс обновит её из модификаторов. С настоящим «Ctrl зажат»: клик по телу рамки даёт `('node', 'node_8')` и выделение `{node_6, node_8}`, клик по ребру — `('edge', ('node_42','node_52'))`, `selected_edges` = 1 |

### 36в. Гейты пункта

| # | что | команда | дата | результат |
|---|---|---|---|---|
| 36.16 | Набор зелёный | `python -X utf8 -m pytest tests/ui/test_gesture_dispatch.py -q` | 2026-08-18 | **19 passed** за 1.45 с |
| 36.17 | Новых красных нет | `python -X utf8 tools/suite_baseline.py --check` | 2026-08-18 | exit **0**; собрано **866** (было 847 + 19 моих), красные **57** без изменений (37 failed + 20 errors) — то есть в ПОЛНОМ прогоне набор тоже зелёный (отравления `cv2`/PySide6 на этом пути нет) |
| 36.18 | Сбор чист | `python -X utf8 -m pytest --collect-only -q` | 2026-08-18 | 866 собрано, 0 ошибок |
| 36.19 | Долг линтеров не вырос | `python -X utf8 tools/lint_gate.py --check` | 2026-08-18 | exit **0**: ruff BLE001/E722 — 199 в 73 файлах (эталон 199), mypy 0 ошибок |
| 36.20 | Сторож доков | `python -X utf8 -m pytest tests/test_docs_match_code.py -q` | 2026-08-18 | 16 passed (правка `TESTING.md` его не задела) |

### 36г. Д1 — набор проверен инъекциями в диспетчер (каждая красит своё)

| # | инъекция в боевой код | дата | что покраснело |
|---|---|---|---|
| 36.21 | `base_graph_editor.py`: убрана ветка `elif event.button() == LeftButton: self.ctrl_pressed = False` (защёлка Ctrl не снимается) | 2026-08-18 | `test_plain_lmb_press_disarms_stale_ctrl_latch` — 1 failed / 17 passed |
| 36.22 | `base_graph_editor.py`: убран сторож «кадр без кнопки» в `mouseMoveEvent` | 2026-08-18 | `test_button_less_move_ends_drag_and_node_stops_following` — 1 failed / 17 passed |
| 36.23 | `advanced_graph_editor.py`: `_node_drag_allowed()` → `return True` (узел таскается при активном инструменте) | 2026-08-18 | `test_active_tool_keeps_node_geometry_untouched` + `test_add_connector_tool_creates_node_without_moving_the_clicked_one` — 2 failed / 16 passed |
| 36.24 | `base_graph_editor.py`: `focusOutEvent` больше не зовёт `_reset_drag_state()` | 2026-08-18 | `test_focus_loss_mid_drag_finishes_gesture` — 1 failed / 17 passed |
| 36.25 | ⛔ **`DRAG_THRESHOLD` 5.0 → 0.0** (первая версия теста НЕ поймала) | 2026-08-18 | **18 passed, exit 0** — тест брал сдвиг как `DRAG_THRESHOLD / 2`, то есть ехал вместе с константой. Исправлено: порог пинится абсолютными числами с двух сторон (2 px — клик, 10 px — протяжка). После правки: `0.0` → красный `test_tremor_under_two_pixels_is_a_click_not_a_drag`; `50.0` → красные `test_deliberate_ten_pixel_move_is_a_drag`, `test_button_less_move_ends_drag_and_node_stops_following`, `test_focus_loss_mid_drag_finishes_gesture` |

Дерево после каждой инъекции восстанавливалось (`git checkout -- <файл>`, `git status` чист).

---

## 37. Пункт 0.3x дороги — гигиена тестовой инфраструктуры (2026-08-18)

**Что проверялось.** Пять хвостов ревизии 0.3: (1) модуль-уровневые моки `sys.modules`
в `tests/observability/`, (2) спящий очаг `test_bundled_fonts.py`, (3) constraints-файл для
CI, (4) перенос пина `shapely` из workflow в требования, (5) пол базовой линии. Боевой код
не правился ни строкой: дифф — тесты, требования, workflow, доки.

### 37а. Проверка адресов пункта (до первой строки кода)

| # | что | команда | дата | результат |
|---|---|---|---|---|
| 37.1 | Очаги из ревизии 0.3 живы | `grep -rn "sys.modules" tests/observability/ tests/ui/test_bundled_fonts.py` | 2026-08-18 | все четыре адреса подтверждены построчно: `test_detection_errors.py:20`, `test_ocr_errors.py:26`, `test_postprocess_log_level.py:38`, `test_skeleton_extension_substeps.py:35`; `test_bundled_fonts.py:28-31` тоже жив |
| 37.2 | ⛔ **РАСХОЖДЕНИЕ С QUEUE: файлов ПЯТЬ, а не четыре** | обход `tests/**` через `ast` (зонд `scan_sysmodules.py`) | 2026-08-18 | пятый — `tests/observability/test_segmentation_errors.py:32-33,39-46`: тот же мок `cv2` (плюс torch/tqdm и пять `pipe_segmentation.*`), но поставлен **циклом** `for _mod in (...): sys.modules.setdefault(_mod, ...)`, поэтому грепом по строке `"cv2"` не находился. Всего **13 записей в 6 файлах** |
| 37.3 | ⛔ **Мок ставился поверх РЕАЛЬНОЙ зависимости** | `python -c "import cv2; print(cv2.__version__)"`; `grep -n opencv requirements/ui.txt` | 2026-08-18 | `cv2` **4.11.0** стоит; `opencv-python>=4.9.0` объявлен в `requirements/ui.txt:26`, то есть есть и на раннере. Комментарий «cv2 отсутствует в тест-среде (§9 #2)» во всех четырёх файлах — **протух**: заглушка не замещала недостающее, а закрывала собой настоящее. То же у `skimage` (`scikit-image` в `ui.txt`) |
| 37.4 | Цена мины — прямой замер | `pytest tests/ui/test_square_size_ops.py -q` против `pytest tests/observability/test_detection_errors.py tests/ui/test_square_size_ops.py -q` | 2026-08-18 | в одиночку **12 passed**; в паре с ОДНИМ файлом-нарушителем — **6 passed, 11 errors** (`ValueError: not enough values to unpack (expected 4, got 0)` — `connectedComponentsWithStats` вернул `MagicMock`) |
| 37.5 | ⛔ **РАСХОЖДЕНИЕ: зазор пола вырос втрое** | `python -c "json…['min_collected']"` + `pytest --collect-only -q` | 2026-08-18 | в QUEUE записано «собрано 793 при поле 757, зазор 36»; на входе в пункт — **собрано 866, пол 757, зазор 109**. База не пересъёмывалась с 0.8 (`3bd604c`), а после неё легли 0.10 (+18), 0.11 (+21), 0.12 (+16), ВН1б (+14), 0.4 (+19). Вывод пункта не меняется, лечится тем же пересъёмом |
| 37.6 | Пин `shapely` действительно дублируется | `grep -rn "shapely==" requirements/ .github/` | 2026-08-18 | `worker.txt:38` (с обоснованием про API `STRtree`) и отдельная строка `pip install shapely==2.1.2` в workflow — два места, ни одно не сверяется с другим |

### 37б. Гейты пункта

| # | что | команда | дата | результат |
|---|---|---|---|---|
| 37.7 | База до правки | `python -X utf8 tools/suite_baseline.py --check` | 2026-08-18 | exit **0**: собрано 866, `37 failed / 796 passed / 13 skipped / 20 errors`, новых красных нет |
| 37.8 | ⭐ **База после правки: 11 чужих красных позеленели** | та же команда | 2026-08-18 | собрано **869**, `37 failed / 809 passed / 14 skipped / **9 errors**` — ошибок 20 → 9, и все 11 ушедших из `tests/ui/test_square_size_ops.py`. Гейт при этом **краснеет** (exit 1, «позеленело сразу 11 > порога 10» + «skipped вырос на прогоне, где что-то позеленело») — ровно как задумано: такой объём починки обязан объяснить себя пересъёмом базы (Д6, §37в) |
| 37.9 | ⭐ **Constraints воспроизводят базу** | тот же `--check`, но интерпретатором чистого venv (`api+ui+dev+shapely`, 76 пакетов) | 2026-08-18 | собрано **871**, `37 failed / 811 passed / 14 skipped / 9 errors` — счётчики и множество красных совпали с машиной разработки, разошлись только в числе собранных (то же дерево, замер сделан после +2 тестов пина). То есть окружение из `constraints-ci.txt` — не гипотеза: набор на нём даёт тот же цвет |
| 37.10 | Сбор чист | `python -X utf8 -m pytest --collect-only -q` | 2026-08-18 | **871** собрано, 0 ошибок |
| 37.11 | Долг линтеров не вырос | `python -X utf8 tools/lint_gate.py --check` | 2026-08-18 | exit **0**: ruff BLE001/E722 — 199 в 73 файлах (эталон 199), mypy 0 ошибок |
| 37.12 | Сторож доков не задет | `pytest tests/test_docs_match_code.py -q` | 2026-08-18 | 16 passed (правки `TESTING.md` его не касаются) |
| 37.13 | Тесты, чьи заглушки переехали | `pytest tests/observability tests/ui/test_bundled_fonts.py tests/ui/test_square_size_ops.py -q` | 2026-08-18 | 178 passed, 14 skipped (13 из них — `test_bundled_fonts` на Windows-offscreen, как и до правки) |
| 37.14 | Откуда взялся +1 `skipped` (13 → 14) | `pytest -q -rs --tb=no` | 2026-08-18 | это мой же `test_declared_dependency_is_not_shadowed[**skimage**]`: «в этой сессии не импортирован — подменять нечего». Вариант `[cv2]` **проходит** (в сессии живёт настоящий 4.11.0). Ни один чужой тест в skip не превратился |

### 37в. Д1 — сторожа проверены инъекциями (каждая красит своё)

| # | инъекция | дата | что покраснело |
|---|---|---|---|
| 37.15 | Вернуть `sys.modules.setdefault("cv2", MagicMock())` на уровень модуля в `test_detection_errors.py` | 2026-08-18 | оба слоя: `test_no_module_level_sys_modules_stubs` (структурный, назвал адрес) **и** `test_declared_dependency_is_not_shadowed[cv2]` (рантайм: `sys.modules['cv2'] = <MagicMock>`) — 2 failed / 5 passed |
| 37.16 | `requirements/dev.txt`: `shapely==2.1.2` → `2.1.1` | 2026-08-18 | `test_exact_pins_agree_between_worker_and_dev`: «пин разъехался … {'shapely': ('2.1.2', '2.1.1')}» — 1 failed / 2 passed |
| 37.17 | Состояние ДО правки (13 записей в 6 файлах) | 2026-08-18 | структурный сторож красный с полным списком адресов; рантайм-слой красный по `cv2`. То есть оба теста «упали бы до правки» — Д1 показан не рассуждением, а прогоном |

Дерево после каждой инъекции восстанавливалось (`git diff` пуст).

### 37г. Пересъём базовой линии (Д6, отдельный коммит) и пол

| # | что | команда | дата | результат |
|---|---|---|---|---|
| 37.18 | Пересъём базы | `python -X utf8 tools/suite_baseline.py --write-baseline` | 2026-08-18 | красных **57 → 46** (37 failed + 20 errors → 37 failed + **9 errors**); собрано 775 → **871**; пол `min_collected` **757 → 853**. Ушли ровно 11 идентификаторов `tests/ui/test_square_size_ops.py`, ни один красный не добавлен |
| 37.19 | Гейт после пересъёма | `python -X utf8 tools/suite_baseline.py --check` | 2026-08-18 | exit **0**, «новых красных нет» |
| 37.20 | ⛔ **Пол против ЧИСТОГО дерева** (условия CI) | `git worktree add … HEAD` + `pytest --collect-only -q` в нём | 2026-08-18 | в чистом дереве собрано **855** при поле **853** — запас **2**. Разница с локальными 871 — **16**, и `comm` по спискам сбора показывает: все 16 до единого — параметры `test_canvas_pipeline_golden.py::test_t10_corpus_chain` (корпус не в git), других расхождений ноль |
| 37.21 | ⚠ **Запас пола тает от чужих прогонов** | сопоставление с §31 (пункт 0.8) | 2026-08-18 | 0.8 мерил ту же разницу как **14** при 17 диаграммах в `storage/`; сейчас **16** — диаграмм стало 19 (прогоны 0.12 и ВН1б добавили `3263039b` и `0fc9d04c`). Механика: каждая новая диаграмма на машине разработки поднимает локальный сбор, пол считается от него, а CI столько не соберёт. Осталось 2 диаграммы до ЛОЖНОГО красного «набор усох» в CI. Поднимать `FLOOR_MARGIN` выше 22 нельзя — там потеря `shapely` перестанет ловиться; лечится счётом пола от чистого дерева. Кода стенда не трогал: это чужой пункт |

---

## 38. Пункт 0.3y дороги — пол набора отвязан от локального `storage/` (2026-08-18)

**Что проверялось.** Находка 0.3x: пол базовой линии снимается на машине разработки и
сверяется в том числе на раннере, а разница между ними — параметризация по корпусу из
локального `storage/`. Значит пол ехал вверх от каждого прогона конвейера. Правка — в
`tools/suite_baseline.py` (плюс второй потребитель пола, `tests/test_collection_clean.py`);
боевой код не тронут.

### 38а. Проверка адресов и чисел пункта (до первой строки кода)

| # | что | команда | дата | результат |
|---|---|---|---|---|
| 38.1 | Состав корпуса | `python -c "from tools import corpus; ..."` | 2026-08-18 | всего **19**, в git **3** (`089feca2`, `6e7144d5`, `d74eb9f1`), только в `storage/` — **16**. Разделение уже умеет сам загрузчик: `corpus_paths(include_storage=False)` |
| 38.2 | База на входе | чтение `tools/bench/suite_baseline.json` | 2026-08-18 | собрано **871**, `min_collected` **853**, красных 46 — числа пункта подтверждены |
| 38.3 | ⭐ **Из чего складывается разница 871 ↔ 855** | `pytest --collect-only -q` + разбор идентификаторов по uid8 | 2026-08-18 | **16 параметров, все до единого** — `test_canvas_pipeline_golden.py::test_t10_corpus_chain[<uid>]`; git-видимых **855**, и это ровно то число, которое собрал CI в прогоне 32139504030. То есть «что видит чистое дерево» считается арифметикой, без второго прогона |
| 38.4 | Потеря `shapely` = 22 теста | чистое venv, `pip uninstall shapely` + `--collect-only` | 2026-08-18 | собрано **871 → 849**; корпусных вне git в обоих случаях 16, значит git-видимых **855 → 833**. Порог «запас обязан быть уже 22» подтверждён замером, а не памятью |
| 38.5 | Кто ещё читает пол | `grep -rn min_collected` | 2026-08-18 | два потребителя: `tools/suite_baseline.py` и `tests/test_collection_clean.py:55`. Правка одного без другого развела бы их — счёт вынесен в `count_local_corpus`, тест его импортирует |
| 38.6 | Идиома «git-only» в проекте уже есть | `tools/layout_determinism.py:168,184` | 2026-08-18 | `--git-only` → `corpus.corpus_paths(include_storage=not args.git_only)`. Взял тот же источник истины, своего списка uid не завёл |

### 38б. Дефект показан прямым замером, а не рассуждением

| # | что | команда | дата | результат |
|---|---|---|---|---|
| 38.7 | ⛔ **Три «новые диаграммы» в `storage/`** (зонд: три каталога с копией фикстуры `d74eb9f1`) | `pytest --collect-only -q` + обе формулы | 2026-08-18 | собрано **879**, корпусных вне git **19**, git-видимых **860 — не изменилось**. Пол по СТАРОЙ формуле (собрано − 18) стал бы **861**, а раннер способен собрать только 860 → **ложный красный «набор усох»**. Пол по НОВОЙ (git-видимые − 5) — **855**, запас 5 |
| 38.8 | Инвариант в выводе самого стенда | `suite_baseline.py --check` до зонда и с зондом | 2026-08-18 | «собрано 876 (git-видимых **860**, корпус вне git 16)» → «собрано 879 (git-видимых **860**, корпус вне git 19)». Диаграммы прибавились, git-видимое число не дрогнуло |
| 38.9 | Зондовые каталоги сняты | `rmtree` по маске `zz0prob*` + пересчёт | 2026-08-18 | корпус вернулся к 19 (вне git 16), `storage/diagrams/` — те же 22 каталога, `git status` чист |

### 38в. Гейты пункта

| # | что | команда | дата | результат |
|---|---|---|---|---|
| 38.10 | Стенд на СТАРОЙ базе (коммит кода самодостаточен) | `python -X utf8 tools/suite_baseline.py --check` | 2026-08-18 | exit **0**: собрано 876 (git-видимых 860), пол 853 — новая семантика с прежним значением ключа не конфликтует, пересъём отдельным коммитом |
| 38.11 | Тесты стенда | `pytest tests/test_suite_baseline.py -q` | 2026-08-18 | **18 passed** (13 прежних + 5 новых: счёт корпуса вне git, фикстура git не вычитается, пустой storage, независимость пола от числа диаграмм, усыхание git-видимой части) |
| 38.12 | Сторож сбора | `pytest tests/test_collection_clean.py -q` | 2026-08-18 | 2 passed |
| 38.13 | ⭐ **Зонд «усыхание» (§25.8) по-прежнему красит** | `collect_ignore += ["ui"]` в `tests/conftest.py` | 2026-08-18 | сторож — `assert 661 >= 853` КРАСНЫЙ; стенд — exit **1**, «набор усох: в git-видимой части **661** < 853». Дерево восстановлено (`git diff` пуст) |
| 38.14 | ⭐ **Зонд «потеря shapely» по-прежнему красит** | чистое venv без `shapely`, `suite_baseline.py --check` | 2026-08-18 | exit **1**, «набор усох: в git-видимой части **838** < 853» (838 = 860 − 22) + 4 новых красных `test_client_spec.py`. `shapely` возвращён в venv |
| 38.15 | Долг линтеров не вырос | `python -X utf8 tools/lint_gate.py --check` | 2026-08-18 | exit **0**: ruff BLE001/E722 — 199 в 73 файлах (эталон 199), mypy 0 ошибок |

### 38г. Пересъём базовой линии (Д6, отдельный коммит)

| # | что | команда | дата | результат |
|---|---|---|---|---|
| 38.16 | Пересъём под новую семантику пола | `python -X utf8 tools/suite_baseline.py --write-baseline` | 2026-08-18 | множество красных **не изменилось** (46 = 37 failed + 9 errors); собрано 871 → **876** (+5 тестов стенда), в базе появились `corpus_local` **16** и `collected_git_visible` **860**; пол `min_collected` **853 → 855** — теперь это «860 git-видимых минус запас 5», а не «876 минус 18» |
| 38.17 | Гейт после пересъёма | `python -X utf8 tools/suite_baseline.py --check` | 2026-08-18 | exit **0**, «новых красных нет» |
| 38.18 | Что теперь значит пол на раннере | сверка с прогоном CI пункта 0.3x | 2026-08-18 | CI собирает ровно git-видимую часть: в 0.3x это было 855 при локальных 871. Значит запас между полом 855 и раннером — те самые **5**, и от числа диаграмм в `storage/` он больше не зависит: добавление трёх диаграмм оставило git-видимое число тем же (§38.7–38.8) |

## 39. Пункт 10.3 дороги — снос мёртвой тройки `ui/windows/` (2026-08-18)

**Что проверялось.** Три standalone-`QMainWindow` (`graph_validation_window.py` 524 +
`mask_validation_window.py` 555 + `cvat_window.py` 253 = 1332 строки) — двойники живых
вкладок. Пункт переносился из волны 10 в волну 0, чтобы убрать вечные красные из базовой
линии до 0.5. Живым в пакете остаётся только `main_window.py` (279).

### 39а. Проверка адресов и утверждений пункта (до первой строки кода)

| # | что | команда | дата | результат |
|---|---|---|---|---|
| 39.1 | Объём сноса | `wc -l ui/windows/*.py` | 2026-08-18 | 253 + 524 + 555 = **1332**, `main_window.py` **279**, `__init__.py` 4 — числа пункта подтверждены |
| 39.2 | Внешних импортёров нет (пересъём §7.2) | греп `ui\.windows\|CVATWindow\|MaskValidationWindow\|GraphValidationWindow` по репо без `_scratch/.claude/storage/models/dist` | 2026-08-18 | тройку импортируют только `ui/windows/__init__.py:2-4` и `tests/test_stage7_graph_flow.py`. Формы `from ui.windows import X` в коде **0**. `MainWindow` живёт своим прямым импортом `ui/main.py:34` |
| 39.3 | PyInstaller-спека | чтение `deploy_ready/pid_client_windows.spec` | 2026-08-18 | ссылок на `ui.windows` нет; пакет попадает в сборку через `collect_submodules('ui')` (`:18`) — снос модулей просто убирает их из обхода |
| 39.4 | Базовая линия файла тестов | `python -m pytest tests/test_stage7_graph_flow.py -q` | 2026-08-18 | **24 failed** — совпадает с §24.5 (2026-08-17) |
| 39.5 | Состав 24 тестов | разбор файла | 2026-08-18 | 13 держатся за `GraphValidationWindow` (3 импорт-проверки + 8 логики + 2 кросс-модульных), 11 — «грепы по исходнику» `ui/widgets/diagram_workspace.py` |
| 39.6 | ⛔ **Из 11 «чистых грепов» переезжают только 6** | прогон 11 утверждений по живому `ui/widgets/diagram_workspace.py` | 2026-08-18 | **6 верны, 5 ложны**: `_simple_graph_done` (флаг + сброс в `load_diagram` + установка в `_on_simple_graph_confirmed` + переоткрытие + ветвление в `_open_graph_validation`). Фаз две, но они разведены по методам `_open_graph_validation` (`:1905`, SimpleGraphTab) и `_open_graph_editor` (`:1937`, AdvancedGraphTab), порядок ведёт цепочка бусин |
| 39.7 | ⭐ **Флага не было никогда** | `git log --all -S"_simple_graph_done" -- ui/widgets/diagram_workspace.py` | 2026-08-18 | **пусто**. Строка встречается в истории только в самом тест-файле (`058aba9`, `7904c18`) — 5 проверок были красны с рождения, а не протухли |
| 39.8 | Почему красны и остальные 6 | чтение `tests/test_stage7_graph_flow.py:236` | 2026-08-18 | `PROJECT_ROOT` берётся как «каталог, в котором есть `ui/`», а `tests/ui/` существует → корень уезжает в `tests/`, и `read_text` бьёт `FileNotFoundError`. В новом файле корень — `parents[2]`, как у соседа `tests/ui/test_save_dialog_dir.py:24` |

### 39б. Д1 — переехавшие проверки красят на инъекции в `diagram_workspace.py`

Инъекция — правка живого файла с восстановлением `git checkout` (дерево чистое после каждой).

| # | инъекция | дата | результат |
|---|---|---|---|
| 39.9 | `SimpleGraphTab`→`SimpleGraphPanel`, `AdvancedGraphTab`→`AdvGraphPanel`, `def _on_simple_graph_confirmed`→`…_ok` | 2026-08-18 | **3 failed** — по одному на свою проверку |
| 39.10 | дописан `from ui.tabs.graph_tab import GraphTab`; из docstring `_open_graph_validation` убран `SimpleGraphTab` | 2026-08-18 | **2 failed** — запрет старого таба и docstring |
| 39.11 | ⭐ **Первая инъекция «переименованием с суффиксом» ничего не покрасила** | 2026-08-18 | `…TabZZ` содержит `…Tab` как префикс, и подстрочный `in` остаётся истинным. Проверки подстрокой ловят удаление, но не переименование — инъекция обязана убирать подстроку, а не удлинять её |
| 39.12 | ⛔ **`complete_graph_validation` в теле метода — ложная проверка** | 2026-08-18 | подмена вызова на `finish_graph_validation` оставила тест **зелёным**: имя есть в самом docstring метода (`:2183`). Проверка переписана на вызов `self.api_client.complete_graph_validation(` — та же инъекция стала **1 failed** |

### 39в. Гейты пункта (класс «снос мёртвого кода»)

| # | что | команда | дата | результат |
|---|---|---|---|---|
| 39.13 | Импорт боевого API | `python -c "import app.main"` | 2026-08-18 | OK |
| 39.14 | Реэкспорт пакета | `python -c "from ui.windows import MainWindow"` | 2026-08-18 | OK, `MainWindow.__module__ = ui.windows.main_window` |
| 39.15 | Сбор pytest | `python -m pytest --collect-only -q` | 2026-08-18 | **858 собрано, 0 ошибок** (876 − 24 снесённых + 6 переехавших) |
| 39.16 | Все модули `ui.*` импортируются | обход `pkgutil.walk_packages(ui.__path__)` под `QT_QPA_PLATFORM=offscreen` | 2026-08-18 | **53 из 54**. Единственный отказ — `ui/editors/test_editors.py` (`No module named 'square_mask_editor'`): дев-скрипт от 2026-04-07 с топ-левел импортами, к сносу отношения не имеет, чужой долг |
| 39.17 | ⭐ **Все вкладки открываются** | зонд «класс на процесс» (`show()` + `processEvents()`), offscreen, MagicMock-клиент | 2026-08-18 | **8 из 8**: SimpleGraphTab, AdvancedGraphTab, ContourTab, CvatTab, FrameTab, JunctionTab, OcrBindingTab, PipeTab. Плюс `MainWindow` открывается отдельно. Один процесс на вкладку обязателен: часть вкладок держит фоновые QThread и сама не завершается; `CvatTab` берёт `cvat_url`, а не `api_client`; `FrameTab` в `__init__` грузит `original_image` и на ошибке открывает МОДАЛЬНЫЙ `QMessageBox` — под offscreen он висит вечно (в зонде подложен настоящий PNG) |
| 39.18 | ⭐ **Сборка клиента PyInstaller** | `python -m PyInstaller --noconfirm --clean --distpath <scratch> deploy_ready/pid_client_windows.spec` | 2026-08-18 | exit **0**, `PID-Client.exe` собран, 699 с, 5.3 ГБ. В TOC сборки от пакета остались ровно `ui.windows` и `ui.windows.main_window`; предупреждений о ненайденных `ui.windows.*` в warn-файле нет |
| 39.19 | Долг линтеров | `python -X utf8 tools/lint_gate.py --check` | 2026-08-18 | до пересъёма exit **0** с сообщением «долг оплачен»: `graph_validation_window.py` 4 → 0, `mask_validation_window.py` 4 → 0, всего **199 → 191** |
| 39.20 | ⛔ **Три новых красных на первом прогоне** | `python -X utf8 tools/suite_baseline.py --check` | 2026-08-18 | exit 1. Два — про сами эталоны (`test_collection_clean::test_suite_does_not_shrink` — пол 855 против 842 git-видимых; `test_lint_gate::test_baseline_holds_only_tracked_files` — эталон линтера называл снесённые файлы), лечатся пересъёмом. Третий — настоящая находка, см. §39.21 |
| 39.21 | ⭐ **Что пропустил греп адресов** | `tests/test_refactoring.py:695` | 2026-08-18 | `TestDeadCodeRemoved::test_graph_validation_window_imports_from_advanced` читал снесённый файл, но собирал путь из кусков — `PROJECT_ROOT / "ui" / "windows" / "graph_validation_window.py"`. Ни `ui/windows`, ни `ui.windows`, ни `GraphValidationWindow` в строке нет, поэтому греп §39.2 его не увидел. **Урок: путь, собранный по частям, не ловится грепом по литералу пути** — искать ещё и по basename модуля. Тест заменён на `test_dead_windows_deleted` (идиома класса: `assert not path.exists()` для всех трёх файлов), Д1 — инъекция возвратом `cvat_window.py` красит его |

### 39г. Пересъём эталонов (Д6, отдельный коммит)

| # | что | команда | дата | результат |
|---|---|---|---|---|
| 39.22 | Эталон линтера | `python -X utf8 tools/lint_gate.py --write-baseline` | 2026-08-18 | 199 → **191** нарушений, из `per_file` ушли две строки снесённых окон (4 + 4) |
| 39.23 | Эталон набора | `python -X utf8 tools/suite_baseline.py --write-baseline` | 2026-08-18 | красных **46 → 22** (13 failed + 9 errors), **ушли 24, новых 0**; собрано 876 → **858**, git-видимых 860 → **842**, пол `min_collected` 855 → **837** |
| 39.24 | ⚠ Пересъём набора пришлось делать ДВАЖДЫ | тот же `--write-baseline` | 2026-08-18 | первый прогон записал 24 красных — в их числе два, красных лишь из-за старых эталонов (§39.20). Порядок обязателен: сначала эталон линтера, потом эталон набора, иначе набор консервирует чужую красноту. После второго прогона — 22 |
| 39.25 | Гейт после пересъёма | `python -X utf8 tools/suite_baseline.py --check` + `lint_gate.py --check` | 2026-08-18 | обе exit **0**: «новых красных нет» (858 собрано, git-видимых 842, пол 837), «долг не вырос» (191 при эталоне 191) |

## 40. Пункт 0.5 дороги — один `ArtifactDownloader` вместо четырёх + слияние `_RecognizeWorker` (2026-08-18)

Область пункта сужена решением Максима 2026-08-18 (строка 0-14 `QUEUE.md`) по замерам §40а.

### 40а. Проверка утверждений пункта до первой строки кода (AST-сверка, не на глаз)

| # | что | команда | дата | результат |
|---|---|---|---|---|
| 40.1 | Копий `ArtifactDownloader` | `grep -rn "class .*ArtifactDownloader" ui/ app/ worker/ tests/ tools/` | 2026-08-18 | **4**, не 6: `base_graph_tab.py:661`, `junction_tab.py:27`, `pipe_tab.py:27`, `ocr_binding_tab.py:59`. Две из шести жили в `ui/windows/`, снесённом пунктом 10.3 — план подтверждён с поправкой |
| 40.2 | ⛔ **«`JunctionTab`/`PipeTab` → `BaseGraphTab`, 4 метода байт-в-байт» ОПРОВЕРГНУТО** | AST-разбор: тела методов классов сравнены построчно | 2026-08-18 | байт-в-байт с базой совпадают **3** метода = **9 строк** (`_appearance_editor` 2, `_on_download_error` 4, `_undo` 3). Четвёртый (`has_unsaved_changes`) совпадает у Junction с Pipe, но **не** с базой. Остальные 9 общих имён (`__init__`, `_setup_ui`, `_on_downloaded`, `_download_artifacts`, `_on_confirm`, `_build_appearance_controls`, `apply_saved_appearance`, `apply_default_appearance`) — разные. `BaseGraphTab` при этом абстрактен по `_create_editor() -> BaseGraphEditor` (`:798`), сохраняет через `upload_validated_graph` (`:1152`) и считает грязь по `undo_mgr.revision` (`:1146`) — у масочных вкладок нет ни того, ни другого, ни третьего |
| 40.3 | `AppearanceMixin` | грепом по `ui/` | 2026-08-18 | **1** класс (`ui/widgets/appearance_panel.py:203`), потребителей **4** (Base/Junction/Pipe/Ocr). «5 копий» плана — обвязка, а не класс; аудит §0.5 подтверждён |
| 40.4 | `MaskEditorBase` «~200 строк инфраструктуры» | AST + `difflib` по 15 общим методам `PolylineMaskEditor` ↔ `SquareMaskEditor` | 2026-08-18 | совпадает **198 строк из 479**, но байт-в-байт — **3** (`_update_status`). Расхождения именами (`bg_item`↔`original_item`, `_shift_pressed`↔`shift_pressed`) и поведением (`coco_full_data`, `_cancel_rect_erase`). **Перенесён в волну 10 (10-16)** решением Максима |
| 40.5 | `_RecognizeWorker` | чтение обоих | 2026-08-18 | **2** копии подтверждены; расходятся на `isinstance(resp, dict)`-guard (есть в ocr-версии, нет в графовой) и типизацию сигнатуры |
| 40.6 | Строгость политики ошибок графовой вкладки | чтение `base_graph_tab.py:712-731` | 2026-08-18 | необязательные артефакты глотают **только `APIError`**; junction/pipe/ocr глотают `(APIError, Exception)` = всё. Разница несущая: тихо потерянный `graph_canvas` даёт «холст пересобран заново, прежние правки не сохранятся» (`:934-945`), тихо потерянные контуры — выброшенные правки оператора |

### 40б. Тест до правки (шаг 3 протокола — зона без покрытия)

| # | что | команда | дата | результат |
|---|---|---|---|---|
| 40.7 | Характеризационный набор на СТАРОМ коде | `pytest tests/ui/test_artifact_downloader.py -q` на `3d1e574` | 2026-08-18 | **53 passed**. Фиксируют: карту «ключ → имя файла» всех 5 конфигураций вкладок, цепочки фолбэков, обязательность, политику ошибок, sha256 бит-в-бит |
| 40.8 | Тот же набор ПОСЛЕ правки | та же команда | 2026-08-18 | **53 passed** — двинут только адаптер `_downloader()` (одна функция теста), ни одного утверждения |

### 40в. Д1 — зубастость набора: девять инъекций, каждая красит своё

Инъекция — правка живого файла с восстановлением из индекса (дерево чисто после каждой).

| # | инъекция | дата | результат |
|---|---|---|---|
| 40.9 | порядок кандидатов графа: `graph_json` первым | 2026-08-18 | **4 failed**: `test_downloaded_files_are_bit_exact[graph]`, `[graph_canvas]`, `test_server_is_asked_only_for_what_the_tab_needs[graph]`, `[graph_canvas]` |
| 40.10 | снят `swallow=(APIError,)` у контуров | 2026-08-18 | **1 failed**: `test_contours_non_api_error_stops_the_tab` |
| 40.11 | снят `failure_key` у контуров | 2026-08-18 | **1 failed**: `test_contours_non_404_sets_unknown_flag` |
| 40.12 | снят `swallow=(APIError,)` у `graph_canvas` | 2026-08-18 | **1 failed**: `test_graph_canvas_non_api_error_stops_the_tab` |
| 40.13 | pipe: фолбэк отдаёт ключ первого кандидата | 2026-08-18 | **1 failed**: `test_pipe_mask_key_depends_on_the_candidate` |
| 40.14 | junction: маска перестала быть обязательной | 2026-08-18 | **1 failed**: `test_required_artifact_missing_gives_error[junction-missing3]` |
| 40.15 | ocr: `coco_predicted` кладётся под своим ключом | 2026-08-18 | **1 failed**: `test_ocr_coco_falls_back_to_predicted_with_own_filename` |
| 40.16 | загрузчик: не-404 `APIError` считается 404 | 2026-08-18 | **1 failed**: `test_optional_404_does_not_raise_contours_flag` |
| 40.17 | загрузчик: цепочка не пробует второго кандидата | 2026-08-18 | **8 failed** — все проверки фолбэков разом |

### 40г. Гейты пункта (класс [реф])

| # | что | команда | дата | результат |
|---|---|---|---|---|
| 40.18 | ⭐ **Бит-в-бит на реальном uid** | зонд: поддельный `APIClient` отдаёт настоящие файлы из `storage/diagrams/0fc9d04c…/`; карты «ключ → имя файла + sha256» сняты в worktree на `3d1e574` (старые классы) и в рабочем дереве (новый) | 2026-08-18 | `diff` **пуст** — 5 конфигураций вкладок, 22 артефакта, все sha256 совпали. Проверены и обе ветки холста: `graph_canvas` и `contours_validated` реально приезжают |
| 40.19 | Все вкладки открываются | зонд «класс на процесс» offscreen с `MagicMock`-клиентом (идиома §39.17) | 2026-08-18 | **8 из 8**: SimpleGraphTab, AdvancedGraphTab, ContourTab, CvatTab, FrameTab, JunctionTab, OcrBindingTab, PipeTab |
| 40.20 | Импорт боевого API | `python -c "import app.main"` + импорт всех правленых модулей `ui.tabs.*` | 2026-08-18 | OK |
| 40.21 | Долг линтеров | `python -X utf8 tools/lint_gate.py --check` | 2026-08-18 | exit **0**, «долг оплачен»: **191 → 178** (`base_graph_tab` 3→1, `junction_tab` 4→2, `ocr_binding_tab` 15→8, `pipe_tab` 5→3); mypy 0 ошибок, оба новых модуля добавлены в `files` по правилу «храповик по включению» |
| 40.22 | Сирот от правки нет | `ruff --isolated --select F401,F811,F821 ui/tabs ui/services` до и после | 2026-08-18 | **12 → 10**: правка не завела ни одного неиспользуемого имени, а два убрала (`QObject`, `Signal`) |
| 40.23 | Базовая линия | `python -X utf8 tools/suite_baseline.py --check` | 2026-08-18 | exit **0**, «новых красных нет»: собрано **911** = 858 + 53 теста, красных **22** без изменений (13 failed + 9 errors) |
| 40.24 | Сбор | `python -X utf8 -m pytest --collect-only -q` | 2026-08-18 | **911 собрано, 0 ошибок** |
| 40.25 | Соседние стенды не тронуты | `tools/edit_bench.py --all --check`, `tools/pair_bench.py --check` | 2026-08-18 | «рост дефектов: 0», «рост правок: 0», обе exit 0 |

### 40д. Заявленные расхождения (только графовая вкладка; три остальные не изменились)

| # | что | дата | результат |
|---|---|---|---|
| 40.26 | Загрузка стала параллельной | 2026-08-18 | было последовательно, стало 3 (или 5 при `USE_CANVAS`) потоков — как у трёх остальных вкладок. Набор файлов и их байты те же (§40.18) |
| 40.27 | Текст статус-строки во время загрузки | 2026-08-18 | «Загрузка graph_validated…» → «Загрузка артефактов…» + «Загружен graph_json» (стиль трёх остальных вкладок) |
| 40.28 | Логи | 2026-08-18 | `Loaded graph_validated` / `Loaded graph_json (no validated version)` → единая строка загрузчика «артефакт %s загружен как %s»: информация о сработавшем кандидате сохранена |
| 40.29 | `AdvancedGraphTab` и не-dict ответ `recognize_boxes` | 2026-08-18 | было `AttributeError` в сигнал `error`, стало пустой список (guard перенесён из ocr-версии по требованию `PLAN_AUDIT` §0.5) |

---

## 41. Пункт 1.10 дороги — приёмник логов клиента (2026-08-18)

Первый пункт волны 1. Резервная копия волны (решение Максима 2026-08-17) на месте:
`C:\project\pid_backup\wave1_pre\storage_2026-08-18.zip` (157 МБ) +
`pid_pipeline_2026-08-18.sql` (456 КБ).

### 41а. Проверка утверждений пункта до первой строки кода

| # | что | команда | дата | результат |
|---|---|---|---|---|
| 41.1 | «Логи выбрасываются»: приёмника нет | `grep -rn "FileHandler\|RotatingFile" app ui worker modules` | 2026-08-18 | **0 попаданий** во всём боевом коде — подтверждено |
| 41.2 | `sys.excepthook` нигде не ставится | `grep -rn "excepthook" app ui worker modules tools tests` | 2026-08-18 | **0 попаданий** — подтверждено |
| 41.3 | `ui/main.py:21-25` = `basicConfig(stream=sys.stdout)` | чтение файла | 2026-08-18 | подтверждено дословно |
| 41.4 | `console=False` в спеке | `grep -n "console=" deploy_ready/pid_client_windows.spec` | 2026-08-18 | строка **:82** (в мастер-плане :83 — поправка `PLAN_AUDIT` верна) |
| 41.5 | ⛔ Премисса «63 файла из 69 логируют» — ЛОЖНА (подтверждение `PLAN_AUDIT` §1.10) | `find ui -name '*.py'` / `grep -rl "getLogger\|logging\." ui` | 2026-08-18 | файлов в `ui/` — **68** (не 69: один ушёл с `ui/windows/` в 10.3), логируют **22**. Приёмник делает видимыми логи 22 файлов из 68, а не 63 из 69 |
| 41.6 | `client.cfg` умеет задать переменную приёмнику | чтение `client_main.py:25-44` | 2026-08-18 | `_load_client_cfg` кладёт любые `KEY=VALUE` в `os.environ` (setdefault) **до** импорта `ui.main` → `PID_LOG_DIR`/`PID_LOG_LEVEL` работают из `client.cfg` |
| 41.7 | Куда НЕЛЬЗЯ писать лог | чтение `client_main.py:42` | 2026-08-18 | `os.chdir(_data_dir())` — CWD собранного клиента это распакованный `_MEIPASS` (временный). Относительный путь = потерянный лог (родня дефекта 1.19) |

### 41б. Замеры поведения (то, ради чего пункт)

| # | что | команда | дата | результат |
|---|---|---|---|---|
| 41.8 | PySide6 отдаёт исключение слота в `sys.excepthook`, а не роняет процесс | зонд: `QTimer.singleShot` → `raise`, свой хук, offscreen | 2026-08-18 | хук вызван, цикл событий продолжился, процесс завершился **rc=0**. PySide6 **6.11.1**. Значит `excepthook` — правильная и достаточная точка перехвата |
| 41.9 | ⭐ Условие собранного клиента: `console=False` → `sys.stdout` и `sys.stderr` = `None` | зонд: оба потока сняты в `None`, затем боевой `setup_client_logging()` + запись + падение | 2026-08-18 | файл получил **все 7 строк**, включая трассировку; процесс **не упал** (rc=0). Консольный хендлер там молча пустеет (`Handler.handleError` при `sys.stderr is None` ничего не делает) — файловый от этого не страдает |
| 41.10 | Корреляционные поля файлового хендлера самодостаточны | тест `only_file_handler`: с корня сняты все хендлеры, кроме файлового | 2026-08-18 | строка по-прежнему несёт `uid=…`. Без своего `ContextFilter` на файловом хендлере поля приходили бы «в подарок» от консольного (он отрабатывает первым и правит саму запись) — в `.exe` такого подарка нет |

### 41в. Д1 — зубастость набора: семь инъекций, каждая красит своё

Инъекция — правка живого файла с восстановлением из копии, снятой ДО инъекции
(не `git checkout`, см. PROTOCOL §5). Дерево после каждой чисто.

| # | инъекция | дата | результат |
|---|---|---|---|
| 41.11 | снят `install_excepthook()` из `ui/main.py:main()` | 2026-08-18 | **2 failed**: `test_slot_exception_lands_in_log_file`, `test_scenario_line_carries_uid_and_client_survives` |
| 41.12 | снят `setup_client_logging()` из `main()` | 2026-08-18 | **2 errors**: файла лога нет вовсе — фикстура сценария валится с выводом процесса |
| 41.13 | `encoding="cp1251"` вместо utf-8 | 2026-08-18 | **6 failed + 2 errors**: эмодзи/кириллица рвут запись, сценарий не находит своей строки |
| 41.14 | снят `handler.addFilter(ContextFilter())` | 2026-08-18 | **2 failed**: обе проверки `uid` (после ужесточения по §41.10 — до него инъекция проходила незамеченной) |
| 41.15 | `MAX_BYTES = 0` (ротация формально «есть») | 2026-08-18 | **1 failed**: `test_rotation_is_bounded_from_both_sides` |
| 41.16 | снята стартовая строка «Лог клиента: …» (Д3) | 2026-08-18 | **1 failed**: `test_startup_line_names_the_log_file` |
| 41.17 | снят `bind_uid(uid)` из `DiagramWorkspace.load_diagram` | 2026-08-18 | **1 failed**: `test_open_diagram_binds_its_uid` |

### 41г. Гейты пункта (класс [ui])

| # | что | команда | дата | результат |
|---|---|---|---|---|
| 41.18 | **Гейт QUEUE: искусственное исключение в слоте → строка в файле лога** | `pytest tests/ui/test_client_logging.py -q` | 2026-08-18 | **17 passed**. Сценарий поднимает клиент его же точкой входа `ui.main.main()` (подменены только `MainWindow` и класс `QApplication`, цикл событий живой, offscreen, отдельный процесс), роняет слот и находит в ФАЙЛЕ: сообщение, `RuntimeError`, `Traceback`, уровень `CRITICAL`, `uid=0fc9d04c`; клиент переживает падение (rc=0) |
| 41.19 | Базовая линия | `python -X utf8 tools/suite_baseline.py --check` | 2026-08-18 | exit **0**, «новых красных нет»: собрано **928** = 911 + 17, красных **22** без изменений (13 failed + 9 errors), passed 875 → 892 |
| 41.20 | Сбор | `python -X utf8 -m pytest --collect-only -q` | 2026-08-18 | **928 собрано, 0 ошибок** |
| 41.21 | Долг линтеров | `python -X utf8 tools/lint_gate.py --check` | 2026-08-18 | exit **0**: ruff 178/эталон 178 (новый модуль не добавил широких `except`), mypy **0 ошибок** — `ui/services/client_logging.py` внесён в `files` по правилу «храповик по включению» |
| 41.26 | CI на финальном коммите пункта | `gh run watch 32178215839` | 2026-08-18 | **зелёный, все 5 шагов**: сбор **912** = ровно предсказанная git-видимая часть базы, «новых красных нет», ruff 178/178, воспроизводимость раскладки. Сценарный тест прошёл и там — на раннере PySide6 **6.11.2** против 6.11.1 локально, поведение `excepthook` то же |
| 41.27 | Пересъём базы (Д6, отдельный коммит 8a70f49) | `python -X utf8 tools/suite_baseline.py --write-baseline` | 2026-08-18 | собрано 911 → **928**, git-видимых 895 → **912**, пол 890 → **907**, passed 875 → **892**; множество красных **не изменилось** (22) |

### 41д. Заявленные расхождения поведения

| # | что | дата | результат |
|---|---|---|---|
| 41.22 | Формат строки клиента | 2026-08-18 | был `%(asctime)s | %(name)s | %(levelname)s | %(message)s`, стал общий с сервером `LOG_FORMAT` (`app/core/logging.py`) — добавились `uid/phase/step/attempt/task/dur`. Так лог клиента сшивается с серверным по `uid=` |
| 41.23 | Момент настройки | 2026-08-18 | было на импорте модуля `ui.main`, стало первой строкой `main()`. Следствие: импорт `ui.main` в тестах больше не трогает корневой логгер и не создаёт файлов |
| 41.24 | Уровни сторонних библиотек | 2026-08-18 | `setup_logging()` глушит `httpx`/`httpcore`/`uvicorn.access`/`PIL` до `WARNING`; при прежнем `basicConfig(level=INFO)` они писали наравне с нашими (`httpx 0.28.1` — 2 вызова `logger.info` в `_client.py`, строка «HTTP Request: …» на каждый опрос статуса, а опрос идёт раз в 2 с) |
| 41.25 | Единственная зависимость `ui/` от `app/` | 2026-08-18 | до пункта их было **0** (`grep -rn "^from app\." ui` пуст). Заведена одна, в одном файле, по требованию Д2 («встраиваться в obs + ContextFilter, не заводить своё») — это же и шов для этапа 13 |

## 42. Пункт 1.2 дороги — окно невидимости брокера (Б10) (2026-08-18)

Второй пункт волны 1. Класс [сма]: сначала воспроизведение текущего поведения на живом
брокере, потом правка. Стенд — `tools/redelivery_bench.py` (отдельная база Redis 15 и своя
очередь `probe_redelivery`; боевых очередей базы 0 не касается).

### 42а. Проверка утверждений пункта до первой строки кода

| # | что | команда | дата | результат |
|---|---|---|---|---|
| 42.1 | Адрес `worker/celery_app.py:49-50` | чтение файла | 2026-08-18 | подтверждён дословно: `task_time_limit=3600`, `task_soft_time_limit=3300`; `task_acks_late=True` :53, `task_reject_on_worker_lost=True` :54 |
| 42.2 | Адрес `worker/tasks/detection.py:56-57` | чтение файла | 2026-08-18 | подтверждён дословно: `time_limit=5400`, `soft_time_limit=5340`, `acks_late=True` :58 |
| 42.3 | `broker_transport_options` не задан нигде | `grep -rn "visibility_timeout\|broker_transport_options" --include=*.py --include=*.yml .` | 2026-08-18 | **0 вхождений в коде** (только тексты планов) — подтверждает замер 4.3 от 2026-08-14 |
| 42.4 | Дефолт kombu | чтение установленного `kombu/transport/redis.py` | 2026-08-18 | `visibility_timeout = 3600` у `Channel`, ключ читается из `from_transport_options` — то есть `broker_transport_options` и есть правильная ручка |
| 42.5 | Все `time_limit` по декораторам | `tools/redelivery_bench.py --leg C` | 2026-08-18 | задач с лимитом **12**, максимум **5400** (`task_detect_yolo`); дефолт 3600 короче него в 1.5 раза и ровно равен лимиту сегментации и OCR |
| 42.6 | Что реально видит брокер ДО правки | `celery_app.connection().default_channel.visibility_timeout` | 2026-08-18 | **3600** и на хосте, и в боевом контейнере `pid_worker`; `conf.broker_transport_options` = `{}` |
| 42.7 | Версии на хосте и в бою совпадают | `python -c "import celery,kombu,redis"` там и там | 2026-08-18 | celery **5.6.3**, kombu **5.6.2**, redis **6.4.0** — стенд на хосте меряет то же, что стоит в бою |
| 42.8 | ⛔ **ПОПРАВЛЕНО 2026-08-19 по возврату ревизора: строка мерила НЕ ТУ ВЕЛИЧИНУ.** Было: «ETA/countdown длиннее окна нет → ловушка не грозит», и сравнивался голый `countdown` (60 с) с окном 7200. Правильная мера — **возраст записи в `unacked`** | перечитано в установленном kombu: `redis.QoS.append` пишет `zadd(unacked_index, {delivery_tag: time()})`; `restore_visible` берёт `ceil = time() − visibility_timeout` | 2026-08-19 | Отсчёт идёт **от ДОСТАВКИ сообщения воркеру, а не от начала исполнения** (подтверждено и прямым наблюдением: `score` записи не менялся все 2.5 минуты, пока задача считалась). При `acks_late` подтверждение уходит только по завершении, поэтому возраст = countdown + ожидание свободного слота + само исполнение. Арифметика по лимитам самого кода (не замер): ретрай детекции 60 + ожидание за занятыми слотами до 5400 + исполнение до 5400 ≈ **10860 > 7200** → под нагрузкой передоставка ретрая возвращается. **Вывод: конечным окном этот остаточный путь не закрывается — это не про число 7200.** Наследника Б10 заводит архитектор строкой QUEUE; фактические `default_retry_delay` в коде: 60 у восьми задач, 30 у FXML (`graph.py:462`), явных `countdown=`/`eta=` нет |

### 42б. Воспроизведение дефекта (то, ради чего пункт)

Боевая связка `visibility_timeout=3600 < time_limit=5400` сжата в 720 раз: окно 5 с, задача 150 с.
Задача-заглушка зарегистрирована ПОД ИМЕНЕМ боевой детекции, тело — `sleep`, боевой код не исполняется.

| # | что | команда | дата | результат |
|---|---|---|---|---|
| 42.9 | Дубль ДО правки (нога A, Windows/threads) | `python -X utf8 tools/redelivery_bench.py --leg A --check` | 2026-08-18 | **доставок 2** при одной отправке, `task_id` ОДИН И ТОТ ЖЕ (`e4705be5…`) — это передоставка, а не второй диспатч. В логе Celery две строки `Task worker.tasks.detection.task_detect_yolo[e4705be5…] received`, разрыв **17.9 с** |
| 42.10 | Тот же дубль в боевом контейнере (Linux, **prefork**) | `docker exec -w /app pid_worker python -X utf8 tools/redelivery_bench.py --leg A --check` | 2026-08-18 | **доставок 2** (дубль через 81.6 / 137.8 / 137.7 с в трёх прогонах) — механизм не зависит от пула и воспроизводится в боевом образе. ⚠ Эти три прогона шли ДО находки 42.20 (воркер стенда сидел на базе 0): сама передоставка в них настоящая — один и тот же `task_id` пришёл дважды, — но чистые числа сняты после пина базы, см. 42.13 |
| 42.11 | Почему нога A держит 150 с, а не 20 | чтение kombu + прогоны | 2026-08-18 | воркер зовёт `maybe_restore_messages` каждые **10 с**, а тело `restore_visible` отрабатывает каждый **10-й** вызов → скан «просроченных» идёт примерно раз в **100 с**. Замер подтвердил разброс: дубль приходил через **17.9 с** (скан на 10-й секунде) и через **104.9 с** (следующий скан) |
| 42.20 | ⛔ ЛОВУШКА, найденная стендом: `CELERY_BROKER_URL` **сильнее** аргумента `Celery(broker=...)` | строка `Connected to redis://redis:6379/0` в логе воркера стенда, запущенного в контейнере | 2026-08-18 | воркер стенда в контейнере садился на **боевую базу 0** вместо 15, хотя стенд печатал `/15`: переменная окружения контейнера перебивает конструктор. Следствие — контакт прогонов между собой (в один заход приходил uid прошлого) и мусор в базе 0: 2 записи `unacked` + пустая очередь `probe_redelivery` + её binding, всё с routing_key стенда (боевые очереди `default`/`gpu`/`ocr`/`sam2` пусты и не тронуты; `flushdb` стенд делает только в базе 15). Закрыто пином окружения при импорте модуля стенда |
| 42.21 | ⛔ Где ногу A судить НЕЛЬЗЯ: Windows + пул `threads` | 5 прогонов стенда на хосте, база 15, чистые флаши | 2026-08-18 | дубль воспроизвёлся **2 раза из 5**: 17.9 и 104.9 с, затем три прогона подряд (150, 200, 300 с задачи) — **ноль передоставок** при том, что сообщение всё это время лежало в `unacked` с просроченным окном (проверено поштучным опросом ключей `unacked`/`unacked_index` раз в 12 с: `score` не менялся, запись не восстанавливалась). То есть периодический скан kombu на этом пуле до задачи не доходит. Вывод для дороги: **нога A судится в боевой среде (Linux + prefork), на Windows стенд честно отдаёт «судить нечем» (exit 2), а не зелёный** |

### 42в. Приёмка после правки

| # | что | команда | дата | результат |
|---|---|---|---|---|
| 42.12 | Опция доехала до брокера | `--leg C` на хосте и в контейнере | 2026-08-18 | канал отдаёт **7200** при 7200 в конфиге (было 3600 при пустом конфиге) — в обеих средах |
| 42.13 | ⭐ ГЕЙТ ПУНКТА — стенд целиком в БОЕВОЙ среде (Linux, prefork, чистая база 15) | `docker exec -w /app pid_worker python -X utf8 tools/redelivery_bench.py --check` | 2026-08-19 | exit **0**: A — 2 доставки одного `task_id` при окне 5 с (дубль через **79.9 с**, с первой попытки), B — ровно 1 доставка при окне 200 с и задаче 120 с (порог заперт с двух сторон), C — четыре проверки конфига. На хосте (Windows/threads) тот же `--check` отдаёт по ноге A «судить нечем» — см. 42.21 |
| 42.14 | Инварианты конфига (идут в CI) | `pytest tests/test_worker/test_broker_visibility.py -q` | 2026-08-18 | **6 passed** |
| 42.15 | Зонд 1: окно короче детекции (7200 → 5000) | подмена значения, прогон тестов | 2026-08-18 | краснеют **2** теста (`..._is_configured`, `..._outlasts_every_task`) |
| 42.16 | Зонд 2: опция снята совсем | удаление строки, прогон тестов | 2026-08-18 | краснеют **3** теста (добавляется `..._outlasts_global_limit`) |
| 42.17 | Зонд 3: новая длинная задача (детекция 5400 → 9000) | подмена лимита, прогон тестов | 2026-08-18 | краснеют **3** теста (`..._scanner_sees_every_task`, `..._longest_task_is_detection`, `..._outlasts_every_task`) — то есть задача, переросшая окно, гейт не проскочит |
| 42.18 | Долг линтеров | `python -X utf8 tools/lint_gate.py --check` | 2026-08-18 | exit **0**: ruff 178/эталон 178, mypy **0 ошибок** — новый стенд внесён в `files` по правилу «храповик по включению» |
| 42.19 | База набора | `python -X utf8 tools/suite_baseline.py --check` | 2026-08-18 | exit **0**: собрано 934 = 928 + 6 новых, красные без изменений (13 failed / 9 errors) |
| 42.22 | CI на финальном коммите пункта | `gh run watch 32189156896` | 2026-08-19 | **зелёный, все 5 шагов** (сбор, база набора, ruff-храповик, воспроизводимость раскладки) на коммите da794e2 |
| 42.23 | Пересъём базы (Д6, отдельный коммит da794e2) | `python -X utf8 tools/suite_baseline.py --write-baseline` | 2026-08-19 | собрано 928 → **934**, git-видимых 912 → **918**, пол 907 → **913**, passed 892 → **898**; множество красных **не изменилось** (22) |
| 42.24 | Операционное: когда правка начнёт действовать | `docker ps` + чтение | 2026-08-19 | работающий `pid_worker` держит СТАРОЕ окно в памяти (стартовал до правки, конфиг читается при импорте) — новое подхватится при рестарте стека. На бой (`deploy`) не выкатывалось по Р-1 |

### 42г. Доработка по возврату ревизора (2026-08-19)

Перепроверено по коду заново, вывод первого захода источником не служил.

| # | что | команда | дата | результат |
|---|---|---|---|---|
| 42.25 | Возраст `unacked` считается от ДОСТАВКИ (основание поправки 42.8) | чтение установленного kombu: `redis.QoS.append` | 2026-08-19 | `zadd(unacked_index_key, {delivery_tag: time()})` — момент выдачи сообщения воркеру; `restore_visible` сравнивает с `time() − visibility_timeout`. Значит окно покрывает не «исполнение», а весь путь сообщения от доставки до подтверждения |
| 42.26 | Окно флота = **минимум по живым потребителям** (основание правила Д4) | `python -c "kombu.transport.redis.Channel.unacked*_key"` + `docker exec pid_worker_ocr ps` | 2026-08-19 | ключи `unacked` / `unacked_index` / `unacked_mutex` — **глобальные на базу**, без суффикса очереди и потребителя, а restore режет по окну СКАНИРУЮЩЕГО канала. Потребителей боевого брокера двое: `pid_worker` (`-Q default,gpu,sam2`) и `pid_worker_ocr` (`-Q ocr`), оба живьём крутят `celery -A worker.celery_app`. Частичный рестарт оставляет в флоте старое окно 3600 — и оно решает за всех |
| 42.27 | Пин базы больше не побочный эффект импорта | `pytest tests/test_worker/ -q` + зонд | 2026-08-19 | приложение стенда строится только при запуске скриптом (`__main__`) или при `PROBE_LOG` в окружении (его ставит `run_leg`), поэтому импорт ради `task_time_limits` окружение процесса не трогает. **23 passed**; зонд «вернуть пин на уровень модуля» красит ровно новый тест (`CELERY_BROKER_URL` уезжает в `/15`, боевой `celery_app` — туда же), дерево восстановлено копией, md5 сверен. Оба рабочих пути живы: воркер с `PROBE_LOG` строит приложение и регистрирует задачу, скрипт `--leg C` exit 0 |

---

## 43. Пункт 3.x1 дороги — Flower: мониторинг Celery глазами (2026-08-19)

Пункт перенесён из этапа 3 в волну 1 ради того, чтобы дубли задач (Б10, пункт 1.2)
было видно **глазами**, а не специальным стендом. Утверждение пункта — о рантайме,
чтением кода не проверяется (вердикт ❔ в `PLAN_AUDIT`), поэтому всё ниже — живой стек.

### 43а. Проверка адресов и утверждений пункта (до первой строки кода)

| # | что | команда | дата | результат |
|---|---|---|---|---|
| 43.1 | `[TODO: добавить Celery Flower]` в доке жив | `grep -rn "flower" docs/ docker-compose.yml requirements/` | 2026-08-19 | **одно** вхождение на весь репо — `docs/DEPLOYMENT.md:478`; сервиса в compose нет, пакета в requirements нет |
| 43.2 | Предпосылки дубля на месте (вход из 1.2) | чтение `worker/celery_app.py` | 2026-08-19 | `task_acks_late=True` :62, `task_time_limit=3600` :49, `broker_transport_options={"visibility_timeout": 7200}` :59 — правка 1.2 в дереве, дубль на бою больше не воспроизводится сам |
| 43.3 | ⛔ План обещает «очередей три» (`gpu`, `ocr`, `sam2`) | `curl /api/queues/length` + `inspect active_queues` | 2026-08-19 | очередей **четыре**: `default`, `gpu`, `ocr`, `sam2` — подтверждает опровержение ✖ `3.14` из `PLAN_AUDIT` живым замером, а не чтением. `default` слушает `pid_worker` (`Dockerfile.worker:112-116`), `ocr` — `pid_worker_ocr` |
| 43.4 | `worker_send_task_events` в конфиге нет | `grep -rn "send_task_events" worker/ app/` | 2026-08-19 | **0 вхождений** — подтверждено. Но события НЕ нужны в конфиге: Flower сам периодически шлёт воркерам `enable_events` (замер 43.7) |
| 43.5 | Образ Flower доступен офлайн | `docker images` | 2026-08-19 | `mher/flower:2.0` (2.0.1, 136 МБ) уже в локальном кэше — сеть для подъёма не нужна |
| 43.6 | Осадок стенда 1.2 в базе 0 (предупреждён архитектором) | `redis-cli -n 0 HLEN unacked / LLEN probe_redelivery` | 2026-08-19 | **0 и 0** — на момент пункта осадка уже нет; боевые `default`/`gpu`/`ocr`/`sam2` = 0. Расследование не велось (чужая находка) |

### 43б. Что Flower показывает (то, ради чего пункт)

| # | что | команда | дата | результат |
|---|---|---|---|---|
| 43.7 | События задач без правки конфига воркера | `curl /api/task/info/<id>` через 1 с после отправки | 2026-08-19 | задача видна **с первой секунды**: `state=SUCCESS`, `name`, `worker=celery@d57eee94cfa8`, `result` — то есть Flower включил события боевым воркерам сам, `pid_worker` не перезапускался |
| 43.8 | Очереди и воркеры | `curl /api/queues/length`, `/api/workers?refresh=1` | 2026-08-19 | 4 очереди с глубиной 0; 2 живых воркера: `celery@d57eee94cfa8` (`default,gpu,sam2`), `celery@b70a787b5ae0` (`ocr`) — совпадает с `-Q` из обоих Dockerfile |
| 43.9 | Единица, в которой будет виден дубль | `/api/task/info` | 2026-08-19 | `uuid` задачи + `worker` + состояние — то же поле, по которому 1.2 ловил передоставку (два `received` с ОДНИМ `task_id`). ⚠ Живой дубль в этой сессии не провоцировался: он чинится правкой 1.2, а провокация требует рестарта боевого воркера |
| 43.10 | Цена наблюдения | `docker stats` не снимался; образ и порт | 2026-08-19 | один контейнер 136 МБ, порт **только `127.0.0.1:5555`** — у Flower по умолчанию нет аутентификации, а в UI есть управление (отозвать задачу, снять воркера) |

### 43в. Гейт пункта (класс [infra]) и его зонды

Гейт `tools/flower_gate.py --check` кладёт в боевую очередь `default` встроенную
`celery.accumulate` (чистая функция, `ignore_result=True`) и требует увидеть её в **Flower**.
Живость брокера и воркеров зонд выясняет напрямую через celery — иначе «судить нечем»
и «опровергнуто» слились бы в один ответ.

| # | что | команда | дата | результат |
|---|---|---|---|---|
| 43.11 | ⭐ ГЕЙТ — доказано | `python -X utf8 tools/flower_gate.py --check` | 2026-08-19 | exit **0**: очереди `['default','gpu','ocr','sam2']` совпали с тем, что сообщили воркеры; зондовая задача видна в Flower за **1 с** |
| 43.12 | Зонд 1 — Flower не поднят (**красный**, а не «судить нечем») | `docker stop pid_flower` + `--check` | 2026-08-19 | exit **1**, `[ОПРОВЕРГНУТО] Flower не отвечает на /api/queues/length` — гейт краснеет ровно на том, что обязан ловить |
| 43.13 | Зонд 2 — задача до Flower не долетает | подмена `send_task` заглушкой, `TASK_WAIT_S=6` | 2026-08-19 | exit **1**, `[ОПРОВЕРГНУТО] зондовая задача не видна … /api/task/info — HTTP 404` — ветка «очереди есть, задач нет» проверяется отдельно от ветки «Flower лежит» |
| 43.14 | Третий исход — судить нечем | `--check --broker redis://localhost:6399/0` | 2026-08-19 | exit **2**, `[судить нечем] брокер недоступен` (первая версия падала трейсбеком: мёртвый брокер приходит как `kombu.exceptions.OperationalError`, а не `OSError` — поймано этим же зондом) |
| 43.15 | Д1 в CI: порт наружу не уехал | `pytest tests/test_flower_service.py -q` | 2026-08-19 | **5 passed** |
| 43.16 | Зонд 3 — публикация порта всем | подмена `"127.0.0.1:5555:5555"` → `"5555:5555"` | 2026-08-19 | краснеет `test_port_is_bound_to_loopback_only`; дерево восстановлено из копии файла, не `git checkout` |
| 43.17 | Долг линтеров | `python -X utf8 tools/lint_gate.py --check` | 2026-08-19 | exit **0**: ruff 178/эталон 178, mypy 0 ошибок |
| 43.18 | База набора | `python -X utf8 tools/suite_baseline.py --check` | 2026-08-19 | exit **0**: собрано 939 = 934 + 5, красные без изменений (13 failed / 9 errors) |

### 43г. Пересъём базовой линии (Д6, отдельный коммит)

| # | что | команда | дата | результат |
|---|---|---|---|---|
| 43.19 | Пересъём базы | `python -X utf8 tools/suite_baseline.py --write-baseline` | 2026-08-19 | собрано 934 → **939**, passed 898 → **903**; множество красных **не изменилось** |

### 43д. Возврат ревизора: Flower на голом URL отменял фикс 1.2 (2026-08-19)

Ревизор вернул пункт: Flower был поднят как `celery --broker=redis://redis:6379/0 flower`,
то есть без приложения проекта. Окно невидимости у kombu — свойство КАНАЛА; ключ
`unacked_index` один на всю базу, и `restore_visible` режет по окну сканирующего.

| # | что | команда | дата | результат |
|---|---|---|---|---|
| 43.20 | Механизм по коду установленного kombu | чтение `kombu/transport/redis.py` | 2026-08-19 | `restore_visible:415` `ceil = time() - self.visibility_timeout` (окно СКАНЕРА, `:457-458`); `unacked_index_key = 'unacked_index'` `:640` — один на базу, не по очередям; дефолт `visibility_timeout = 3600` `:644`. Триггеры: пустой опрос у блокирующего потребителя `:606` и `call_repeatedly(10, cycle.maybe_restore_messages)` на событийном цикле `:1382` |
| 43.21 | ⭐ Механизм воспроизведён живьём (база 15) | прототип: жертва с окном 120 держит unacked 6 с, сканер с окном 5 на ДРУГОЙ очереди зовёт `maybe_restore_messages()` | 2026-08-19 | сообщение вернулось в очередь жертвы: `llen 0 → 1`, `zcard(unacked_index) 1 → 0`. Ревизор прав: канал с коротким окном режет чужой unacked через очередь |
| 43.22 | Граница ноги A: частота, а не механизм | тот же прототип через `drain_events(timeout=1)` ×3 | 2026-08-19 | `_vrestore_count` остался **0** — у блокирующего потребителя `MultiChannelPoller.get` почти всегда получает событие от BRPOP и до `maybe_restore_messages` не доходит (`redis.py:606` — ветка «нет событий»). Поэтому стенд зовёт триггер ЯВНО и доказывает механизм, а не частоту; инвариант гейта — «ни один канал на брокере не несёт окно короче воркерского» |
| 43.23 | Образ `mher/flower:2.0` кода проекта не содержит | `docker run --rm --entrypoint sh mher/flower:2.0 -c "ls /; python -c 'import worker'"` | 2026-08-19 | каталога `/app` нет, `ModuleNotFoundError: No module named 'worker'` — подтверждено дословно: `-A worker.celery_app` на этом образе невозможен |
| 43.24 | Новая сборка: стадия `flower` образа воркера | `docker compose build flower` | 2026-08-19 | слои общие с воркером, докладывается только `flower 2.1.0` (+tornado, prometheus_client, humanize, pytz); `FROM worker AS flower`, сервис `worker` собирается с `target: worker` и стадии не видит |
| 43.25 | Цена наблюдения | `docker stats --no-stream` | 2026-08-19 | `pid_flower` **62.3 МиБ** RSS против 166.3 МиБ у `pid_worker`: `import_default_modules()` тянет `worker.tasks.*`, но ML-веса при импорте не грузятся — опасение «Flower утащит torch в память» не подтвердилось |
| 43.26 | ⭐ ГЕЙТ целиком | `python -X utf8 tools/flower_gate.py --check` | 2026-08-19 | exit **0**, все четыре ноги: A — окно 5 с вернуло сообщение, которое держат под окном 60 с; B — при равных окнах не вернуло; C — в контейнере команда `celery -A worker.celery_app flower`, канал **7200** > самой длинной задачи 5400; D — очереди `['default','gpu','ocr','sam2']` и зондовая задача видна |
| 43.27 | Зонд ноги C — Flower мимо приложения проекта | контейнер `mher/flower:2.0` с `celery --broker=... flower`, `--container pid_flower_bare` | 2026-08-19 | exit **1**, `[ОПРОВЕРГНУТО] Flower поднят МИМО приложения проекта` — прежняя версия пункта этой ногой краснеет |
| 43.28 | Зонд ноги C — контейнера нет | `--leg C --container pid_flower_missing` | 2026-08-19 | exit **2** «судить нечем» (не путается с красным) |
| 43.29 | Зонды ноги D (повторно после переделки) | `docker stop pid_flower`; `--broker redis://localhost:6399/0` | 2026-08-19 | exit **1** (не отвечает) и exit **2** (нет брокера) — на всех ногах сразу |
| 43.30 | Зонды тестов CI | подмена `-A worker.celery_app` на `--broker=...` в `Dockerfile.worker`; снятие `target: worker` | 2026-08-19 | краснеют ровно два теста: `test_flower_starts_with_the_project_app`, `test_worker_image_does_not_include_flower_stage`. Дерево восстановлено из копий файлов, не `git checkout` |
| 43.31 | Долг линтеров и база после доработки | `lint_gate --check`, `suite_baseline --check` | 2026-08-19 | ruff 178/178, mypy 0; собрано 943 = 939 + 4 (тестов Flower стало 9 вместо 5), красные без изменений |

## 44. Пункт 1-26 дороги (ГЕЙТ-2) — пересъём базы перестал легализовать красных (2026-08-19)

`tools/suite_baseline.py:221-250` — `cmd_write` не загружал предыдущую базу ВООБЩЕ: проверял
код возврата pytest и наличие итоговой строки, после чего перезаписывал поле `red` тем, что
красно сейчас. Сверка с базой жила только в `cmd_check`. Пересъём по Д6 идёт **отдельным
коммитом**, поэтому красный флаг №4 протокола («эталон изменён тем же коммитом, что и код»)
эту дорогу не видел вовсе (red-team C1).

### 44а. Дыра и правка

| # | что | команда | дата | результат |
|---|---|---|---|---|
| 44.1 | База до правки | `python -X utf8 tools/suite_baseline.py --check` | 2026-08-19 | exit **0**: собрано 939 (git-видимых 923, корпус вне git 16), 13 failed + 9 errors = 22 красных |
| 44.2 | ⛔ Дыра в лоб, ДО правки | `python -X utf8 -m pytest tests/test_suite_baseline.py -q` с 5 новыми тестами | 2026-08-19 | **2 failed, 21 passed**: на подложенном красном стенд печатает «база записана: **4** красных» вместо 3; на оборванном хвосте — «база записана: **1** красных» при счётчиках 2 failed + 1 error. Оба раза exit 0 и файл переписан |
| 44.3 | Тот же файл после правки | то же | 2026-08-19 | **23 passed** (18 было + 5) |

### 44б. Гейт и зонды (класс [infra])

Зонды гонялись на живом дереве системным `python` 3.11.9. ⚠ Обстановка: в дереве в это время
лежали незакоммиченные правки трёх чужих пунктов (см. 44.10), поэтому в состав красных
подмешивались чужие — они видны в выводе и учтены в базах зондов.

| # | что | команда | дата | результат |
|---|---|---|---|---|
| 44.4 | ⭐ ГЕЙТ, зонд 1 — подложенный красный тест | `cp зонд tests/test_gate_probe_1_26.py` + `--write-baseline` | 2026-08-19 | exit **1**, `[ОТКАЗ] множество красных выросло против базы (2026-08-19): 22 → 25, новых 3` со списком: мой подложенный `test_planted_red_1_26` и два ЧУЖИХ (`test_flower_service.py`) — гейт поймал и то, что подложили нарочно, и чужую регрессию. Эталон после отказа **бит-в-бит** равен коммиченному |
| 44.5 | Зонд 2 — «зонд убрали → пересъём проходит» + законный путь «красные ушли» | зонд-файл удалён; в базу подложен призрак `tests/test_ghost_1_26.py::test_ghost` (плюс 2 чужих красных, иначе они читаются как рост) | 2026-08-19 | exit **0**, «база записана: 24 красных, собрано 956»; призрак из базы выпал — позеленение пересъём не блокирует |
| 44.6 | Зонд 3 — законный путь «состав тот же» | `--write-baseline` повторно, база = состав предыдущего прогона | 2026-08-19 | exit **0**: состав входной базы и записанной совпал (22 = 22), `collected` 960 → 960 — рядовой Д6-пересъём пункта дороги проходит |
| 44.7 | Побочный зонд (не планировался): чужая сессия сломала сбор посреди прогона | `--write-baseline` | 2026-08-19 | exit **1**, «сбор pytest сломан (exit 2) — это пункт 0.0, а не база» (`test_flower_service.py` импортировал `PROBE_QUEUE`, которого в `tools/flower_gate.py` ещё не было). Эталон не тронут: файл до и после равен |
| 44.8 | Долг линтеров | `python -X utf8 tools/lint_gate.py --check` | 2026-08-19 | exit **0**: ruff BLE001/E722 178/эталон 178, mypy 0 ошибок |
| 44.9 | База после всех зондов (эталон восстановлен КОПИЕЙ, не `git checkout`) | `python -X utf8 tools/suite_baseline.py --check` | 2026-08-19 | exit **0**, `[OK] новых красных нет`; эталон на диске бит-в-бит равен `HEAD` |

### 44в. Почему Д6-пересъёма в этом пункте НЕТ

| # | что | команда | дата | результат |
|---|---|---|---|---|
| 44.10 | ⛔ Дерево в момент работы не моё | `git status --porcelain` в 01:45 и в 02:03 | 2026-08-19 | 01:45 — модифицированы только 3 road-дока; 02:03 — **16 файлов** трёх чужих пунктов (1-25: `layout_determinism.py`, `edit_bench.py`, `test_edit_bench.py`; 1-4: `docker-compose.yml`, `Dockerfile.worker`, `flower_gate.py`; доработка 1.2), собрано 960 вместо 939 |
| 44.11 | Цена пересъёма на таком дереве | арифметика пола: `git_visible` 944 − `FLOOR_MARGIN` 5 | 2026-08-19 | пол стал бы **939**, тогда как чистое дерево с одной моей правкой собирает **928** (923 + 5 моих тестов) → `--check` покраснел бы «набор усох» у ревизора и в CI. Из +21 собранного мои — 5. Пересъём отложен: он законен только после того, как чужие пункты закоммитятся (или будут откачены) |

---

## 45. Пункт ГЕЙТ-1 дороги — два стенда научились отвечать «судить нечем» (2026-08-19)

Пункт заведён замером red-team A4 (2026-08-19). Правило `PROTOCOL §5`: гейт обязан различать
ТРИ исхода — доказано · опровергнуто · **судить нечем**. Два стенда его нарушали: на усечённом
корпусе молча печатали зелёное. Тронуто три файла: `tools/layout_determinism.py`,
`tools/edit_bench.py` и шаг CI, который научен различать 1 и 2.

### 45а. Адреса пункта перепроверены по коду (шаг 2 жизненного цикла)

| # | что | команда | дата | результат |
|---|---|---|---|---|
| 45.1 | `verdict()` обходит ЗАМЕР, а не эталон | чтение `tools/layout_determinism.py:124-146` | 2026-08-19 | подтверждено дословно: единственный цикл — `for uid8, shas in sorted(result.items())`, эталон читается только точечно `known.get(uid8)`; выражения вида `set(known) - set(result)` в файле нет |
| 45.2 | `_compare()` обходит ЗАМЕР | чтение `tools/edit_bench.py:97-114` | 2026-08-19 | подтверждено: `for name, res in rows.items()`; ветка «файл замера нет в базе» есть (`if b is None → пропуск`), обратной («файл базы не измерен») нет |
| 45.3 | Корпус судьи вне git | `.gitignore:37` + `git ls-files tools/bench` | 2026-08-19 | `tools/bench/edit_corpus/` в .gitignore; в git — только эталон `edit_baseline.json` (**20** записей), файлов корпуса **0**. На машине 20 из 20 |
| 45.4 | Образец правильного поведения на месте | чтение `tools/pair_bench.py:503-519`, `tools/napravlenie_audit.py:400-403` | 2026-08-19 | оба отдают **exit 2** словами «судить нечем» — идиома скопирована, а не изобретена |
| 45.5 | Теста на дыру не было | `tests/test_layout_determinism.py` (84 строки), `tests/test_edit_bench.py` | 2026-08-19 | у первого 4 исхода вердикта, ветки «uid эталона не измерен» нет; второго файла не существовало вовсе |
| 45.6 | Эталон ПР1 против того, что видит CI | `determinism_baseline.json` vs `corpus.fixture_paths()` | 2026-08-19 | в эталоне **17** графов (9 воспроизводимых, **8 неповторимых**), в git — **3** (`089feca2`, `6e7144d5`, `d74eb9f1`) |

### 45б. Дыра ДО правки — замер, а не мнение

| # | что | команда | дата | результат |
|---|---|---|---|---|
| 45.7 | ⭐ Стенд ПР1 на усечённом корпусе | `python -X utf8 tools/layout_determinism.py --git-only --runs 2 --check` | 2026-08-19 | exit **0**, «[OK] регресса воспроизводимости нет» — при том, что измерены **3 графа из 17**, а 14 (среди них все 8 известных неповторимых) не тронуты ни разу |
| 45.8 | ⭐ Судья «Ручной правки» на части корпуса | `python -X utf8 tools/edit_bench.py tools/bench/edit_corpus/graph_edited_star.json --check` | 2026-08-19 | exit **0**, «рост дефектов: 0» — измерен **1 файл из 20** |
| 45.9 | Новые тесты краснеют на старом коде (тест ДО правки) | `pytest tests/test_edit_bench.py tests/test_layout_determinism.py -q` | 2026-08-19 | **10 failed, 9 passed** |

### 45в. После правки — три исхода на обоих стендах

| # | что | команда | дата | результат |
|---|---|---|---|---|
| 45.10 | ⭐ ГЕЙТ, зонд 1 «убрать корпус из видимости» | `... --git-only --runs 2 --check` | 2026-08-19 | exit **2**: «[СУДИТЬ НЕЧЕМ] не измерено 14 графов эталона из 17 — корпус усечён: 06b938ec, 0aea61c0, 13d1ef5f, …». Зелёного на том же входе больше нет |
| 45.11 | ⭐ ГЕЙТ, зонд 2: часть корпуса судьи | `... graph_edited_star.json --check` | 2026-08-19 | exit **2**: «судить нечем: не измерено 19 файлов эталона из 20» |
| 45.12 | ⭐ ГЕЙТ, зонд 3: корпуса нет вовсе | `--all --check` с `CORPUS` на несуществующем каталоге | 2026-08-19 | exit **2**: «судить нечем: … нет ни одного graph_edited*.json — корпус вне git (.gitignore:37)». До правки эта ветка отдавала usage-ошибку argparse «нет входных файлов» |
| 45.13 | Положительный контроль судьи: весь корпус | `python -X utf8 tools/edit_bench.py --all --check` | 2026-08-19 | exit **0**: 20 файлов из 20, рост дефектов 0 — зелёное осталось зелёным |
| 45.14 | ⭐ Положительный контроль ПР1: ровно 17 uid эталона | `... --runs 2 --check --uid ×17` | 2026-08-19 | exit **0**, «[OK] регресса воспроизводимости нет»: измерены все 17 графов эталона — вердикт вынесен, ложного «судить нечем» на полном эталоне нет. Три графа показали «воспроизводятся впервые» (`54a60fd1`, `6e7144d5`, `a6d28736`) — при `--runs 2` это документированная выборка (`TESTING §8.2`), эталон не трогал |
| 45.15 | Полный корпус машины (19 графов) | `python -X utf8 tools/layout_determinism.py --runs 2 --check` | 2026-08-19 | exit **1**, и это НЕ про правку: ветка «новый граф корпуса неповторим» диффом не тронута. Причина — дрейф данных: корпус машины вырос **17 → 19** (`0fc9d04c`, `3263039b` в эталоне отсутствуют), и `0fc9d04c` неповторим. Пересъём эталона (Д6) — не этот пункт |
| 45.16 | Логика шага CI на всех трёх кодах | прогон точного тела шага в git-bash с подставным кодом возврата | 2026-08-19 | стенд 0 → шаг **0**; стенд 1 → шаг **1** (регресс на видимых трёх графах по-прежнему красит CI); стенд 2 → `::warning` «стенд не судит» + шаг **0** |
| 45.17 | Тесты после правки | `pytest tests/test_edit_bench.py tests/test_layout_determinism.py -q` | 2026-08-19 | **20 passed** (7 — новый файл тестов судьи, 13 — ПР1) |

### 45г. Гейты класса [infra]

| # | что | команда | дата | результат |
|---|---|---|---|---|
| 45.18 | Долг линтеров | `python -X utf8 tools/lint_gate.py --check` | 2026-08-19 | exit **0**: ruff 178/эталон 178, mypy 0 ошибок |
| 45.19 | База набора | `python -X utf8 tools/suite_baseline.py --check` | 2026-08-19 | exit **0** «новых красных нет»: собрано **960** (git-видимых 944, корпус вне git 16), пол 935 |
| 45.20 | ⚠ Первый прогон базы — «судить нечем» по обстановке, а не по коду | тот же `--check` | 2026-08-19 | exit 1 «сбор pytest сломан (exit 2)»: `tests/test_flower_service.py` импортировал `PROBE_QUEUE`, которого в ту секунду не было в `tools/flower_gate.py` — файл писала соседняя сессия (mtime 01:58:10, прогон 01:58). Через две минуты сбор чист. **Пересъёма Д6 не делал** — правило доски «пересъём запрещён, пока в дереве чужие незакоммиченные правки»; пол набора это и не требует: он минимум, а тесты только добавлены |

### 43е. Второй возврат ревизора: четыре дыры (2026-08-19)

| # | что | команда | дата | результат |
|---|---|---|---|---|
| 43.32 | ⛔ В1 — Д1 не стерёг дефект, за который вернули | инъекция в ТЕКУЩЕЕ дерево: голая `command: celery --broker=… flower` в `docker-compose.yml` | 2026-08-19 | до правки **9/9 зелёных** (тест, смотревший на `flower["command"]`, был удалён тем же коммитом, что и фикс); после правки краснеет `test_compose_does_not_override_the_project_app_command`. `command:` в compose СИЛЬНЕЕ `CMD` образа — проверки стадии образа мало |
| 43.33 | ⛔ В2 — инвариант был заперт с одной стороны | `docker exec pid_worker celery -A worker.celery_app inspect conf --json` | 2026-08-19 | флот отвечает: `celery@b70a787b5ae0: 7200`, `celery@d57eee94cfa8: 7200`. **Тревога «флот несёт 3600» снята замером** — оба контейнера пересозданы доработкой, рестарт не нужен и не делался |
| 43.34 | Зонд В2 — потребитель с окном 3600 во флоте | временный воркер в `pid_worker` с `broker_transport_options={"visibility_timeout": 3600}`, очередь `probe_fleet_bad`, ~40 с | 2026-08-19 | нога C **краснеет**: `у живых потребителей окно не 7200: {…: 3600} — решает минимум по флоту, дубль вернётся на 3601-й секунде`. После снятия зонда снова зелено; `HLEN unacked` = 0 и `LLEN probe_fleet_bad` = 0 до и после, боевые очереди не тронуты |
| 43.35 | ⛔ В3 — гейт сам нарушал правило, которое охраняет | чтение `tools/flower_gate.py` (нога D зовёт `inspect` на боевом брокере) | 2026-08-19 | `Celery(broker=…)` без `broker_transport_options` = канал с дефолтом kombu **3600** на базе 0. Лечится одной строкой; вынесено в `probe_app()` и заперто тестом `test_gate_itself_is_not_a_short_window_consumer` (инъекция «снять строку» его красит) |
| 43.36 | ⛔ В4 — дыру внесла сама доработка | `curl -o /dev/null -w "%{http_code}" http://127.0.0.1:5555/api/workers` | 2026-08-19 | ДО правки **200** без авторизации при 12 боевых задачах в реестре Flower (`POST /api/task/async-apply/<name>` запускает любую); ПОСЛЕ (basic-auth из `.env`, `FLOWER_UNAUTHENTICATED_API` снят) — **401** и на `/api/*`, и на UI `/`, а с верным `user:pass` — **200** |
| 43.37 | Три исхода ноги D по авторизации | `--leg D` без переменной / с неверным паролем / с верным | 2026-08-19 | **2** «судить нечем: нечем представиться» · **1** «опровергнуто» (HTTP 401) · **0** — исходы не смешиваются |
| 43.38 | ⭐ ГЕЙТ целиком после доработки | `python -X utf8 tools/flower_gate.py --check` | 2026-08-19 | exit **0**: A — окно 5 с вернуло чужое сообщение; B — при равных окнах нет; C — команда контейнера с `-A worker.celery_app`, окно 7200 у Flower и у обоих живых воркеров > 5400; D — очереди и зондовая задача видны |
| 43.39 | Тесты и долг линтеров | `pytest tests/test_flower_service.py -q`, `lint_gate --check` | 2026-08-19 | **12 passed** (было 9); ruff 178/178, mypy 0 |

## 46. Пункт ГЕЙТ-3 дороги — откат пункта перестал быть «усыханием набора» (2026-08-19)

Пункт заведён замером red-team D1 (2026-08-19): `git revert` закрытого пункта — обещанная
`PROTOCOL §6` точка возврата — ронял `suite_baseline --check`. Тронуто четыре файла:
`tools/suite_baseline.py`, `tests/test_suite_baseline.py`, `tests/test_collection_clean.py`,
`docs/TESTING.md`; эталон набора — отдельным коммитом (Д6).

### 46а. Адреса и числа пункта перепроверены по коду (шаг 2 жизненного цикла)

| # | что | команда | дата | результат |
|---|---|---|---|---|
| 46.1 | ⚠ Адрес `FLOOR_MARGIN` в строке очереди сдвинут | `git show dab8b79:tools/suite_baseline.py` + grep | 2026-08-19 | утверждение верно (`FLOOR_MARGIN = 5`), но строка **:81**, а не `:71` из QUEUE — дрейф адреса, вывод пункта не меняется |
| 46.2 | Числа эталона из строки пункта | чтение `tools/bench/suite_baseline.json` | 2026-08-19 | подтверждены дословно: собрано **963**, git-видимых **947**, пол **942**, красных **22** (13 failed + 9 errors) |
| 46.3 | ⭐ Потребителей пола ДВА, а не один | `grep -rn min_collected` вне `docs/` | 2026-08-19 | `tools/suite_baseline.py` и `tests/test_collection_clean.py:62`. Второй в строке пункта не назван: правка одного стенда оставила бы откат красным через сам набор (см. 46.8) |
| 46.4 | Откат ПОСЛЕДНЕГО пункта дефекта не показывает | `git show 1e6075e --stat` + карта файлов | 2026-08-19 | 3.x1 принёс **+3** теста — меньше запаса 5: 947 − 3 = 944 ≥ 942, зелено. Дефект начинается с пункта в 6 тестов |
| 46.5 | Ближайший закрытый пункт, на котором он виден | откат тега `road-ГЕЙТ-1` (`c73883c`) | 2026-08-19 | снимает **11** git-видимых тестов: `tests/test_edit_bench.py` 7 → 0 (файл снят целиком) и `tests/test_layout_determinism.py` 13 → 9. 947 − 11 = **936** |
| 46.6 | Копия дерева судит так же, как боевое | `suite_baseline --check` в копии до всяких правок | 2026-08-19 | exit **0**, собрано 963 (git-видимых 947), красных 22 — копия верна. Полный прогон стенда **36 с** |
| 46.7 | ⚠ Ловушка замера: BOM в перехвате консоли | ручной `pytest --collect-only > файл` из PowerShell vs `split_collected` | 2026-08-19 | PowerShell дописывает BOM, и первый файл сбора приезжает с невидимым U+FEFF в начале пути — в ручном счёте появлялся 90-й «файл»-призрак. Стенд читает stdout процесса напрямую и BOM не видит: файлов **89** |

### 46б. Дефект ДО правки — замер, а не рассуждение

| # | что | команда | дата | результат |
|---|---|---|---|---|
| 46.8 | ⛔ **Легальный откат пункта красит гейт** | на КОПИИ дерева: `git revert --no-commit c73883c` (конфликт только в `MEASUREMENTS.md` — журнал append-only, разрешён текущим текстом; код и тесты откатились чисто) → `suite_baseline --check` | 2026-08-19 | exit **1**: «набор усох: в git-видимой части **936** < **942**». Тег перестал быть точкой возврата, как обещает `PROTOCOL §6` |
| 46.9 | Тот же откат красит и НАБОР, не только стенд | тот же прогон | 2026-08-19 | сверх пола — новый красный `tests/test_collection_clean.py::test_suite_does_not_shrink`: второй потребитель пола падает независимо, починка одного стенда пункт бы не закрыла |

### 46в. После правки — откат зелёный, настоящее усыхание красное

| # | что | команда | дата | результат |
|---|---|---|---|---|
| 46.10 | ⭐ **Тот же откат на исправленном стенде** | свежая копия на `f7b0470`, `git revert c73883c` доведён до коммита → `suite_baseline --check` | 2026-08-19 | exit **0**, «новых красных нет». Стенд печатает, что именно ушло: `[в диффе] 2 файлов эталона отдали меньше тестов (−11): test_edit_bench.py 7 → 0, test_layout_determinism.py 13 → 9`. Собрано 962 (git-видимых 946) при базе 973/957 — и это больше не провал |
| 46.11 | ⭐ **Зонд обратной полярности: `collect_ignore` каталога** | `collect_ignore.append("observability")` в `tests/conftest.py` → `--check` | 2026-08-19 | exit **1**: «набор усох: **тихо потеряно 145** тестов (допуск 5) — эти файлы в диффе не менялись», перечислены все 25 файлов `tests/observability/`. Сбор при этом чист (rc 0, собрано 828) — красит именно пол, а не «сбор сломан» |
| 46.12 | ⚠ Ловушка зонда: `collect_ignore` уже есть в conftest | `collect_ignore = ["observability"]` (присваивание вместо `.append`) | 2026-08-19 | затирает штатный `collect_ignore = ["test_ocr_phase0.py"]`, а этот модуль зовёт `sys.exit()` на уровне модуля → `INTERNALERROR`, сбор **rc 3** и обрезан (334 из 973). Зонд надо ДОПИСЫВАТЬ в список, иначе он меряет не то, что думает |
| 46.13 | ⭐ Зонд: тест-файл стёрт мимо git | `rm tests/test_seat_contract.py` (без `git rm`) → `--check` | 2026-08-19 | exit **1**: «тихо потеряно **37** тестов … `tests/test_seat_contract.py: 37 → 0`» + красный `test_suite_does_not_shrink`. Файл, которого нет в дереве, но который git числит, откатом не считается |
| 46.14 | Допуск заперт с двух сторон | `pytest tests/test_suite_baseline.py::test_silent_loss_is_judged_against_an_absolute_margin` | 2026-08-19 | 30 → 25 (потеря 5) зелено, 30 → 24 (потеря 6) красно. Числа абсолютные, из `FLOOR_MARGIN` не вычисляются — расширить допуск до размера отката незаметно нельзя |
| 46.15 | Отпечаток файла не зависит от переводов строк | `pytest tests/test_suite_baseline.py::test_digest_ignores_line_endings` + предупреждение git при коммите эталона | 2026-08-19 | CRLF и LF дают один отпечаток, чужой текст — другой. Проверка не теоретическая: git на этой машине сам сказал «in the working copy … CRLF will be replaced by LF» — без нормализации в CI «изменённым» выглядел бы каждый файл карты, и пол ослеп бы целиком |

### 46г. Обвязка пункта

| # | что | команда | дата | результат |
|---|---|---|---|---|
| 46.16 | Коммит кода самодостаточен на СТАРОЙ базе | `suite_baseline --check` до пересъёма (база без карты файлов) | 2026-08-19 | exit **0**: собрано 973 (git-видимых 957), пол 942 — ветка совместимости со старой базой работает, пересъём отдельным коммитом |
| 46.17 | Тесты стенда | `pytest tests/test_suite_baseline.py tests/test_collection_clean.py -q` | 2026-08-19 | **34 passed** (было 25: +10 тестов пункта, минус ни одного) |
| 46.18 | Пересъём базы (Д6, отдельный коммит) | `python -X utf8 tools/suite_baseline.py --write-baseline` | 2026-08-19 | собрано 963 → **973**, git-видимых 947 → **957**, пол 942 → **952**, новое поле `per_file` на **89** файлов. Множество красных не тронуто: **22**, ушедших 0, новых 0 — красный флаг №4 чист |
| 46.19 | Гейты пункта на боевом дереве | `suite_baseline --check`, `lint_gate --check` | 2026-08-19 | обе exit **0**: «новых красных нет» (973 = база 973); ruff 178/178, mypy 0 |
---

## 47. Пункт 1-5 дороги (1.1) — алиасинг снимка отката и `finalize()` вне `try/finally` (2026-08-19)

Работа шла в отдельном worktree `C:/project/pid_wt_1.1` на ветке `road/1.1` (в главном
дереве параллельно 1-27). Корпус здесь — только 3 git-видимые фикстуры; docker/Redis
не трогались ни одной командой.

### 47а. Адреса пункта перепроверены по коду (шаг 2 жизненного цикла)

| # | утверждение пункта | команда | дата | результат |
|---|---|---|---|---|
| 47.1 | `edit_smooth.py:622-628` — `restore()`; `:595-601` — `restore_all()`; `:645` — безусловный `restore()` в ветке КОЛЕНА | чтение файла | 2026-08-19 | **все три адреса точны** (файл не двигался с 2026-08-13) |
| 47.2 | `drop_collinear:350` делает `pop()`; `IndexError :322` | `grep -n "wps.pop(i)\|w = wps\[k\]"` | 2026-08-19 | **:350 и :322 точны** до строки |
| 47.3 | `advanced_graph_editor.py:874-890` — `cmd.execute()` до работы, `finalize()`/`push_executed` после `try/finally` | чтение файла | 2026-08-19 | **точно**: `execute()` на `:858`, `try` на `:874`, `finalize()`/`push` на `:889-890` вне блока |
| 47.4 | Механизм алиаса | чтение `restore()` + `graph_data.py:97` | 2026-08-19 | `cur.update(old)` копирует dict ПОВЕРХНОСТНО → живое ребро получает тот же список `waypoints`, что в снимке; `model.nodes[id]` — те же объекты, что в `graph_data['nodes']`, поэтому порча видна и модели |
| 47.5 | Кто зовёт движок | `grep -rn edit_smooth --include=*.py` (без `_scratch/`, `.claude/`, `tests/`) | 2026-08-19 | **единственный потребитель** — `advanced_graph_editor.smooth_canvas` (кнопка «Авто-выравнивание»). Движок НЕ на пути авто-раскладки, стенды `layout_bench`/`cmp_bitexact`/`layout_determinism` правкой не задеты |

### 47б. Воспроизведение дефекта — замер, а не чтение

Развёртка 4000 синтетических холстов «рамка → рамка/коннектор» (генератор: рамка
30/40/60/90 px в (100, 100…180), цель на x∈{360,420,500,620}, ступенька 3…21 px,
0/2/4 излома), движок **HEAD** (`git show HEAD:modules/graph/core/edit_smooth.py`)
против дерева. «Роутер-отказ» = `lambda e: False` — штатный ответ лестницы редактора,
когда колено не строится.

| # | что | дата | результат |
|---|---|---|---|
| 47.6 | ⭐ Боевой путь (роутер передан) | 2026-08-19 | **до правки 22 падения `IndexError` из 4000** и **25 тихих расхождений холста**; после правки — **0 и 0** |
| 47.7 | Путь без роутера (`route_fn=None`) | 2026-08-19 | **0 падений и 0 расхождений в обе стороны** — дефект взводится ТОЛЬКО тем, что движку передан роутер (`restore()` на `:645` безусловен). Подтверждает формулировку пункта «на боевом пути работает 1 проба из 4» |
| 47.8 | Падающая фикстура (в тестах) | 2026-08-19 | труба (115,140)→(200,140)→(200,142)→(400,140)→(400,150)→(620,150): до правки `IndexError` в `shift_run` (`:322`), после — `излом 1 + коннектор 1`, труба прямая на y=140, рамка не тронута |
| 47.9 | ⭐ Тихая фикстура (в тестах) | 2026-08-19 | ступенька 10 px, конец на коннекторе: до правки лестница доезжала до лекарства 4 и **двигала ОБОРУДОВАНИЕ** (`bbox [85,125,115,155] → [85,135,115,165]`, `узел 1`); после — лекарство 3, рамка стоит, едет только точка-коннектор (`коннектор 1`, `узел 0`) |
| 47.10 | Цена `deepcopy` при откате | 2026-08-19 | корпус, 3 git-видимые фикстуры × 2 режима роутера: **холст и stats совпали бит-в-бит во всех 6 прогонах**; время −6 %…+8 % (шум): `089feca2` 4.61→4.82 с и 4.98→4.76 с, `6e7144d5` 4.09→4.29 с и 4.24→4.39 с, `d74eb9f1` 0.30→0.28 с и 0.27→0.29 с |

### 47в. Гейт пункта (класс [ui]) и зонды

| # | что | команда | дата | результат |
|---|---|---|---|---|
| 47.11 | Тесты пункта ДО правки | `pytest tests/test_edit_smooth.py tests/ui/test_smooth_undo_invariant.py -q` | 2026-08-19 | **5 failed** — 4 движковых (падение и подмена лекарства) + 1 редакторный (шага undo нет); зелёный путь `smooth_canvas` проходил и до правки |
| 47.12 | Те же тесты ПОСЛЕ правки | та же | 2026-08-19 | **23 passed** (17 прежних движковых + 6 новых: 4 в `test_edit_smooth.py`, 2 в новом `tests/ui/test_smooth_undo_invariant.py`) |
| 47.13 | ⛔ Зонд 1 — вернуть алиас в `restore()` (`restore_all()` оставить чинёным) | инъекция в ТЕКУЩЕЕ дерево, восстановление из копии файла | 2026-08-19 | **4 движковых теста краснеют**, редакторный зелен — набор стережёт именно ту строку |
| 47.14 | ⚠ Зонд 2 — вернуть алиас в `restore_all()` | та же инъекция во вторую функцию | 2026-08-19 | **65 passed, ноль красных**: `restore_all()` зовётся ОДИН раз, в `finally` последней строкой `_try_remedies`, и следующий вызов делает снимок заново — алиас там латентный, тестом не наблюдаем. Правка второй половины — по адресу пункта (`:595-601`) и против расхождения близнецов, а не по гейту; это заявлено, а не выдано за покрытие |
| 47.15 | ⛔ Зонд 3 — вернуть `finalize()`/`push_executed` за пределы `finally` | инъекция в `advanced_graph_editor.py` | 2026-08-19 | **краснеет** `test_engine_crash_still_leaves_the_undo_step` (`stack_depth 0 != 1`), остальные 28 зелены |
| 47.16 | Вся вкладка UI на Qt offscreen | `pytest tests/ui -q` (`QT_QPA_PLATFORM=offscreen`) | 2026-08-19 | **264 passed, 13 skipped** |
| 47.17 | Долг линтеров | `python tools/lint_gate.py --check` | 2026-08-19 | exit **0**: ruff 178/эталон 178, mypy 0 ошибок |
| 47.18 | База набора | `python tools/suite_baseline.py --check` | 2026-08-19 | exit **0** «новых красных нет»: собрано **953** git-видимых (было 947 после пересъёма 3.x1, +6 тестов пункта), пол 942; `failed 13 / errors 9` — как в базе. **Д6-пересъёма НЕ делал**: в главном дереве идёт параллельная сессия, пересъём запрещён (`PROTOCOL §Д6`) |

## 48. Пункт 1-6 дороги (1.4) — откат превью панели «Размеры» (Р9) (2026-08-19)

Работа шла в отдельном worktree `C:/project/pid_wt_1.4` на ветке `road/1.4` (в главном
дереве параллельно 1-28). Корпус здесь — только 3 git-видимые фикстуры, `storage/` пуст;
docker/Redis/postgres не трогались ни одной командой. Стенд — Qt offscreen, системный
`python` 3.11.9.

### 48а. Адреса пункта перепроверены по коду (шаг 2 жизненного цикла)

| # | утверждение пункта | команда | дата | результат |
|---|---|---|---|---|
| 48.1 | `advanced_graph_editor.py:4046-4047` — сброс базлайна | `git show HEAD:… \| sed -n 4044,4051p` | 2026-08-19 | **метод тот, строки уехали +2…+4**: `_update_resize_panel` на `:4047`, сброс `_resize_model_base`/`_resize_base` на `:4049-4051` |
| 48.2 | `:4059` — снятие базлайна | то же | 2026-08-19 | **уехало +2**: `_capture_resize_base` на `:4061` (`:4059` — комментарий-разделитель) |
| 48.3 | `:4122-4137` — превью | то же | 2026-08-19 | **уехало +4**: `preview_resize` на `:4126-4141` |
| 48.4 | `:4295-4312` — применение | то же | 2026-08-19 | **уехало +4**: `apply_resize` на `:4299`, снятие базлайна для `SnapshotCommand` на `:4313-4314` |
| 48.5 | «`preview_resize` мутирует модель напрямую, `undo_mgr.revision` не растёт» | зонд на фикстуре `d74eb9f1` | 2026-08-19 | **ПОДТВЕРЖДЕНО**: два тика превью — `revision` 0→0, `len(undo_stack)` 0, геометрия узла изменена |
| 48.6 | Ловушка аудита «геометрия уже идемпотентна, `apply_resize` уже пишет `SnapshotCommand`» | тот же зонд | 2026-08-19 | **ПОДТВЕРЖДЕНА**: bbox после двух тиков `90×90` бит-в-бит совпал; `undo` после «Применить» вернул геометрию И пины бит-в-бит. Чинились только два остатка |
| 48.7 | ⚠ Числа пункта (`dx` 21.0 → 45.0 → 96.43, после «Применить» 206.63 при ширине 90) | пересъём на git-фикстуре | 2026-08-19 | **невоспроизводимы здесь и не опровергнуты**: они сняты на графе с базовой рамкой 42 px (`sx`=90/42=2.1429 — ряд сходится арифметически), а такой схемы в git-видимом корпусе нет (`storage/` пуст). Механизм тот же, числа сняты свои — см. 48.8 |

### 48б. Воспроизведение обоих дефектов — замер, а не чтение

Фикстура `d74eb9f1` (66 узлов / 63 ребра), класс `armatura_ruchn` (17 экземпляров, все
боксы), узел `node_11`: bbox `[25, 215, 45, 251]` = 20×36, centroid `[233, 35]`. Пин
поставлен `set_edge_pin` на середину правой грани → `dx=10.0, dy=0.0`. Цель превью
90×90 (стороны равны — ориентация рамки на результат не влияет), `sx` = 90/20 = 4.5.

| # | что | дата | результат |
|---|---|---|---|
| 48.8 | ⭐ Дефект «пины вне базлайна»: два тика ОДНИМ И ТЕМ ЖЕ 90×90 | 2026-08-19 | bbox идемпотентен (`[-10,188,80,278]` оба раза), **пин `dx` 10.0 → 45.0 → 202.5**; после «Применить» — **911.25 при полуширине узла 45** (конец трубы в двадцати рамках от узла). После правки: 45.0 на всех трёх шагах, `cx+dx` = 80.0 = правая грань новой рамки |
| 48.9 | ⭐ Дефект «брошенное превью»: выход из режима после превью | 2026-08-19 | bbox остаётся `[-10,188,80,278]` вместо `[25,215,45,251]`, `undo_stack` пуст — **вернуть нечем**. После правки: геометрия и пины всей схемы возвращаются **бит-в-бит** (сравнение json-дампа всех узлов и всех пинов), глубина стека не меняется |
| 48.10 | Область мутации превью | 2026-08-19 | превью трогает ТОЛЬКО узлы набора и пины инцидентных им рёбер: 49 узлов вне набора не шелохнулись (bbox+centroid) — поэтому откат сделан точечным от базлайна, а не полным `model.restore` |
| 48.11 | Ключи геометрии в корпусе (можно ли откатывать присваиванием) | 2026-08-19 | 3 фикстуры, 261 узел: **без `area` — 0, `area is None` — 0, без `bbox` — 0**, без `segmentation` — 253 (только полигоны его несут). Откат присваиванием точен; ветка «ключа не было» в реальных графах не встречается |

### 48в. Гейт пункта (класс [ui]) и зонды

| # | что | команда | дата | результат |
|---|---|---|---|---|
| 48.12 | Тесты пункта ДО правки | `pytest tests/ui/test_resize_preview_revert.py -q` | 2026-08-19 | **6 failed, 3 passed** — красные ровно оба дефекта (3 пиновых + 3 откатных), зелёные — характеризация, которую правка ломать не должна |
| 48.13 | Те же тесты ПОСЛЕ правки | та же | 2026-08-19 | **9 passed** |
| 48.14 | ⛔ Зонд 1 — снять `_restore_resize_pins()` из `_apply_sizes_from_base` | инъекция в ТЕКУЩЕЕ дерево, восстановление из копии файла (не `git checkout`) | 2026-08-19 | **3 пиновых теста краснеют**, откатные зелены |
| 48.15 | ⛔ Зонд 2 — снять захват пинов в базлайн (`_capture_resize_pins`) | та же | 2026-08-19 | **6 краснеют** — без базлайна пинов не работает ни идемпотентность, ни откат |
| 48.16 | ⛔ Зонд 3 — снять откат из `_exit_resize_objects` | та же | 2026-08-19 | краснеет **ровно 1** — `test_abandoned_preview_reverts_on_escape` |
| 48.17 | ⛔ Зонд 4 — снять откат из `_update_resize_panel` | та же | 2026-08-19 | краснеют **2** — смена набора и утечка базлайна в следующее «Применить» |
| 48.18 | ⭐⛔ Зонд 5 — не снимать базлайн в `apply_resize` (ловушка самой правки) | та же | 2026-08-19 | краснеют **3**, включая характеризационный `test_apply_survives_panel_refresh`: `apply_resize` заканчивается тем же `_update_resize_panel`, который чинит брошенное превью, и без `_drop_resize_baseline()` он отменял бы только что применённое. Тест написан ПОД эту ловушку и её ловит |
| 48.19 | Дерево после зондов | `md5sum` файла против копии | 2026-08-19 | совпало (`4b2b3876f3e9d9b0ecc95be31d3e6a09`) |

### 48г. ⭐ Регрессия, которую поймал зонд, и сторож протухшего базлайна

Зонд «Ctrl+Z посреди живого превью» ловил не дефект пункта, а **дефект, внесённый самой
правкой**: откат брошенного превью отменял чужой Ctrl+Z. Это тот случай, ради которого
PROTOCOL §5 требует зонда, — без него правка уехала бы в коммит.

| # | что | дата | результат |
|---|---|---|---|
| 48.20 | ⛔ Регрессия правки: превью → Ctrl+Z → выход из режима | 2026-08-19 | `undo` честно вернул `[25,215,45,251]`, но откат по протухшему базлайну **воскресил отменённое** `[-10,188,80,278]`. До правки этого пути не было (откат не делался вовсе) — регрессия моя, не наследство |
| 48.21 | ✖ Сторож 1 — сравнение `nodes`/`edges_data` по ссылке | 2026-08-19 | **не годится**: ловит только `model.restore` (`SnapshotCommand`). `DragNodeCommand` правит узлы НА МЕСТЕ и модель не пересобирает — зонд «снять сторожа в превью» остался зелёным, потому что сторож там ничего и не сторожил |
| 48.22 | ⭐ Сторож 2 — `undo_mgr.revision` | 2026-08-19 | **годится**: счётчик растёт на `execute`/`push_executed`/`undo`/`redo`, то есть на ЛЮБОЙ команде, включая правящие на месте. Базлайн живёт, пока `revision` не изменилась |
| 48.23 | Наблюдаемость протухания — где она есть и где её нет | 2026-08-19 | через саму рамку **не видна**: геометрия идемпотентна, и `20→90→60` даёт те же 60×60, что и `20→60` (пин так же сходится: `10→45→20` = `10→20`). Видна в двух местах: центроид (протянутый и отменённый узел — превью садится вокруг точки, из которой узел вернулся: `[305,403,365,463]` вместо `[5,203,65,263]`) и точка возврата следующего «Применить» (Ctrl+Z уводит туда, где оператора не было). Тесты написаны именно на них, а не на размер рамки |
| 48.24 | ⛔ Зонд 6 — сторож всегда «жив» | инъекция, восстановление из копии | 2026-08-19 | **краснеют 3** теста протухания |
| 48.25 | ⛔ Зонды 7–9 — сторож снят по одной точке (откат / превью / «Применить») | те же | 2026-08-19 | каждая точка краснит **ровно свой** тест: откат → `..._undo_during_live_preview_is_not_resurrected`, превью → `..._preview_after_undone_drag_...`, «Применить» → `..._apply_after_undo_returns_to_the_state_operator_saw` |
| 48.26 | Дерево после зондов 6–9 | `md5sum` против копии | 2026-08-19 | совпало (`dd798d621d0a820e1019a1a317460042`) |

### 48д. Итоговый гейт пункта

| # | что | команда | дата | результат |
|---|---|---|---|---|
| 48.27 | Гейт пункта — тесты 0.4 | `pytest tests/ui/test_gesture_dispatch.py -q` | 2026-08-19 | **19 passed** |
| 48.28 | Тесты пункта | `pytest tests/ui/test_resize_preview_revert.py -q` | 2026-08-19 | **12 passed** (6 на дефекты пункта, 3 на протухание базлайна, 3 характеризационных) |
| 48.29 | Вся вкладка UI на Qt offscreen | `pytest tests/ui -q` (`QT_QPA_PLATFORM=offscreen`) | 2026-08-19 | **276 passed, 13 skipped** (было 264+13, +12 тестов пункта) |
| 48.30 | Долг линтеров | `python tools/lint_gate.py --check` | 2026-08-19 | exit **0**: ruff 178/эталон 178, mypy 0 ошибок |
| 48.31 | База набора | `python tools/suite_baseline.py --check` | 2026-08-19 | exit **0** «новых красных нет»: собрано **975** git-видимых в 91 файле (было 963 в 90), `failed 13 / errors 9` — как в базе. **Д6-пересъёма НЕ делал**: в главном дереве идёт параллельная сессия 1-28, пересъём запрещён (`PROTOCOL §Д6`) |

### 48е. Что осталось за границей пункта (заявлено, не сделано)

- **Сохранение поверх незафиксированного превью.** Порядок «превью → Сохранить → выход»
  теперь оставляет на сервере превью, а в модели — исходное. Это сужение прежней дыры,
  а не новая: до правки брошенное превью жило в модели вечно и автосейв (120 с) увозил
  его при ближайшем тике в любом случае. Остаток закрывает **1-7 (1.5, дёрти-флаг)** —
  ровно поэтому 1.4 стоит перед ним в очереди.
---

## 49. Пункт 1-28 дороги (GATE-4) — пересъём эталона перестал легализовать регрессии (2026-08-19)

Пункт собран из находок 1-25 и 1-26: обе сессии видели дыры **на пути ЗАПИСИ** и оставили их
строкой в журнал, правкой не трогали. `--check` у трёх стендов уже судит трёхзначно (1-25),
не даёт легализовать красных (1-26) и не краснеет от отката пункта (1-27); менял же эталон
всё это время `--write-baseline`, и он не сверялся ни с чем. Тронуто 3 стенда + 3 файла
тестов + `docs/TESTING.md`.

Среда всех замеров: Windows 10, **системный** `python` 3.11.9 (не `.venv311` — там нет pytest,
`PROTOCOL §5`), ветка `road/GATE-4` от `384d23b`, дерево чистое, чужих правок нет.
Каждая инъекция снималась **копией файла** (`scratchpad/backup/`), не `git checkout`.

### 49а. Адреса пункта перепроверены по коду (шаг 2 жизненного цикла)

| # | утверждение строки | по коду на 2026-08-19 | вердикт |
|---|---|---|---|
| 49.1 | (а) `layout_determinism --write-baseline` пишет эталон из того, что померили | `tools/layout_determinism.py:231-238`: `stable` собирается из `result`, обращения к `read_baseline()` в ветке записи нет | ✔ точно |
| 49.2 | (б) `tools/lint_gate.py:138` эталон ruff не сверяет вовсе | адрес **дословно верен**: `if args.write_baseline:` стоит на `:138`, `read_baseline()` зовётся только на `:148` в ветке `--check` | ✔ точно |
| 49.3 | (в) `suite_baseline.cmd_write` не проверяет пол набора | `write_blocked(base, red, totals)` судит только состав красных и полноту вывода; ни `floor_problems`, ни `min_collected`, ни `per_file` в ветке записи не читаются | ✔ точно |
| 49.4 | (г) «судить нечем» и «отказ» на записи дают один код | `_measure` → `sys.exit("сбор pytest сломан…")` и `cmd_write` → `sys.exit("[ОТКАЗ]…")`: у `sys.exit(str)` код процесса **1** в обоих случаях | ✔ точно |
| 49.5 | ⭐ РАСХОЖДЕНИЕ, найдено зондом: у (в) есть СОСЕД, который судит вместо стенда | `tests/test_collection_clean.py::test_suite_does_not_shrink` считает тот же `floor_problems` и при потере тестов становится НОВЫМ красным — то есть отказ 1-26 срабатывает раньше пола. Ловушка `PROTOCOL §5` (случай 1.10): зонд краснеет не за то, что проверяет | разобрано в 48.12 |

### 49б. Дефекты замерены ДО правки — каждый на живом дереве

| # | зонд | команда | дата | результат |
|---|---|---|---|---|
| 49.6 | (а) пересъём на усечённом корпусе | `python -X utf8 tools/layout_determinism.py --git-only --runs 2 --write-baseline` | 2026-08-19 | **exit 0**, «эталон переснят». В файле осталось **3 графа вместо 17**, вычеркнуты все 14 невидимых в git — среди них **все 8 известных неповторимых** (§45.7). Заодно `runs` уехал **6 → 2**, а `6e7144d5` записан `true`, хотя следующий же `--check` при тех же `--runs 2` назвал его НЕПОВТОРИМ (`[ПРОВАЛ] перестали воспроизводиться`, exit 1): запись фиксирует число, которому стенд сам противоречит через две секунды |
| 49.7 | (б) широкий `except` в чистом файле → пересъём | приписан `_probe_gate4()` с `except Exception:` в `tools/corpus.py`; `lint_gate.py --check`, затем `--write-baseline`, затем снова `--check` | 2026-08-19 | до пересъёма **exit 1** `[ПРОВАЛ] tools/corpus.py: широких except 0 -> 1` (179 в 72 файлах против эталона 178); пересъём **exit 0** «эталон переснят (179 нарушений)»; после — **exit 0 «[OK] долг не вырос»**. Долг легализован ОДНОЙ командой, и по Д6 она идёт отдельным коммитом, поэтому красный флаг №4 её не видит |
| 49.8 | (в) пересъём в среде, где тесты пропали молча | `collect_ignore.append("observability")` + `.append("test_collection_clean.py")` в `tests/conftest.py` (дописыванием, не присваиванием — урок §46.12), затем `suite_baseline.py --write-baseline` | 2026-08-19 | **exit 0**, «база записана». Пол **952 → 811**, git-видимых **957 → 816**, собрано 973 → 832, карта файлов **89 → 64** (25 файлов выпали из карты навсегда); состав красных **не изменился** (22), поэтому сверка 1-26 промолчала |
| 49.9 | (в) потеря стала нормой | зонд снят (дерево здорово), уехавшая база оставлена, `suite_baseline.py --check` | 2026-08-19 | **exit 0 «[OK] новых красных нет»** при собранных 979 / git-видимых 963 против базы 832 / 816. После одного пересъёма 141 тест может пропасть снова, и гейт этого не увидит |
| 49.10 | (г) «судить нечем» на пути записи | `import _gate4_no_such_module` в `tests/conftest.py`, затем `--write-baseline` | 2026-08-19 | **exit 1**, «сбор pytest сломан (exit 4) — это пункт 0.0, а не база» — тот же код, что у отказа 48.11 |
| 49.11 | (г) «отказ» на пути записи | подложенный красный (зонд 1-26) | 2026-08-19 | **exit 1**, `[ОТКАЗ] множество красных выросло`. Различить 48.10 и 48.11 вызывающему нечем |
| 49.12 | ⭐ (в) со СТОРОЖЕМ в наборе — почему зонд пришлось изолировать | тот же `collect_ignore`, но **без** снятия `test_collection_clean.py` | 2026-08-19 | **exit 1**, но причина ЧУЖАЯ: `[ОТКАЗ] множество красных выросло … + tests/test_collection_clean.py::test_suite_does_not_shrink`. Сторож считает тот же пол, краснеет, и отказ 1-26 срабатывает по факту нового красного — пол в стенде при этом не проверяется ни разу. Сторож заменой стенду не является: он сам из набора и уходит вместе с тем, что его съело (48.8), и достаточно ему один раз попасть в базу красных, чтобы маскировка исчезла молча |

### 49в. Правка

`write_blocked()` в форме соседнего `verdict()` — в каждом из трёх стендов; возвращает
**пару** списков `(судить нечем, отказ)`, пустые обе = пересъём законен. Разведение:

* **exit 2 «СУДИТЬ НЕЧЕМ»** — замер не годен, судить не о чем: корпус усечён (ПР1);
  прогон ruff убит (ПР5); сбор сломан, прогон убит, вывод оборван (база набора);
* **exit 1 «ОТКАЗ»** — замер годен и говорит, что пересъём узаконил бы регрессию:
  воспроизводимый граф сломался; долг широких `except` вырос; набор тихо потерял тесты;
* приоритет — как у `--check` в 1-25: **доказанная регрессия сильнее неполноты**.

Пол на пути записи считается тем же `floor_problems`, что у `--check` и у сторожа сбора, —
третий потребитель одной арифметики (1-27 свёл первых двух). `debt_grown()` в `lint_gate`
выделен по той же причине. У `--check` коды возврата **не менялись** (48.22).

### 49г. Зонды ПОСЛЕ правки — на каждый из четырёх, обеих полярностей

| # | зонд | команда | дата | результат |
|---|---|---|---|---|
| 49.13 | (а) корпус усечён | `--git-only --runs 2 --write-baseline` | 2026-08-19 | **exit 2** `[СУДИТЬ НЕЧЕМ] не измерено 14 графов эталона из 17 — корпус усечён, пересъём вычеркнул бы их` + список; эталон **бит-в-бит** равен копии |
| 49.14 | (а) отказ: эталон числит граф воспроизводимым, а он сломался | эталон подменён на `{089feca2: true, 6e7144d5: true, d74eb9f1: true}`, `--uid ×3 --runs 6 --write-baseline` | 2026-08-19 | **exit 1** `[ОТКАЗ] перестали воспроизводиться: 6e7144d5 — пересъём записал бы поломку нормой`; файл эталона не тронут |
| 49.15 | (а) положительный контроль (дешёвый) | эталон сужен до трёх git-графов, `--git-only --runs 2 --write-baseline` | 2026-08-19 | **exit 0** «эталон переснят»; записано `089feca2: true, 6e7144d5: false, d74eb9f1: true` — неповторимость видна и записана честно |
| 49.16 | (б) долг вырос | тот же `_probe_gate4()`, `lint_gate.py --write-baseline` | 2026-08-19 | **exit 1** `[ОТКАЗ] долг широких except вырос … tools/corpus.py: 0 -> 1`; эталон **бит-в-бит** равен копии |
| 49.17 | (б) прогон ruff убит | `select = []` в `pyproject.toml`, `--write-baseline` | 2026-08-19 | **exit 2** `[СУДИТЬ НЕЧЕМ] ноль нарушений при непустом эталоне — прогон ruff убит, а пересъём записал бы пустой долг`; эталон цел |
| 49.18 | (б) положительный контроль | зонд снят, `--write-baseline` | 2026-08-19 | **exit 0** «эталон переснят (178 нарушений)», и записанный файл **бит-в-бит** равен коммиченному |
| 49.19 | (в) пол уехал вниз | зонд 48.8 (с изоляцией от сторожа), `--write-baseline` | 2026-08-19 | **exit 1** `[ОТКАЗ] набор усох: тихо потеряно 147 тестов (допуск 5)` с перечислением всех **26** файлов (25 из `tests/observability/` плюс сам сторож); эталон **бит-в-бит** равен копии. Причина названа своя, а не «новый красный» |
| 49.20 | (в) положительный контроль | здоровое дерево, `--write-baseline` | 2026-08-19 | **exit 0**: «база записана: 22 красных, собрано 994 (git-видимых 978 в 90 файлах)» — это и есть законный Д6-пересъём; эталон возвращён копией |
| 49.21 | (г) «судить нечем» ≠ «отказ» на записи | сломанный сбор → `--write-baseline` | 2026-08-19 | **exit 2** (было 1, §49.10). Отказ по-прежнему **1** (48.16, 48.19) |
| 49.22 | (г) обратная сторона: путь ЧТЕНИЯ не тронут | тот же сломанный сбор → `--check` | 2026-08-19 | **exit 1** — как и до правки. Шаг CI на этом стенде 1 и 2 не различает, менять его код значило бы править чужой пункт |

### 49д. Д1 — тесты писались ДО правки и краснели на старом коде

| # | что | команда | дата | результат |
|---|---|---|---|---|
| 49.23 | Новые тесты на СТАРОМ коде | `pytest tests/test_layout_determinism.py tests/test_lint_gate.py tests/test_suite_baseline.py` до правки стендов | 2026-08-19 | **11 failed, 60 passed** — красны ровно ветки пункта: пересъём отдавал 0 там, где обязан 1 или 2, и переписывал эталон |
| 49.24 | Они же после правки | та же команда + `tests/test_collection_clean.py` | 2026-08-19 | **73 passed** (было 58: +15 тестов пункта — 4 у ПР1, 5 у ПР5, 6 у базы набора) |
| 49.25 | Допуск заперт с двух сторон **абсолютными** числами | `test_write_tolerates_a_loss_inside_the_margin` | 2026-08-19 | 30 → 25 пересъём пропускает, 30 → 24 отказывает. Числа не вычисляются из `FLOOR_MARGIN` (урок 0.4), поэтому расширить допуск незаметно нельзя |

### 49е. Полный корпус — положительный контроль (а) на настоящем эталоне

| # | что | команда | дата | результат |
|---|---|---|---|---|
| 49.26 | ⭐ Пересъём ПР1 на ПОЛНОМ корпусе | `python -X utf8 tools/layout_determinism.py --runs 2 --write-baseline`, ~15 мин (два прогона по 19 графов: 7 м 25 с каждый) | 2026-08-19 | **exit 0** «эталон переснят»: все **17** графов эталона измерены, ни один `true` не сломался, отказа нет. Записано 19 графов (плюс `0fc9d04c`, `3263039b` — те, из-за которых заведён 1-29). Эталон **возвращён копией**, `sha256 a9842673` совпал: Д6 для ПР1 — это пункт 1-29, не мой |
| 49.27 | ⛔ ЧТО ЭТОТ ЖЕ ПРОГОН ПОКАЗАЛ ПРО 1-29 (находка, не правка) | сравнение записанного с эталоном в git | 2026-08-19 | при `--runs 2` **4 из 8** известных неповторимых вышли `true`: `54a60fd1`, `620cc50d`, `6e7144d5`, `751116c9` (эталон снят при `runs 6`). Это ровно «удачная выборка», о которой предупреждает `TESTING §8.2`, и мой отказ её НЕ ловит по замыслу: `false → true` — законное «стало лучше», так же судит и `--check` с 1-25. Следствие для 1-29: **пересъём ПР1 обязан идти при `--runs` не меньше эталонных 6**, иначе эталон ослабнет вчетверо молча. Стенд этого не стережёт: `runs` он пишет из аргумента (§49.6) |

---

## 50. Пункт 1-7 дороги (1.5) — дёрти-флаг: на сервер уходит только зафиксированное (Р13) (2026-08-19)

Работа шла в отдельном worktree `C:/project/pid_wt_1.4` на ветке `road/1.5` (в главном
дереве параллельно 1-29). Корпус здесь — только 3 git-видимые фикстуры, `storage/` пуст;
docker/Redis/postgres не трогались ни одной командой. Стенд — Qt offscreen, системный
`python` 3.11.9. Фикстура замеров — `d74eb9f1`, узел `node_11` (bbox `[25, 215, 45, 251]`,
centroid `[233, 35]`), класс `armatura_ruchn`, цель превью 90×90.

### 50а. Адреса пункта перепроверены по коду (шаг 2 жизненного цикла)

| # | утверждение пункта | команда | дата | результат |
|---|---|---|---|---|
| 50.1 | `base_graph_tab.py:1146-1150` — `has_unsaved_changes()` | `grep -n "has_unsaved_changes\|_saved_revision" ui/tabs/base_graph_tab.py` | 2026-08-19 | **метод тот, адрес уехал вверх**: `def has_unsaved_changes` на `:1103`, тело `undo_mgr.revision != self._saved_revision` на `:1106`. `:1146-1150` теперь `_on_confirm` |
| 50.2 | «автосейв 120 с, включён по умолчанию» | `ui/services/autosave.py` + `ui/services/ui_settings.py` | 2026-08-19 | **ПОДТВЕРЖДЕНО**: гард `has_unsaved_changes()` — `autosave.py:81`, дефолты `autosave_enabled=True` / `autosave_interval_sec=120` — `ui_settings.py`. Ловушка Л56 подтверждается: в `autosave.py:19-26` лежит `_SAVE_METHODS`, а не период |
| 50.3 | четыре пути записи графа | `grep -rn "_save_graph\b" ui/` | 2026-08-19 | **все четыре сходятся в `BaseGraphTab._save_graph`**: кнопка (`:799`), «Подтвердить» (`:1163`), диалог закрытия (`diagram_workspace.py:1313`), автосейв (`autosave.py:23`). `ContourTab._save_graph:399` зовёт `super()` |
| 50.4 | вход из 1.4: «сравнение модели по ссылке декоративно, признак — `undo_mgr.revision`» | чтение `_resize_baseline_alive` (`advanced_graph_editor.py:4083-4097`) | 2026-08-19 | **ПОДТВЕРЖДЕНО в коде**: сторож 1.4 уже сравнивает `_resize_model_rev == undo_mgr.revision`. Дёрти-флаг строился на нём же и до пункта — менять его не потребовалось |

### 50б. Дефект замерен ДО правки — обоими путями записи

Зонд: вкладка `AdvancedGraphTab` с подставным API-клиентом, редактор из git-фикстуры,
`_download_artifacts` снят (сети нет). «Сервер» = JSON, который вкладка отдала клиенту.

| # | что | сценарий | дата | результат |
|---|---|---|---|---|
| 50.5 | ⭐ «превью → Сохранить → выход» | `preview_resize(90, 90)` → `tab._save_graph()` → `set_mode("idle")` | 2026-08-19 | **сервер разошёлся с моделью**: на сервере `node_11` = `[-10.0, 188.0, 80.0, 278.0]`, в модели после выхода — исходные `[25, 215, 45, 251]`. `has_unsaved_changes()` при этом `False`, `revision` 0 |
| 50.6 | ⭐ «протяжка → превью → тик автосейва → выход» | `drag → (500, 400)` → `preview_resize(90, 90)` → `AutoSaveService._on_tick()` → `set_mode("idle")` | 2026-08-19 | **автосейв увёз брошенную порчу**: на сервере `[455.0, 355.0, 545.0, 445.0]` (протяжка + превью), в модели после выхода `[490.0, 382.0, 510.0, 418.0]` (одна протяжка) |
| 50.7 | флаг слеп к превью — и это правильно | `preview_resize` → `has_unsaved_changes()` | 2026-08-19 | `False` при изменённой геометрии. Делать флаг грязным от превью **нельзя**: тогда автосейв отправлял бы именно его (это и есть «1.5 раньше 1.4») |

### 50в. Правка

`BaseGraphEditor.drop_uncommitted_preview()` — no-op в базе; `AdvancedGraphEditor`
переопределяет (`_revert_resize_preview` + `_redraw_resize_frames`, панель не сбрасывается —
`_update_resize_panel` затёр бы набранные оператором ширину/высоту медианами);
`BaseGraphTab._save_graph` зовёт его перед сериализацией. Диффа — 33 строки в 3 файлах,
ни одной удалённой.

### 50г. Д1 — тест писался ДО правки и краснел на текущем дереве

| # | что | команда | дата | результат |
|---|---|---|---|---|
| 50.8 | Новый набор на коде ДО правки | `pytest tests/ui/test_upload_only_committed.py` | 2026-08-19 | **3 failed, 6 passed** — красны ровно три теста путей записи (Сохранить / автосейв / Подтвердить), характеризация зелена |
| 50.9 | Он же после правки | та же команда | 2026-08-19 | **9 passed** |

### 50д. Зонды после правки — обеих полярностей, с проверкой «кто именно покраснел»

Прогон каждой инъекции — по всему `tests/ui` (285 тестов), чтобы видеть чужое красное.
Восстановление — копией файла, не `git checkout --`.

| # | инъекция | ожидание | дата | результат |
|---|---|---|---|---|
| 50.10 | И1: вкладка не зовёт `drop_uncommitted_preview` | краснеют три теста путей записи | 2026-08-19 | **3 failed, 282 passed** — ровно они, ни одного чужого |
| 50.11 | И2: `AdvancedGraphEditor.drop_uncommitted_preview` → пустой `return` | то же (метод есть, работы не делает) | 2026-08-19 | **3 failed, 282 passed** — те же три |
| 50.12 | И3: снятие превью реализовано как `self.undo()` (правдоподобная ошибка) | краснеет характеризация | 2026-08-19 | **6 failed, 279 passed**: плюс `test_applied_resize_reaches_server`, `test_save_without_any_preview_uploads_model_as_is`, `test_dropping_preview_leaves_undo_stack_alone` |
| 50.13 | ⛔ И4: превью объявляет себя изменением (`revision += 1` в `preview_resize`) — ловушка «1.5 раньше 1.4» | краснеет сторож ловушки | 2026-08-19 | **3 failed, 282 passed**: `test_live_preview_does_not_make_tab_dirty`, `test_open_spoil_close_without_saving_leaves_server_untouched`, `test_confirm_during_live_preview_uploads_original` |
| 50.14 | ⛔ **Первый прогон И4 не покраснел, а ПОВИС** | `pytest tests/ui` с И4 | 2026-08-19 | `_on_confirm` открывает **модальный** `QMessageBox.question` при грязном флаге — регрессия подвешивала бы весь набор вместо того, чтобы его уронить. Тест исправлен: диалог подменяется и его вызов проверяется (`asked == []`). Это правило PROTOCOL §5 «зонд не покраснел — подозревай обстановку теста» в третьем варианте: обстановка была не в тесте и не в стороже, а в **модальности UI** |
| 50.15 | И5: снять фикс 1.4 (выход из режима не откатывает превью) — чей это гейт? | проверка атрибуции красного | 2026-08-19 | **2 failed**: `test_resize_preview_revert.py::test_abandoned_preview_reverts_on_escape` (1.4) **и** мой `test_open_spoil_close_without_saving_leaves_server_untouched`. Честная запись: полный сценарий гейта опирается на 1.4; собственное доказательство 1.5 — трио путей записи (50.10, 50.11) |

### 50е. Итоговый гейт пункта

| # | что | команда | дата | результат |
|---|---|---|---|---|
| 50.16 | набор UI (Qt offscreen) | `python -m pytest tests/ui -q` | 2026-08-19 | **285 passed, 13 skipped** (было 276+13 после 1.4: +9 тестов пункта) |
| 50.17 | долг линтера | `python -X utf8 tools/lint_gate.py --check` | 2026-08-19 | **exit 0**: ruff BLE001/E722 — 178 нарушений в 71 файле (эталон 178), mypy 0 |
| 50.18 | набор не усох, новых красных нет | `python -X utf8 tools/suite_baseline.py --check` | 2026-08-19 | **exit 0** «новых красных нет»: собрано 999 (git-видимых 999 в 92 файлах), `13 failed / 960 passed / 17 skipped / 9 errors` — красных ровно столько же, сколько в базе |
| 50.19 | откуда рост 973 → 999 (база записана до мержей 1-28 и 1.4) | `pytest --collect-only -q --ignore=tests/ui/test_upload_only_committed.py` | 2026-08-19 | **990** без моего файла → +9 мои. Д6-пересъём базы НЕ делался: в главном дереве параллельная сессия 1-29 (запрет `PROTOCOL §Д6`) |
| 50.20 | ни один путь записи не остался без базового метода | `grep -n "class .*Tab\|_create_editor" ui/tabs/*.py` | 2026-08-19 | все три вкладки на `BaseGraphTab._save_graph` держат редактор от `BaseGraphEditor` (`SimpleGraphEditor`, `AdvancedGraphEditor`, `ContourEditor`); `OcrBindingTab` — не `BaseGraphTab` (`AppearanceMixin, QWidget`), её `_save_graph` уходит в `_save_binding` и правки не касается |

### 50ж. Что осталось за границей пункта (заявлено, не сделано)

| # | находка | почему не здесь |
|---|---|---|
| 50.21 | Тик автосейва посреди живого превью теперь **визуально откатывает** превью (модель + жёлтые рамки; набранные в панели ширина/высота остаются). Окно — 120 с, попасть в него можно | Это осознанный размен пункта: брошенное превью на сервере хуже, чем откат на экране с внятной строкой в логе. Альтернатива — учить `AutoSaveService` пропускать тик при живом превью — правит общий сервис шести типов вкладок, то есть чужую область |
| 50.22 | «Испортить» в гейте = **живое превью**. Зафиксированная командой правка (протяжка), которую автосейв увёз до закрытия вкладки, на сервере остаётся — и это штатная работа автосейва, а не дефект Р13 | Дефект пункта — именно «брошенная» (неподтверждённая) порча. Менять смысл автосейва — не 1.5 |

## 51. Пункт 1-8 дороги (1.3) — молчаливые откаты-«самолечение» при открытии и закрытии (2026-08-19)

Работа шла в отдельном worktree `C:/project/pid_wt_1.4` на ветке `road/1.3` (в главном
дереве параллельно 1-29). `storage/` здесь пуст, `.env` нет; docker/Redis/postgres не
трогались ни одной командой. Стенд — Qt offscreen, системный `python` 3.11.9, сервер
поддельный. **Среда, в которой гейт доказуем:** любая с PySide6 — сценарий не зависит ни
от брокера, ни от корпуса, ни от реальной диаграммы (`storage/` не читается вовсе).

### 51а. Адреса пункта перепроверены по коду (шаг 2 жизненного цикла)

| # | утверждение пункта | команда | дата | результат |
|---|---|---|---|---|
| 51.1 | `diagram_workspace.py:711-731` — `_self_heal_stuck_stage`, из `load_diagram:672` | `grep -n "_self_heal_stuck_stage\|def load_diagram" ui/widgets/diagram_workspace.py` | 2026-08-19 | **ПОДТВЕРЖДЕНО, адрес уехал на 3 строки**: `def _self_heal_stuck_stage` на `:714`, вызов из `load_diagram` на `:675` |
| 51.2 | `:1338-1363` — откат при закрытии вкладки | чтение `_rollback_if_validating` | 2026-08-19 | **ПОДТВЕРЖДЕНО**: `:1341-1366`, зовётся из ДВУХ путей — `_close_active_tab:1325` и `_force_close_tab:1336` |
| 51.3 | ⭐ ключи разошлись: `graph_val` против `val_graph` | `grep -n "_open_tab(" ui/widgets/diagram_workspace.py` | 2026-08-19 | **ПОДТВЕРЖДЕНО**: `_open_tab(tab, "graph_val")` на `:1931` (план говорил `:1928`), таблица `_VALIDATING_ROLLBACK` знает `"val_graph"` — `:1284` (план говорил `:1282`). Ветка закрытия для графовой вкладки не срабатывает никогда |
| 51.4 | самолечение идёт **без preserve-флагов** | живой прогон воркспейса на поддельном API (`load_diagram` при `validating_graph`) | 2026-08-19 | **ПОДТВЕРЖДЕНО в рантайме**: `rollback_diagram(uid, "built")`, kwargs пустые — `preserve_ocr=False`, `preserve_contours=False` |
| 51.5 | ловушка `PLAN_AUDIT`: «наивное исправление ключа включит штатный откат» | зонд И3, ниже | 2026-08-19 | **ПОДТВЕРЖДЕНО числом**: при совпавших ключах откат уносит уже не 9 артефактов, а 3 — `fxml`, `graph_canvas`, `graph_validated`. Холст гибнет всё равно |

### 51б. ⭐ Замер, который решил пункт: откат не покупал НИ ОДНОЙ кнопки

Считано из таблиц самого воркспейса (`_buttons_for_status` + поправка `_MANUAL_INPROGRESS`
из `_update_buttons:944-948`, `_beads_for_status`), для всех четырёх ручных этапов — пара
«`*ING`-статус против стабильного, к которому откатывали»:

| # | пара | дата | различие кнопок | различие бусин |
|---|---|---|---|---|
| 51.6 | `cleaning_frame` → `uploaded` | 2026-08-19 | **нет** | `frame`: IN_PROGRESS против AVAILABLE |
| 51.7 | `validating_junctions` → `detected_junctions` | 2026-08-19 | **нет** | `junction`: IN_PROGRESS против AVAILABLE |
| 51.8 | `validating_masks` → `skeletonized` | 2026-08-19 | **нет** | **нет** |
| 51.9 | `validating_graph` → `built` | 2026-08-19 | **нет** | **нет** |

Вывод: заявленную цель самолечения («чтобы этап снова стал доступен») уже держит соседний
механизм — поправка `#3` в `_update_buttons`, которая возвращает кнопку ручного `*ING`-этапа
в `available`. Откат добавлял к этому один цвет бусины и снос артефактов. Поэтому оба
молчаливых отката сняты целиком, а цвет бусины восстановлен той же поправкой в
`_update_beads` (под условием «вкладка не открыта» — пока вкладка открыта, синяя «в
процессе» честна).

### 51в. Цена молчаливого отката — замерена ДО правки

Стенд: `tests/ui/test_no_silent_rollback.py`. Поддельный сервер держит множество артефактов,
а **что именно сносит откат, считает настоящая `app.api.rollback._artifacts_to_delete`** —
иначе стенд судил бы по своему представлению о сносе.

| # | сценарий | дата | результат |
|---|---|---|---|
| 51.10 | ⭐ «Проверка схемы»: открыть — закрыть — открыть, 3 раза | 2026-08-19 | **на первом же цикле с сервера исчезло 9 артефактов из 11**: `contours_auto`, `contours_validated`, `fxml`, `graph_canvas`, `graph_validated`, `ocr_binding`, `ocr_cleaned`, `ocr_result`, `ocr_validation`. Уцелели двое — `graph_json`, `graph_overlay`. Журнал: три отката `('built', False, False)` |
| 51.11 | ⭐ «Очистка рамки»: сохранил очистку → «← Назад» | 2026-08-19 | **сохранённая очистка снесена** откатом `('uploaded', False, False)` из пути закрытия (на сервере это ещё и возврат `image_raw.png` поверх `image.png` — дефект 1.17 плана) |
| 51.12 | тот же жест «зашёл — вышел» | 2026-08-19 | **ни одного вопроса оператору**: `QMessageBox` не звался ни разу — откат был именно молчаливым |

### 51г. Правка

Снято: `_self_heal_stuck_stage` (заменён на `_note_unconfirmed_stage` — строка в лог с `uid`
и сообщение в статусбар, Д2/Д3), таблица `_VALIDATING_ROLLBACK`, метод
`_rollback_if_validating` и оба его вызова. Добавлено: поправка бусины в `_update_beads`.
После правки в файле остался **ровно один** вызов `rollback_diagram` — явный, за вопросом
оператору (`_on_button_click:1473`).

### 51д. Д1 — тест писался ДО правки и краснел на текущем дереве

| # | что | дата | результат |
|---|---|---|---|
| 51.13 | `pytest tests/ui/test_no_silent_rollback.py` на дереве ДО правки | 2026-08-19 | **5 failed, 3 passed** из 8 |
| 51.14 | он же ПОСЛЕ правки | 2026-08-19 | **8 passed** |
| 51.15 | `pytest tests/ui` (Qt offscreen) | 2026-08-19 | **293 passed, 13 skipped** (было 285+13 после 1.5, +8 мои) |

### 51е. Зонды — гейт проверен инъекциями (`PROTOCOL §5`)

Дерево восстанавливалось копией файла, не `git checkout --`.

| # | инъекция | дата | что покраснело | вердикт |
|---|---|---|---|---|
| 51.16 | И1 — вернуть откат в путь ОТКРЫТИЯ | 2026-08-19 | 5 тестов моего файла, **чужих ноль** | гейт ловит |
| 51.17 | И2 — вернуть откат в путь ЗАКРЫТИЯ (с исходным ключом `val_graph`) | 2026-08-19 | **только** `test_frame_tab_close_keeps_saved_cleaning` | ⭐ корень пункта воспроизведён: графовые тесты остались зелёными — ветка закрытия до графовой вкладки не доходит из-за расхождения ключей |
| 51.18 | И3 — «наивное исправление ключа» `val_graph`→`graph_val` (ловушка аудита) | 2026-08-19 | 3 теста; потеря упала с 9 артефактов до 3 — `fxml`, `graph_canvas`, `graph_validated`, откаты `('built', True, True)` | ⭐ ловушка подтверждена: совпадение ключей скрыло бы две трети ущерба и оставило гибель холста |
| 51.19 | И4 — снять поправку бусины в `_update_beads` | 2026-08-19 | оба `test_stage_stays_reachable_without_rollback` (бусина IN_PROGRESS) | поправка не декоративна |
| 51.20 | И5 — сломать ЯВНЫЙ откат оператора (снять `preserve_ocr`) | 2026-08-19 | `test_operator_rollback_still_works` | порог заперт с другой стороны: сняты откаты, которых не просили, а не право откатить |
| 51.21 | ⛔ И6 — снять подмену `QMessageBox` в самом стенде | 2026-08-19 | **набор ПОВИС**: 5 тестов прошло, шестой (`test_operator_rollback_still_works`) завис на модальном диалоге, `timeout 90` → exit 124 | четвёртый исход `PROTOCOL §5` в моей зоне подтверждён: подмена диалога — несущая, а не украшение; факт вызова диалога проверяется утверждением |

### 51ж. Замечено рядом — в правку не вошло

| # | находка | почему не здесь |
|---|---|---|
| 51.22 | `_close_active_tab`: локальная `tab_key = self._active_tab_key` не читается. Проверено, что мёртвой она была **и до правки** (в теле метода на `HEAD` одно вхождение — само присваивание): `_rollback_if_validating` читал поле, а не локальную | чужой мёртвый код, `PROTOCOL §4` — строкой в журнал, не правкой |
| 51.23 | `api_client.skip_frame_removal` (`:411`) не зовётся из UI ни разу; кнопки «Рамки нет» в `FrameTab` нет. Из-за этого `cleaning_frame` не имеет хода вперёд иначе как через сохранённую очистку — но и откат к `uploaded` хода не давал: `detect` серая при обоих статусах (замер 51.6) | тупик существовал до пункта и не создан правкой; это класс А (1.11–1.16), не 1.3 |
| 51.24 | Долг ruff в `diagram_workspace.py` упал 12 → 11 (снят `except Exception` из `_rollback_if_validating`), общий 178 → 177 | эталон `lint_gate` не пересматривался: `--check` зелёный (долг не вырос), а пересъём эталона — это Д6, запрещённый при чужой сессии в главном дереве |

---

## 52. Пункт 1-9 дороги (1.6) — защёлка resize-режима и ручки размера на контуре (Р2+Г1) (2026-08-19)

Работа шла в отдельном worktree `C:/project/pid_wt_1.4` на ветке `road/1.6` (в главном
дереве параллельно 1-29, третья сессия — ревизия связки в `pid_wt_rev2`). `storage/` пуст,
корпус — три git-видимые фикстуры; docker/Redis/postgres не трогались ни одной командой.
Стенд — Qt offscreen, системный `python` 3.11.9. **Среда, в которой гейт доказуем:** любая
с PySide6 — сценарий не читает ни `storage/`, ни брокера, ни сервер; корпусные графы берутся
загрузчиком `tools/corpus.py` и лежат в git, поэтому набор судит и в CI.

### 52а. Адреса пункта перепроверены по коду (шаг 2 жизненного цикла)

| # | утверждение пункта | команда | дата | результат |
|---|---|---|---|---|
| 52.1 | перезапись «предыдущего режима» — `simple_graph_editor.py:276` | чтение `_enter_resize_mode` | 2026-08-19 | **ПОДТВЕРЖДЕНО дословно**: `self._mode_before_resize = self._current_mode or "add_edge"` на `:276`, безусловна |
| 52.2 | источников защёлки два независимых: `advanced:5499`, `simple:480` | `grep -n "_enter_resize_mode" ui/editors/*.py ui/editors/mode_handlers/*.py` | 2026-08-19 | **ПОДТВЕРЖДЕНО, адрес advanced уехал**: двойной клик Advanced зовёт вход на `:5618` (в плане `:5499`), Simple — на `:480`. Спящий третий (`add_equipment_node(enter_resize=True)`) на `:207-209` — там же, где обещал план |
| 52.3 | ловушка `PLAN_AUDIT`: Esc-обработчик УЖЕ есть (`simple:464-469`), честный путь переключения ручек — `handlers:168-169` | чтение обоих | 2026-08-19 | **ПОДТВЕРЖДЕНО**: оба на месте, новых обработчиков выхода не заводилось. `handlers:168` зовёт `_stop_resize()` ПЕРЕД входом, поэтому сам защёлку не взводит |
| 52.4 | «после защёлки мертвы все три выхода» | живой прогон, 52б | 2026-08-19 | **ПОДТВЕРЖДЕНО и уточнено**: Esc не просто бессилен — он зациклен (`_stop_resize` → `set_mode("resize_node")` → снова тот же режим); ручки при этом ИСЧЕЗАЮТ, поэтому оператор видит «вышел», а инструмент потерян |
| 52.5 | `_commit_resize` трогать нельзя (решение Ф5-1) | дифф | 2026-08-19 | **СОБЛЮДЕНО**: метод не изменён ни одной строкой |
| 52.6 | поправка 1.x5: не поднимать `_node_geom_kind`, звать базовый метод | `grep -n "_node_has_polygon\|_draws_polygon" ui/editors/base_graph_editor.py` | 2026-08-19 | **ПОДТВЕРЖДЕНО**: база несёт `_node_has_polygon:861` поверх виртуального `_draws_polygon:852`. Обе новые развилки зовут его; `_node_geom_kind` (`advanced:3918`) не тронут и в базу не поднимался |
| 52.7 | корпус для Г1 | `python -c` по трём git-фикстурам | 2026-08-19 | **контурные equipment есть только в `089feca2`: 4 узла** (`node_4`, `node_6`, `node_29`, `node_30`); в `d74eb9f1` (на котором стоял весь набор 0.4) их **0**, в `6e7144d5` — **0**. Отсюда вторая фикстура в наборе |

### 52б. ⭐ Дефект Р2 замерен ДО правки — на данных, не на поле режима

Сценарий оператора: инструмент «Добавить ребро» → Ctrl+2ЛКМ по узлу (вошёл в правку
размера) → Ctrl+2ЛКМ по нему же ещё раз → Esc → два клика инструментом.

| # | сценарий | дата | рёбер | шагов undo | ручек после клика | что это значит |
|---|---|---|---|---|---|---|
| 52.8 | контроль: ОДИН двойной клик | 2026-08-19 | 63 → **64** | 0 → 1 | 0 | инструмент вернулся |
| 52.9 | дефект: ДВА двойных клика | 2026-08-19 | 63 → **63** | 0 → 0 | **4** | инструмент потерян, клик снова открыл ручки |

Внутреннее состояние (в тесты не вошло — `PROTOCOL` запрещает набору 0.4 утверждения про
`_current_mode`; здесь только как объяснение): после второго входа `_mode_before_resize`
= `'resize_node'`, и первый же Esc возвращает редактор в тот самый режим, из которого
выходил. Два Esc подряд — тот же результат.

### 52в. ⭐ Дефект Г1 замерен ДО правки — расхождение рамки и контура

Узел `node_4` схемы `089feca2` (контур 40 точек), «Проверка схемы», Ctrl+2ЛКМ → протяжка
правой-нижней ручки на +30/+20 px:

| # | величина | дата | до | после |
|---|---|---|---|---|
| 52.10 | `bbox` | 2026-08-19 | `[1343, 101, 1972, 732]` | `[1343, 101, 2002, 752]` |
| 52.11 | `segmentation`, 40 точек | 2026-08-19 | — | **не изменился ни на пиксель** |

То есть рамка и центроид уезжают, а форма узла остаётся на месте: концы рёбер
пересчитываются по новому центроиду вокруг старого контура. Порча тихая — ни ошибки,
ни предупреждения.

### 52г. Дверь два: та же дыра в общем хендлере (находка плана 8.9 подтверждена)

| # | жест | дата | до правки | после правки |
|---|---|---|---|---|
| 52.12 | «Ручная правка»: из правки размера рамки кликнуть по контурному узлу | 2026-08-19 | ручек 4 → **4**, `_resizing_node` = `node_4` — ручки сели на контур | ручек 4 → **0**, выход из режима |
| 52.13 | «Ручная правка»: двойной клик по контурному узлу (дверь один) | 2026-08-19 | ручек **0**, открылась правка точек — развилка здесь была и до пункта | без изменений |
| 52.14 | контроль: переключение ручек между двумя РАМОЧНЫМИ узлами | 2026-08-19 | ручек 4 → 4 | ручек 4 → 4 (жест не задет) |

### 52д. Ловушка `PLAN_AUDIT` 1.x5 проверена со скинами — поведение НЕ нейтрально

`_node_has_polygon` виртуален через `_draws_polygon`, который Advanced переопределяет по
`show_skins`. В git-фикстурах случая нет (все 4 контурных узла — класс `unknow`,
`get_skin_info` = `None`), поэтому он собран синтетически: тому же `node_4` подставлен
скиновый класс `armatura_ruchn`.

| # | режим показа | дата | `_node_has_polygon` | `_node_geom_kind` | жест «из resize кликнуть по нему» |
|---|---|---|---|---|---|
| 52.15 | скины ВЫКЛ | 2026-08-19 | `True` | `'poly'` | ручек 4 → 0 (ручки запрещены) |
| 52.16 | скины ВКЛ | 2026-08-19 | `False` | `'poly'` | ручек 4 → 4 (ручки разрешены) |

Вывод: у СКИНОВОГО контурного узла право на ручки зависит от режима показа — и это
осмысленно (при включённых скинах форма узла в FXML = bbox+скин, контур игнорируется,
править рамку законно). Отсюда же асимметрия, оставленная в коде осознанно: дверь один
в Advanced судит по `_node_geom_kind` (всегда `'poly'`), дверь два — по `_node_has_polygon`.
Свести их — это и есть пункт `1.x5`, а моей строке запрещено трогать `_node_geom_kind`.

### 52е. Гейты и зонды

| # | что | дата | результат |
|---|---|---|---|
| 52.17 | `pytest tests/ui/test_gesture_dispatch.py` ДО правки | 2026-08-19 | **3 failed, 2 passed** из 5 новых: красные — три дефекта, зелёные — два контроля |
| 52.18 | тот же файл ПОСЛЕ правки | 2026-08-19 | **24 passed** (было 19) |
| 52.19 | `pytest tests/ui` | 2026-08-19 | **298 passed, 13 skipped** (до пункта 293 + 13) |
| 52.20 | `python tools/lint_gate.py --check` | 2026-08-19 | exit 0 — ruff 177 при эталоне 178, mypy 0 |
| 52.21 | `python tools/suite_baseline.py --check` | 2026-08-19 | exit 0 — собрано 1012 (git-видимых 1012 в 93 файлах), красных `{failed 13, errors 9}` = база |
| 52.22 | И1 — снять правку Р2 (вернуть безусловную перезапись) | 2026-08-19 | **1 failed** — ровно `test_repeated_double_click_leaves_the_tool_alive_after_escape`, чужих ноль |
| 52.23 | И2 — снять развилку по контуру в двойном клике «Проверки схемы» | 2026-08-19 | **1 failed** — ровно `test_double_click_on_contour_node_gives_no_size_handles` |
| 52.24 | И3 — снять развилку по контуру во второй двери (`ResizeNodeHandler`) | 2026-08-19 | **1 failed** — ровно `test_click_from_resize_mode_does_not_move_handles_onto_a_contour_node` |
| 52.25 | И4 — обратная полярность: развилка судит ШИРОКО (рамочный узел объявлен контурным в обеих точках) | 2026-08-19 | **1 failed** — контроль `test_click_from_resize_mode_still_switches_between_boxes`: порог заперт с двух сторон |
| 52.26 | ⛔ И5 — сломать сам жест `add_edge` в `simple_handlers.AddEdgeHandler` | 2026-08-19 | **24 passed — зонд НЕ покраснел.** Обстановка зонда, а не декоративный гейт: Advanced перерегистрирует `add_edge` на `AddEdgeWithWaypointsHandler` (`advanced_graph_editor.py:166`), то есть сломан был код, который в этом наборе не исполняется |
| 52.27 | И5' — тот же зонд по верному адресу (`advanced_handlers.AddEdgeWithWaypointsHandler`) | 2026-08-19 | **2 failed** — оба теста Р2, включая контроль: набор наблюдает именно появление ребра, а не косвенный признак |
| 52.28 | И6 — клик по ДРУГОЙ рамке перестал переключать ручки (число ручек прежнее, 4) | 2026-08-19 | **2 failed** — оба теста второй двери: координатное утверждение «ручки сидят на той рамке, по которой кликнули» не декоративно, числом ручек такая подмена не ловится |

⚠ Доработка теста, найденная собственной вычиткой ДО тега: контроль переключения ручек между рамками
утверждал `ed._resizing_node == box_b` — то есть читал внутреннее поле режима, которое уезжает вместе
с декомпозицией UI (10.1) и запрещено принципом набора 0.4. Заменено наблюдаемым: центры видимых ручек
сверяются с углами рамки узла (`resize_overlay._update_handle_positions` кладёт ручку центром в угол).
Утверждений про `_current_mode`/`_current_handler`/`_resizing_node` в наборе снова ноль — проверено грепом.

Первый зонд (И4) был отвергнут как слишком грубый: подмена самого `_node_has_polygon`
в базовом классе роняет отрисовку и даёт 24 error на фикстуре, то есть красит загрузку
графа, а не развилку. Обстановка подогнана под проверяемый механизм — инъекция внесена
в две мои точки вызова.

### 52ж. Замечено рядом — в правку не вошло

| # | находка | почему не здесь |
|---|---|---|
| 52.29 | Дверь один в «Ручной правке» (`advanced_graph_editor.py:5615`) судит о контуре третьей копией проверки — `_node_geom_kind`, которая про скины не знает. При включённых скинах скиновый контурный узел уходит в правку точек по двойному клику и получает ручки размера по клику из режима resize (замер 52.15–52.16) | сведение копий — пункт `1.x5`; моей строке прямо запрещено трогать `_node_geom_kind` |
| 52.30 | `_stop_resize` не чистит `_resize_start_pins`, хотя чистит `_resize_start_bbox/_centroid/_area` — снимок пинов от 1.4 переживает выход из режима | соседняя граница (пункт 1.7 — `ResizeNodeCommand.undo` и концы рёбер), в сценарии этого пункта не наблюдаема |
| 52.31 | Д6 не делался сознательно: параллельно идут две сессии, пересъём эталона вмуровал бы чужое промежуточное состояние (`PROTOCOL`, Д6). `--check` при этом рабочий: пол — минимум, +5 моих тестов его не роняют | запрет протокола, не недоделка |

---

## 53. Пункт 1-29 дороги (D6-PR1) — пересъём эталона ПР1 и пол по числу прогонов (2026-08-19)

Пункт заведён находкой 1-25: корпус машины вырос **17 → 19**, и полный прогон
`layout_determinism --check` отдаёт exit 1 старой веткой «новый граф корпуса неповторим».
Это **дрейф ДАННЫХ, а не регресс кода** (`PROTOCOL §2`, «протухло ≠ опровергнуто»): диффом
1-25 эта ветка не тронута, момент расхождения — появление в `storage/` двух графов
2026-08-18. Пункт = пересъём эталона (Д6) **с разбором**, откуда неповторимость у нового
графа, плюс защита пути записи от слабого замера (вход из 1-28, §49.27).

Среда: Windows 10, **системный** `python` 3.11.9 (не `.venv311` — там нет pytest,
`PROTOCOL §5`), ветка `road/D6-PR1` от `6d6dd1b`, дерево чистое, чужих правок нет
(вторая сессия идёт в отдельном worktree `C:\project\pid_wt_1.4`). Копии файлов до
инъекций — в скрэтчпаде сессии, восстановление копией, не `git checkout` (`PROTOCOL §5`).

### 53а. Состояние корпуса и эталона перепроверено по коду (шаг 2 жизненного цикла)

| # | что | команда | дата | результат |
|---|---|---|---|---|
| 53.1 | Сколько графов в корпусе сейчас | `corpus.corpus_paths()` против `determinism_baseline.json` | 2026-08-19 | корпус **19**, эталон **17**, новых ровно два: `0fc9d04c`, `3263039b`; пропавших **нет**. В `storage/diagrams/` 22 каталога, `graph_validated.json` лежит в 19 — расхождение 22/19 штатное (три диаграммы до этапа графа не дошли). Из git видно по-прежнему **3** графа |
| 53.2 | Когда появились новые графы | `ls -la storage/diagrams/{0fc9d04c,3263039b}*/graph/graph_validated.json` | 2026-08-19 | оба **2026-08-18** (13:14 и 14:07) — то есть после того, как эталон был снят (коммит `76827fa`, 0.10). Дрейф данных подтверждён датой, а не рассуждением |
| 53.3 | При каком `runs` снят действующий эталон | `determinism_baseline.json` | 2026-08-19 | `"runs": 6, "routing": true`, 17 графов, из них **8** `false`. Совпадает с §32.14–32.15 |
| 53.4 | Какие ветки `verdict()` срабатывают на дрейфе | чтение `tools/layout_determinism.py:163-190` | 2026-08-19 | `was is None and not now` → `fresh` → `[ПРОВАЛ] новый граф корпуса неповторим`, exit **1**. Ветка `broke` (регресс кода) не при чём; `unmeasured` пуст — корпус машины полон. Утверждение строки 1-29 подтверждено по коду |

### 53б. ⭐ Разбор: почему новый граф неповторим — тот же libavoid, а не новый дефект

| # | что | команда | дата | результат |
|---|---|---|---|---|
| 53.5 | ⭐ Оба новых графа, роутинг включён | `python -X utf8 tools/layout_determinism.py --uid 0fc9d04c --uid 3263039b --runs 6` | 2026-08-19 | `0fc9d04c` — **НЕПОВТОРИМ (2 исхода из 6)**; `3263039b` — **воспроизводим**. То есть exit 1 полного прогона даёт ровно один граф из двух, второй в эталон уйдёт как стабильный |
| 53.6 | ⭐ **Локализация: тот же этап роутинга** | `python -X utf8 tools/layout_determinism.py --uid 0fc9d04c --runs 6 --no-routing` | 2026-08-19 | **1 исход из 6** — воспроизводим. Расстановка и раздвигание детерминированы, расходится только обход чужих форм через vendored libavoid. Признак дословно тот же, что у `6e7144d5` в §32.7 |
| 53.7 | ⭐ **Качество не гуляет** | вывод «качество» тех же двух прогонов | 2026-08-19 | `0fc9d04c` — дефекты **70 → 0** одни и те же во всех 6 прогонах и с роутингом, и без; `3263039b` — **0 → 0**. Ровно как на восьми известных (§32.18): шумит побайтовое сравнение, счётчики дефектов стабильны |
| 53.8 | **Вывод разбора** | 53.5–50.7 | 2026-08-19 | Новый неповторимый граф — **не новый дефект и не регресс кода**, а девятый случай уже описанного недетерминизма `vendor/adaptagrams` (ВН3, волна 9). Пересъём законен: он записывает наблюдение, а не узаконивает поломку |

### 53в. Дефект пути записи ЗАМЕРЕН до правки — на живых графах

Зонд гоняет настоящую раскладку трёх git-графов, подменён только **путь** эталона: рядом
шёл полный прогон, который читает файл эталона в конце. Эталон зонда — ровно те три графа,
что и меряются, поэтому ветка «корпус усечён» молчит и красное может прийти только от
проверяемого механизма (урок 1-28 про чужой красный, §49.12).

| # | зонд | команда | дата | результат |
|---|---|---|---|---|
| 53.9 | ⭐ (а) пересъём слабее эталона | эталон `{runs: 6, 089feca2: true, 6e7144d5: false, d74eb9f1: true}`, `--git-only --runs 2 --write-baseline` на СТАРОМ коде | 2026-08-19 | **exit 0**, «эталон переснят». В файле `runs` уехал **6 → 2**, а `6e7144d5` — известный неповторимый (2 исхода из 30, §32.19) — записан **`true`**. Ослабление эталона одной командой, отдельным коммитом по Д6, мимо красного флага №4 |
| 53.10 | ⭐ (б) пересъём другим ключом роутинга | тот же эталон, `--git-only --runs 6 --no-routing --write-baseline` на СТАРОМ коде | 2026-08-19 | **exit 0**, «эталон переснят»: `routing` уехал `true → false` и **все три** графа записаны `true`, включая `6e7144d5`. Без роутинга раскладка воспроизводима вся (§32.7), так что одна такая команда стирает список неповторимых целиком — дыра шире, чем малый `runs` |

### 53г. Правка и зонды ПОСЛЕ неё — обе полярности на каждую

Правка: `write_blocked()` получил два аргумента замера (`runs`, `routing`) и две новые
причины **«судить нечем»** — форма и приоритет исходов от 1-28 не тронуты (доказанная
поломка сильнее неполноты). Класс причины выбран сознательно: замер слабее эталона — это
не «пересъём узаконил бы регрессию» (тогда был бы отказ, 1), а «этим замером судить нечем».

| # | зонд | команда | дата | результат |
|---|---|---|---|---|
| 53.11 | (а) слабее эталона → отказ в записи | тот же зонд 50.9 на НОВОМ коде | 2026-08-19 | **exit 2** `[СУДИТЬ НЕЧЕМ] замер слабее эталона: прогонов 2 против 6 … Переснимать при --runs не меньше 6`; эталон **бит-в-бит цел** |
| 53.12 | (б) другой ключ роутинга → отказ в записи | тот же зонд 50.10 на НОВОМ коде | 2026-08-19 | **exit 2** `[СУДИТЬ НЕЧЕМ] замер снят другим ключом: роутинг выкл против вкл у эталона`; эталон **бит-в-бит цел** |
| 53.13 | ⭐ положительный контроль (иначе стенд просто «всегда отказывает») | `--git-only --runs 6 --write-baseline` на НОВОМ коде | 2026-08-19 | **exit 0** «эталон переснят», и записанный файл **бит-в-бит равен** входному: `6e7144d5` при 6 прогонах снова `false` — неповторимость видна и записана честно |
| 53.14 | Храповик вверх | `--git-only --runs 7 --write-baseline` на НОВОМ коде | 2026-08-19 | **exit 0**, в эталоне `runs: 7` — следующий пересъём будет судиться уже по семи. Вверх дорога открыта, вниз закрыта |
| 53.15 | ⭐ зонд гейта ЧТЕНИЯ: эталон врёт про стабильный граф | эталон `{6e7144d5: true}`, `--git-only --runs 6 --check` | 2026-08-19 | **exit 1** `[ПРОВАЛ] перестали воспроизводиться: 6e7144d5` — гейт 1-25/0.10 краснеет по-прежнему, моя правка пути чтения не касается |
| 53.16 | обратная полярность того же | эталон `{6e7144d5: false}`, та же команда | 2026-08-19 | **exit 0** «[OK] регресса воспроизводимости нет». Хеши совпали с §32.27 дословно (`f4510d0b989c015b`, `484bb015b4e473b8`, `51e20ed60bb97cdc`) — независимая сверка спустя сутки |
| 53.17 | Д1: новые тесты на СТАРОМ коде | `pytest tests/test_layout_determinism.py` до правки стенда | 2026-08-19 | **3 failed, 19 passed** — красны ровно ветки пункта (малый `runs`, чужой ключ роутинга, приоритет «поломка сильнее слабой выборки») |
| 53.18 | Д1: они же после правки | та же команда | 2026-08-19 | **22 passed** (+5 тестов: 17 было). Порог заперт **абсолютными** числами с двух сторон: эталон 6 → `--runs 5` отказ, `--runs 6` и `--runs 7` пересъём; ни одно число не вычисляется из проверяемого поля (урок 0.4) |

### 53д. ⭐ ПЕРЕСЪЁМ №1 — и находка, которой пункт не ждал

Пересъём при `--runs 6` по 19 графам прошёл штатно (**exit 0**, 27 м 08 с) — и записал
`0aea61c0` **воспроизводимым**. Это ровно то, что `TESTING §8.2` запрещает делать молча,
и именно про этот граф док и предупреждает.

| # | что | команда | дата | результат |
|---|---|---|---|---|
| 53.19 | ⭐ **Пол по `runs` необходим, но НЕ достаточен** | `python -X utf8 tools/layout_determinism.py --runs 6 --write-baseline`, 27 м 08 с | 2026-08-19 | **exit 0**, записано 19 графов. Дифф против эталона в git: `0fc9d04c` → `false` (ожидаемо, 50.5), `3263039b` → `true` (ожидаемо), и ⛔ **`0aea61c0`: `false` → `true`** — при том, что §32.14 наблюдал у него **3 разных исхода из 6**. Пересъём шёл при том самом `runs 6`, который этот пункт и ввёл: у графа с редким расхождением шести прогонов не хватает. Неповторимых в файле осталось **8**, хотя должно было стать **9** |
| 53.20 | Почему это не «стало лучше» | §32.14 против §32.16 и 50.19 | 2026-08-19 | четыре независимые выборки по 6 прогонов: **неповторим · неповторим · один хеш · один хеш**. Два разных исхода доказывают недетерминизм положительно; одинаковая серия не опровергает ничего. В коде раскладки за это время не менялось НИЧЕГО (мой дифф — только стенд), значит объяснения «что починили» не существует, а `TESTING §8.2` требует его прямым текстом |
| 53.21 | Что сделано вместо записи неправды | эталон возвращён копией (`PROTOCOL §5`, не `git checkout`) | 2026-08-19 | `git status` по файлу эталона — **0 изменений**, вернулся к коммиченному бит-в-бит. Результат замера сохранён в скрэтчпаде как данные, руками эталон не правился |

### 53е. Вторая защита: `false` в эталоне липкий (`merge_stable`)

Правка вторая, того же класса и в той же функции пересъёма: неповторимость — наблюдение
положительное, стереть его тихим замером нельзя. Явная починка остаётся возможной ключом
`--allow-fixed <uid>`: без него храповик стал бы вечным и починку ВН3 записать было бы нечем.

| # | зонд | команда | дата | результат |
|---|---|---|---|---|
| 53.22 | ⭐ липкость на живых графах | эталон зонда числит `089feca2` неповторимым (он стабилен), `--git-only --runs 6 --write-baseline` | 2026-08-19 | **exit 0**, `[ВЫБОРКА, НЕ ПОЧИНКА] 089feca2: … оставлено «неповторим»`; в файл ушёл **`false`**. Соседние графы не тронуты: `6e7144d5` измерен неповторимым и записан честно |
| 53.23 | обратная полярность: починка объявлена | то же + `--allow-fixed 089feca2` | 2026-08-19 | **exit 0**, `[ПОЧИНКА по --allow-fixed] 089feca2: неповторим -> воспроизводим`; записан **`true`**. Односторонней правки нет: стенд умеет и удержать, и отпустить |
| 53.24 | Д1 на липкость | `pytest tests/test_layout_determinism.py` до правки | 2026-08-19 | **2 failed из 24** (`--allow-fixed` не существовало как ключа, а `false` стирался). После — **24 passed**. Заодно исправлен положительный контроль 1-28: он проверял ровно стирание `false` как норму |

### 53ж. ⭐ ПЕРЕСЪЁМ №2 — эталон Д6 и его сверка

| # | что | команда | дата | результат |
|---|---|---|---|---|
| 53.25 | ⭐ **Пересъём с липким `false`** | `python -X utf8 tools/layout_determinism.py --runs 6 --write-baseline`, 26 м 08 с | 2026-08-19 | **exit 0**. Дифф против эталона в git — РОВНО две строки: `+ "0fc9d04c": false`, `+ "3263039b": true`. Ни один старый графа вердикта не сменил, `runs` и `routing` на месте |
| 53.26 | ⭐ Пятая выборка по `0aea61c0` | тот же прогон, строка `[ВЫБОРКА, НЕ ПОЧИНКА]` **не печаталась** | 2026-08-19 | значит в этом замере граф снова дал расхождение — он измерен неповторимым, а не удержан храповиком. Итог пяти независимых выборок по 6 прогонов: **неповторим · неповторим · один хеш · один хеш · неповторим**. Липкость сработала бы, но не понадобилась: вывод 50.20 подтверждён шестым замером независимо |
| 53.27 | Что стало эталоном | `tools/bench/determinism_baseline.json` | 2026-08-19 | **19 графов**, `runs 6`, `routing true`, неповторимых **9**: `0aea61c0`, `0fc9d04c`, `302171b3`, `54a60fd1`, `620cc50d`, `6e7144d5`, `751116c9`, `a5264536`, `a6d28736`. Было 17 и 8 |

### 53з. Гейт пункта

| # | что | команда | дата | результат |
|---|---|---|---|---|
| 53.28 | ⭐ **Гейт: полный корпус после пересъёма** | `python -X utf8 tools/layout_determinism.py --runs 6 --check`, 35 м 07 с | 2026-08-19 | **exit 0** «[OK] регресса воспроизводимости нет». Свежая выборка сошлась с новым эталоном полностью: все **9** неповторимых вышли неповторимыми, все **10** воспроизводимых — воспроизводимыми, ни `[ПРОВАЛ]`, ни `[СУДИТЬ НЕЧЕМ]`, ни «стало лучше» |
| 53.29 | ⭐ Шестая выборка по `0aea61c0` | тот же прогон | 2026-08-19 | **НЕПОВТОРИМ (2 исхода из 6)** — независимое подтверждение 50.20 второй раз подряд. Записать его `true` (что сделал пересъём №1) было бы прямой неправдой в эталоне |
| 53.30 | Качество не гуляет на всех 19 | тот же прогон, блок «качество» | 2026-08-19 | у **каждого** из 19 графов дефекты одинаковы во всех 6 прогонах, включая новые (`0fc9d04c` 70→0, `3263039b` 0→0) и все девять неповторимых. Вывод §32.18 держится на выросшем корпусе: шумит только побайтовое сравнение |
| 53.31 | Зонд чтения на ФИНАЛЬНОМ коде | эталон зонда врёт «`6e7144d5` воспроизводим», `--git-only --runs 6 --check` | 2026-08-19 | **exit 1** `[ПРОВАЛ] перестали воспроизводиться`; честный эталон — **exit 0**. Гейт 0.10/1-25 моими правками не задет |
| 53.32 | База набора | `python -X utf8 tools/suite_baseline.py --check` | 2026-08-19 | **exit 0** «новых красных нет»: собрано 1013 (git-видимых 997 в 91 файле), красные `13 failed / 9 errors` — как в базе |
| 53.33 | Линтеры | `python -X utf8 tools/lint_gate.py --check` | 2026-08-19 | **exit 0**, ruff 178/178, mypy 0 |
| 53.34 | CI на ветке | run **32238387723** на `3286527` | 2026-08-19 | **success**, 2 м 46 с. Шаг воспроизводимости отдал «[СУДИТЬ НЕЧЕМ] не измерено **16** графов эталона из **19**» и `::warning` — было 14 из 17; сборка не падает, как и задумано в 1-25 |

### 53и. Находки строкой, не правкой

| # | что | дата | результат |
|---|---|---|---|
| 53.35 | Малый `--runs` врёт и на пути ЧТЕНИЯ | 2026-08-19 | `--check` при `--runs` меньше эталонных печатает «воспроизводятся впервые» про заведомо неповторимые графы (ветка `fixed`) и отдаёт **exit 0**. Ложного красного там быть не может (расхождение — наблюдение положительное), поэтому пол на чтении я не ставил: это не дыра гейта, а вводящий в заблуждение вывод. Кандидат в 1-30 |
| 53.36 | Стенды с тем же вопросом | 2026-08-19 | у `edit_bench` и `lint_gate` понятия «сила замера» нет вовсе (у первого — числа прогонов, у второго — конфигурации ruff). Проверять там нечего; но `edit_bench` остаётся четвёртым стендом с дырой пути записи (уже в 1-30) |
| 53.37 | Текст предупреждения CI протух от МОЕГО пересъёма | 2026-08-19 | `.github/workflows/tests.yml:16,81` говорил «3 графа корпуса из 17» — поправлено на 19 той же правкой (Д4), потому что число сделал неверным именно этот пункт |

---

## 54. Пункт 1-12 дороги (1.9) — ложь «✅ Сохранено» во вкладке «Привязка подписей» (2026-08-19)

Область: `ui/tabs/ocr_binding_tab.py` (`_save_binding`), тест
`tests/ui/test_ocr_save_failure_is_visible.py`. Среда: win32, CPython 3.11.9,
`QT_QPA_PLATFORM=offscreen`.

| # | что | дата | результат |
|---|---|---|---|
| 54.1 | Адрес плана уехал | 2026-08-19 | план указывает `:1746-1751`, фактический глушитель — `:1653-1656` (`try: save_ocr_validation / except Exception: logger.warning`); механизм совпадает с описанием пункта дословно, файл 1735 строк |
| 54.2 | Дефект замерен ДО правки | 2026-08-19 | `python -m pytest tests/ui/test_ocr_save_failure_is_visible.py -q` → **4 failed / 3 passed**: при отказе сервера на `save_ocr_validation` вкладка возвращала `True`, ставила `_saved = True` (дёрти-флаг чист, диалога при закрытии не будет), писала в статус `✅ Сохранено: 1 привязок, 2 блоков` и не показывала оператору ничего |
| 54.3 | Единственный след — `logger.warning` в консоль | 2026-08-19 | в файле лога клиента (1.10) строка была, но уровня WARNING и без трассировки; внешний `except` не логировал ВООБЩЕ — то есть отказы `save_ocr_binding` и `upload_validated_graph` до файла лога не доходили никак, оператору оставался один `QMessageBox` |
| 54.4 | Правка | 2026-08-19 | 7 строк в одном файле: глушитель снят (отказ идёт во внешний `except`, как у двух других шагов записи), во внешний `except` добавлен `logger.error(..., exc_info=True)`. `_saved`/статус не трогались — они и так стоят на пути `return True` |
| 54.5 | Гейт после правки | 2026-08-19 | тот же набор — **7 passed**; сервер отказал → `_save_binding()` = `False`, `has_unsaved_changes()` = `True`, «Сохранено» в статусе нет, ровно один `QMessageBox.warning` с причиной, в ФАЙЛЕ `client.log` — сообщение + `Traceback` + `uid=1f9defec` |
| 54.6 | Зонд И1 (вернуть глушитель) | 2026-08-19 | те же 4 failed / 3 passed — красит ровно четыре теста дефекта, контрольные три остаются зелёными |
| 54.7 | Зонд И2 (снять `logger.error`) | 2026-08-19 | **1 failed / 6 passed** — краснеет только `..._leaves_trace_in_client_log`; то есть у следа в логе свой сторож, а не общий с диалогом |
| 54.8 | Набор не виснет | 2026-08-19 | `QMessageBox.warning/critical/information` подменены с утверждением о ФАКТЕ вызова (PROTOCOL §5, ловушка 1.5/1.3); полный файл отрабатывает за 0.6 с |
| 54.9 | Базовые гейты | 2026-08-19 | `pytest tests/ui` → **305 passed / 13 skipped** (было 298+13); `python tools/lint_gate.py --check` → exit 0 (ruff 175/178, `ocr_binding_tab.py` 8 → 6); `python tools/suite_baseline.py --check` → exit 0, собрано 1026, красных 13 failed + 9 errors — без изменений |
| 54.10 | Общей механики с 1.5 нет | 2026-08-19 | 1.5 чинил ЧТО уходит на сервер (`drop_uncommitted_preview` в `base_graph_tab._save_graph`); здесь дыра в ЧТЕНИИ ответа — проглоченное исключение. `OcrBindingTab._save_graph` перекрыт и в базовый путь не заходит вовсе, поэтому второй раз чинить нечего и общего кода у пунктов нет |
## 55. Пункт 1-10 дороги (1.7) — `ResizeNodeCommand.undo` не возвращал концы рёбер (2026-08-19)

**Обстановка.** worktree `C:\project\pid_wt_1.4`, ветка `road/1.7` от `dev`
(в базе 1.1, 1.4, 1.5, 1.3, 1.6 и гейтовые пункты). Системный `python` 3.11.9,
Qt offscreen (`QT_QPA_PLATFORM=offscreen`). Корпус — git-фикстура `d74eb9f1`
(66 узлов / 63 ребра), `storage/` пуст, docker/Redis/postgres не трогались.

**Проверка реальности (пункт мог быть закрыт Э5).** `ui/editors/commands/simple_commands.py`
действительно уже несёт `old_pins`/`new_pins`, и `undo()` зовёт `_apply_pins(self._old_pins)` —
но это про ПИНЫ (смещения от центроида), а утверждение пункта про
`source_point`/`target_point`. Пункт не протух: замером подтверждён.

**Замер до правки** (Ctrl+2ЛКМ по рамке → протяжка правой-нижней ручки +30/+20 px → Ctrl+Z,
`AdvancedGraphEditor`, `/tmp/probe17.py`, `/tmp/probe17b.py`):

| узел | степень | bbox вернулся | рёбер тронул ресайз | не вернул undo |
|---|---|---|---|---|
| `node_2` | 1 | да ([1456, 0, 1549, 34]) | 1 | **1** |
| `node_3` | 2 | да | 2 | **2** |

Числа по ребру `node_2 -> node_3`: `source_point` [13, 1547] → после ресайза
[27.255520504731862, 1577.7917981072558] → после Ctrl+Z **[27.2555…, 1577.7918…]**
(не вернулся; Δ = 14.26 px по y, 30.79 px по x).

**Сколько полей на самом деле правит ресайз** (сравнение ПОЛНЫХ словарей рёбер до/после,
`/tmp/probe17c.py`) — пять, а не два:
`source_point`, `target_point`, `waypoints`, `_src_side`, `_tgt_side`.
Отсюда `ResizeNodeCommand.ROUTE_KEYS` из пяти полей и сравнение словаря целиком в тесте:
восстановление части полей иначе выдало бы себя за откат.

**Правка.** Снимок маршрутов инцидентных рёбер, симметричный снимку пинов Э5:
`SimpleGraphEditor._snapshot_edge_routes` (вход в режим, `_commit_resize` и
«start для следующего drag») + `ResizeNodeCommand._apply_routes` в `execute`/`undo`.
Поле, отсутствующее в снимке, при откате УДАЛЯЕТСЯ (`_src_side`/`_tgt_side` заводит
сам ресайз — без удаления они переживали бы undo).

**Замер после правки:** «не вернул undo» = **0** на обеих степенях; `source_point`
ребра `node_2 -> node_3` возвращается в [13, 1547] побайтово.

**Гейт и его проверка зондом** (`tests/ui/test_resize_undo_edge_ends.py`, 3 теста):

| прогон | результат |
|---|---|
| до правки | `3 failed` |
| после правки | `3 passed` |
| инъекция A — снять `_apply_routes(self._old_routes)` из `undo()` | `3 failed`, причина именно «undo не вернул концы рёбер» / «после undo у рёбер остались изменённые поля: `_src_side`, `_tgt_side`, `target_point`, `waypoints`» |
| инъекция B — сузить `ROUTE_KEYS` до `('source_point', 'target_point')` | `3 failed` (колени и стороны остаются от ресайза) |
| дерево восстановлено из копии (не `git checkout`) | `3 passed` |

Обе инъекции падают за ~3 с, не виснут: модального диалога в пути ресайза нет
(проверено временем прогона, а не таймаутом).

**Базовые гейты:**

```
python -m pytest tests/ui -q            -> 301 passed, 13 skipped
python tools/lint_gate.py --check       -> [OK] пол не пробит (BLE001/E722: 177 при поле 178)
python tools/suite_baseline.py --check  -> [OK] набор целиком тут; собрано 1022 (пол 973),
                                           failed 13 / errors 9 — ровно пол, без роста
```

**Что НЕ делалось:** Д6-пересъём (в дереве параллельно идут сессии 1.4-fix и 1.9 —
`PROTOCOL §Д6`), корпусные стенды (корпус вне git, «судить нечем»), docker/Redis.

---

## 56. Пункт 1-6 дороги (1.4), ДОРАБОТКА по возврату ревизии связки — превью, вваренное в команду (2026-08-19)

Возврат ревизии связки 1.1+1.4+1.5: дефект найден на ШВЕ и поштучно невидим — ни один из
трёх собственных гейтов его не показал. Продолжение §48 (тот же механизм, та же фикстура).
Работа шла в отдельном worktree `C:/project/pid_wt_14fix` на ветке `road/1.4-fix`;
параллельно 1-29 в главном дереве и 1-9 в `pid_wt_1.4`. Корпус здесь — только 3 git-видимые
фикстуры, `storage/` пуст; docker/Redis/postgres не трогались ни одной командой. Стенд —
Qt offscreen, системный `python` 3.11.9. Фикстура `d74eb9f1`, класс `armatura_ruchn`,
`node_11`: bbox `[25, 215, 45, 251]` = 20×36, превью 90×90 даёт `[-10.0, 188.0, 80.0, 278.0]`.

### 56а. Дефект воспроизведён своим зондом, ОБЕ ветки кнопки (перепроверка по коду, не по отчёту)

Зонд: живая вкладка `AdvancedGraphTab` + поддельный сервер, режим «Размеры», один тик
превью 90×90, затем боевой `tab._auto_fix()`, затем `undo()`, затем `_save_graph()`.
Ветка выбирается `canvas_state.has_layout(editor.graph_data)`: корпус-фикстура метки не
несёт (ветка `auto_fix`), вторая ветка (`smooth_canvas`) ставится меткой холста.

| # | ветка кнопки | что замерено | дата | результат |
|---|---|---|---|---|
| 56.1 | `auto_fix` (фолбэк-холст) | bbox `node_11` после превью → после кнопки → после Ctrl+Z → на сервере | 2026-08-19 | `[-10.0, 188.0, 80.0, 278.0]` во ВСЕХ ЧЕТЫРЁХ точках: превью пережило команду, `revision` 0 → 1, стек 1, Ctrl+Z вернул модель С превью, «Сохранить» увёз его на сервер |
| 56.2 | `smooth_canvas` (холст после раскладки) | то же | 2026-08-19 | то же самое, `revision` 0 → 1, стек 1; ветка отработала 2.4 с |
| 56.3 | корень: чем именно жив дефект | чтение кода + 56.1 | 2026-08-19 | `SnapshotCommand._before` снимается с модели КАК ЕСТЬ (`undo_manager.py:71`) — вместе с превью. Дальше `revision` растёт, `_resize_baseline_alive()` объявляет базлайн протухшим, и лечение §48г «протух → бросить без отката» превью уже не трогает |
| 56.4 | почему поздний откат не лечит | вывод из 56.3, проверен правкой | 2026-08-19 | `_before` отравлен НЕОБРАТИМО: даже если снять превью после команды, Ctrl+Z вернёт его из снимка. Значит снятие обязано идти ДО построения точки возврата — что и сделано |

### 56б. Семья шва шире одной кнопки: какие ещё команды-на-месте достижимы при живом превью

В режиме `resize_objects` жесты мыши перехвачены `ResizeObjectsHandler`, а `_node_drag_allowed()`
= False — протяжки и прочие жестовые команды недостижимы. Достижимы кнопки вкладки и клавиши.
Замер тем же зондом (превью → действие → Ctrl+Z → «Сохранить»):

| # | путь | дата | результат |
|---|---|---|---|
| 56.5 | «Авто-выравнивание» (`_auto_fix`, обе ветки) | 2026-08-19 | **ОТРАВЛЕНО** (56.1, 56.2) |
| 56.6 | «Оптимизировать все» (`_optimize_all_edges`) | 2026-08-19 | **ОТРАВЛЕНО**: `revision` 0 → 1, стек 0 → 36, Ctrl+Z и сервер несут `[-10.0, 188.0, 80.0, 278.0]` |
| 56.7 | «Удалить выделенное» / Delete (`batch_delete`) | 2026-08-19 | **ОТРАВЛЕНО**: `revision` 0 → 1, стек 0 → 1, Ctrl+Z и сервер несут то же |
| 56.8 | `batch_delete` при ПУСТОМ выделении | 2026-08-19 | **чисто**: уходит в «Нечего удалять», `revision` 0 → 0, команды нет; сервер получает исходные `[25, 215, 45, 251]` — снятие превью на записи (1.5) работает, потому что базлайн жив |
| 56.9 | смена режима любой кнопкой режима | 2026-08-19 | **чисто**: `_exit_resize_objects` → `_revert_resize_preview` при живом базлайне, штатный откат 1.4 |

Вывод 56.8 и есть граница правки: превью снимается там, где команда СТРОИТСЯ, а не там, где
кнопку нажали. Кнопка, не построившая точки возврата, вваривать нечего — и картинку у
оператора не отбирает.

### 56в. Правка и её доказательство

Правка — 4 точки в `ui/editors/advanced_graph_editor.py`, по одному вызову
`self.drop_uncommitted_preview()` перед снятием точки возврата: `smooth_canvas`, `auto_fix`,
`optimize_all_edges` (под `if edges_to_optimize`), `batch_delete` (после раннего выхода
«Нечего удалять»). Метод уже существовал — его завёл 1.5 для пути записи; доработка добавила
второго зовущего, докрутив его докстроку. Своей логики отката не заводилось.

| # | гейт | команда | дата | результат |
|---|---|---|---|---|
| 56.10 | Д1: набор сценария | `pytest tests/ui/test_preview_not_welded_into_command.py` ДО правки | 2026-08-19 | **5 failed / 3 passed** — красные ровно пять дефектных, зелёные три характеризационных |
| 56.11 | тот же набор после правки | то же | 2026-08-19 | **8 passed** |
| 56.12 | базовый набор UI | `pytest tests/ui -q` | 2026-08-19 | **301 passed, 13 skipped** (было 293+13 после 1.3; +8 — мои) |
| 56.13 | долг линтера | `python tools/lint_gate.py --check` | 2026-08-19 | exit 0: ruff 177/178, mypy 0 (долг не вырос; 177 при эталоне 178 — незакрытый Д6-долг 1.3, не мой) |
| 56.14 | база набора | `python tools/suite_baseline.py --check` | 2026-08-19 | exit 0 «новых красных нет»: собрано 1015 (git-видимых 1015 в 94 файлах), `failed 13 / errors 9` — как в базе |

### 56г. Зонд: каждая точка сторожа краснит РОВНО свой тест

Инъекции — байтами, копия файла снята до правки, восстановление сверено `md5`
(`967ce743b3f142030dc558ab18a7f796` до и после). После каждой инъекции гонялся ВЕСЬ
`tests/ui`, чтобы видеть не только «покраснело», но и КТО покраснел (`PROTOCOL §5`).

| # | инъекция | дата | что покраснело |
|---|---|---|---|
| 56.15 | И1: снять снятие превью в `auto_fix` | 2026-08-19 | 2 failed / 299 passed — `..._fallback_branch_undo_returns_to_state_before_preview` и `..._does_not_upload_it`, оба мои; чужих ноль |
| 56.16 | И2: то же в `smooth_canvas` | 2026-08-19 | 1 failed / 300 passed — `..._layout_branch_undo_returns_to_state_before_preview`; чужих ноль |
| 56.17 | И3: то же в `optimize_all_edges` | 2026-08-19 | 1 failed / 300 passed — `test_optimize_all_edges_over_preview_leaves_nothing_to_weld`; чужих ноль |
| 56.18 | И4: то же в `batch_delete` | 2026-08-19 | 1 failed / 300 passed — `test_batch_delete_over_preview_leaves_nothing_to_weld`; чужих ноль |
| 56.19 | И5: обратная полярность — снимать превью ДАЖЕ когда команды не будет (`drop` до раннего выхода `batch_delete`) | 2026-08-19 | 1 failed / 300 passed — `test_command_that_pushes_nothing_keeps_the_preview_alive`; граница 56.8 заперта с обеих сторон |

### 56д. ⛔ ЧЕТВЁРТЫЙ ИСХОД §5, ТРЕТЬЕ НЕЗАВИСИМОЕ ПОДТВЕРЖДЕНИЕ: зонд И6 ПОВИС

`tab._auto_fix` глушит исключение `QMessageBox.warning`. Зонд И6: сломать ветку кнопки
(`editor.auto_fix` → `raise RuntimeError`) и позвать боевой `_auto_fix` **без** подмены
диалога, под `timeout 90`. Результат — `traceback` напечатан, дальше процесс **завис**,
exit **124**. То есть подмена `QMessageBox` в наборе доработки — несущая конструкция, а не
удобство, и факт вызова диалога проверяется утверждением (`dialogs == []` в каждом тесте),
а не таймаутом. Правило из 1.5 подтвердилось в третий раз и третьей сессией (1.5 → 1.3 → эта):
в путях кнопок «Ручной правки» модальность есть всегда.

### 56е. Замечено рядом, в правку НЕ вошло (`PROTOCOL §4`)

| # | находка | почему не здесь |
|---|---|---|
| 56.20 | `ocr_layer_mixin.py:526` строит `SnapshotCommand` тем же способом; путь — фоновое распознавание OCR (`_run_ocr_recognize` → `_on_recognize_done`). Достижим он только при непустом наборе добавленных пустых блоков, то есть после работы в состоянии ОКР | не проверен зондом: путь идёт через `QThread` и сеть, стенда под него в `tests/ui` нет. Класс тот же — команда-на-месте; если состояние ОКР и режим «Размеры» окажутся совместимы, лечится тем же одним вызовом. Архитектору — строкой, не правкой |
| 56.21 | `advanced_graph_editor.py:2788`, `:5270`, `:5475` — ещё три места, снимающие `model.snapshot()` руками, мимо `SnapshotCommand` | все три сидят в жестовых путях (`_node_drag_allowed()` = False в режиме «Размеры»), при живом превью недостижимы — см. 52б |
| 56.22 | Единой точки «перед любой командой» в редакторе нет: команды строятся в ~25 местах `advanced_graph_editor.py`. Дешёвого сторожа на все не существует — `push_executed` зовётся уже ПОСЛЕ операции, а хук на `model.snapshot()` сделал бы читающий по имени метод мутирующим | сознательный отказ от абстракции: закрыты 4 достижимых пути, границы названы числами (52б), инвариант заперт набором |

## 57. Повторная узкая ревизия пункта 1-6 дороги (1.4) — превью vs команда-на-месте (2026-08-19)

Зонд ревизора, СВОЙ (не прогон чужого набора): живая вкладка `AdvancedGraphTab` +
поддельный сервер, корпус-фикстура `d74eb9f1`, класс `armatura_ruchn`, `node_11`
(20×36, bbox `[25, 215, 45, 251]`), превью 90×90. Все модальные диалоги подменены
с журналом фактов вызова (ловушка «зонд повисает», `PROTOCOL §5`). Инъекции — правкой
байтов с восстановлением копией файла; `md5 967ce743…` до и после совпал,
`git checkout --` не применялся. Гейты — системным python 3.11.9.

| # | что мерили | как | результат |
|---|---|---|---|
| 57.1 | Полная цепочка, фолбэк-ветка: превью → боевой `tab._auto_fix()` → `undo()` → `tab._save_graph()` | зонд; ветка доказана описанием шага стека `Auto-Fix (chains)` | `node_11` = `[25, 215, 45, 251]` во всех четырёх точках (после кнопки · после Ctrl+Z · в payload сервера · в модели после записи); вся геометрия после Ctrl+Z == до превью; диалогов 0 |
| 57.2 | То же, ветка раскладки (шаг стека `Сглаживание`, метка `layout_applied`) | зонд | те же четыре точки — исходная рамка; диалогов 0 |
| 57.3 | «Оптимизировать все» поверх превью | зонд | после кнопки рамка исходная; откат всего стека возвращает геометрию до превью бит-в-бит |
| 57.4 | «Удалить выделенное» поверх превью (жертва `node_20`) | зонд | жертва удалена и вернулась одним Ctrl+Z; рамка исходная во всех точках |
| 57.5 | Обратная граница И5: `batch_delete` без выделения | зонд | стек не вырос, превью ЖИВО (`[-10.0, 188.0, 80.0, 278.0]`) — картинку у оператора не отобрали |
| 57.6 | Несущее утверждение «поздним откатом не лечится», половина 1: дроп сразу ПОСЛЕ `cmd.execute()` (раньше для «после» некуда) | зонд на `SnapshotCommand` руками | модель вычищена, но Ctrl+Z вернул превью из отравленного `_before` — дефект жив |
| 57.7 | Половина 2: дроп после `push_executed` (revision вырос) | зонд | сторож базлайна отказывает в откате — превью остаётся в модели. Обе половины вместе: дроп ДО точки возврата — необходимость механизма, не стиль |
| 57.8 | Контрфактика стека «Оптимизировать все» (свежая вкладка) | дроп нейтрализован подменой метода в рантайме | стек 0→**37**, revision 0→**37** во всех трёх состояниях (до правки / после правки / без превью); до правки bbox отравлен `[-10.0, 188.0, 80.0, 278.0]`, после — исходный. ⚠ Числа §56.6 «стек 0→36, revision 0→1» на свежей вкладке НЕ воспроизводятся — вероятно, сняты на вкладке с уже израсходованным состоянием (момент замера в §56.6 не назван); сути «ОТРАВЛЕНО/закрыто» не меняет |
| 57.9 | Гейты | системный python 3.11.9 | `test_preview_not_welded_into_command.py` **8 passed**; `pytest tests/ui` **301 passed / 13 skipped**; `lint_gate --check` exit 0 (ruff 177/178, mypy 0); `suite_baseline --check` exit 0 (собрано 1015, failed 13 / errors 9 — как в базе). Все числа исполнителя воспроизведены |
| 57.10 | Инъекция И-А «до правки» (сняты все 4 дропа) | правка байтами + restore | **5 failed / 3 passed**; красные — ровно пять дефектных тестов (обе ветки кнопки, upload, optimize_all, batch_delete). Заявка «до правки 5 failed / 3 passed» воспроизведена; набор дефект стережёт |
| 57.11 | Инъекция И-Б «снимать всегда» (дроп выше раннего выхода `batch_delete`) | правка байтами + restore | краснеет ровно `test_command_that_pushes_nothing_keeps_the_preview_alive`, остальные 7 зелёные — граница заперта с двух сторон |
| 57.12 | Пятый достижимый путь (свип по коду) | grep всех мест построения команд (`SnapshotCommand`/`AutoFixCommand`/`undo_mgr.execute`/`push_executed`) + чтение гейтов достижимости | синхронного пятого пути НЕТ: жесты и копипаст заперты `_node_drag_allowed()` (=False в режиме; Ctrl+C/V — `advanced_graph_editor.py:4789/:4798`), Ctrl+ПКМ перехвачен режимом (`:2807` — исключение из набора, команд не строит), рёберные/waypoint/стилевые операции живут в чужих mode handlers (смена режима → `on_exit` → `_exit_resize_objects` → штатный откат), undo/redo пересобирает модель (лечение 1.4), запись закрыта 1.5 (`base_graph_tab.py:1121`), `apply_resize` — штатная фиксация превью. Асинхронный OCR-путь (`apply_ocr_results` → `_ocr_push_snapshot`, §56.20) — честно заявленная граница: гонка через QThread+сеть, в объём доработки не входит, у архитектора |
| 57.13 | Тег и CI | `git rev-parse road-1.4^{commit}` · `git ls-remote origin 'refs/tags/road-1.4^{}'` · `gh run view 32245209879` | локально и на origin (peeled) → `e0f2dd6` = голова `road/1.4-fix`; CI run 32245209879 — success на том же SHA и той же ветке |

Вердикт: **ПРИНЯТЬ** (полный отчёт — журнал `BOARD.md`).
## 58. Пункт 1-11 дороги (1.8) — «липкий ресайз» и «брошенная тяга» (2026-08-19)

**Обстановка.** worktree `C:\project\pid_wt_1.4`, ветка `road/1.8` от `dev` (в базе
1-10/road-1.7). Системный `python` 3.11.9, Qt offscreen. Корпус — git-фикстура
`d74eb9f1` (66 узлов / 63 ребра), `storage/` и docker/Redis не трогались
(в главном дереве параллельно идёт корпусный прогон, две чужие сессии рядом).

**Проверка реальности (оба механизма воспроизведены чтением кода и жестом, как
и обещал PLAN_AUDIT ❔/🪤 по 1.8):**

- (а) «липкий ресайз», только `SimpleGraphEditor`: press по ручке в радиусе
  перехвата `find_node_at` (по ЦЕНТРОИДУ; в canvas-режиме `CLICK_THRESHOLD` =
  **8.0** из `_VIS_CANVAS`, `base_graph_editor.py:112`, а не 20 — из-за этого
  первый вариант обстановки теста не собрался) уходит отложенным решением
  клик/drag (`mousePressEvent:1653`); release без движения зовёт
  `_on_ctrl_lmb_click:1691` → `handler.on_press(ed, x, y, None)` →
  `ResizeNodeHandler.on_press` (`simple_handlers.py:156`) начинал тягу УЖЕ ПОСЛЕ
  отпускания кнопки. Дальше рамку вёз голый mouseMove (`mouseMoveEvent:1759` →
  `on_move` → `overlay.is_dragging` → `drag_to`): гард залипшего drag
  (`:1733-1737`) гасит только `_ctrl_lmb_*`, overlay-тягу не видит — ровно как
  сказано в PLAN_AUDIT («защита есть только для ctrl-drag узлов»). В «Ручной
  правке» путь мёртв: `_node_drag_allowed` (advanced `:5151`) вне idle не отдаёт
  press перехвату узла. В корпусе узлов под сценарий нет (полудиагонали > 20 при
  пороге перехвата 8) — тест создаёт узел 20×20 боевым `add_equipment_node`
  (ровно сценарий оператора «добавил узел → ручки открылись → клик по ручке»).
- (б) «брошенная тяга», оба редактора: Esc при нажатой кнопке
  (simple `keyPressEvent:492`; advanced `_handle_escape:5187`) звал
  `_stop_resize`, который прятал ручки и обнулял снимки, НЕ откатывая модель —
  а каждый кадр тяги уже мутировал её (`_on_node_resized`: bbox/centroid/area,
  `rescale_edge_pins`, пересадка концов). Коммита нет → нет шага undo, дёрти-флаг
  чист (он считает `undo_mgr.revision` — 1.5).
- `resize_node` в advanced НЕ перерегистрирован (`register_mode` только в
  simple `:48`) — фикс в базовом хендлере исполняется в обоих редакторах
  (перерегистрирован только `add_edge`, как предупреждал вход 1.6).

**Гейт до правки** (`tests/ui/test_gesture_dispatch.py`, секция 1.8 — 5 тестов,
жесты настоящими QMouseEvent/QKeyEvent, контроль по данным и видимым ручкам):

| тест | до правки |
|---|---|
| клик по перехваченной ручке не начинает тягу (simple, узел 20×20) | **failed**: «рамка поехала за курсором при отпущенной кнопке (липкий ресайз)» |
| Esc посреди протяжки возвращает модель [ed] | **failed**: «Esc не вернул рамку: брошенная тяга осталась в модели» |
| Esc посреди протяжки возвращает модель [simple] | **failed**: то же |
| Esc после «потянул-отпустил-потянул» откатывает только хвост | **failed**: «Esc откатил дальше последнего коммита» |
| контроль: настоящая протяжка за ручку в simple жива | passed (характеризация) |

**Правка (2 файла кода):**
1. `simple_handlers.py` `ResizeNodeHandler.on_press` — `start_drag` только при
   настоящем нажатии (`event is not None`); синтетический клик ручку не тянет.
2. `simple_graph_editor.py` — `_revert_uncommitted_resize()`: расхождение
   текущего состояния с последним снимком (`_resize_start_*` — вход в режим /
   `_commit_resize` / «start для следующего drag», три точки 1.7) откатывается
   `ResizeNodeCommand(...).undo()` БЕЗ записи в стек — тем же кодом и тем же
   снимком, что Ctrl+Z (пять полей `ROUTE_KEYS`, заведённые ресайзом поля
   удаляются). Вызов — из `_stop_resize` (обе двери Esc сходятся там же, клик
   мимо и переключение узла — тоже). Порядок: сначала `overlay.hide()`, потом
   откат — `_redraw_all` внутри отката сносит всё с Z > 0, и hide() после него
   удалял бы осиротевшие item. След в лог клиента (Д3) — по образцу
   `_revert_resize_preview` (1.4).

**Зонды (дерево восстанавливалось копией файла, не `git checkout`):**

| зонд | результат |
|---|---|
| И1 — вернуть безусловный `start_drag` | `1 failed`: ровно sticky-тест |
| И2 — снять вызов `_revert_uncommitted_resize` из `_stop_resize` | `3 failed`: оба Esc-теста + хвостовой |
| И3 — недооткат: `old_routes=None` (рёбра не возвращаются) | `3 failed`, причина именно «Esc не вернул рёбра (сравнение словарей целиком — как в 1.7)» — рамка при этом вернулась, красит проверяемый механизм |
| И4 — снять обновление снимка «start для следующего drag» в `_commit_resize` | `1 failed`: ровно хвостовой, «Esc откатил дальше последнего коммита» |
| после восстановления | `29 passed` (весь файл набора 0.4) |

Прогоны зондов 2.5–5.9 с — ничего не повисло (модального диалога в пути нет).

**Базовые гейты:**

```
python -m pytest tests/ui -q            -> 313 passed, 13 skipped
python tools/lint_gate.py --check       -> [OK] долг не вырос (BLE001/E722: 175 при эталоне 178)
python tools/suite_baseline.py --check  -> [OK] новых красных нет; собрано 1034
                                           (git-видимых 1034 в 95 файлах), failed 13 / errors 9 — ровно пол
```

**Остаточные дыры того же класса (строкой, правкой не тронуты):**
- потерянный release при ЧЕСТНОЙ тяге (alt-tab с зажатой кнопкой): overlay-тяга
  остаётся активной и после возврата фокуса едет за голым mouseMove —
  `focusOutEvent`/`_reset_drag_state` гасят только ctrl-drag узлов, а гард
  `mouseMoveEvent:1733` overlay не видит. После 1.8 порча больше не тихая
  (клик мимо / Esc снимет её откатом), но до этого рамка ездит. Класс
  `test_button_less_move_ends_drag_and_node_stops_following`, кандидат в 10.x;
- прямой `set_mode` при активной тяге (минуя `_stop_resize`) бросил бы тягу без
  отката через `ResizeNodeHandler.on_exit` (он только прячет overlay). Пути с
  зажатой кнопкой мыши не найдено (тулбар требует release, хоткеев смены режима
  в редакторах нет) — не чинилось как недостижимое.

**Что НЕ делалось:** Д6-пересъём (рядом две чужие сессии — `PROTOCOL §Д6`),
корпусные стенды (корпус вне git — «судить нечем»), docker/Redis.

## 60. Пункт 1-31 дороги (1.x7) — близнец 1.1 в легаси `auto_fix()` (2026-08-19)

**Обстановка.** worktree `C:\project\pid_wt_1.4`, ветка `road/1.x7` от `dev`
(в базе 1-11/road-1.8). Системный `python` 3.11.9, Qt offscreen. Фикстура
синтетическая — косая труба «рамка → рамка» на белом растре 800×600, та же, что
у 1.1 (`tests/ui/test_smooth_undo_invariant.py`); `storage/`, docker и Redis не
трогались (в главном дереве идёт корпусный прогон, рядом две чужие сессии).

### 60а. Проверка реальности (шаг 2) — все адреса сверены по коду

- Путь ЖИВ, как и сказал архитектор: `ui/tabs/advanced_graph_tab.py:790`
  зовёт `self._editor.auto_fix()` в фолбэк-ветке (`canvas_state.has_layout()`
  ложно — холст без раскладки). Второго вызывающего у метода нет
  (греп по репо без `_scratch/`: одно попадание кода + одна строка в докстринге
  `ui/editors/autofix_chains.py:25`). Значит пункт остаётся ФИКСОМ.
- Механизм подтверждён дословно, `ui/editors/advanced_graph_editor.py:3896`:
  `cmd.execute()` открывает шаг, `auto_fix_graph(...)` работает, и только потом
  `_redraw_all()` / `rebuild_edge_data_index()` / `cmd.finalize()` /
  `push_executed(cmd)` — всё ВНЕ `try/finally`.
- ⭐ **Условия взвода, в отличие от 1.1, НЕТ.** В 1.1 дефект взводился самим
  фактом передачи роутера (`route_fn`), при `route_fn=None` не воспроизводился
  вовсе. Здесь взводить нечего: `auto_fix_graph` объявлен «`nodes` и
  `edges_data` МОДИФИЦИРУЮТСЯ in-place» (`autofix_chains.py:232-233`) и
  безусловно мутирует их в Step 4 (`:349-374`: `centroid`, `bbox`,
  `segmentation`), а весь хвост функции идёт ПОСЛЕ этой мутации —
  расталкивание наложений, пересадка рёбер каноном
  `modules/graph/core/seating.reseat_edge` + `port_model.pinned_port` (внешний
  код, `:409-447`), финальная статистика. То есть окно дефекта — не узкий
  случай, а весь хвост движка.
- Внутренних глушителей у движка нет: `except`/`raise` в
  `ui/editors/autofix_chains.py` — **0 попаданий**. Падение долетает до
  вызывающего, у кнопки свой `except` с `QMessageBox` (`advanced_graph_tab.py:793`).

### 60б. Замер ДО правки (зонд `probe_autofix.py`, подмена движка)

Движок подменялся на «успел сдвинуть узлы на 17 px и упал `IndexError`» —
порча ложится ДО падения, как в бою.

| наблюдение | зелёный путь | падение движка |
|---|---|---|
| `undo_mgr.stack_depth` | 0 → 1 | **0 → 0** |
| `undo_mgr.revision` | 0 → 1 | **0 → 0** |
| `can_undo` | True | **False** |
| холст изменён | True | **True** |
| `undo()` | `'Auto-Fix (chains)'`, холст вернулся | **нечего отменять** |

⭐ **Тихая половина ЕСТЬ, и она хуже громкой** (тот же вывод, что в 1.1).
`_redraw_all()` тоже стоял за `try`, поэтому после падения модель уехала, а
сцена осталась прежней:

- модель после падения: `low (237.0, 417.0)`, `high (277.0, 137.0)`;
- сцена после падения: `low (220.0, 400.0)`, `high (260.0, 120.0)` — ровно то,
  что было ДО вызова; расхождение 17 px по обеим осям у обоих узлов.

Оператор видит один холст, а в модели лежит другой — и именно модель уходит на
сервер и в экспорт. При этом `revision` не вырос, то есть вкладка считает
`has_unsaved_changes()` ложным (`ui/tabs/base_graph_tab.py`) и закрывается без
вопроса. Это та же семья, что 1.5 («на сервер уходит не то, что видел оператор»).

### 60в. Правка

`ui/editors/advanced_graph_editor.py`, 19 строк вместо 12 в одном методе: вызов
движка обёрнут в `try`, четыре закрывающие строки перенесены в `finally`.
Порядок внутри — тот же, что был на зелёном пути этого метода
(`_redraw_all` → `rebuild_edge_data_index` → `finalize` → `push_executed`);
в `smooth_canvas` порядок другой (`rebuild` → `redraw`), и он НЕ приводился к
общему виду — это была бы правка поведения зелёного пути сверх пункта.
Строка `drop_uncommitted_preview()` (пункт 1-6) в диффе не участвует.

Замер ПОСЛЕ правки тем же зондом: зелёный путь без изменений (0 → 1, 0 → 1,
`undo()` возвращает холст); падение движка — `stack_depth` 0 → 1,
`revision` 0 → 1, `can_undo` True, сцена = модель (`237.0, 417.0` в обеих),
расхождений 0. То есть `_redraw_all()` на состоянии после падения (индекс рёбер
ещё не перестроен) отрабатывает штатно — проверено исполнением, не рассуждением.

### 60г. Д1 и зонды (`tests/ui/test_auto_fix_undo_invariant.py`, 3 теста)

Движок подменяется в `ui.editors.advanced_graph_editor`, а НЕ в
`ui.editors.autofix_chains`: редактор связывает имя обычным `from ... import`
на уровне модуля (`advanced_graph_editor.py:48`), и подмена в исходном модуле
до вызова не доходит — тест остался бы зелёным при любом дефекте. Записано в
докстринг файла как ловушка.

| инъекция в ТЕКУЩЕЕ дерево | результат |
|---|---|
| И1 — снять `try/finally` целиком (исходный дефект) | **2 failed / 1 passed**: краснеют обе крашевые проверки, зелёный путь цел. Сообщения называют проверяемый механизм: «холст изменён, а шага отмены нет» и «сцена расходится с моделью … {'low': ((237.0, 417.0), (220.0, 400.0)) …}» |
| И2 — закрытие шага вернуть в `finally`, `_redraw_all` оставить снаружи | **1 failed / 2 passed**: краснеет ТОЛЬКО тихая половина. Половины заперты раздельно, каждая своим тестом |
| И3 — снять `push_executed` | **3 failed**: зелёный путь тоже краснеет, то есть он не декоративен |

Ничего не повисло (0.3–0.8 с на прогон): тест зовёт редактор напрямую, модальный
`QMessageBox` живёт у кнопки вкладки и в путь инъекции не попадает.
Дерево после инъекций восстановлено копией файла, не `git checkout`.

### 60д. Гейт пункта

| команда | результат |
|---|---|
| `python -m pytest tests/ui -q` | **324 passed, 13 skipped** |
| `python tools/lint_gate.py --check` | **exit 0** (ruff 175 при эталоне 178, mypy 0) |
| `python tools/suite_baseline.py --check` | **exit 0**, собрано 1045, красные `{failed: 13, errors: 9}` — ровно эталон, новых нет |

### 60е. Границы пункта, заявлены исполнителем

- **Падение самого `_redraw_all` шаг undo по-прежнему не закроет** — он стоит
  первым в `finally`, и его исключение унесёт `finalize`/`push_executed`.
  Ровно ту же границу заявил 1.1 и спекулятивную страховку не брал; не беру и я.
- **Тест уровня редактора, не вкладки** — как у 1.1. Через вкладку путь идёт
  сквозь `QMessageBox`, а сам дефект целиком внутри `auto_fix()`.

### 60ж. Замечено рядом, в правку НЕ вошло (`PROTOCOL §4`)

- ⛔ **Второй носитель той же формы, тот же файл:**
  `advanced_graph_editor.py:4442-4476`, метод `apply_resize` («Размер
  объектов»). Тот же рисунок: `SnapshotCommand` + мутации
  (`_apply_sizes_from_base`, `_spread_overlaps`, `_reseat_after_resize`,
  тот же `auto_fix_graph` на `:4467`) и `finalize`/`push_executed` без
  `try/finally`. Класс тот же — тихая потеря работы оператора. Правкой не
  тронут: это ДРУГАЯ команда, а не адрес моей строки, и её снимок завязан на
  `_resize_model_base`/`_drop_resize_baseline` — территорию 1-6. Кандидат в
  отдельный пункт (родня 1-32).
- **Фокус после отказа кнопки:** `advanced_graph_tab.py:791` возвращает фокус
  редактору строкой ВНУТРИ `try` («иначе Ctrl+Z не дойдёт и не отменит» —
  комментарий авторов), поэтому на пути падения `setFocus()` пропускается, а
  сверху ещё открывается модалка. Шаг undo при этом существует и достижим
  кнопкой «Отменить» тулбара (`base_graph_tab.py:788` → `_undo` →
  `editor.undo()`, фокуса не требует), поэтому цель моей строки буквой
  достигается и сверхбуквенная правка чужого файла не бралась. Адрес — 1-22
  (ДН3), у которого эта же функция уже записана.

**Что НЕ делалось:** Д6-пересъём (рядом две чужие сессии — `PROTOCOL §Д6`),
корпусные стенды (пункт `modules/graph/` не трогает), docker/Redis.

## 61. Пункт 1-24 дороги (1.x6) — «Очистка рамки»: загрузка и модалка из конструктора (2026-08-19)

**Обстановка.** worktree `C:\project\pid_wt_rev2`, ветка `road/1.x6` от `dev`
(в базе 1.1, 1.4, 1.5, 1.3, 1.6, 1.7, 1.8, 1.9 и гейтовые пункты). Системный
`python` 3.11.9, PySide6 6.11.1, `QT_QPA_PLATFORM=offscreen`. Область:
`ui/tabs/frame_tab.py`, тест `tests/ui/test_frame_tab_opens_on_broken_artifact.py`.
Сети, docker, Redis и `storage/` не трогались — сервер подставной.

| # | что | дата | результат |
|---|---|---|---|
| 61.1 | Адреса пункта сверены по коду (шаг 2) | 2026-08-19 | подтверждены дословно, файл 173 строки: `:35` класс `FrameTab`, `:48` `self._load_original()` последней строкой `__init__`, `:126` сам метод, `:134` `except (APIError, Exception)`, `:135` `QMessageBox.warning`. Расхождений с формулировкой пункта нет |
| 61.2 | Зонд «класс на процесс» ДО правки | 2026-08-19 | `timeout 60 python -X utf8 probe_frame_open.py` (offscreen, битый артефакт: сервер кладёт `NOT-A-PNG`) → **exit 124**, маркер `TAB-OPENED` не напечатан. Четвёртый исход `PROTOCOL §5` («зонд повис») воспроизведён на этом пункте — четвёртый раз на дороге после 1.5, 1.3, 1-6 |
| 61.3 | Тест ДО правки | 2026-08-19 | `pytest tests/ui/test_frame_tab_opens_on_broken_artifact.py -q` → **7 failed** (6 внутрипроцессных за 0.8 с + сценарий-процесс, упавший по таймауту 60 с). Дословно: «конструктор уже сходил на сервер: ['original_image']»; «модальный диалог всё ещё открывается: ('Ошибка', 'Не удалось загрузить изображение: …')» — и на битом файле, и на отказе сервера; в ФАЙЛЕ лога клиента нет ничего, кроме строки о настройке лога |
| 61.4 | Находка сверх описания пункта | 2026-08-19 | на УСПЕШНОЙ загрузке терялись ВСЕ ТРИ подсказки оператору (`status_message` пуст): воркспейс подключает сигнал строкой НИЖЕ конструктора (`ui/widgets/diagram_workspace.py:1752-1759`), поэтому «Обведите внутреннюю часть чертежа полигоном» и две строки редактора уходили в никуда. То есть модалка была не единственным следствием — вкладка вообще не могла ничего сказать оператору из конструктора |
| 61.5 | Правка | 2026-08-19 | 4 куска в одном файле: загрузка отложена `QTimer.singleShot(0, self, self._load_original)` (форма с контекстом — вкладку удалили раньше, Qt снимет вызов), строка отказа `error_label` под тулбаром, `logger.exception` в ветке отказа (Д2), `logger.info` на успехе (Д3). Клауза `except (APIError, Exception)` (долг BLE001) НЕ тронута |
| 61.6 | Гейт после правки | 2026-08-19 | тот же файл — **7 passed за 1.25 с**; сценарий-процесс печатает `TAB-OPENED`, rc 0 (было: висяк 60 с). Битый артефакт → вкладка открыта, в ней видимая строка «❌ Не удалось загрузить изображение: …», диалогов ноль, в `client.log` сообщение + `Traceback` + `uid=0fc9d04c` |
| 61.7 | Зонд И1 — вернуть модалку вместо строки | 2026-08-19 | **4 failed / 3 passed**, и сценарий-процесс снова ПОВИС (60 с). Вывод, которого не было в пункте: дело не в «диалог из конструктора», а в диалоге на пути загрузки ВООБЩЕ — отложенный на 0 мс диалог вешает процесс ровно так же, потому что закрыть его некому |
| 61.8 | Зонд И2 — вернуть синхронный `self._load_original()` в конструктор, строку отказа оставить | 2026-08-19 | **3 failed / 4 passed**, сценарий-процесс ЗЕЛЁНЫЙ. Половины дефекта разделены: висяк даёт модалка, потерю всего сказанного оператору — конструктор |
| 61.9 | Зонд И3 — снять `logger.exception` | 2026-08-19 | **1 failed** — только `test_broken_artifact_leaves_trace_in_client_log`: у следа в логе свой сторож, а не общий со строкой |
| 61.10 | Зонд И4 — снять `logger.info` | 2026-08-19 | **1 failed** — только `test_successful_load_is_logged` (Д3) |
| 61.11 | Ложное красное у первого зонда | 2026-08-19 | первая попытка И1 через heredoc внесла в f-строку реальный перевод строки — файл перестал разбираться, и покраснели ВСЕ 7. По `PROTOCOL §5` («покраснел — проверь, кто именно») зонд признан негодным и перевнедрён отдельным скриптом. Дерево между зондами восстанавливалось КОПИЕЙ файла (md5 `3b688f15cdcc8f0be7ec4c158dd417de`), не `git checkout` |
| 61.12 | Базовые гейты | 2026-08-19 | `pytest tests/ui` → **328 passed / 13 skipped** (было 321 + 13); `python -X utf8 tools/lint_gate.py --check` → exit 0, ruff 174 нарушения в 71 файле (эталон 178), mypy 0; `python -X utf8 tools/suite_baseline.py --check` → exit 0, собрано **1049** (было 1042), красных 13 failed + 9 errors — без изменений |
| 61.13 | Счётчик BLE001 по моему файлу | 2026-08-19 | **3 → 2, то есть УПАЛ**: ruff не считает blind-except, в теле которого есть `logging.exception`. Клаузу я не переписывал — счётчик снизило появление следа в логе (Д2). Роста нет ни по файлу, ни по итогу (175 → 174) |
| 61.14 | Почему НЕ `ArtifactDownloader` в `QThread`, как у графовых вкладок | 2026-08-19 | воркспейс НЕ зовёт `tab.cleanup()` ни на одном пути закрытия: `_close_active_tab` (`ui/widgets/diagram_workspace.py:1313`) → `_close_tab_and_restore_header` → `_remove_tab_widget`, там только `deleteLater()`; `cleanup()` вкладок зовут одни тесты. Поток, заведённый во вкладке, пережил бы её закрытие — это новый тупик вместо старого. Отложенный вызов в GUI-потоке даёт и «вкладка открывается», и `uid=` в строке лога (в QThread контекст свой, `uid=-` — `UI_GUIDE §11.5`) |

**Что НЕ делалось:** Д6-пересъём (рядом две чужие сессии — `PROTOCOL §Д6`), корпусные
стенды (корпус вне git — «судить нечем»), docker/Redis, сборка PyInstaller.

**Находки мимо пункта (кода не трогал):**
- `UI_GUIDE §3.4` врёт: `_close_active_tab()` не зовёт `tab.cleanup()` — см. 61.14. Дока
  описывает контракт, которого в коде нет; чинить это вместе с 10-8 (единый жизненный цикл вкладок).
- После отказа загрузки тулбар вкладки остаётся кликабельным: «Сохранить» на пустом
  редакторе даст свой диалог из `_on_save_only`. Путь оператора после отказа пункт не описывает.
- `ui/editors/frame_editor.py:30` импортирует `QToolBar`/`QPushButton`/`QLabel`/`QStatusBar` —
  все четыре встречаются в файле ровно один раз, в самой строке импорта. Мёртвый импорт, не мой файл.
---
## 62. Долг пункта 1-5 (1.1) — полный корпус в обоих режимах роутера + стенд ПР1 (2026-08-19)

**Обстановка.** Главное дерево `C:\project\pid_pipeline_deploy` (единственное с полным
корпусом: 22 каталога в `storage/diagrams/`, `graph_validated.json` у 19), ветка
`road/1.1-corpus` от `dev` (HEAD `2301b30`), дерево чистое. Системный `python` 3.11.9
(не `.venv311`). Дерево несёт СВЕРХ 1.1 пункты 1.3, 1.5, 1.6, 1.7, 1.9 и гейтовые —
осознанно: корпус проверяет то, что поедет на бой. Кода не трогалось; docker/Redis —
тоже. Долг признан ревизией связки 2026-08-19 (строка 1-5 QUEUE): пункт трогает
`modules/graph/`, локальный прогон полного корпуса не делался ни разу — корпус был
занят параллельной сессией.

### 62а. Шаг 2: чем гейт строки считается — сверено по коду

| # | что | команда | дата | результат |
|---|---|---|---|---|
| 62.1 | «Корпус бит-в-бит в обоих режимах роутера» — это стенд или скрипт | скрэтчпад сессии 1-5 (`6e78da05…/scratchpad/corpus_ba.py`) против `tools/*.py` | 2026-08-19 | **разовый скрипт, не стенд репо** — методика §47.10: движок до/после на копиях одного входа, режимы `route_fn=None` и роутер-отказ `lambda e: False`. Скрипт найден и воспроизведён; ни один стенд репо движок `edit_smooth` не зовёт |
| 62.2 | §47.5 («движок не на пути раскладки») сегодня | `grep -rn edit_smooth` без `_scratch/`, `.claude/`, `storage/` | 2026-08-19 | подтверждён: единственный боевой потребитель — `smooth_canvas` (`advanced_graph_editor.py:877`); `advanced_graph_tab.py:788` зовёт тот же `smooth_canvas` |
| 62.3 | «До» для сравнения | `git log --oneline bf3dcef..HEAD -- modules/graph/` + `git diff bf3dcef^ bf3dcef` | 2026-08-19 | после `bf3dcef` каталог `modules/graph/` не менялся **ни одним коммитом** → дифф движка против `bf3dcef^` изолирует ровно правку 1.1 (две функции восстановления снимка, `update(old)` → `update(deepcopy(old))`). Зависимости движка (`edit_checks`, `ports`, `graph_access`) у обеих версий общие — текущие |
| 62.4 | Движок дерева == тегу | `git show bf3dcef:...edit_smooth.py` против файла на диске | 2026-08-19 | равны после нормализации EOL (git отдаёт LF, диск CRLF — ловушка `PROTOCOL §Гейты`) |
| 62.5 | Корпус против эталона ПР1 | `corpus.corpus_paths()` против `determinism_baseline.json` | 2026-08-19 | **19 == 19, один в один**; в `storage/diagrams/` 22 каталога, у трёх графа нет (штатно, §53.1) |

### 62б. Зонд механизма сравнения — до полного прогона

| # | что | команда | дата | результат |
|---|---|---|---|---|
| 62.6 | Смоук: методика воспроизведена | `corpus_ba_full.py d74eb9f1` (движок `bf3dcef^` против дерева) | 2026-08-19 | бит-в-бит True в обоих режимах; времена сошлись с §47.10 (0.29/0.28 и 0.27/0.36 с против 0.30/0.28 и 0.27/0.29) |
| 62.7 | ⛔ Зонд: сравнение обязано краснеть | копия движка дерева + сдвиг одной координаты `source_point` на 0.5 в конце `smooth()` → тот же прогон | 2026-08-19 | **exit 1, «бит-в-бит: False, структурно: False» в обоих режимах при «stats: True»** — красит именно холст, а не соседа; дерево не тронуто (зонд жил в скрэтчпаде) |

### 62в. ⭐ Прогон 1: движок 1.1 до/после, полный корпус, оба режима роутера

`PYTHONHASHSEED=1 python -X utf8 corpus_ba_full.py` (скрэтчпад сессии, лог
`corpus_ba_full.log`), 14:47:53–15:58:47, **70.9 мин**. 19 графов × 2 режима роутера
(`route_fn=None` | роутер-отказ) × движок `bf3dcef^` против дерева = **38 сравнений**.

| # | что | дата | результат |
|---|---|---|---|
| 62.8 | ⭐ **Холст** | 2026-08-19 | **38/38 бит-в-бит** — `json.dumps` байтово (та же проекция, что у `cmp_bitexact`/`layout_determinism`) и структурно; **stats движка равны** во всех 38; **исключений 0** в обе стороны |
| 62.9 | Почему бит-в-бит применим ко ВСЕМ 19, включая 9 неповторимых | 2026-08-19 | движок детерминирован (stdlib, роутер инертный, вход из файла) — libavoid в прогоне не участвует; неповторимость 9 графов — свойство layout-роутинга, к движку не относится |
| 62.10 | Цена по времени (справка, не гейт) | 2026-08-19 | самый тяжёлый `51b339ab` (938 узлов) — 170–193 с/прогон, ~12.4 мин на граф; `0aea61c0` ~9.4 мин; мелкие (65–72 узла) — 0.3–8 с/прогон. Время «до» против «после» — шум ±15 % без знака |

### 62г. Прогон 2: стенд ПР1 по полному корпусу (требование `PROTOCOL §Гейты` для правок `modules/graph/`)

`python -X utf8 tools/layout_determinism.py --runs 6 --check`, 15:59:18–16:22:20,
**23.0 мин** (в 1-29 тот же прогон стоил 35; машина была свободнее), **exit 0**.

| # | что | дата | результат |
|---|---|---|---|
| 62.11 | ⭐ **Вердикт** | 2026-08-19 | «[OK] регресса воспроизводимости нет»: все **10** стабильных воспроизводимы, все **9** неповторимых остались неповторимыми (0aea61c0 — 3 исхода из 6, 751116c9 и a5264536 — 5 из 6, 302171b3 — 4 из 6, остальные по 2); ни `[ПРОВАЛ]`, ни `[СУДИТЬ НЕЧЕМ]`, ни «стало лучше» |
| 62.12 | ⭐ Счётчики дефектов стабильны и совпали с историей | 2026-08-19 | у каждого из 19 дефекты одни и те же во всех 6 прогонах; **все 12 графов с историческими числами — дословно** (§32.18: 0aea61c0 156→11, 302171b3 13→0, 54a60fd1 70→4, 620cc50d 51→0, 6e7144d5 14→0, 751116c9 44→0, a5264536 34→0, a6d28736 128→7; §53.7: 0fc9d04c 70→0, 3263039b 0→0; §32.27: 089feca2 6→0, d74eb9f1 0→0). У семи остальных исторических determinism-чисел в журнале не было — сняты впервые: 06b938ec 92→1, 13d1ef5f 23→3, 51b339ab 302→11, 89ca7583 36→2, 8d14cf73 120→0, 8d517a35 116→1, c2f79462 19→8 |
| 62.13 | Бонус: бит-в-бит с ИСТОРИЕЙ на git-графах | 2026-08-19 | хеши совпали с §32.27/§32.6 дословно: `089feca2` `f4510d0b989c015b`, `d74eb9f1` `51e20ed60bb97cdc`, `6e7144d5` `6474ccb5695dc1ed` (один из двух его известных исходов) — раскладка на git-тройке не сдвинулась со времён 0.10 |

**Итог: долг погашен, регресса нет ни по движку (38/38 бит-в-бит), ни по раскладке
(exit 0, счётчики стабильны).** Тег `road-1.1` остаётся на `bf3dcef` — кода эта сессия
не трогала, двигать тег не на что.

**Что НЕ делалось:** Д6-пересъём любых эталонов (прямой запрет `PROTOCOL §Д6` — рядом
две чужие сессии в своих worktree; и переснимать нечего: оба прогона зелёные), полный
прогон `--no-routing` (гейт строки говорит о режимах роутера ДВИЖКА — оба прогнаны;
режим `--no-routing` стенда ПР1 — другая ось, эталон снят при `routing true`),
`suite_baseline`/`lint_gate` (кода в дереве не менялось — базовые гейты пункта сняты
в §47 и CI 32223366812).

## 63. Ревизия связки 1-9+1-10+1-11 (1.6+1.7+1.8) — шов «снимок ресайза × Ctrl+Z × выход из режима» (2026-08-19)

**Обстановка.** worktree `C:/project/pid_wt_1.4`, ветка `rev/resize-seam` от `dev`
(`4475b5d`: связка + 1-24 + 1-31 влиты). Системный `python` 3.11.9, Qt offscreen,
фикстура `d74eb9f1` из git (`tools/corpus.py`). Кода не трогал; зонды — скрипт вне
дерева (scratchpad), обстановка скопирована с набора 0.4: настоящие
`QKeyEvent`/`QMouseEvent` через публичные обработчики, суждение только по ДАННЫМ
(гео узлов + ПОЛНЫЕ словари рёбер + глубина стека undo). `storage/`, docker,
корпусные стенды не трогались (корпус занят чужой сессией; связка класса [ui],
корпус ей не нужен). Каждый зонд — свежий редактор.

### 63а. Числа трёх пунктов перепроверены исполнением

| # | что | команда | дата | результат |
|---|---|---|---|---|
| 63.1 | набор 0.4 + тесты 1.7 | `python -X utf8 -m pytest tests/ui/test_gesture_dispatch.py tests/ui/test_resize_undo_edge_ends.py -q` | 2026-08-19 | **32 passed** (29 набора 0.4 + 3 теста 1.7) за 2.7 с — состав совпал с §52/§55/§58 |
| 63.2 | весь `tests/ui` | `python -X utf8 -m pytest tests/ui -q` | 2026-08-19 | **331 passed, 13 skipped** = 313 (после 1.8, §58) + 11 (1-31) + 7 (1-24) — рост объяснён составом, расхождения НЕТ |
| 63.3 | база набора | `python -X utf8 tools/suite_baseline.py --check` | 2026-08-19 | exit 0 «новых красных нет»; собрано **1052** (1034 после 1.8 + 18 тестов 1-24/1-31), красные 13 failed / 9 errors — ровно пол |
| 63.4 | линт | `python -X utf8 tools/lint_gate.py --check` | 2026-08-19 | exit 0, mypy 0 |
| 63.5 | запрещённые утверждения набора 0.4 | греп `_resizing_node`/`_resize_start_`/`_current_mode`/`_current_handler` по двум файлам тестов связки | 2026-08-19 | **0 утверждений** — все попадания в докстрингах |

### 63б. ⛔ ДЕФЕКТ ШВА (внесён 1.8, поле — снимок 1.7): выход из ресайза после Ctrl+Z возвращает отменённую порчу

Сценарий Z1, «Ручная правка», узел `node_2` (bbox `[1456, 0, 1549, 34]`),
протяжка правой-нижней ручки +30/+20 px:

| # | шаг | состояние модели (гео узлов + полные словари рёбер) | стек undo |
|---|---|---|---|
| 63.6 | ресайз закоммичен (release) | ПОСЛЕ РЕСАЙЗА; у инцидентных рёбер заведены `_src_side`/`_tgt_side` | 1 |
| 63.7 | Ctrl+Z — В РЕЖИМЕ, ручки были открыты | ИСХОДНОЕ — undo честно вернул рамку и рёбра (гейт 1.7 не врёт) | 0 |
| 63.8 | Esc | **ПОСЛЕ РЕСАЙЗА — порча вернулась целиком**: рамка, центроид, area, пины и все 5 полей `ROUTE_KEYS` | 0 |
| 63.9 | Ctrl+Z ещё раз | ПОСЛЕ РЕСАЙЗА — стек пуст, отменять нечего: порча вварена БЕЗ шага undo | 0 |

| # | вариация | дата | результат |
|---|---|---|---|
| 63.10 | Z2: вместо Esc — «клик мимо» | 2026-08-19 | тот же возврат порчи (любой выход через `_stop_resize`) |
| 63.11 | Z4: две тяги → Ctrl+Z ×2 → Esc | 2026-08-19 | возврат к состоянию ПОСЛЕ ТЯГИ-2 — Esc возвращает ВСЁ отменённое до последнего снимка |
| 63.12 | Z8: «Проверка схемы», вход из `add_edge` | 2026-08-19 | тот же возврат порчи; защёлка 1.6 при этом РАБОТАЕТ — режим вернулся в `add_edge`, ручек 0 |

Механизм (по коду, подтверждён зондами): `ResizeNodeCommand.undo` двигает модель
и зовёт `_redraw_all`, но `_resize_start_*` (три точки снимка 1.7) никто не
инвалидирует; `_revert_uncommitted_resize` (1.8) отличает «брошенную тягу» от
«тяги не было» сравнением текущего состояния со снимком — расхождение, созданное
undo, неотличимо от расхождения, созданного тягой, и «откат» применяет протухший
снимок. Внесено 1.8: до него `_stop_resize` данных не трогал (Esc после Ctrl+Z
был чист). Тяжесть выше закрытого дефекта: 1.8 закрывал порчу, которую оператор
видел на экране; здесь возвращается порча, которую оператор ЯВНО отменил и видел
откаченной, — и вернуть её отменой уже нельзя.

### 63в. Контроли — что на шве ЧИСТО (те же зонды)

| # | сценарий | дата | результат |
|---|---|---|---|
| 63.13 | Z7: resize → Esc → Ctrl+Z (вне режима) | 2026-08-19 | чисто: ПОСЛЕ РЕСАЙЗА → ИСХОДНОЕ побайтово (штатный путь 1.7 жив) |
| 63.14 | Z6: commit → повторный 2ЛКМ (жест 1.6) → Esc → Ctrl+Z | 2026-08-19 | чисто: повторный вход переснимает снимок, Esc не прыгает, undo возвращает исходное |
| 63.15 | Z10: Ctrl+Z → Ctrl+Shift+Z (redo) → Esc | 2026-08-19 | чисто: redo синхронизирует модель со снимком, Esc не трогает |
| 63.16 | Z3: Ctrl+Z → смена инструмента `set_mode` (путь тулбара) | 2026-08-19 | данные ЦЕЛЫ (путь минует `_revert_uncommitted_resize`), но `_resizing_node` остаётся висеть — та самая дыра «прямой set_mode мимо _stop_resize» из находок 1.8 (10-1); парадокс: сейчас это единственный безопасный выход после Ctrl+Z |
| 63.17 | Z9: Ctrl+Z в режиме → press для новой тяги | 2026-08-19 | ручек на сцене **0** (осиротевший overlay: `_redraw_all` внутри undo их снёс, режим жив) — press по углу стал «кликом мимо» и тем же дефектом 63.8 вернул порчу |
| 63.18 | слепота собственных гейтов | 2026-08-19 | весь `tests/ui` **331 passed** при живом дефекте 63.8 в дереве — ни один гейт трёх пунктов шва не видит (тест 1.7 гоняет Ctrl+Z в режиме, но из режима не выходит; тесты 1.8 выходят, но undo не жмут) |

**Замечание строкой (в возврат не входит):** `_stop_resize` по-прежнему не чистит
`_resize_start_pins`/`_resize_start_routes` (замечание §52.30 осталось незакрытым
1.7/1.8); сегодня мусор заперт гардом `start_bbox=None`, но доработке 1-11 стоит
закрыть той же правкой.

**Что НЕ делалось:** Д6 (запрещено промптом ревизии), корпусные стенды (корпус
занят чужой сессией, классу [ui] не нужен), docker/Redis, мержи в `dev`.

## 64. Пункт 1-11 дороги (1.8), ДОРАБОТКА по возврату ревизии связки — «отмена, которая возвращает» (2026-08-19)

**Обстановка.** worktree `C:/project/pid_wt_rev2`, ветка `road/1.8-fix` от `dev`
(`07ea021`). Системный `python` 3.11.9 (не `.venv311`), Qt offscreen, фикстуры
корпуса из git (`d74eb9f1`, `089feca2` — `tools/corpus.py`). `storage/`, docker,
Redis, корпусные стенды не трогались (класс [ui]). Номер раздела назначен
архитектором; §63 (замеры ревизии) на момент правки лежит в ветке
`rev/resize-seam` и в `dev` ещё не влит — ссылки ниже указывают на него.
Д6 не делался (промпт). Зонды восстанавливались копией файла в скрэтчпаде,
`git checkout` не применялся.

### 64а. Шаг 2 — механизм дефекта перепроверен по коду, не по отчёту

| # | утверждение возврата | где | дата | результат |
|---|---|---|---|---|
| 64.1 | снимок живёт в трёх точках и при undo/redo не инвалидируется | `simple_graph_editor.py:284` (`_start_resize`), `:442-446` (хвост `_commit_resize`), `:525-534` (`undo`/`redo` — только проверка «узел исчез»; после правки `:550-556`) | 2026-08-19 | **точно**: ни одна из трёх точек к undo/redo не привязана |
| 64.2 | `_revert_uncommitted_resize` судит по расхождению со снимком | `simple_graph_editor.py:448-490` | 2026-08-19 | **точно**: сравнение bbox + `cur_pins` + `cur_routes` со снимком; источник расхождения не различается |
| 64.3 | `ResizeNodeCommand.undo` двигает модель и зовёт `_redraw_all` | `commands/simple_commands.py:386-394` | 2026-08-19 | **точно** (Z>0 со сцены сносит `base_graph_editor.py:449`) |
| 64.4 | тесты, названные ревизором «874/906» | `tests/ui/test_gesture_dispatch.py:874`, `:906` | 2026-08-19 | это `test_escape_mid_drag_reverts_the_uncommitted_resize` (2 параметра) и `test_escape_reverts_only_the_tail_after_a_committed_drag` — **3 passed** до и после правки |

### 64б. Замер ДО правки: дефект воспроизведён своими тестами на ОБОИХ редакторах

`tests/ui/test_gesture_dispatch.py`, секция «пункт 1.8, доработка», 8 тестов
написаны ДО правки (`PROTOCOL §3`); жесты — настоящие `QMouseEvent`/`QKeyEvent`,
суждение только по данным (гео узлов + ПОЛНЫЕ словари рёбер + глубина стека)
и по СЦЕНЕ (центры видимых ручек против углов рамки).

| # | тест | до правки |
|---|---|---|
| 64.5 | Ctrl+Z в режиме → **Esc** → модель осталась откаченной [ed] и [simple] | **failed** ×2: «выход из режима вернул отменённую порчу (рамка/центроид/площадь)» |
| 64.6 | то же, выход **кликом мимо** [ed] и [simple] | **failed** ×2: та же причина — дефект не про Esc, а про `_stop_resize` |
| 64.7 | две тяги → Ctrl+Z ×2 → Esc [ed] и [simple] | **failed** ×2: «Esc воскресил отменённые тяги (рамка)» — возврат к состоянию после ВТОРОЙ тяги (§63.11) |
| 64.8 | ручки после Ctrl+Z — по сцене [ed] и [simple] | **failed** ×2: «Ctrl+Z в режиме снёс ручки со сцены», `assert 0 == 4` (§63.17) |

Проверки ДО выхода из режима при этом зелёные: `undo` возвращает и рамку,
и полные словари рёбер — гейт 1.7 честен, как и сказано в возврате.

### 64в. Правка — один файл, 23 строки

`ui/editors/simple_graph_editor.py`:

1. `_resize_resync_overlay()` — после undo/redo пересъём снимка и ручек на том,
   что стало моделью. Лекарство выбрано из двух названных ревизором направлений:
   **синхронизация снимка**, а не признак «тяга была». Причина: снимок обязан
   быть синхронен модели и без тяги — иначе следующая же тяга посчитает дельту
   от отменённого размера. Пересъём делается вызовом существующего
   `_start_resize(node_id)`: он снимает ручки, переснимает все пять полей снимка
   (bbox/центроид/площадь/пины/маршруты) и ставит ручки на восстановленную рамку.
2. `undo()`/`redo()` — зовут пересъём вместо частной проверки «узел исчез»
   (проверка сохранена внутри пересъёма).
3. Хвост §52.30: `_stop_resize` теперь чистит `_resize_start_pins`
   и `_resize_start_routes` вместе с остальными полями снимка.

**Граница правки заявлена явно:** прямой `set_mode` мимо `_stop_resize` НЕ
трогался (`QUEUE` 10-1: сегодня это единственный безопасный выход из режима
и единственный чистый контроль ревизора); ресайз на оверлей не переносился;
`ResizeNodeHandler`, `advanced_graph_editor.py` и `resize_overlay.py`
не менялись — `resize_node` в advanced не перерегистрирован, правка базового
редактора исполняется в обоих.

**Следствие, заявленное отдельно:** undo/redo ВНУТРИ режима теперь пишут
в статусную строку подсказку ресайза поверх «Undo: …» (цена переиспользования
`_start_resize`). Данных не касается, во вкладках проверено набором.

### 64г. Зонды — что краснит каждая точка правки

| зонд | что сломано нарочно | результат |
|---|---|---|
| И5 | пересъём снят из `undo`/`redo` — «слепое доверие снимку» вернулось | **11 failed** — все 11 мои (включая контроль redo), чужих **0** (332 passed) |
| И6 | прежний overlay брошен без `hide()` (риск второй четвёрки ручек поверх живых) | **343 passed** — сегодня недостижимо: `_redraw_all` зовут и чужие команды, живых ручек к моменту пересъёма не остаётся. Утверждение теста «ровно 4» в сторону удвоения зондом НЕ доказано, записываю как есть |
| И7 | снята ветка «узел снесён undo/redo» | **1 failed** — ровно свой тест. ⭐ Первая редакция теста (судила только по ручкам) осталась ЗЕЛЁНОЙ: ранний выход из `_start_resize` прячет ручки тем же движением. Тест усилен суждением по ДАННЫМ (после отмены жест `add_edge` обязан дать ребро — приём из 1.6), и только тогда зонд покраснел. `PROTOCOL §5` «зонд не покраснел — сначала подозревай обстановку теста» отработал буквально |
| И8 | гард `item.scene() is self._scene` в `resize_overlay.hide()` (первая редакция правки) | **41 passed, предупреждений Qt 0**; отдельный скрипт: повторный `removeItem` по уже снятому item в PySide6 6.x молчит и не падает. Гард наблюдаемой пользы не даёт — **откачен, в диффе его нет** |
| И9 | хвост §52.30 возвращён (снимки пинов/маршрутов не чистятся) | **343 passed** — хвост сегодня не наблюдаем (мусор заперт гардом `start_bbox=None`), закрыт как гигиена, а не как дефект |

### 64д. Гейты доработки (объём назван ревизором, узкий)

```
python -X utf8 -m pytest tests/ui/test_gesture_dispatch.py -q  -> 41 passed (29 + 12 новых)
python -X utf8 -m pytest tests/ui -q                           -> 343 passed, 13 skipped
python -X utf8 tools/lint_gate.py --check                      -> exit 0 (BLE001/E722 174 при эталоне 178, mypy 0)
python -X utf8 tools/suite_baseline.py --check                 -> exit 0, собрано 1064
                                                                  (= 1052 ревизии + 12 моих), failed 13 / errors 9 — ровно пол
```

### 64е. Расхождения и находки строкой

| # | что | дата | результат |
|---|---|---|---|
| 64.9 | «`_redraw_all` зовёт только `ResizeNodeCommand`» — моё утверждение первой редакции комментария | 2026-08-19 | **опровергнуто зондом И5**: `DragNodeCommand._apply_state` (`advanced_commands.py:157`) зовёт тот же `_redraw_all`, поэтому ручки сносит и ЧУЖАЯ отмена. Комментарий и докстринг теста исправлены по замеру |
| 64.10 | обе находки первого захода остались нетронутыми и адресованы 10-1 | 2026-08-19 | потерянный `release` при честной тяге (гард `:1733` overlay не видит) и прямой `set_mode` мимо `_stop_resize`. Второй сознательно НЕ чинился: ревизия назвала его единственным сейчас безопасным выходом после Ctrl+Z (§63.16) |
| 64.11 | контроль ревизии §63.15 (redo) заперт тестом | 2026-08-19 | Ctrl+Z → Ctrl+Shift+Z → Esc: модель остаётся в состоянии ПОСЛЕ ресайза, стек 1, ручки на рамке. Под зондом И5 краснеет — контроль не пустой |

---
## 66. Пункт 1-30 дороги (GATE-5) — четвёртый стенд и хвосты трёх исходов (2026-08-19)

Строка 1-30 — ВОСЕМЬ пунктов, собранных из находок 1-28, ревизии 1-28, долгов 1.3/1-12
и кандидата 1-29 (§53.35). Три стенда научены различать три исхода пунктами 1-25/1-26/1-28;
здесь закрывается четвёртый (`edit_bench`) и хвосты у остальных — на пути ЧТЕНИЯ и на семье
«замер не состоялся».

Среда: Windows 10, **системный** `python` 3.11.9 (не `.venv311` — там нет pytest,
`PROTOCOL §5`), ветка `road/GATE-5` от `dev` (`8346989`), рабочее дерево
`C:\project\pid_wt_1.4`. ⛔ **Полного корпуса в этом дереве нет**: `storage/` пуст,
`corpus.corpus_paths()` отдаёт ровно 3 графа из git (`089feca2`, `6e7144d5`, `d74eb9f1`),
эталон ПР1 покрывает 19. Ни один вывод ниже на полный корпус не опирается; там, где он
нужен, стоит честная строка «не измерено». Копии файлов до инъекций — в скрэтчпаде сессии,
восстановление **копией**, не `git checkout` (`PROTOCOL §5`).

### 66а. Адреса и утверждения строки перепроверены по коду (шаг 2 жизненного цикла)

| # | что | команда | дата | результат |
|---|---|---|---|---|
| 66.1 | `tools/edit_bench.py:185` — тот самый `if args.write_baseline:` | `sed -n '185p' tools/edit_bench.py` | 2026-08-19 | сошлось **дословно**, включая номер строки. Ветка записи `read_baseline()` не звала вовсе |
| 66.2 | у `suite_baseline --check` третьего исхода нет | чтение `cmd_check` | 2026-08-19 | подтверждено: `return 1 if bad else 0`, а `_measure(unjudged=1)` отдавал на сломанном сборе ту же единицу |
| 66.3 | «git не ответил» на записи — ОТКАЗ | чтение `floor_problems` → `write_blocked` | 2026-08-19 | подтверждено: строка уходила в `problems`, а `write_blocked` перекладывала весь `problems` в `refused` → код 1 |
| 66.4 | долг ruff «оплачен до 177 при эталоне 178» | `lint_gate --check` | 2026-08-19 | **число протухло**: долг уже **174** в 71 файле, эталон 178. Оплачены ТРИ файла, а не два: `frame_tab 3→2`, `ocr_binding_tab 8→6`, `diagram_workspace 12→11`. Протухло ≠ опровергнуто: строка верна по сути (эталон отстал), число уехало от чужой работы, влитой в `dev` |
| 66.5 | базовое состояние гейтов до правки | `suite_baseline --check`, `lint_gate --check` | 2026-08-19 | обе exit **0**: собрано **1064** (git-видимых 1064 в 98 файлах), красных 13 failed + 9 errors; ruff 174/178, mypy 0 |

### 66б. Каждый дефект ЗАМЕРЕН до правки — старый код против нового

Старый код брался как `git show HEAD:tools/<стенд>.py` во временный файл В ДЕРЕВЕ (иначе
`REPO = parents[1]` смотрел бы не туда), новый — рядом; обстановка у обоих одна и та же.

| # | дефект | обстановка | старый код | новый код |
|---|---|---|---|---|
| 66.6 | (а) пересъём «Ручной правки» не читает эталон | корпуса в дереве нет (`tools/bench/edit_corpus/` отсутствует) | ветка `--all` отбивала прогон раньше — **exit 2**, дыра снаружи не видна | то же **exit 2**, эталон **бит-в-бит** цел. Сама дыра доказана на подменённом корпусе, §66.20 |
| 66.7 | (б) у чтения нет третьего исхода | подложен `tests/test_zzz_probe_1_30.py` с `raise ImportError` (сбор сломан) | `сбор pytest сломан (exit 2) — это пункт 0.0, а не база`, **exit 1** — тот же код, что «новый красный» | `[СУДИТЬ НЕЧЕМ] сбор pytest сломан (exit 2) …`, **exit 2** |
| 66.8 | (е) вердикты пересъёма шли в stderr | та же обстановка, `2>/dev/null` | stdout **пуст** — сообщение ушло в stderr | сообщение в **stdout**, как у ПР1 и ПР5 |
| 66.9 | (в) «git не ответил» = отказ | `REPO` подменён на каталог вне git (`git ls-files` → 128) | `floor_problems` вернул **2 провала** («git не ответил» + «набор усох: тихо потеряно 20»), `write_blocked` → **код 1 (ОТКАЗ)** | 0 провалов, **1 «судить нечем»**, `write_blocked` → **код 2** |
| 66.10 | (г) убитый ruff | в `pyproject.toml` подменено правило `BLE001` → `NOSUCHRULE001`, `ruff` возвращает **2** | **трейсбек** `RuntimeError: ruff вернул 2` и **exit 1** | `[СУДИТЬ НЕЧЕМ] замер линтеров не состоялся: ruff вернул 2 …`, **exit 2**, трейсбека нет. На `--write-baseline` — то же 2, эталон бит-в-бит цел |
| 66.11 | (г) убитый дочерний прогон ПР1 | в `child_main` вставлен `raise SystemExit(77)` (в обе копии стенда) | **трейсбек** `RuntimeError: прогон (seed=1) вернул 77` и **exit 1** | `[СУДИТЬ НЕЧЕМ] замер не состоялся: …`, последняя строка `unjudged=environment`, **exit 2** |
| 66.12 | (з) слабый замер на ЧТЕНИИ | временный эталон из 3 графов git, `runs 6`, `d74eb9f1` помечен неповторимым; прогон `--git-only --runs 2 --check` | `[стало лучше] воспроизводятся впервые: d74eb9f1` + `[OK] регресса воспроизводимости нет`, **exit 0** | `[ВЫБОРКА, НЕ ПОЧИНКА] … «воспроизводятся впервые» из него не следует: d74eb9f1`, `[СУДИТЬ НЕЧЕМ] замер слабее эталона: прогонов 2 против 6`, `unjudged=environment`, **exit 2** |
| 66.13 | (г, шире записанного) git не запустился вовсе | `subprocess.run` подменён на `FileNotFoundError` | **обе** функции (`suite_baseline.tracked_by_git`, `lint_gate.tracked`) падали `FileNotFoundError` мимо проверки `returncode != 0` → трейсбек, exit 1 | «git не запустился: …» в причине «судить нечем» / `RuntimeError`, который ловит `main()` → **exit 2** |

### 66в. Положительные контроли — «судить нечем» не стало новым молчанием

| # | что | дата | результат |
|---|---|---|---|
| 66.14 | ПР1, замер НЕ слабее эталона | 2026-08-19 | `--git-only --runs 6 --check` по временному эталону из 3 графов — **exit 0**, вердикт вынесен (5.5 с) |
| 66.15 | ПР1, реальный эталон, форма CI | 2026-08-19 | `--git-only --runs 10 --check` — **exit 2**, последняя строка `unjudged=corpus`, тело шага CI → `::warning` и **exit 0** (как и до правки) |
| 66.16 | ПР5, живой ruff после восстановления конфига | 2026-08-19 | `lint_gate --check` **exit 0**, 174/178, mypy 0 |
| 66.17 | база набора, здоровое дерево | 2026-08-19 | `suite_baseline --check` **exit 0**, собрано **1089**, красных 13 failed + 9 errors без изменений |
| 66.18 | тело шага CI прогнано на всех ЧЕТЫРЁХ исходах | 2026-08-19 | 0 → **0**; 1 → **1**; 2 + `unjudged=corpus` → `::warning` и **0**; 2 + `unjudged=environment` → **2** (красное). Именно эта четвёртая строка и есть цена честной двойки: без разбора причины убитый прогон проехал бы зелёным |

### 66г. Д1 — тесты уровня дефекта

| # | что | дата | результат |
|---|---|---|---|
| 66.19 | объём набора стендов | 2026-08-19 | 87 → **112** собранных в пяти файлах (`test_edit_bench` 7→13, `test_lint_gate` 14→20, `test_layout_determinism` 20→34, `test_suite_baseline` 39→43 по `def test_`, `test_collection_clean` 2) |
| 66.20 | те же тесты на стендах уровня предыдущего коммита | 2026-08-19 | **42 failed из 112** (стенды откачены копией `git show HEAD:`, тесты новые, потом восстановлены копией) |
| 66.21 | порог `--runs` заперт с двух сторон АБСОЛЮТНЫМИ числами | 2026-08-19 | эталон 6: `--runs 5` → «судить нечем», `--runs 6` и `7` → вердикт. Ни одно число не вычисляется из проверяемого поля (урок 0.4) |
| 66.22 | приоритет «доказанное сильнее неполноты» проверен на всех четырёх стендах | 2026-08-19 | `edit_bench`: рост + усечённый корпус → **1**; `suite_baseline`: новый красный + убитый прогон → **1**; ПР1 чтение: поломка + слабая выборка → **1**; ПР1 запись: поломка + малый `runs` → **1** (тест 1-29 не тронут) |
| 66.23 | обратные полярности, чтобы правка не была односторонней | 2026-08-19 | у каждого из восьми — свой зелёный путь: пересъём «Ручной правки» по всему эталону пишет файл; `--check` с новым красным по-прежнему **1**; молчащий git не глушит пол по файлам НА МЕСТЕ («тихо потеряно 30» — по-прежнему провал); живые линтеры судят; `--runs 6` выносит вердикт |

### 66д. Д6 — пересъём эталона ruff (отдельный коммит, пункт «ж»)

| # | что | дата | результат |
|---|---|---|---|
| 66.24 | окно Д6 | 2026-08-19 | оба дерева чисты, чужих незакоммиченных правок нет (`PROTOCOL §Д6`); пересъём сделан ПОСЛЕ коммита кода, отдельным коммитом — красный флаг №4 |
| 66.25 | дифф эталона | 2026-08-19 | ровно **4 строки**: `total 178 → 174`, `frame_tab 3→2`, `ocr_binding_tab 8→6`, `diagram_workspace 12→11`. Ни один файл не вырос |
| 66.26 | гейт после пересъёма | 2026-08-19 | `lint_gate --check` **exit 0** (174/174, mypy 0); повторный пересъём отдаёт файл **бит-в-бит** равный закоммиченному |

### 66е. Находки строкой, не правкой

| # | что | дата | результат |
|---|---|---|---|
| 66.27 | «[стало лучше] воспроизводятся впервые» вводит в заблуждение и на ПОЛНОМ замере | 2026-08-19 | замер §66.14: при `--runs 6` — том самом поле, что ввёл 1-29, — `6e7144d5` (эталон числит его неповторимым, §32.7) вышел с одним хешем, и чтение напечатало «воспроизводятся впервые». Тот же класс, что §50.19. На запись он не влияет (там `false` липкий), на код возврата чтения тоже, поэтому правкой не тронут: липкость на ЧТЕНИИ — решение 1-29, переигрывать его исполнителю нельзя |
| 66.28 | `MAX_FIXED` и «красный превратили в skip» оставлены в исходе 1 | 2026-08-19 | оба — суждения о СОСТОЯВШЕМСЯ замере («позеленело слишком много», «красный стал skip»), а не о сломанной обстановке, поэтому в «судить нечем» не переводились. Граница осознанная и записана в `TESTING §7.1` |
| 66.29 | корпус «Ручной правки» в этом дереве недоступен | 2026-08-19 | `tools/bench/edit_corpus/` нет вовсе (данные только в главном дереве, занятом чужой сессией). Живой прогон по 20 холстам не делался; вердикт стенда на них не снимался ни до, ни после правки — ни один вывод пункта на них не опирается |

## 67. Пункт 1-17 дороги (1.20) — липкий флаг `_check_masks_completion` (2026-08-19)

**Обстановка.** worktree `C:\project\pid_wt_rev2`, ветка `road/1.20` от `dev`.
Системный `python` 3.11.9, PySide6, `QT_QPA_PLATFORM=offscreen`. Область:
`ui/widgets/diagram_workspace.py`, новый тест
`tests/ui/test_masks_completion_once.py`, `docs/UI_GUIDE.md §3.4/§3.6`.
Сети, docker, Redis, `storage/` и сервер не трогались — сервер подставной,
правок в `app/` нет.

| # | что | дата | результат |
|---|---|---|---|
| 67.1 | Адреса пункта сверены по коду (шаг 2) | 2026-08-19 | подтверждены со сдвигом: `:1327` мастер-плана → **`:1352`** (`self._check_masks_completion()` последней строкой `_close_active_tab`, безусловно, при ЛЮБОМ `tab_key`); `:1410` → **`:1405`** (сам метод). Сдвиг ожидаем: 1.3 (`00ce8d5`) вырезал из того же метода `_rollback_if_validating` |
| 67.2 | Липкость флага замерена по всем точкам | 2026-08-19 | `_pipe_confirmed` поднимается в `_detect_saved_mask:1403` и `_on_pipe_confirmed:2101`, снимается ТОЛЬКО в `__init__:499` и `load_diagram:653` — то есть при заходе в другую диаграмму. Внутри сеанса не снимается никогда. Утверждение пункта подтверждено |
| 67.3 | Ловушка `PLAN_AUDIT §Этап 1` подтверждена | 2026-08-19 | сам `_check_masks_completion` ничего не спрашивает; «отказ» — это ответ «Нет» в диалоге о несохранённом (`_close_active_tab:1324-1340`), после которого путь идёт дальше и упирается в липкий флаг |
| 67.4 | Чем оборачивается лишняя команда на сервере | 2026-08-19 | `app/api/validation.py:406-417` принимает 10 статусов, `:481-491` считает «ушли вперёд» 9 из них — `validating_masks`, `skeletonized` и **`validated_masks` в этот список НЕ входят**, значит повторная команда в этом окне диспатчит `task_skeletonize_simple` заново (`:499-503`). На `validating_junctions` — 400, который клиент глотает в `except Exception:1423` |
| 67.5 | ТЕСТ ДО ПРАВКИ — три жеста оператора, числа абсолютные | 2026-08-19 | `pytest tests/ui/test_masks_completion_once.py -q` → **3 failed / 3 passed**. «Подтвердил трубы → перекрёстки → правил → «Назад» → **Нет, не сохранять**»: команд **2** вместо 1 (вторая на `validating_junctions`). «Три чистых выхода из чужой вкладки»: команд **4** вместо 1. «Подтвердил → тут же зашёл посмотреть → вышел»: скелетизация диспатчена **2 раза** вместо 1 (вторая команда на `validated_masks`) |
| 67.6 | Что уже было зелёным и обязано остаться | 2026-08-19 | 3 passed до правки: подтверждение отправляет ровно одну команду; двойная страховка (`_confirmed` у вкладки, сигнал потерян) отправляет её при закрытии вкладки труб; откат этапа + повторное подтверждение отправляет вторую. Это порог с другой стороны — правка не имеет права их погасить |
| 67.7 | Правка | 2026-08-19 | 5 кусков в одном файле, +26/−4: одноразовый замок `_masks_completion_sent` (объявление в `__init__`, сброс в `load_diagram`, проверка+взвод в `_check_masks_completion`, снятие в `_on_pipe_confirmed`) и сторож по вкладке `if tab_key == "pipe"` в хвосте `_close_active_tab`. `_pipe_confirmed` НЕ тронут — его читают бусина (`:924`), гифка (`:943`) и кнопка (`:999`), пока сервер не доехал до `validated_masks` |
| 67.8 | Гейт после правки | 2026-08-19 | тот же файл — **6 passed за 0.45 с** |
| 67.9 | Зонд И1 — снять одноразовый замок, сторожа по вкладке оставить | 2026-08-19 | **1 failed / 5 passed**: краснеет `test_second_look_into_pipe_tab_does_not_redispatch` (скелетизация 2 раза). Красное СВОЁ — мой тест в моём файле, не сосед (`PROTOCOL §5`). Замок доказан |
| 67.10 | Зонд И2 — снять сторожа по вкладке, замок оставить | 2026-08-19 | **6 passed, ЗЕЛЁНЫЙ**. Сторож по вкладке независимым зондом НЕ доказуем: `_pipe_confirmed` поднимается ровно в двух местах, и оба немедленно ведут к отправке, поэтому состояние «флаг поднят, команда не отправлена, закрывается чужая вкладка» кодом недостижимо. То есть сторож — второй эшелон, а не второй дефект; заявлен в отчёте отдельно, снимается одной строкой |
| 67.11 | Восстановление между зондами | 2026-08-19 | КОПИЕЙ файла (`scratchpad/dw_1_20.bak`, 111902 байта), не `git checkout` (`PROTOCOL §5`). После восстановления дифф тот же: 26 вставок / 4 удаления, 6 passed |
| 67.12 | Базовые гейты | 2026-08-19 | `pytest tests/ui -q` → **349 passed / 13 skipped** (было 343 + 13, рост ровно на 6 моих); `python tools/lint_gate.py --check` → **exit 0** (ruff BLE001/E722 174 нарушения в 71 файле при эталоне 178, mypy 0); `python tools/suite_baseline.py --check` → **exit 0**, собрано **1070** при поле 973, красных 13 failed + 9 errors — ровно как в эталоне; `pytest tests/test_docs_match_code.py` → 14 passed / 2 skipped |
| 67.13 | Счётчик BLE001 по моему файлу | 2026-08-19 | **12 → 11 показывает `lint_gate`, но это НЕ моя правка**: `except Exception` в файле как было 13 на `HEAD`, так и осталось 13 после правки. Пол в эталоне протух до меня (та же картина у `frame_tab.py` 3→2 и `ocr_binding_tab.py` 8→6 — следы 1-24 и 1-12). `--check` при этом exit 0: пол — минимум, снижение его не роняет |

**Что НЕ делалось:** Д6-пересъём эталонов (запрещён при чужих правках рядом —
`PROTOCOL §Д6`, параллельно работают две сессии); корпусные стенды (корпус вне
git и вне области `[ui]`-пункта); docker/Redis; сборка PyInstaller; **правки
в `app/api/validation.py`** — см. находку ниже.

**Находки мимо пункта (кода не трогал):**
- ⚠ Гонка двойной скелетизации закрыта только со стороны UI. `PLAN_AUDIT §Этап 6`
  (п. 6.6) называет три ноги сразу: липкий вызов клиента (закрыт здесь), дыра
  `already_past` в `app/api/validation.py:481-491` и гейт задачи
  `worker/tasks/skeleton.py`, пускающий `SKELETONIZING_FINAL`. Две оставшиеся —
  не `[ui]` и не мой пункт.
- `UI_GUIDE §3.6` называет сигнал вкладки труб `pipe_confirmed`; в коде он
  `confirmed` (`ui/tabs/pipe_tab.py`), а `pipe_confirmed` — имя поля воркспейса.
  Дрейф доки в строке, которую я правил рядом; по `PROTOCOL §4` не трогал.
- `UI_GUIDE §3.4` (список шагов `_close_active_tab`) продолжает утверждать шаг
  «вызывает `tab.cleanup()`», которого в коде нет — находка §61.14, уже
  адресована пункту 10-8. Мой шаг 7 дописан рядом и этой ошибки не касается.