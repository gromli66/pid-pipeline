[pytest]
testpaths = tests
# collect_ignore здесь не действует — это переменная conftest.py, а не опция
# ini (pytest ругался PytestConfigWarning: Unknown config option). Исключения
# сбора живут в tests/conftest.py.
