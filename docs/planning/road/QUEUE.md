# Очередь исполнения — дорога 2026-08-17

Порядок выведен заново из мастер-плана (167 пунктов извлечены и сверены с кодом 2026-08-17,
см. `PLAN_AUDIT.md`) по четырём правилам самого плана: (1) сначала опора — гейты и стенды,
без них «без поломки» недоказуемо; (2) затем кровь — падения и порча данных; (3) сведение
дублей — непосредственно перед фронтом, который их правит; (4) перемещение файлов — последним.

**Правило волн:** волна N начинается, когда её замки (⛔) закрыты. Внутри волны порядок
сверху вниз; пункты без стрелок можно брать в любом порядке после жёстких. Один пункт =
одна сессия исполнителя. Класс/гейт — по таблице в `PROTOCOL.md`.

Обозначения: ⚠ — правит бой или массовая правка, после неё обязателен чат-ревизор;
⛔ — замок (не начинать, пока не закрыт); ✅ — сделано до создания дороги, проверить и закрыть
на доске; [класс] — класс изменения из `PROTOCOL.md`.

**Колонка «модель»** — какой моделью запускать чат-исполнитель пункта:
- **Opus 5** — дефолт для всех пунктов. Запасной вариант при лимитах — Opus 4.8
  (допустим на [док]/[снос]/[infra]; на ⚠ и [сма]/[гео] — только Opus 5).
- **Fable 5** — 13 архитектурно тяжёлых пунктов (кэш моделей, transitions, базовая
  задача+диспетчеризация, посадка Г5б, дедуп рёбер, запись концов, разбор AGE и god-функций,
  seat-rect, graph_to_fxml): там решения, а не механика.
- Чат-ревизор — всегда Fable 5 (проверяющий не слабее проверяемого); диспетчер — можно Opus 4.8.
- Режим: пункты с ⚠ начинать в **план-режиме** (план правки → ваше утверждение → код);
  полный auto-accept разрешений не давать никому.
- **Размышления** — рекомендованный режим стоит в колонке «модель» у каждого пункта:
  `обычн.` / `расшир.` (дописать «думай тщательно») / `ultrathink`. Включается Tab'ом
  в Claude Code или словом в конце промпта. Ревизор — всегда ultrathink, диспетчер — обычный.
  Максимум всем подряд НЕ включать: на механике длинные размышления провоцируют
  «улучшить заодно» (красный флаг №6).

## ⛔ Открытые решения Максима (напоминания продублированы в своих волнах)

| # | вопрос | когда решать |
|---|---|---|
| Р-2 | Канон seat-rect: **A** — свести к серверному `seating.py` с явной развилкой, **B** — перевести сервер на редакторский контракт | вход в волну 7 (запирает 7-3 и 8-3) |
| Р-4 | 4б: подтвердить типографику (кегль 18, жирность ~0.3, диаметры не печатать) по двум рендерам, когда СТ7 восстановит числа | волна 4, перед 4-10 |
| Р-5 | `_breaks_anchor`: оживить или снести — по замеру «сколько узлов корпуса несут пин» | волна 10, пункт 10-12 |
| Р-6 | Биндинг: дожать миграцию или снести `engine.py` (снятие лжи в 3 местах решения не ждёт) | вход в волну 13 |

Решённые: **Р-1** (2026-08-17: всё на dev + локальный docker, deploy заменяется финальным
merge — см. §«Решения приняты») · **Р-3** (снят аудитом: развилки PIN_TOL в коде нет) ·
**Р-7** (2026-08-18: вариант A — очередь остаётся как написана; профиль правок оператора —
MEASUREMENTS §29, журнал доски).

---

## Волна 0 — Опора (этап 0 + ВН1а)

Цель: тесты исполняются, базовая линия зафиксирована, корпус в git, данные прочитаны
ДО того, как их удалять или чинить то, чем оператор не страдает.

| # | ID | что | предусловия | класс → гейт | модель |
|---|---|---|---|---|---|
| 0-1 | 0.0 ✅ | ЗАКРЫТ (road-0.0, ea9bbda; ревизия ПРИНЯТЬ 2026-08-17). Диагноз «2 битых файла tests/ui» опровергнут исполнителем: виновник — модульные заглушки `sys.modules` в `test_stage7_graph_flow`, отравлявшие весь сбор. Добавлен сторож `tests/test_collection_clean.py` | — | [infra] `pytest --collect-only` без ошибок — 732/0, воспроизведено ревизором | Opus 5 · обычн. |
| 0-2 | 0.3 ✅ | ЗАКРЫТ (road-0.3 → 302aeba; ревизия ВЕРНУТЬ→ПРИНЯТЬ). База 57 красных в git (`tools/bench/suite_baseline.json`), гейт «ни одного нового красного» с тройной защитой от убитого прогона; CI зелёный; пересъём базы — отдельным коммитом (Д6) | — | [infra] `suite_baseline.py --check` exit 0, CI run 32061251949 — воспроизведено ревизором | Opus 5 · обычн. |
| 0-3 | 0.6 | Починить доки, портящие рассуждения: «OCR выключен» ↔ `enabled: true`, таймауты в WORKER_TASKS.md втрое занижены. + дрейф TESTING.md из ревизии 0.0: §1 «все тесты без Docker/GPU/БД» — замеренно ложно; хвосты §4/§6 — в журнале доски | — | [док] каждая правка доки сверена с кодом, команда в MEASUREMENTS.md | Opus 5 · обычн. |
| 0-4 | 0.1 ✅ | canvas_to_fxml + contours_merge закоммичены (4039d1a, 8f99544, тесты в git) | — | закрыть на доске: их тесты зелёные | Opus 5 · обычн. |
| 0-5 | ВН1а ✅ | ЗАКРЫТ (road-ВН1а, 4df6b69+c69c1e0). ⛔ Дефект ВН1 «труба вырезается из pipe_mask» ПОЧИНЕН ещё a1e0e14 (2026-06-22, и на deploy): фича внедрена целиком, отработала на 16 схемах (722 бокса, 721 с направлением). Доки переехали: `docs/NAPRAVLENIE.md` + `NAPRAVLENIE_E2E_CHECKLIST.md` — старый чек-лист v1.0 искал несуществующие `type=="direction"`/`dirnode_*` | — | [док] сторож доков 16 passed; `--check` exit 0 | Opus 5 · обычн. |
| 0-6 | 0.9 ✅ | ЗАКРЫТ (road-0.9, 2b6302d). Профиль правок оператора (144 намерения, 10 холстов): перемещение узла 67 %, посадка конца 26 %, размер 5 %, ±ребро 3 %, ноль добавлений узлов; машинная цена схемы p50 484 с, 52 % машинного времени — перезапуски, OCR 37 %; `cvat_validation.duration` занижает ожидание в 31 раз. **Р-7 решён: вариант A** — очередь не двигается | — | [данные] MEASUREMENTS §29; `--check` exit 0 | Opus 5 · обычн. |
| 0-7 | 0.2 ✅ | ЗАКРЫТ (road-0.2, 92d759e+e0d7ec7). Производитель починен (`base_graph_editor.py:282` — абсолютный путь по идиоме проекта); корпус перенесён в `tools/bench/edit_corpus/` (sha256 20/20), глоб поправлен у ВСЕХ ТРЁХ потребителей; эталон судьи 12→20 файлов отдельным коммитом (Д6). Прим.: подсказка «git rm wave14_smoke.log» была ошибкой аудита — файл в индексе не был никогда (`.gitignore:43`) | — | [ui] `edit_bench --all --check` exit 0 на нетронутом эталоне, затем после пересъёма | Opus 5 · расшир. |
| 0-8 | 0.8 ✅ | ЗАКРЫТ (road-0.8, 1073061+3bd604c). 3 фикстуры в git (sha 3/3 в свежем клоне; ловушка `.gitattributes` eol=lf найдена и закрыта), загрузчик КД7 — `tools/corpus.py`, floor 714→757 (Д6 отдельным коммитом), CI видит 3 корпусные проверки. ⚠ Остаток зам. №7: CI видит 3 графа из 17 — для пунктов по `modules/graph`/`canvas` локальный прогон полного корпуса ОБЯЗАТЕЛЕН сверх CI | — | [infra] worktree-замер: корпусных проверок 0→3, CI run 32109234907 | Opus 5 · обычн. |
| 0-9 | 0.11 ✅ | ЗАКРЫТ (road-0.11, 2f7e515). Стенд `tools/pair_bench.py` — 106 пар «до/после оператора» на 17 схемах, эталон в git, `--check` с зондами. ⭐ Замер: сегментация правится 17/17 (медиана +66171/−19619 px, +26 % площади — модель труб систематически НЕДОРИСОВЫВАЕТ); детекция +77 боксов против 5 снятых (весь дефицит — пропуски, не ложные); контуры SAM2 — ноль правок на 28 узлов при 48 в `contours_auto` (оператор не дошёл, это не качество); граф/OCR/перекрёстки — медианы ~0. Оговорка плана «пара OCR только после id v3» частично опровергнута: `edited_blocks` v2 хватает. ⚠ В CI стенда нет (корпус не в git) — `--check` там честно отдаёт 2 «судить нечем» | — | [infra] `pair_bench --check` exit 0 + 2 зонда; числа в MEASUREMENTS §33 | Opus 5 · обычн. |
| 0-10 | 0.10 ⚠частично | ЗАКРЫТ по ПР4+ПР5 (road-0.10 → 9652557): первый в истории проекта конфиг линтеров (`pyproject.toml`, ruff BLE001/E722 + mypy) с храповиком `tools/lint_gate.py` — долг широких except зафиксирован 199 в 73 файлах и расти не может; `MEASUREMENTS.md` взят в git как есть (573 строки). **ПР1 замерен, но не починен:** недетерминированы 8 графов из 17, виновник — vendored libavoid, не наш код; ⭐ счётчики дефектов при этом СТАБИЛЬНЫ во всех прогонах (156→11, 13→0, 70→4 …) — шумит только побайтовое сравнение. Стенд `tools/layout_determinism.py` с эталоном по графам. Починка/обход — вопрос ВН3 (волна 9) | — | [infra] `lint_gate --check` exit 0; `layout_determinism --check` exit 0 (оба с зондами) | Opus 5 · обычн. |
| 0-11 | 0.4 ✅ | ЗАКРЫТ (road-0.4, b649047). `tests/ui/test_gesture_dispatch.py` — 19 тестов на диспетчер нажатия, на инвариантах ДАННЫХ (ручки считаются сканом сцены по `HANDLE_Z`, чтобы пережить перенос ресайза в 10.1); данные — корпус-фикстура `d74eb9f1` через `tools/corpus.py`, поэтому набор идёт в CI. ⚠ Область сузилась ОСОЗНАННО: редакторы масок (`square`/`polyline`) не покрыты — их геометрия идёт через cv2, замоканный на уровне модуля в `tests/observability` (те самые 11 из 57 красных); сетка под них достраивается после 0.3x. Замерены две особенности кода (не дефекты, кода не трогали): `mapFromScene` округляет до целого пикселя вьюпорта (масштаб 0.33 → до 1.17 px по сцене, отсюда допуск 4 px) | ⛔ 0.0 ✅ | [ui] 19 passed; ноль утверждений про `_current_mode` (проверено грепом); инъекция порога красит тесты с двух сторон | Opus 5 · расшир. |
| 0-12 | 0.12 ✅ | ЗАКРЫТ (road-0.12 → 6935fe2). Стек поднимается локально, диаграмма `3263039b` прошла конвейер end-to-end до FXML. Диаграмма первого прогона снесена по решению Максима (БД каскадом + каталог). ⚠ Прогон шёл на **GPU** и на прогретых воркерах — для волны 2 это НЕ базовая линия (см. замки в строке СТ2). Дефект `GET /api/projects/`: список проектов ПУСТ на всех вызовах после первого (кэш `@lru_cache` живёт в процессе API, травится любым `load()` этапа конвейера) — решением Максима 2026-08-18 сейчас не чинится, вписан в 13-5 | — | [infra] один uid прошёл все этапы; гейты после сноса зелёные, CI run 32128346006 | Opus 5 · обычн. |
| 0-13 | ВН1б ✅ | ЗАКРЫТ (road-ВН1б, 908ef63+8966329). Прогон в рабочей среде: uid `0fc9d04c`, 13 стадий из 13 за 126 с, артефакты 16/16; заведён аудитор `tools/napravlenie_audit.py --check` с тремя зондами. ⛔ Опровергнуто: «133 бокса степени 0» — число снято с поля `degree`, которое ПРОТУХАЕТ после редактора; по рёбрам сирот **96**. Ложная тревога плана §4.2 (68 боксов в `node_mask`) — накрытие рамкой соседа, своей площади ноль из 766. ⭐ Корень найден: из 96 сирот **85 восстановимы однозначно** — труба лежит на `pipe_mask` вплотную к осевой грани; теряется не в сегментации и не в постпроцессе, а между маской и трассировкой (скелет обрывается в 3–10 px от грани, контакт ищется кольцом 1 px) | — | [данные] `napravlenie_audit --check` exit 0 + 3 зонда; MEASUREMENTS §35 | Opus 5 · обычн. |
| 0-18 | 0.3y ✅ | ЗАКРЫТ (road-0.3y, 7411438). Пол базы теперь ВЫЧИСЛЯЕТСЯ: `count_local_corpus` вычитает параметры по корпусу вне git (uid8 из того же загрузчика `tools/corpus.py`, что у `layout_determinism --git-only`). Локальное число git-видимых совпало с раннером символ в символ (860 = 860), запас сжат 18 → 5. Дефект показан замером ДО правки: 3 диаграммы в `storage/` → старый пол 861 > 860 = ложный красный. Оба зонда 0.3 перепроверены прогоном (усыхание, потеря shapely) | — | [infra] `--check` exit 0 после пересъёма (Д6 отдельным коммитом); CI run 32142985248 | Opus 5 · обычн. |
| 0-17 | 10.3 ✅ | ЗАКРЫТ (road-10.3, 3d1e574). Снесены три оконных двойника (1332 строки), `main_window.py` жив. ⭐ Красных базы **46 → 22**, широких except 199 → 191. ⛔ Расхождения: (1) «чистые grep-тесты сохранить» выполнимо наполовину — из 11 верны 6, пять держались за флаг `_simple_graph_done`, которого в `diagram_workspace.py` НЕТ И НЕ БЫЛО (`git log --all -S` пуст), они сняты; (2) греп адресов пропустил `tests/test_refactoring.py:695` — путь к окну собран из кусков, литерала «ui/windows» в строке нет, поймал гейт | — | [снос] `import app.main` + сбор 858/0 + PyInstaller exit 0 + 8 вкладок открываются; CI run 32152214198 | Opus 5 · расшир. |
| 0-14 | 0.5 ✅ | ЗАКРЫТ (road-0.5, 9e30e6a). Один `ArtifactDownloader` вместо четырёх + слиты два `_RecognizeWorker`; 53 характеризационных теста зелены И на старом коде, И на новом; бит-в-бит на реальном uid (5 конфигураций вкладок, 22 артефакта — diff пуст). ⭐ Сохранено явно то, что «сведение в один стиль» тихо испортило бы: графовая вкладка глотает у необязательных артефактов только `APIError`, остальные — всё подряд; политика вынесена полем `swallow` у `Job` и заперта двумя инъекциями. Опровергнуто до кода: наследование от `BaseGraphTab` (9 строк против чужого контракта), «6 копий» (их 4), `AppearanceMixin` (уже един) | — | [реф] бит-в-бит + 8/8 вкладок + `edit_bench`/`pair_bench` exit 0; CI run 32162392791 | Opus 5 · расшир. |
| 0-15 | 0.7 ✅ | Сведение ID плана — план помечает выполненным 2026-08-14 | — | закрыть на доске со ссылкой на раздел плана | Opus 5 · обычн. |
| 0-16 | 0.3x ✅ | ЗАКРЫТ (road-0.3x, 5274082). Заглушки `sys.modules` переехали в фикстуры; запрет `TESTING §6` стал ИСПОЛНЯЕМЫМ (ast-разбор `tests/**` + рантайм-проверка «объявленная зависимость не подменена»), constraints для CI, пин shapely. ⛔ Опровергнуто: нарушителей ПЯТЬ, а не четыре (пятый ставил заглушки циклом — грепу невидим); «cv2 нет в тест-среде» ложно — `opencv-python` объявлен в `ui.txt` и стоит, мок закрывал собой настоящий модуль. ⭐ 11 «красных» тестов масок не были сломаны: ошибок в полном прогоне 20 → 9, красных базы **57 → 46** | — | [infra] `--check` exit 0 после пересъёма (Д6); CI все 5 шагов | Opus 5 · обычн. |

**Выход волны:** приёмка этапа 0 из мастер-плана + отчёт 0.9 прочитан Максимом — это и есть
**Р-7**: подтвердить очередь волн 4–11 или пересобрать её по замеру.

---

## Волна 1 — Кровь (этап 1). Порядок жёсткий

Цель: падения, порча данных, тупики. 1.10 строго первым — без логов клиента ни одна
последующая правка не проверяема «в руках оператора».

| # | ID | что | предусловия | класс → гейт | модель |
|---|---|---|---|---|---|
| 1-1 | 1.10 | Логи клиента: FileHandler + `sys.excepthook`. ПЕРВЫМ | — | [ui] искусственное исключение в слоте → строка в файле лога | Opus 5 · расшир. |
| 1-2 | СТ11 | Смоук собранного клиента (.exe стартует, лог пишется, API виден) | 1.10 | [infra] смоук в CI сборки | Opus 5 · обычн. |
| 1-3 | 1.2 ⚠ | Б10: `visibility_timeout` — брокер перестаёт переоткрывать детекцию на 60-й минуте. ⭐ чинит бой без выката | — | [сма] в логе Celery нет повторного `task_detect_yolo` с тем же uid | Opus 5 · расшир. |
| 1-4 | 3.x1 | Flower поднять СЕЙЧАС (перенесён из этапа 3): глазами проверяет гипотезу дублей, ради которой 1.2 | — | [infra] Flower показывает очереди/задачи | Opus 5 · обычн. |
| 1-5 | 1.1 | И1: алиасинг снимка undo + `finalize()` в try/finally. До любого замера Э4 | — | [ui] тест на undo-инвариант | Opus 5 · расшир. |
| 1-6 | 1.4 | Р9: откат превью панели «Размеры» | ⛔ до 1.5 | [ui] тест 0.4 | Opus 5 · расшир. |
| 1-7 | 1.5 | Р13: дёрти-флаг. ТОЛЬКО после 1.4, иначе автосейв заливает брошенную порчу на сервер | ⛔ 1.4 | [ui] сценарий: открыть-испортить-закрыть без сохранения | Opus 5 · обычн. |
| 1-8 | 1.3 | Убрать молчаливые откаты-«самолечение» при открытии/закрытии | — | [ui] сценарий открыть/закрыть ×3 — холст на месте | Opus 5 · обычн. |
| 1-9 | 1.6 | Р2+Г1: защёлка resize-режима. С поправкой 1.x5: НЕ поднимать `_node_geom_kind` в базу — звать базовый метод; `_commit_resize` не трогать | — | [ui] тест 0.4 | Opus 5 · расшир. |
| 1-10 | 1.7 | `ResizeNodeCommand.undo` не восстанавливает концы | — | [ui] Ctrl+Z возвращает рамку и концы | Opus 5 · расшир. |
| 1-11 | 1.8 | «Липкий ресайз» и «брошенная тяга» | — | [ui] тест 0.4 | Opus 5 · расшир. |
| 1-12 | 1.9 | Ложь «✅ Сохранено» в OCR-вкладке | — | [ui] сценарий: сбой записи → оператор видит ошибку | Opus 5 · расшир. |
| 1-13 | 1.11–1.16 ⚠ | Шесть тупиков конвейера (красная кнопка о гейт статуса; серые кнопки direction; коммит статуса до send_task ×10; откат вне allowed_statuses; ретрай direction в начало; None из async_safe_dispatch в 5/6 мест). По одному пункту за сессию | — | [сма] на каждый — тест перехода до правки | Opus 5 · расшир. |
| 1-14 | 1.17 | Открытие в CLEANING_FRAME стирает очистку рамки | — | [сма] тест перехода | Opus 5 · расшир. |
| 1-15 | 1.18 | `closeEvent` клиента (нужен и 10.15) | — | [ui] закрытие пишет лог, батчи уходят | Opus 5 · расшир. |
| 1-16 | 1.19 | Ctrl+S редактора труб пишет в CWD. + находка 0.2 (та же семья): в графовом редакторе кнопка 💾 обещает «Сохранить на сервер (Ctrl+S)», а Ctrl+S открывает ЛОКАЛЬНЫЙ диалог — кнопка и хоткей делают разные вещи, свести | — | [ui] файл ложится в storage-путь; кнопка и хоткей делают одно и то же | Opus 5 · расшир. |
| 1-17 | 1.20 | Липкий флаг `_check_masks_completion` | — | [ui] тест | Opus 5 · расшир. |
| 1-18 | 1.21 | «← Назад» с грязной вкладкой — без вопроса | — | [ui] сценарий | Opus 5 · расшир. |
| 1-19 | 1.22 ⚠ | Ретраи неидемпотентных POST в api_client | — | [сма] тест: повтор POST не дублирует эффект | Opus 5 · расшир. |
| 1-20 | 1.23 | 404 на graph_validated → клиент молча берёт graph.json. + родня из red-team 0.5: НЕ-404 на `graph_canvas` глотается молча (`base_graph_tab.py:872`), предупреждающая модалка только в ветке `if saved:` (:892) — при 5xx холст тихо пересобирается, и Ctrl+S затрёт серверный canvas; теста на 5xx-канвас нет | — | [ui] сценарий: оператор видит предупреждение | Opus 5 · расшир. |
| 1-21 | 1.x3 | Несуществующий тип артефакта в pipe_tab | — | [ui] тест | Opus 5 · расшир. |
| 1-22 | ДН3 | Отчёт оператора: приложить холст+заметку к error_report_dialog (место в плане не задано — решение здесь) | 1.10 | [ui] отчёт содержит холст | Opus 5 · расшир. |
| 1-23 | 1.24 ⚠ | Ревизия данных после Б10: ДВА захода — сначала отчёт «что нашли», правка данных только после просмотра отчёта Максимом | ⛔ 1.2 | [данные] отчёт → решение → правка | Opus 5 · расшир. |
| 1-24 | 1.x6 | **Находка 10.3 (новая строка, диспетчер 2026-08-18):** `FrameTab.__init__` грузит `original_image` СИНХРОННО и на ошибке открывает модальный `QMessageBox` прямо из конструктора — под offscreen это вечный висяк (отсюда обязательный зонд «класс на процесс»), на бою — модалка поверх открывающейся вкладки. Класс тот же, что у остальной волны: тупик клиента | — | [ui] сценарий: битый артефакт рамки → вкладка открывается, ошибка видна строкой, не модалкой из конструктора | Opus 5 · расшир. |

**Выход волны:** приёмка этапа 1 из мастер-плана (открыть/закрыть ×3; Ctrl+Z; исключение → лог).

---

## Волна 2 — Боевой сервер (этап 2) ⚠ бой

Цель: производительность и таймауты. НИ ОДНОЙ правки B-класса без замера.

⛔ **Гейт качества для перф-правок (добавлен 2026-08-18 по замеру road-0.11).** План требует от волны 2 только время и RSS — этого мало: замер показал, что весь дефицит детекции это ПРОПУСКИ (+77 добавленных боксов против 5 снятых), а маска труб систематически недорисована (+26 % площади дорисовывает оператор). Любая правка, меняющая инференс (2.2 NLM, 2.3 ансамбль ×3, 2.4 TTA, 2.6 SAM2), обязана показать, что выход модели **не стал дальше** от валидированных артефактов: перепрогон корпуса локально (0.12) + `tools/pair_bench.py`. «Ускорил, но модель стала пропускать больше» = провал пункта, а не компромисс. ⚠ Читая `pair_bench`, держать `8d14cf73` ОТДЕЛЬНО от общей суммы (находка 0.11): стороны его пары `junction` сняты на разных обрезках листа (сдвиг +26/−21 px), пара `layout` — с нарушенным порядком; это единственная схема корпуса, дающая крупные числа почти во всех парах.

| # | ID | что | предусловия | класс → гейт | модель |
|---|---|---|---|---|---|
| 2-1 | СТ2 | Стенд ML-конвейера: холодное/тёплое время этапа + RSS воркера. ПЕРВЫМ пунктом этапа. ⛔ **Ловушки, вскрытые прогоном 0.12:** (1) локальный стек идёт на **GPU**, а бой **CPU-only** — мерить обязательно с принудительным CPU (`PID_DEVICE=cpu`), иначе все числа волны 2 не про прод; (2) в 0.12 воркеры были прогреты — **холодного замера не существует вовсе**, СТ2 обязан снять его сам с рестартом воркеров; (3) числа прогона 0.12 (детекция 11.6 с, перекрёстки 9.0, OCR 47.8, контуры 17.7 — MEASUREMENTS §34.6) сняты на GPU и прогретыми, данных под ними больше нет — это ориентир, НЕ базовая линия | ⛔ 0.12 ✅ | [infra] числа базовой линии в MEASUREMENTS.md: холодное+тёплое, CPU, RSS обоих процессов | Opus 5 · расшир. |
| 2-2 | 2.x1 | Ping брокера прямо в async `/health` — чинить ДО любого мониторинга | — | [сма] /health не блокирует event loop (тест) | Opus 5 · расшир. |
| 2-3 | 2.1 ⚠ | Кэш моделей: ModelRegistry (КД4) + манифест весов (КД8). Вариант A+D принят УСЛОВНО — фиксируется только после замера RSS (СТ2); если пик подтвердит ADR-004 — правильный ответ C. Решение → новый ADR (Д5). За YAML-флагом (ПР2) | ⛔ СТ2 | [перф] тёплое время ↓, RSS в бюджете, флаг выключает | **Fable 5** · ultrathink |
| 2-4 | 2.2 | NLM по полному 17-Мп кадру | СТ2, 0.12 | [перф] до/после + гейт качества (pair_bench) | Opus 5 · расшир. |
| 2-5 | 2.4 | TTA-дефолты True в трёх местах. ⚠ TTA включали НАМЕРЕННО, а дефицит детекции — пропуски: выключение бьёт ровно туда | СТ2, 0.12 | [перф] до/после + гейт качества: полнота детекции не хуже | Opus 5 · расшир. |
| 2-6 | 2.3, 2.6, 2.7 | Ансамбль ×3; SAM2 batch=1 и O(n²); тайлы текста по одному — по замеру, в порядке выигрыша | СТ2 | [перф] до/после | Opus 5 · расшир. |
| 2-7 | 2.5 | Потоки torch не запиннены | СТ2 | [перф] до/после | Opus 5 · расшир. |
| 2-8 | 2.8 | Понизить ГП9 и ГП11 (не делать) | — | [док] пометка в плане-аудите | Opus 5 · обычн. |
| 2-9 | 2.9 | Две задачи без доков + мёртвый `device: cuda`. ⚠ аудит: таблица 4.1 WORKER_TASKS уже врёт про детекцию (1800 против боевых 5400) — чинить и её. + находка 0.6: `worker/tasks/direction.py` (лимиты 600/540) в `WORKER_TASKS.md` §4 не описан ВООБЩЕ — это дыра доки, а не дрейф числа; сторож `tests/test_docs_match_code.py` её не видит (fail-open по форме, см. 13-8) | — | [док/снос] | Opus 5 · обычн. |
| 2-10 | 2.10 ⚠ | Autocast-guard в junction: план считал оффлайном, но `worker/tasks/junction.py:79,211` зовёт `run_inference` с `use_amp=True` на каждой диаграмме (аудит, ✖) — чинить СЕЙЧАС, не в этапе 13 | — | [перф] прогон junction на CPU без warning/no-op на батч | Opus 5 · расшир. |
| 2-11 | 2.11 ⚠ | Healthcheck воркерам | ⛔ 2.x1 | [infra] убитый воркер сам перезапускается | Opus 5 · расшир. |
| 2-12 | 2.12 ⚠ | Лимиты памяти воркерам — писать С НУЛЯ: в compose закомментирован GPU-блок, а не лимиты («раскомментировать по плану» = включить nvidia на CPU-сервере, аудит ✖); `deploy.resources.limits` без swarm игнорируется — использовать `mem_limit` | ⛔ СТ2 (знать реальный пик RSS, иначе OOM-загон) | [infra] docker stats показывает потолок | Opus 5 · расшир. |
| 2-13 | 2.13 ⚠ | Redis: maxmemory как ограничитель роста. Обоснование плана неверно (дефолт Redis 7 УЖЕ noeviction, «вытеснения по умолчанию» нет — аудит ✖); маленький maxmemory при noeviction начнёт ОТКАЗЫВАТЬ в записи — размер только по замеру | ⛔ СТ2 | [infra] `evicted_keys = 0`, запас по замеру | Opus 5 · расшир. |
| 2-14 | 2.14 | `/health` отдаёт не-200 при degraded | ⛔ 2.x1 | [сма] тест | Opus 5 · расшир. |
| 2-15 | 2.x2 | Судьба ADR-004: по замеру СТ2 — либо новый ADR «SAM2 в общем воркере», либо восстановить 004 (вариант C). ADR-001 править вместе | ⛔ СТ2 | [док] Д5: новый ADR | Opus 5 · обычн. |
| 2-16 | ВН1в ⚠ | **Расширить поиск контакта для боксов `napravlenie`** (сегодня кольцо 1 px, скелет обрывается в 3–10 px от осевой грани — диагноз ВН1б). Не «дорисовать трубу», а ПОДКЛЮЧИТЬ существующую: 85 из 96 сирот восстановимы однозначно. Цель по решению Максима: **10 → 4 на 17 схемах**, выброс `51b339ab` считать отдельно (**86 → 7**). От СТ2 не зависит — можно брать первым пунктом волны 2. + находка ВН1б: у `13d1ef5f` один бокс `napravlenie` из 26 без `direction` с 2026-07-23 — единственный на корпусе; классификатор его либо не видел, либо отдал low-conf до появления записи атрибута (не путать с сиротами) | ⛔ ВН1б ✅ | [гео] `napravlenie_audit --check` на 17 схемах: сироты в цели; `pair_bench`/`layout_bench` не хуже | Opus 5 · расшир. |

**Выход волны:** приёмка этапа 2 (детекция не переоткрывается; воркер перезапускается; числа до/после).

---

## Волна 3 — Целостность данных (этап 3)

| # | ID | что | предусловия | класс → гейт | модель |
|---|---|---|---|---|---|
| 3-1 | СТ4 | Тесты на `app/api/` и `worker/tasks/` — зафиксировать текущие переходы ДО правок | — | [infra] набор зелёный на текущем коде | Opus 5 · обычн. |
| 3-2 | 3.5 ⚠ | Unique на артефакт: constraint УЖЕ в БД (миграция 0001) — объявить в ORM (`__table_args__`), миграцией НЕЛЬЗЯ (`already exists`); упорядочить `upsert_artifact` (файл портится ДО IntegrityError) | ⛔ СТ4 | [сма] конкурентный тест: параллельный аплоад без порчи | Opus 5 · расшир. |
| 3-3 | 3.6 ⚠ | `Diagram.version` (БД1) — оптимистическая блокировка. ⚠ аудит: гонку номера диаграммы НЕ чинить — advisory lock уже стоит (`diagrams.py:105`); остаток только salted `hash()` ключа при `--workers>1` | ⛔ СТ4 | [сма] конкурентная запись → исключение, не тихая перезапись | Opus 5 · расшир. |
| 3-4 | 3.2 | Пароли по умолчанию в compose (`changeme`/`admin123` без .env) | — | [infra] стек без .env не поднимается | Opus 5 · обычн. |
| 3-5 | 3.3, 3.4 | zip-slip + path traversal (понижены до «устойчивости» — контур закрыт) | — | [сма] тест на злой архив/путь | Opus 5 · расшир. |
| 3-6 | 3.7 | Storage: 16 мест пишут мимо сервиса | СТ4 | [реф] бит-в-бит | Opus 5 · расшир. |
| 3-7 | 3.8 | Ретраи HTTP (tenacity уже в base.txt, в коде 0) | — | [сма] тест: обрыв → ретрай идемпотентного | Opus 5 · расшир. |
| 3-8 | 3.9 | Сузить глушение ошибок — ТОЛЬКО список из 10 (топ: `validation.py` — слияние OCR падает, статус «валидировано» всё равно ставится); остальные 200+ попутно в своих этапах | — | [сма] на каждый из 10 — тест | Opus 5 · расшир. |
| 3-9 | 3.10 | Наблюдаемость: НЕ ломать 25 тестов `tests/observability/` | — | [реф] те 25 зелёные | Opus 5 · расшир. |
| 3-10 | 3.11–3.13 | Диагностика на БД: /inflight, /stuck, /errors (работают при мёртвом брокере) | — | [сма] тесты + брокер выключен → эндпоинты живы | Opus 5 · расшир. |
| 3-11 | 3.14 | /monitoring/queues: глубина через LLEN; inspect() только в отдельном потоке с таймаутом. ⚠ аудит (✖): очередей ЧЕТЫРЕ — план пропустил `default`, где живут skeleton/graph/layout; LLEN по трём из плана ослепнет на половину конвейера | ⛔ 2.x1 (не повторить дефект /health) | [сма] тест при мёртвых воркерах; все 4 очереди видны | Opus 5 · расшир. |
| 3-12 | 3.x2 | Логи сервера в клиент (готовый план PLAN_client_logs.md; 2 дефекта наброска §4 исправить ДО реализации) | — | [ui] сценарий | Opus 5 · расшир. |
| 3-13 | 3.x3 | Метрика Postgres: число соединений | — | [infra] | Opus 5 · обычн. |

**Выход волны:** приёмка этапа 3 (параллельный аплоад; стек без .env; диагностика при мёртвом брокере).

---

## Волна 4 — Текст в FXML (этап 4, часть 4а; 4б ждёт решений)

Замок волны: 0.1 ✅ (уже в git). Часть 4б (Т1.1→Т1.4) заперта решением Максима по В1/В2 —
до него делать только 4а. ⛔ **Р-4**: решение принимается по двум рендерам одного холста
(кегль 18 и 12), когда СТ7 восстановит числа — прежние «0/161» и «31 из 39» не воспроизводились.

| # | ID | что | предусловия | класс → гейт | модель |
|---|---|---|---|---|---|
| 4-1 | 4.1 | `text_edited` никогда не взводится | — | [ui] правка двигает подпись в выгрузке | Opus 5 · расшир. |
| 4-2 | 4.2 | Т1.5: угол как данные | — | [гео] рендер до/после | Opus 5 · расшир. |
| 4-3 | 4.3 | Свести dup-1 (правило «вертикальный блок») | — | [реф] бит-в-бит FXML | Opus 5 · расшир. |
| 4-4 | 4.4 | Свести dup-3 (nearest_bind_side) | — | [реф] бит-в-бит FXML | Opus 5 · расшир. |
| 4-5 | 4.5 | Т1.7 → Т1.6 (строго в этом порядке) | ⛔ 4.3 | [гео] рендер до/после | Opus 5 · расшир. |
| 4-6 | 4.6 | Порог поворота разошёлся | — | [гео] рендер до/после | Opus 5 · расшир. |
| 4-7 | СТ7 | Стенд выхода FXML: печатаемые подписи/наложения/вылеты/повороты | — | [infra] числа воспроизводимы, эталон в git | Opus 5 · обычн. |
| 4-8 | 4.7 ⚠ | Разобрать `graph_to_fxml.py` (2201 строка) — только под СТ7 | ⛔ СТ7, 4.1–4.6 | [реф] СТ7 бит-в-бит; ревизор | **Fable 5** · ultrathink |
| 4-9 | 4.x2 | Приоритет подписей внутри чужого узла + 5-е место kksVisible | — | [гео] СТ7 | Opus 5 · расшир. |
| 4-10 | 4.x1 ⛔ | Часть 4б: Т1.1→Т1.2→Т1.3→Т1.4. Ждёт В1/В2 (сначала СТ7 чинит числа, потом рендеры Максиму) | ⛔ решение Максима | [гео] СТ7 до/после | Opus 5 · расшир. |

---

## Волна 5 — Реестр стадий и статусов (этап 5)

| # | ID | что | предусловия | класс → гейт | модель |
|---|---|---|---|---|---|
| 5-1 | 5.1 | Однострочный фикс отката OCR — ПЕРВЫМ, отдельно от сведений | — | [сма] репро 5.1: «переделать OCR» не сносит SAM2-контуры | Opus 5 · расшир. |
| 5-2 | СТ10 | Сценарные тесты вкладок/workspace. + пробел, оставшийся после 10.3: **двухфазность открытия графовых вкладок** (`diagram_workspace.py:1905` → SimpleGraphTab, `:1937` → AdvancedGraphTab, порядок ведёт цепочка бусин) характеризации не имеет — пять снятых в 10.3 проверок тестировали несуществующий флаг `_simple_graph_done`. + две дыры грепа, доказанные ревизией 10.3 инъекциями: закомментированный вызов `complete_graph_validation` оставляет тест зелёным, а вызов фазы 1 `complete_simple_graph_validation` не проверяется ничем — сценарий должен звать обе фазы, а не грепать исходник. Сценарии: (открыть-править-сохранить-закрыть; смена статуса; сетевая ошибка) | ⛔ 0.4 | [infra] набор зелёный | Opus 5 · обычн. |
| 5-3 | 5.2 ⚠ | `transitions.py` — единственный писатель `diagram.status` (сейчас 77 присваиваний в 20 файлах) | ⛔ СТ4, СТ10 | [сма] ни одного `diagram.status =` вне transitions.py + тесты переходов | **Fable 5** · ultrathink |
| 5-4 | 5.3+5.4 ⚠ | Один реестр стадий (КД2), UI-таблицы производны; мёртвые статусы/бусины — вместе | ⛔ 5.2 | [сма] СТ10 зелёные | Opus 5 · расшир. |
| 5-5 | 5.6 | `DiagramEvent` (БД4) + слушатель переходов (3 регистрации!) + серверный канон порядка стадий. Ретеншн назначить сразу. + ⛔ замер 0.9 (§29.7–29.9): `upload`, `mask_validation`, `graph_validation` НЕ пишутся в `processing_stages` вовсе — ноль строк за всю историю, а `app/api/stats.py:40-45` исключает две из них из p50 как существующие; 52 % машинного времени — перезапуски (до 27 попыток на стадию); методическое: сшивать стадии через `max(A)/min(B)` нельзя — поздний перезапуск уводит разрыв в минус и стык исчезает из отчёта, нужна построчная сшивка | ⛔ 5.3; ⛔ до 6.11 | [сма] тест отката транзакции у слушателя | Opus 5 · расшир. |
| 5-6 | 5.5 | Свести dup-5a («OCR готов → кнопки») | — | [реф] | Opus 5 · расшир. |

---

## Волна 6 — Фронты 2–3 + Celery (этап 6) ⚠ бой

Порядок жёсткий: Б2 → Б6 → Б7 → Б3 → Н2(+`validation.py:1057-1066`) → Н3 → Н1 → Н7 → Н4 → Б5 → Б4.
Решение принято: гейт запирается комплектом Б3+Б6+Б7 (Б7 — единственный ключ).
Серверная половина (Н1, Н2, Н7, Н8) опирается на код, которого нет на `deploy` — до выката
работает только локально; Б12–Б14 и 6.8–6.10 от выката не зависят. **Р-1 решён (2026-08-17):**
выкат один, финальным merge — серверная половина принимается на локальном стеке (0.12) и стендах.

| # | ID | что | предусловия | класс → гейт | модель |
|---|---|---|---|---|---|
| 6-1 | 6.1 ⚠ | Гейт layout_gate: Б3+Б6+Б7 одним комплектом; `tests/test_layout_gate.py` переписать одним заходом | ⛔ СТ4 | [сма] тесты гейта | Opus 5 · расшир. |
| 6-2 | 6.2 | Развести две правки под номером Б3 (оба фолбэка или ни одного) | 6.1 | [сма] | Opus 5 · расшир. |
| 6-3 | 6.3 ⚠ | Н2 вместе с `validation.py:1057-1066` | 6.1 | [сма] | Opus 5 · расшир. |
| 6-4 | 6.4 | Откат не отзывает бегущие задачи | — | [сма] | Opus 5 · расшир. |
| 6-5 | 6.5 | Ретрай финальной скелетизации отматывает на сегментацию | — | [сма] | Opus 5 · расшир. |
| 6-6 | 6.6 | Две живые финальные скелетизации — свести детерминированно | — | [сма] | Opus 5 · расшир. |
| 6-7 | 6.7 | Запереть вход в редактор (Б4 переформулировать, Н3) | — | [сма] СТ10 | Opus 5 · расшир. |
| 6-8 | 6.8+6.11 ⚠ | Базовая Celery-задача (КД3) вместо 10 копий пролога + сигналы Celery (слой 2 статистики) одним заходом | ⛔ 3.5, 5.6 | [сма] 12 fault-тестов observability зелёные + контрактные на идемпотентность | **Fable 5** · ultrathink |
| 6-9 | 6.9+6.10 ⚠ | Единая диспетчеризация (перехват `before_task_publish`) + снос мёртвых путей вместе. ⚠ аудит: периметр плана неполный — сверх 11 send_task есть 12-я точка `worker/tasks/skeleton.py:768` и `chain().apply_async` в `segmentation.py:97-100`; в 6.10 `skeleton.py` — ДВА разных файла (app/api и worker/tasks) | ⛔ 6.8 | [сма] тест: провал диспетчеризации виден (Д3) | **Fable 5** · ultrathink |

**Выход волны:** приёмка этапа 6 (зависшая раскладка перезапускается; повторное подтверждение не гасит «Ручную правку»).

---

## Волна 7 — Единая геометрия (этап 7). Прямо перед 8 и 9

⛔ **Р-2** — принять на входе в волну (запирает 7-3 здесь и 8-3 в волне 8): какой канон
seat-rect главный. **A** — свести к серверному `seating.py` (бит-эталоны целы, канон не трогаем),
редактору — явная развилка; **B** — перевести сервер на редакторский контракт (дороже:
эталоны пересъёмывать). Материал для решения готовит чат «вопрос-решение» из PROMPTS.md.

| # | ID | что | предусловия | класс → гейт | модель |
|---|---|---|---|---|---|
| 7-1 | 7.4 | Свести IoU + зафиксировать канон bbox (КД5 на границах столкновения). Начать с мёртвых копий — безрисково. Пороги не трогать | — | [реф] характеризационный тест contours_merge на смешанных форматах ДО правки | Opus 5 · расшир. |
| 7-2 | 7.2 | Один допуск ортогональности. ⚠ аудит: «пятый допуск» `_axes.ORTHO_TOL` мёртв (0 использований), зато есть неучтённая живая копия `_gate.py:27 STRAIGHT_TOL=1.5` — сводить её | — | [реф] все допуски — одно число | Opus 5 · расшир. |
| 7-3 | 7.1 ⚠ | Два канона seat-rect — свести с явной развилкой. Канон `seating.py` не трогать. Решение A/B — у Максима, оформить ADR (Д5) | ⛔ решение Максима | [гео] замер популяции | **Fable 5** · ultrathink |
| 7-4 | 7.3 | PIN_TOL: развилки значений НЕТ — обе копии 0.5 с рождения (аудит ✖, git-история), заморожено решением 2026-08-14. Остаётся дедуп двух копий константы. Решение Максима НЕ требуется | — | [реф] обе точки читают одну константу | Opus 5 · расшир. |
| 7-5 | 7.5 | Хрупкие импорты без префикса `modules.` | — | [снос] импорт-гейт | Opus 5 · обычн. |

---

## Волна 8 — Посадка концов и дубли рёбер (этап 8) ⚠ бой

Порядок проверкой плана: 8.2 → 8.1 (Г5б) → Г5а → 8.3 (D2) + 8.4 (Г7) вместе → 8.5 → 8.6+8.7+8.8 → 8.9.
Тесты, которые заведомо упадут (переснять осознанно, Д6): `test_seat_contract.py:85,:96,:131-136`,
`tests/ui/test_connection_point.py:77-101`, `test_simple_seat_lift.py`, `test_interactive_drag.py:1163`.

| # | ID | что | предусловия | класс → гейт | модель |
|---|---|---|---|---|---|
| 8-1 | СТ6 | Стенд качества сборки графа: узлы/рёбра/дубли пар/висячие/изолированные | ⛔ 0.8 | [infra] базовая линия в MEASUREMENTS.md | Opus 5 · обычн. |
| 8-2 | 8.2 | Ветка коннектор↔коннектор в диспетчере «Контуров» | — | [гео] тест | Opus 5 · расшир. |
| 8-3 | 8.1 ⚠ | Г5б: ветвление по типу узла в диспетчере. `_get_node_bbox` (`base_graph_editor.py:1040-1051`) НЕ трогать — уронит этап 9 | ⛔ 8.2, 7.1 | [гео] замер популяции: концы мимо формы = 0 | **Fable 5** · ultrathink |
| 8-4 | 8.3+8.4 ⚠ | D2 (дешёвая замена переносу) + Г7 одним заходом; перенос connect_* — только при живой претензии Максима после | 8.1 | [гео] глазами Максима + замер | Opus 5 · расшир. |
| 8-5 | 8.5 | Пересадка — под стек команд и дёрти-флаг (механизм 1.4/1.5) | 1.4, 1.5 | [ui] Ctrl+Z возвращает | Opus 5 · расшир. |
| 8-6 | 8.6+8.7+8.8 ⚠ | Дедуп рёбер при сборке (решение: «дублей не бывает — все дефекты») + снос ~350 строк легаси-близнецов трассировки одним заходом + порог просвета по СТ6 (255 уничтожит реальную трубу — пик яркости 191) | ⛔ СТ6 | [гео] СТ6: дублей пар 0 на 4 контрольных, реальные трубы целы | **Fable 5** · ultrathink |
| 8-7 | 8.9 ✅ | Закрыт: воронка уже едина — все 4 входа идут через единственный `_enter_resize_mode` (подтверждено кодом, аудит); работа растворена в 1.6. Проверить при приёмке 1.6 и закрыть на доске | 1.6 | [ui] тест из 1.6 | Opus 5 · расшир. |

---

## Волна 9 — Роутинг (этап 9) ⚠ бой целиком

Путей ДВА, они не пересекаются кодом: сервер `layout/avoid_router.py` против редактора
`edit_avoid.py` (разошлись даже буфером: 6.0 против 6.25). Мерить оба отдельно.
Перед волной — ВН3: день разведки Adaptagrams (356 символов вкомпилировано, используем 6) —
единственный рычаг, способный убрать фронт целиком.

| # | ID | что | предусловия | класс → гейт | модель |
|---|---|---|---|---|---|
| 9-0 | ВН3 | Разведка Adaptagrams (1 день, только замер — не переписывание). + два вопроса детерминизма из 0.10: (1) можно ли усмирить недетерминизм libavoid (8 графов из 17; на Windows воспроизводится и между машинами — раннер GitHub дал те же хеши, значит источник внутри одного прогона, не в железе); (2) как ведёт себя Linux-бинарь `_adaptagrams.so` — не проверялось никогда | — | [данные] отчёт Максиму | Opus 5 · обычн. |
| 9-1 | СТ8 | Стенд роутинга по двум путям ОТДЕЛЬНО: наложения/отказы/длина. ⛔ начинать с пересъёма чужих эталонов (Д6, отдельным коммитом): `tools/layout_bench.py` на полном корпусе расходится со своей таблицей `EXPECT` на двух графах (`51b339ab` 302→11 против 310→8; `8d517a35` 116→1 против 99→0) — предсуществующее, замерено в 0.8. + `tools/cmp_bitexact.py:66` и `tools/corner_probe.py:77` ищут `storage` сами и держат эталоны в `_scratch/` (вне git) — переводить их на загрузчик `tools/corpus.py` бессмысленно, пока эталоны вне git (та же оговорка у волны 12) | ⛔ 0.8, 0.10 | [infra] базовая линия обоих путей | Opus 5 · обычн. |
| 9-2 | 9.1 ⚠ | Запись принятых концов в `_avoid_apply_route` — СТРОГО ПЕРВОЙ, до М1. ⚠ аудит (✖): сервер УЖЕ пишет концы (`avoid_router` ends_out, с 2026-07-31) — объём чисто клиентский; на клиенте повторить серверную связку «пере-посадка + судья сторон», а не просто присвоить pts[0]/pts[-1] | ⛔ СТ8 | [гео] СТ8 до/после | **Fable 5** · ultrathink |
| 9-3 | 9.2 | Эксклюзивность ShapeConnectionPin — отдельной задачей, до М2 | 9.1 | [гео] СТ8 | Opus 5 · расшир. |
| 9-4 | 9.3 | М1 только для концов на гранях оборудования | 9.1 | [гео] СТ8 + Э4-бенч (11.1) до/после — иначе М1 вернёт косые | Opus 5 · расшир. |
| 9-5 | 9.4 | М6 — в двух файлах или ни в одном | — | [гео] СТ8 | Opus 5 · расшир. |
| 9-6 | 9.5 | Понизить М4, отложить М5 (М5 первой правкой строго вредна) | — | [док] | Opus 5 · обычн. |
| 9-7 | 9.6 | М9 не годится как приёмка — заменить | — | [док] | Opus 5 · обычн. |
| 9-8 | 9.7 | Серверный гейт apply_routing откатывает весь прогон | — | [сма] тест | Opus 5 · расшир. |

---

## Волна 10 — Жесты + декомпозиция UI (этап 10)

Метод: каждый шаг механический, тесты 0.4 зелёные до и после, артефакты контрольного uid
бит-в-бит. AGE уменьшается монотонно (следить СТ9). ⛔ **Р-5** — к пункту 10-12: исполнитель
сначала приносит замер (сколько узлов корпуса несут пин), решение оживить/снести — по числу.

| # | ID | что | предусловия | класс → гейт | модель |
|---|---|---|---|---|---|
| 10-0 | СТ9 | Метрики структуры кода (строк/файл, методов/класс, except-счётчик, `diagram.status =`) | — | [infra] скрипт + базовая линия | Opus 5 · обычн. |
| 10-1 | 10.1 | Р14: ресайз с режима на оверлей | ⛔ 0.4, этап 8 | [ui] тесты 0.4 | Opus 5 · расшир. |
| 10-2 | 10.2 | Р10: ручки наружу | 10.1 | [ui] | Opus 5 · расшир. |
| 10-3 | 10.3 | **ПЕРЕНЕСЁН В ВОЛНУ 0** (пункт 0-17, решение Максима 2026-08-18): снос `ui/windows/` делается до 0.5, чтобы не сводить копии `ArtifactDownloader` ради удаляемых файлов и убрать вечные красные из базовой линии. Здесь работ нет | — | закрыть на доске ссылкой на 0-17 | — |
| 10-4 | 10.4 | OcrLayerMixin: только протечка контракта (`_start_rubber_band`/`_cursor_scene_pos`). «Второй undo-стек в миксине» опровергнут аудитом (✖) — настоящий параллельный стек в `OcrBindingEditor:305/2421/2440`, он чинится в 10-9. + долг из red-team 0.5: путь `finished([])` кладёт ПУСТОЙ undo-шаг (`_ocr_push_snapshot`/`_ocr_commit` без no-op-проверки, `ocr_layer_mixin.py:692-707`) — вкладка становится «грязной» при «Распознано 0/0»; на `ui/services/recognize_worker.py` ноль тестов, нужен дымовой | — | [ui] | Opus 5 · расшир. |
| 10-5 | 10.5 | Ввод обратно в ModeHandler. + дефект из 0.4: ветки расширения выделения читают защёлку `ctrl_pressed` ДО обновления её из модификаторов — Ctrl+клик без предшествующего KeyPress в них не попадает | 10.1 | [ui] | Opus 5 · расшир. |
| 10-6 | 10.6 | Мутации модели — под Command. + находка ВН1б: `GraphData.save()` НЕ пересчитывает `degree` — протухло 425 узлов корпуса, вместе с полем врёт `pass_through` (числа, снятые с `degree`, недостоверны — так родилось ложное «133 бокса») | — | [ui] | Opus 5 · расшир. |
| 10-7 | 10.7 | api_client: свернуть 64 метода, DTO (КД6) | — | [реф] | Opus 5 · расшир. |
| 10-8 | 10.8 | Один источник состояния и один поллер. + долг из red-team 0.5: `tab.cleanup()` не зовётся нигде; `deleteLater` при бегущем QThread; progress-лямбда исполняется в потоке загрузчика | — | [ui] СТ10 | Opus 5 · расшир. |
| 10-9 | 10.9 | OcrBindingEditor под BaseGraphEditor | — | [реф] | Opus 5 · расшир. |
| 10-16 | 10.16 | **`MaskEditorBase` для polyline/square** — перенесён из 0.5 решением 2026-08-18. Замер: 2×~1170 строк, 15 общих методов, совпадает 198 строк из 479, но байт-в-байт только 3 — остальное разошлось именами атрибутов (`bg_item`↔`original_item`, `_shift_pressed`↔`shift_pressed`) и поведением. Причина переноса: выигрыш мал, риск велик — это самый нагруженный инструмент оператора (сегментация правится 17/17 схем, замер 0.11), а фронта-потребителя у этого дубля нет. Здесь он идёт вместе с остальной декомпозицией редакторов и получает характеризационную сетку по методу 0.4 | ⛔ характеризационные тесты обоих редакторов ДО слияния | [реф] бит-в-бит артефакты + маски правятся как раньше | Opus 5 · расшир. |
| 10-10 | 10.10 | Разнести ocr_binding_tab.py | — | [реф] | Opus 5 · расшир. |
| 10-11 | 10.11 ⚠ | Разбор AGE по контроллерам — только после 10.1–10.6 | ⛔ 10.1–10.6 | [реф] бит-в-бит + СТ9 | **Fable 5** · ultrathink |
| 10-12 | 10.12 ⛔ | `_breaks_anchor`: оживить или снести (решение по замеру: сколько узлов корпуса несут пин) | ⛔ замер | [снос] одним коммитом с тестом | Opus 5 · обычн. |
| 10-13 | 10.14 | graph_geometry.py разрезать | ⛔ этап 8 закрыт | [реф] | Opus 5 · расшир. |
| 10-14 | 10.13 ⚠ | BaseGraphEditor (тоже god) — последним | ⛔ 10.11 | [реф] | **Fable 5** · ultrathink |
| 10-15 | 10.15 | Активное время оператора (слой 4) — последним; ДО кода проверить гипотезу QWebEngineView | ⛔ 1.18 | [ui] | Opus 5 · расшир. |

---

## Волна 11 — Качество Э4 + ускорение редактора (этап 11)

Вся волна после 1.1. Порядок жёсткий: К-8 → К-10 → К-11 → К-9; затем К-3 → К-2/К-0 → С1.

| # | ID | что | предусловия | класс → гейт | модель |
|---|---|---|---|---|---|
| 11-1 | 11.1 | К-8: стенд `smooth_bench` (СТ5) на сыром корпусе | ⛔ 0.8 | [infra] базовая линия | Opus 5 · обычн. |
| 11-2 | 11.2 | К-10 (поднята до S): ПЕРЕД К-11 — иначе цифры Э4 на откаченном роутинге | 11.1 | [гео] СТ5 | Opus 5 · расшир. |
| 11-3 | 11.3 | К-11: мерить reseat_fn, а не route_fn | 11.2 | [гео] СТ5 | Opus 5 · расшир. |
| 11-4 | 11.4 | Двойной снимок избыточен | — | [перф] СТ5 не хуже | Opus 5 · расшир. |
| 11-5 | 11.5 | Свести dup-4 (страховка: `test_edit_smooth.py:48-60,:226-232`) | — | [реф] | Opus 5 · расшир. |
| 11-6 | 11.6 | Ускорение: К-3 → К-2/К-0 → С1 строго в этом порядке (К-3 меняет тип кэшируемой величины) | 11.3 | [перф] bench_scene | Opus 5 · расшир. |
| 11-7 | 11.7 | Поправить цифры отрасли | — | [док] | Opus 5 · обычн. |

---

## Волна 12 — Ядро конвейера (этап 12)

Только когда всё остальное под тестами. Внутри: характеризационные тесты → разрезание
god-функций → сведение дублей. Ни одной правки поведения без зафиксированного выхода.

| # | ID | что | предусловия | класс → гейт | модель |
|---|---|---|---|---|---|
| 12-1 | 12.x3 | tracing.py: тесты + разрезание (близнецы уже снесены в 8.6+8.7). ⚠ аудит (✖): «вложенные god-функции» — артефакт замера (реально 96/40/13 строк); резать объемлющие `trace_edges_v3` (366), `trace_single_edge_v3` (222), `trace_single_edge` (254) | ⛔ этап 8 | [реф] снимок выхода бит-в-бит | **Fable 5** · ultrathink |
| 12-2 | 12.x4 | nodes.py: тесты + декомпозиция + дубль bbox | — | [реф] бит-в-бит | Opus 5 · расшир. |
| 12-3 | 12.x2 | spread.py: разрезание spread() (610 строк, 14 замыканий) | ⛔ волна 9 | [реф] ⚠ бит-в-бит НЕ годится (замер 0.10: 8 графов из 17 недетерминированы из-за libavoid) — доказывать стабильными счётчиками `layout_bench` + бит-в-бит на 9 стабильных графах | **Fable 5** · ultrathink |
| 12-4 | 12.x1 | skeleton_extension/core.py: тесты + 4 god-функции + дубли | — | [реф] бит-в-бит | Opus 5 · расшир. |
| 12-5 | 12.x5 | pipe_segmentation/cli.py: общий каркас | — | [реф] | Opus 5 · расшир. |
| 12-6 | 12.x6 | direction_nodes.py — эталон качества кода, работ по декомпозиции нет. ⚠ но сам файл не чист: шапка `:19-22` описывает порядок вызовов в `builder.build`, которого нет, а `apply_direction_rules`/`stitch_collinear_stubs` мертвы — их снос закреплён за 13-9 решением Р-ВН1-2 | — | закрыть ссылкой на 13-9 | Opus 5 · обычн. |

Декомпозиция обязана оставить mermaid-схему (DOCUMENTATION_RULES §4.4) — сюда и в волну 10.

---

## Волна 13 — Отделение офлайн-кода, конфиги, зависимости, доки (этап 13). Последняя

Единственная волна, двигающая файлы. Решение Максима: отделение (возможно в ветку), НЕ удаление.
⛔ **Р-6** — к пункту 13-4: дожать миграцию биндинга или снести `engine.py`; первый шаг
(снять ложь о замене в 3 местах) решения не ждёт — делать сразу.

| # | ID | что | предусловия | класс → гейт | модель |
|---|---|---|---|---|---|
| 13-1 | 13.x3 | Профили OCR: вычитать ДО отделения | — | [док] | Opus 5 · обычн. |
| 13-2 | 13.x1, 13.x2 | Легаси-OCR (~2100) + мёртвые OCR-модули (~1900) — отделить | 13.x3 | [снос] импорт+воркер+сборка после каждого выноса | Opus 5 · обычн. |
| 13-3 | 13.x4 | Тренировочный контур из-под прод-импорта (autocast-guard уже починен в 2-10 — боевой путь) | — | [снос] | Opus 5 · обычн. |
| 13-4 | 13.x5 ⛔ | Биндинг: сначала снять ложь о замене в 3 местах; затем решение Максима — дожать или снести engine.py | ⛔ решение | [снос] | Opus 5 · обычн. |
| 13-5 | 13.x6 | Конфиги: невидимый проект tec_boiler, дубли профилей, тихие дефолты. + находка 0.12 (решение Максима 2026-08-18 — сюда, не в волну 1): `GET /api/projects/` и `/api/projects/summary` отдают ПУСТОЙ список на всех вызовах после первого — `load_all()` пропускает подпапку, чей код уже в `self._cache` (`app/services/project_loader.py:579`), а лоадер — процессный синглтон `@lru_cache`; достаточно, чтобы до открытия клиента кто-то обработал одну диаграмму. Сегодня боль спит: проект один, клиент прикрыт хардкодами «Термогидравлика» (`main_window.py:207,218`); всплывёт на втором проекте. Чинится одной строкой + тест | — | [infra] тест: два подряд `GET /api/projects/` после `load()` отдают один и тот же непустой список | Opus 5 · обычн. |
| 13-6 | 13.x7 | Зависимости: конфликт opencv, дыры в пинах | — | [infra] сборка клиента + воркера | Opus 5 · обычн. |
| 13-7 | 13.x8–13.x11 | ADR: снять дрейф 001/004/006, новый ADR кэша (решения уже приняты в волне 2) | ⛔ 2.x2 | [док] Д5 | Opus 5 · обычн. |
| 13-8 | 13.x12–13.x17 | Накопленный дрейф доков по 6 группам. Если список НЕ сократился сам — Д4 не соблюдалось: остановиться и разобраться, а не дописывать задним числом. **+ семь адресов, снятых замерами волны 0** (все ЧУЖИЕ, в своих пунктах не правились): (1) `STATUS_MACHINE.md:145` и `API.md §7` обещают авто-диспатч SAM2-контуров веером после перекрёстков — `validation.py:666-669` прямо ставит `contour_task_id = None`, считает вкладка по требованию (на этом прогон 0.12 встал в первый раз); (2) `STATUS_MACHINE.md §2` не знает фазы очистки рамки — детекция стартует с `frame_cleaned`, а не с `uploaded` (`detection.py:40`); (3) `STATUS_MACHINE.md §6` числит копию `graph.json` в validated среди авто-приёмок — `complete-simple` её не делает (`validation.py:728`, 400 «Граф не сохранён»); (4) `DEV_SETUP.md §3` предлагает поднимать `cvat_worker_annotation`, осознанно удалённый из `docker-compose.yml` (комментарий на :360); (5) `README.md:73` «21 документ» — в `docs/` их 26 (число врало и до дороги); (6) `docs/observability/RUNBOOK_execution_plan.md:359` ссылается на `test_stage7_graph_flow` как на живой долг — файл снесён в 10.3; (7) `tests/test_geometry_guard.py:17` в докстринге обещает «Э5 переведёт graph_validation_window» — окна нет с 10.3. **+ дыра сторожа (ревизия 0.6):** `tests/test_docs_match_code.py` fail-open ПО ФОРМЕ — блок §4 без строк «**Файл:**»/«**Celery name:**» молча выпадает из сверки (так дыра по `direction.py` сторожу невидима), переформулированная фраза про OCR вне регэкспа — тоже; ложных красных нет, но потерю покрытия не ловит | — | [док] | Opus 5 · обычн. |
| 13-9 | 13.x18 | **Хвосты, собранные волной 0** (новая строка, диспетчер 2026-08-18): (а) снос мёртвой штамповки направления по решению Р-ВН1-2 — `apply_direction_rules`/`stitch_collinear_stubs` (`modules/graph/core/direction_nodes.py:295,416`) не зовутся из `builder.build` ни разу, живы только в `tests/test_direction_nodes.py` (6 тестов из 13); ⛔ при сносе НЕ потерять путь `direction` у ребра для треугольника у ручного узла степени 1 (`graph_to_fxml.py:1296-1315`) и поправить шапку `direction_nodes.py:19-22`; (б) `ui/editors/test_editors.py` — дев-скрипт от 2026-04-07, не импортируется вообще (`No module named "square_mask_editor"`), единственный отказ из 54 модулей `ui.*` (находка 10.3); (в) `ruff` печатает на каждый прогон `Invalid # noqa directive on tests/test_refactoring.py:53` (нет списка кодов) — находка 0.10, жива после 0.3x; (г) свип `read_text()` без `encoding` по `tests/**` (находка 0.3: `tests/test_refactoring.py:697`) и по бою — `app/api/cvat.py:566` (находка red-team 0.3) | — | [снос/док] `import app.main` + старт воркера + `pytest --collect-only` + `lint_gate --check` без предупреждений | Opus 5 · обычн. |

---

## Решения приняты — не переигрывать

| решение | дата | где записано |
|---|---|---|
| Аутентификация не нужна — контур закрыт (3.1 снят; 3.3/3.4 понижены до устойчивости) | 2026-08-13 | план, «Контекст» |
| Кэш моделей: вариант A+D, УСЛОВНО — фиксация только после замера СТ2 | 2026-08-13 | план, этап 2 |
| `napravlenie` (ВН1) — делаем; среду готовят 0.0 и 0.12 | 2026-08-14 | план, «За заказчиком» |
| Ревизия данных после Б10 — делаем (пункт 1.24, двумя заходами) | 2026-08-14 | план |
| Гейт раскладки запирается комплектом Б3+Б6+Б7 | 2026-08-14 | план, этап 6 |
| 4б: кегль 18, порог жирного ~0.3, диаметры не печатать | 2026-08-14 | план, этап 4 |
| Дубли рёбер: «дублей не бывает — все дефекты», различать тестом просвета | 2026-08-13 | план, этап 8 |
| Этап 13 — отделение, не удаление | 2026-08-13 | план, этап 13 |
| **Р-1**: всё делаем на `dev`, проверка на локальном docker-стеке (0.12); `deploy` заменяется одним финальным merge. ⚠ при закрытии 1.2 отдельно решить точечный черрипик `visibility_timeout` на `deploy` — иначе дубль детекции живёт на бою до конца дороги | 2026-08-17 | здесь; журнал BOARD |
| **napravlenie (Р-ВН1-1/2)**: степень 1 — законная норма; степень 0 — дефект, восстановление только при 100 % уверенности, иначе оператору; граф остаётся ненаправленным, `direction` на ребре — только для треугольника у ручных узлов степени 1 | 2026-08-18 | `docs/NAPRAVLENIE.md`; коммит c69c1e0 |

Открытые решения перенесены в начало файла (⛔ «Открытые решения Максима») и продублированы
напоминаниями в преамбулах своих волн — здесь их больше нет.
