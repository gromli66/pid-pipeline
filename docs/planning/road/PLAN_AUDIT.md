# Аудит плана по коду — 2026-08-17

Каждый пункт мастер-плана сверен с кодом ветки `dev` (167 пунктов, 14 независимых
проверщиков). Вердикты: ✔ CONFIRMED — код подтверждает · ⏳ STALE — было верно,
код с 2026-08-13 изменился · ✖ REFUTED — не подтверждается · ❔ UNVERIFIABLE — чтением
кода не проверить (нужен стенд/рантайм).

**Как пользоваться:** исполнитель перед своим пунктом читает раздел своего этапа здесь;
✔ не отменяет собственную проверку адресов (код дрейфует), но ✖/⏳ — это места, где
написанному в плане следовать НЕЛЬЗЯ без перепроверки.
❔ — отдельное правило (`PROTOCOL.md §Жизненный цикл, шаг 2`): число пересъёмывается
стендом своей волны ДО любого решения, на нём основанного; до пересъёма это не факт,
а гипотеза плана. Если новое число меняет вывод пункта — стоп и вопрос Максиму.


**Итог по всем этапам:** ✔ 336 · ⏳ 2 · ✖ 15 · ❔ 35; пунктов с проблемными адресами: 5.


## Этап 0 — 30✔ / 1⏳ / 0✖ / 5❔ (пунктов: 13)

Этап 0 в основном опирается на реальный код: из 33 клеймов 27 CONFIRMED (числа сходятся или в допуске: 73 тест-файла vs 72, 235 except vs 215, 6 Downloader-классов ровно, 53+57+92 КБ ровно), 1 STALE, 5 UNVERIFIABLE, 0 REFUTED. Главная ловушка — план частично уже исполнен после 2026-08-14: пункт 0.1 сделан целиком (оба модуля + тесты в git), MEASUREMENTS.md заведён, а диагноз 0.0 протух — fastapi стоит, ноль исполняемых тестов теперь даёт битый PySide6 в двух UI-файлах. Вторая по опасности ловушка — 0.11: имена tools/edit_bench.py и tools/bench/edit_baseline.json уже заняты другим живым инструментом (судья «Ручной правки», 2026-07-31), и новый стенд диффов при бездумном следовании адресам его перезапишет.

**Не-подтверждённое и ловушки:**
- ⏳ `0.0` коллекция pytest прерывается: ModuleNotFoundError: fastapi в tests/observability/test_stage_durations.py — Сегодня `python -m pytest --collect-only tests/observability/test_stage_durations.py` → 13 tests collected, ошибок нет; `import fastapi` → 0.141.1. Коллекция всё ещё прерывается, но ДРУГИМ: 2 ошибки PySide6. Коммит 2e52af5 (2026-08-14) прямо фиксирует, что доки больше не порождают пункт 0.0
- 🪤 `0.0` Диагноз устарел: fastapi давно стоит, ноль тестов даёт битый PySide6 в 2 UI-файлах — чинить/исключать надо их, иначе исполнитель будет искать несуществующую проблему окружения
- ❔ `0.12` Прогнать конвейер локально сегодня нечем — Проверяется только запуском стека (docker, БД, celery). Чтением видно обратное косвенно: docker-compose.yml полный, веса в models/ на этой машине есть — «нечем» как минимум требует уточнения «не проверено», а не «нет компонентов»
- 🪤 `0.12` «5 сервисов» — это только P&ID-часть compose-файла из 15 сервисов; для локального прогона надо явно выбирать подмножество, иначе исполнитель потащит весь CVAT-стек
- 🪤 `0.1` Пункт УЖЕ ВЫПОЛНЕН целиком: оба модуля + тесты (tests/test_contours_merge.py, tests/test_canvas_to_fxml.py, tests/test_canvas_pipeline_golden.py) в git с 2026-08-14 — исполнителю здесь делать нечего, не переделывать
- 🪤 `0.2` wave14_smoke.log не просто мусор рабочего дерева — он в git-индексе: нужен `git rm`, одного удаления/.gitignore мало
- 🪤 `0.3` Базовый прогон сейчас невозможен без решения по двум PySide6-тестам из 0.0 — сборка обрывается до исполнения, маркеры и CI на этом не взлетят
- 🪤 `0.4` Рядом уже есть 2 UI-теста хендлеров (test_mode_exit_finishes_gesture, test_interactive_drag) — новые тесты должны бить именно в mousePressEvent-диспетчер, а не дублировать покрытие хендлеров
- 🪤 `0.5` AppearanceMixin сводить не надо — он уже един, дублируется обвязка в 5 вкладках; при слиянии _RecognizeWorker не потерять isinstance-guard из ocr_binding-версии — это не косметика
- 🪤 `0.6` Те же 1800/1740 стоят ещё в строках 249-250 и 322-323 WORKER_TASKS.md — но для других задач (worker/tasks/graph.py:130-131 реально 1800/1740): править только блок detection, не все вхождения подряд
- 🪤 `0.8` Данные существуют только в локальном storage этой машины (не в git) — фикстуру можно собрать лишь отсюда, на чистом клоне пункт невыполним
- ❔ `0.9` Приоритеты плана расставлены по семи жалобам, а не по замеру — Утверждение о происхождении приоритетов плана — кодом не проверяется (нужен анализ истории решений, не репозитория)
- 🪤 `0.9` ДН2 жёстко зависит от порядка с 0.2 (диффы снять ДО удаления файлов), и 20 файлов — не 20 независимых точек: tools/edit_bench.py называет их «7 сохранений одного чертежа»
- ❔ `0.11` Ноль правок боевого кода — Обещание про будущий инструмент — по коду не проверить. Паттерн поддерживается: tools/ почти не импортируется боевым кодом (единственное исключение — worker/tasks/graph.py:726 импортирует tools.fxml_standardize)
- 🪤 `0.11` Имя нового стенда конфликтует с живым tools/edit_bench.py + tools/bench/edit_baseline.json — их нельзя перезаписать; и «контракт существующих шести» завышен: полный контракт --check+эталон-в-git есть у ОДНОГО инструмента (edit_bench), fail→exit 1 — у четырёх (edit_bench, layout_bench, cmp_bitexact, drag_stress_probe)
- ❔ `0.10` без детерминизма cmp_bitexact краснеет ложно — Недетерминизм доказывается только двумя прогонами раскладки (рантайм). Косвенно: RNG-импортов в modules/graph/core нет, но есть обходы set (напр. spread.py:189 `grp, stack = {seed}, [seed]`) — возможный источник межпроцессной недетерминированности; tools/cmp_bitexact.py реально сравнивает бит-в-бит и возвращает 1 при расхождении (:137)
- 🪤 `0.10` MEASUREMENTS.md уже создан и наполнен (лежит нетрекнутым в корне) — не заводить заново, а закоммитить и дополнять
- ❔ `0.7` 55 из 107 ID не упомянуты в «предлагаемом порядке» — отсутствие читается как отмена — Требует полной переписи ID мастер-плана и сверки с разделом порядка — семантический разбор документа, а не кода; выборочно не проверяется
- 🪤 `0.7` ID «Г1» в four_fronts двоится сам (:394 — гейт статусов workspace, :1383 — ресайз контура), а К1 — не про сам auto_fix_graph, а про гейт _shift_node: при составлении сводной таблицы легко связать не те строки


## Этап 1 — 55✔ / 0⏳ / 1✖ / 3❔ (пунктов: 29)

Этап очень качественный: из ~50 фактических утверждений опровергнуто одно (1.10: «63 файла из 69 используют logging» — на деле 22 из 69, завышение втрое), всё остальное подтверждено кодом на текущих или чуть сдвинувшихся строках; протухших (STALE) нет — файлы-адреса не менялись после 2026-08-13. Неверифицируемыми остались только конкретные числа стендовых замеров (Δ px, «9 узлов из 32», «9 из 12 задач») и утверждения о данных на диске/в БД — их механизмы при этом подтверждены. Главная ловушка этапа: часть защит уже частично лежит в коде (try/finally в edit_smooth._try_remedies, идемпотентный базлайн и undo-команда в apply_resize, Esc-обработчик resize, save_requested_callback у square-редактора) — исполнитель должен чинить остаточные дефекты (алиасинг снимка, пины вне базлайна, перезапись _mode_before_resize), а не накатывать фиксы заново; плюс точечные поправки адресов: спека в deploy_ready/ (:82), base_graph_editor.py:747 — не копия проверки, прямых commit→send_task точек 9, а не 10.

**Проблемные адреса:**
- `1.24`: адреса — данные (storage/diagrams/*, таблицы processing_stages/artifacts, колонка cvat_task_id), кодом не проверяются; scripts/ существует и содержит только init_db.py и download_sam2_base.py

**Не-подтверждённое и ловушки:**
- ✖ `1.10` 63 файла из 69 в ui/ уже используют logging — Пересчёт: find ui -name '*.py' = 69 файлов; rg -l 'logger\.|getLogger|logging\.' ui = 22 файла (с учётом --no-ignore и паттерна '\blog\b|logger|logging' — максимум 23-25). Реально логируют ~22 из 69, а не 63 — расхождение в 3 раза, не объяснимое дрейфом кода (клиентские файлы logging не теряли)
- 🪤 `1.10` Премисса «код прошит логами, потерян только приёмник» завышена втрое: логируют 22 файла из 69, FileHandler+excepthook сделает видимыми только их; и путь спеки — deploy_ready/pid_client_windows.spec:82
- 🪤 `1.1` Часть защиты уже в коде с 2026-08-03: _try_remedies (:583-610) держит внешний try/finally+restore_all — исполнителю чинить алиасинг (deepcopy при откате) в restore/restore_all внутри edit_smooth и переносить finalize/push в try/finally в advanced_graph_editor, а не добавлять ещё один try/finally в движок
- 🪤 `1.2` Редоставка случается только если детекция реально идёт дольше 3600 с — на быстрых листах дубля не будет, воспроизводить надо на тяжёлой диаграмме
- 🪤 `1.3` Наивное «исправление» ключа graph_val→val_graph включит штатный откат со всеми его последствиями — план требует убирать молчаливые откаты, а не чинить их срабатывание
- 🪤 `1.4` Код уже частично переработан: геометрия превью идемпотентна от базлайна, а apply_resize УЖЕ пишет SnapshotCommand в undo (:4311-4341) — остаточные баги только «пины не в базлайне» и «брошенное превью без отката», не переделывать заново всю схему
- 🪤 `1.6` Esc-обработчик формально ЕСТЬ (simple:464-469) и honest-путь переключения ручек тоже (handlers:168-169) — чинить надо именно перезапись :276 (не сохранять 'resize_node' как предыдущий режим), а не добавлять новые обработчики выхода
- ❔ `1.8` Найдено проверкой 2026-08-11 — Провенанс замера; кодом не проверяется. Оба механизма, впрочем, воспроизводимы чтением кода (см. выше)
- 🪤 `1.8` Защита от «залипшего drag» уже есть, но только для ctrl-drag узлов (base_graph_editor.py:1717-1721) — overlay-тягу она не гасит; чинить надо именно overlay-путь
- ❔ `1.9` Аудит этого не видел; тот же класс дефекта, что 1.3–1.5, но в другой вкладке — Утверждение о покрытии аудита (документы плана), не о коде; сам дефект в коде подтверждён
- 🪤 `1.12` Ключ error_stage — именно строка "direction_classification" (не "classifying_direction") — добавлять её
- 🪤 `1.13` Прямых точек в app/api — 9, «десятая» набирается только с учётом validation.py/layout_dispatch (или worker/tasks/skeleton.py:768) — при исполнении брать полный список из пересчёта, а не только 6 файлов из адресов (graph.py×2 в адресах пропущены)
- 🪤 `1.x1` Красный индикатор всё же может мелькнуть один раз при старте с полностью лежащим API — «мёртв» он для деградации и для потери связи ПОСЛЕ старта
- 🪤 `1.20` Сам _check_masks_completion никакого вопроса не задаёт — «отказ» относится к диалогу о несохранённом в _close_active_tab (:1296-1316); липкость флага вдобавок питает гонку двойной скелетизации из 1.x2
- 🪤 `1.23` Чинить надо с различением 404/прочее: рядом (:718-728) для contours_validated это различие уже введено как критичное — образец в том же методе
- ❔ `1.24` 1.2 предотвращает будущее, но не чинит случившееся — последствия уже лежат на диске и в БД — Требует инспекции storage/ и боевой БД (вне области поиска и вне репозитория); сама посылка логически следует из природы фикса visibility_timeout, а классы последствий подтверждены кодом в 1.x4 (create_task неидемпотентен, attempt без блокировки, upsert неатомарен)
- 🪤 `1.24` scripts/ не пуст — «нет ни одного» верно только про эксплуатационные/ремонтные скрипты, установочные там есть
- 🪤 `1.x4` «Блокировок нет нигде» верно для задач, но один advisory lock в проекте всё же есть (app/api/diagrams.py:106, нумерация при upload) — не принимать его за прецедент защиты задач
- 🪤 `1.x5` Замена _node_geom_kind на _node_has_polygon НЕ поведенчески-нейтральна: при включённых скинах скиновый узел станет 'box' вместо 'poly' (это, видимо, и есть желаемое FXML-приоритетом, но точки вызова :4031 и :5496 поменяют поведение — проверять с включёнными скинами); base:747 из списка копий вычеркнуть


## Этап 2 — 29✔ / 0⏳ / 4✖ / 1❔ (пунктов: 16)

Этап добротный: из 30 проверенных утверждений 26 подтверждены кодом с точностью до 1-2 строк (кэш моделей, NLM-хардкод, ансамбль ×3, TTA-дефолты, потоки torch, SAM2 O(n²), тайлы OCR, /health, ADR-004), все числа пересчитаны и сошлись (609 МиБ/808 МиБ весов, 9 таблиц). Опровергнуты 4 утверждения в 4 пунктах: 2.9 — таблица 4.1 WORKER_TASKS.md протухла ещё в июне (1800/1740 против боевых 5400/5340); 2.10 — junction_segmentation/inference.py не оффлайн-скрипт, его зовёт боевой воркер с use_amp=True; 2.12 — в указанных строках compose закомментированы GPU-резервации, а лимитов памяти в файле нет вообще; 2.13 — дефолт Redis 7 это noeviction, «вытеснение по умолчанию» не существует. Главная ловушка этапа: пункты про docker-compose (2.12, 2.13) написаны по памяти, а не по файлу — исполнитель, следуя тексту буквально, «раскомментирует» GPU-блок на CPU-сервере и «починит» у Redis несуществующую проблему вместо реальной (неограниченный рост памяти).

**Проблемные адреса:**
- `2.12`: содержимое не то: docker-compose.yml:158-164 и :205-211 — закомментированные блоки GPU-резервации (deploy.resources.reservations.devices nvidia, «GPU отключён для CPU-сервера (Д5)»), а не лимиты памяти

**Не-подтверждённое и ловушки:**
- ✖ `2.9` Остальные девять таблиц §4 сверены и верны — На дату плана §4 имел ровно девять таблиц (4.10 добавлена коммитом 6bd3c87 2026-08-14), но одна из девяти уже была неверна: 4.1 task_detect_yolo в доке — time_limit 1800/1740 (WORKER_TASKS.md:193-194), в коде — 5400/5340 (worker/tasks/detection.py:56-57, введено a1e0e14 ещё 2026-06-22). Верны восемь из девяти; остальные таблицы (4.2-4.10) пересчитаны против декораторов задач — совпадают, включая 4.10 (layout.py:54-55 1800/1500)
- 🪤 `2.9` Доверившись «остальное верно», исполнитель допишет две задачи и оставит в 4.1 протухшие лимиты 1800/1740 при боевых 5400/5340 — док снова разойдётся с кодом в самом дорогом месте (детекция)
- ✖ `2.10` torch.amp.autocast("cuda", enabled=cfg.amp) без guard в трёх файлах — оффлайн-скрипты, воркером не зовутся — batch_inference.py:145 и find_thresholds.py:69 — действительно офлайн (только `python -m`, воркер не импортирует). Но junction_segmentation/inference.py:177 — БОЕВОЙ путь: worker/tasks/junction.py:79-80 импортирует run_inference и зовёт его на :211-219 с явным use_amp=True — незащищённый autocast("cuda") выполняется воркером на каждой диаграмме (на CPU — warning+no-op на каждый батч тайлов)
- 🪤 `2.10` «Чинить при отделении на Этапе 13, не сейчас» опирается на ложную премису: inference.py — не оффлайн, его зовёт task_detect_junctions с use_amp=True; guard в боевом junction-пути нужен уже сейчас, а не на Этапе 13
- ✖ `2.12` docker-compose.yml:158-164, :205-211 — закомментированные лимиты памяти при restart: unless-stopped — В этих строках закомментирован deploy.resources.reservations.devices (nvidia GPU) — резервация устройства, не memory limits. Лимитов памяти (mem_limit / deploy.resources.limits.memory) в docker-compose.yml нет вообще — ни активных, ни закомментированных (grep: 0). Верна только вторая половина: restart: unless-stopped у обоих воркеров (:153, :201)
- ❔ `2.12` Убит OOM-killer'ом может быть Postgres — Поведение ядра, чтением кода не проверить (нужен стенд). Правдоподобно: без cgroup-лимитов host-wide OOM-killer выбирает жертву по badness, и это может оказаться postgres; но это свойство Linux, не репо
- 🪤 `2.12` «Раскомментировать» нечего: раскомментировав указанные строки, исполнитель включит nvidia-GPU-резервацию и уронит compose на CPU-only сервере; лимиты памяти нужно писать с нуля (и помнить, что deploy.resources.limits без swarm игнорируется — нужен mem_limit/compose v2 формат)
- ✖ `2.13` Политика по умолчанию вытеснение допускает; вытесненный ключ = потерянная задача — У Redis 7 дефолт — maxmemory-policy=noeviction и maxmemory=0 (без лимита): по умолчанию Redis НЕ вытесняет ключи никогда. Реальный риск текущей конфигурации — неограниченный рост памяти до host-OOM, а не вытесненная задача. Сама правка (maxmemory + явный noeviction) осмысленна как ограничитель, но её обоснование в плане неверно
- 🪤 `2.13` Пункт продан как «корректность», но премиса ложна: бездумно поставленный маленький maxmemory при noeviction заставит Redis ОТКАЗЫВАТЬ в записи при заполнении (задачи станут теряться уже по-настоящему) — размер лимита надо выбирать по замеру, а не «просто включить»
- 🪤 `2.x2` «Пять сервисов» — это только P&ID-блок: в том же файле живут ещё 10 сервисов CVAT/traefik, и при восстановлении worker_sam2 сеть/volumes надо брать из существующего worker, а не из ADR


## Этап 3 — 38✔ / 0⏳ / 3✖ / 2❔ (пунктов: 17)

Этап 3 в очень хорошей форме: из ~38 проверяемых утверждений подтверждено ~32, причём ключевые числа сходятся до единицы (215 except Exception с точной раскладкой 32/34/46/103, 54 commit / 0 rollback в app/, ровно 18 мест base_path/artifact.file_path, ровно 2 голых except в modules/); протухших почти нет — только мелкие сдвиги строк (extractall 184→177, tenacity 20→21). Опровергнуто три утверждения в двух пунктах: в 3.6 гонка номера диаграммы уже закрыта pg_advisory_xact_lock (diagrams.py:105-106, стоит минимум с февраля 2026 — чинить не надо, остаточный риск только salted hash() при мультипроцессном API), а в 3.14 очередей четыре, а не три (default с skeleton/graph/layout пропущена), и дефолтный таймаут inspect (1 с) МЕНЬШЕ, чем у /health (2 с), а не больше. Главная ловушка этапа — 3.14: эндпоинт /monitoring/queues, собранный строго по списку плана, молча ослепнет на очередь default, т.е. на половину конвейера.

**Не-подтверждённое и ловушки:**
- ❔ `3.1` Задача снята: контур закрыт — «Контур закрыт» — факт инфраструктуры/деплоя, из кода не проверяется. Косвенно согласуется: в app/ по-прежнему ни одного механизма auth (grep APIKey|OAuth|get_current_user|HTTPBearer|Security( по app/ — 0 совпадений; CORS allow_origins=['*'] в app/main.py:42), т.е. задачу действительно не делали, а сняли.
- 🪤 `3.1` Проверить, что контур действительно закрыт, по коду нельзя — если это не так, все 64+ эндпоинтов открыты без auth; исполнителю не принимать «закрыто» на веру из плана.
- 🪤 `3.2` Чинить только строки 36 и 83 недостаточно: те же дефолтные пароли продублированы в DATABASE_URL трёх сервисов (78, 122, 173) и в CVAT_PASSWORD воркера (127) — пропуск любого оставит дыру.
- 🪤 `3.3` extractall уехал на строку 177, а «нет проверки типа» верно лишь частично — на половине эндпоинтов тип проверяется (JSON/content_type), нет именно лимита размера; не удалить существующие проверки, добавляя новые.
- ✖ `3.6` app/api/diagrams.py:125 `SELECT max(number)+1` без блокировки → IntegrityError на `uq_diagram_project_number` при параллельном аплоаде — Блокировка ЕСТЬ: diagrams.py:105-106 — `lock_key = hash(project_code) % 2**31; SELECT pg_advisory_xact_lock(lock_key)` перед max(number) (:125), с комментарием «advisory lock per-project предотвращает race condition»; по git это было уже до/на 2026-02-09 (447e5e9), т.е. до даты плана. Остаточный нюанс: ключ от питоновского salted hash() — межпроцессно совпадает только при одном процессе API (Dockerfile.api: uvicorn без --workers) или фиксированном PYTHONHASHSEED.
- 🪤 `3.6` Гонку номера диаграммы чинить НЕ надо — advisory lock уже стоит (105-106); слепое выполнение плана даст пустую работу. Реальный остаток — salted hash() как ключ лока (сломается при uvicorn --workers>1).
- 🪤 `3.7` «13 воркерных файлов» на деле 9 файлов (12 вхождений), а 3 места в app/api ходят через settings.STORAGE_PATH, а не os.getenv — сценарий расхождения именно settings-vs-getenv (env выставлен только в .env-файле), учесть при сведении.
- 🪤 `3.8` Формулировка «в вызывающем потоке FastAPI» верна наполовину — API уже прячет поллинг в to_thread; переделка нужна не от блокировки event loop, а от минут занятого потока пула и от молчаливого таймаута :375.
- 🪤 `3.10` «25 тестов» — это 25 тест-ФАЙЛОВ (145 тестов); при рефакторинге логирования ломается сразу большой контракт, не пять ассертов.
- ✖ `3.14` Очередей три: `gpu`, `ocr`, `sam2` (worker/celery_app.py:71-81) — Очередей четыре: task_queues (celery_app.py:57-63) = default, gpu, ocr, sam2; в default роутятся skeleton, graph, layout (:75,77,80); воркеры слушают '-Q default,gpu,sam2' (Dockerfile.worker:112-116) и '-Q ocr' (Dockerfile.worker_ocr:80-84). LLEN только по трём из плана потеряет default.
- ✖ `3.14` У `inspect` таймаут по умолчанию больше, чем у дефектного /health — Наоборот: подпись из установленного celery — Inspect.__init__(..., timeout=1.0, ...) (проверено в .venv311), а /health зовёт celery_app.control.ping(timeout=2) (app/main.py:72). Дефолт inspect (1 с) МЕНЬШЕ таймаута /health (2 с).
- 🪤 `3.14` Главная ловушка этапа: /monitoring/queues по трём очередям из плана ослепнет на default — а именно там живут skeleton/graph/layout; и аргумент «inspect ждёт дольше /health» ложный (1 с против 2 с), хотя вынос inspect из запросного пути всё равно разумен.
- ❔ `3.x1` Дублирование задач брокером в Flower видно сразу (вторая живая `task_detect_yolo` по одной диаграмме на 60-й минуте); сейчас это не видно ниоткуда — Рантайм-поведение; чтением кода проверяются только предпосылки: task_acks_late=True + task_reject_on_worker_lost=True (celery_app.py:53-54), task_time_limit=3600 (:49) и отсутствие переопределения visibility_timeout — redelivery на ~60-й минуте правдоподобен, но нужен живой стенд/Flower.


## Этап 4 — 16✔ / 0⏳ / 0✖ / 3❔ (пунктов: 9)

Этап 4 фактологически крепкий: из 15 проверяемых утверждений 12 CONFIRMED (числа 2201/40/2/484/33, порог 1.4 vs 1.3, мёртвый run_paddle_recognize, мёртвый text_edited с 2 вызовами stamp(), три «резалки» поля angle, 4+1 место kksVisible — всё пересчитано и сошлось), 3 UNVERIFIABLE (14/161, 0 из 39, 53/161 — корпусные замеры по локальному storage, кодом не проверяются), 0 REFUTED и 0 STALE — код с даты замеров не менялся (последний коммит 2e52af5). Главная ловушка этапа — расхождение слов и фактуры в «дублях»: в 4.4 «канон» с возвратом None — это Qt-миксин, а не text_import, куда план велит сводить (у самого text_import голый else, спасаемый гвардом на вызове), а в 4.3 расхождение клампнутого/сырого правила латентно (0 из 3971 блоков корпуса) — исполнитель, ищущий живой репро, потеряет время. Вторая ловушка — все «числа для Максима» из 4.x1/4.x2 невоспроизводимы на чистом клоне и требуют повторного замера на живом storage.

**Не-подтверждённое и ловушки:**
- 🪤 `4.3` Дефект латентный: по замеру корпуса (MEASUREMENTS.md §10.1) расхождение клампнутого и сырого правил — 0 из 3971 блока (боксов со стороной <1 px в данных нет) — живого репро «в редакторе и в выгрузке разная ориентация» не искать, сведение делается ради контракта
- 🪤 `4.4` Слово «канон» в claims указывает НЕ на text_import (куда gist велит сводить), а на Qt-миксин: None при мусорном side возвращает ocr_layer_mixin._bound_block_bbox, а целевой канон text_import.bound_block_bbox сам содержит голый else (его спасает только гвард `side in _SIDES` на вызове :324) — при сведении легко взять за эталон не то поведение
- 🪤 `4.6` Порог 1.4 управляет только поворотом кропа перед распознаванием — «угол как данные» из OCR никуда не сохраняется; зона расхождения (1.3;1.4] редкая (замер §10.2: 9 из 3971 блоков) — дефект живой, но на большинстве листов невидим
- 🪤 `4.7` «4-й по размеру» верен только без vendor/ — сгенерированный vendor/adaptagrams/win/adaptagrams.py (6613 строк) крупнее; и при распиле помнить: символы graph_to_fxml импортируют не только два конвертера, но и ядро раскладки (pretransform.py:100,410), worker/tasks/graph.py:507,682, редактор (advanced_graph_editor.py:372,4517-4532)
- ❔ `4.x1` «подпись-подпись 0/161 при кегле 12» — на деле 14/161 — Число корпусное — нужен локальный storage/diagrams (вне области проверки, невоспроизводим на чистом клоне) и скрипт замера наложений. Провенанс в репо: MEASUREMENTS.md:5-6 (ручной пересчёт дал 14/161) и VERIFY_2026-08-11_deep_check.md:291 — кодом ни 0, ни 14 не проверяются.
- ❔ `4.x1` «31 свободная подпись из 39» не воспроизводится ни в одной постановке; при пороге 0.5 по bbox — 0 из 39 — Корпусное число, кодом не проверить. Провенанс: VERIFY_2026-08-11_deep_check.md:291 и VERIFY_part_crosscut.md:67 («0 из 39 при 0.5»); там же :61 — при containment ~0.3 получается 13 из 39 (основа принятого решения о пороге ~0.3).
- 🪤 `4.x1` Оба «исправленных» числа — из локального корпуса storage/diagrams (в MEASUREMENTS помечен 🖥 «на чистом клоне не воспроизведётся»): исполнитель не сможет их перепроверить чтением кода и обязан замерить заново на живом storage, прежде чем нести Максиму
- ❔ `4.x2` 53 подписи из 161 (33 %) лежат внутри чужого узла — Корпусное число (storage/diagrams, вне области проверки). Повторный замер в репо: MEASUREMENTS.md §10.4 — на листе 620cc50d 57 из 161 (35 %) при containment > 0.3, §10.3 — по корпусу 59 привязанных внутри чужого узла; порядок тот же, точное «53/33 %» не воспроизведено — итог зависит от порога и метода.
- 🪤 `4.x2` «Пятое место» grep по kksVisible не находит — это производитель привязки, а не запись атрибута; и в корпусе nodekks_*-привязок ноль (MEASUREMENTS §10.6), так что корпусной прогон СТ7 регрессию по ручному KKS не поймает — страховать юнит-тестом; число «53/161 (33%)» при повторном замере дало 57/161 (35%) — не считать его точным


## Этап 5 — 17✔ / 0⏳ / 0✖ / 0❔ (пунктов: 6)

Этап 5 — самый точный из проверенных: все 14 фактических утверждений по 6 пунктам подтверждены кодом, чисел-расхождений нет (50/18 и 7/4 присваиваний, 10 кортежей в validation.py, 16 статусов _STAGE_ORDER, 30 значений _STATUS_ORDER, 12 UI-таблиц, 129 против 32 ссылок — всё сошлось до единицы; ~24 набора = 24-27 в зависимости от учёта already_past). Протухших и опровергнутых нет; адреса точны, единственная неточность — _ROLLBACK_TARGET объявлен в _setup_ui, а не буквально в __init__. Главная ловушка этапа — полуправда о «мёртвом»: цифра ROADMAP «~247 мест» завышена в ~4-5 раз (реально 60), CONTOURS_EXTRACTED/OCR_COMPLETED остаются легальными rollback-целями, а у «недостижимых» бусин ocr/fxml мертва только ветка AVAILABLE — сносить их целиком нельзя.

**Не-подтверждённое и ловушки:**
- 🪤 `5.1` Расхождений нет; фикс — добавить "ocr" только в кортеж preserve_contours (:1475), preserve_ocr трогать нельзя (при «Переделать OCR» старые OCR-артефакты обязаны снестись).
- 🪤 `5.2` Оценка объёма по ROADMAP завышена в ~4-5 раз: реальный фронт — 60 присваиваний и ~24 гейта, а не 247 мест; исполнителю мерить по свежему счёту, не по ROADMAP.
- 🪤 `5.3` «_ROLLBACK_TARGET в __init__» дословно не найдётся — он в _setup_ui (:603); и «32 на сервере» — это один rollback.py, весь app/api содержит ~203 ссылки на DiagramStatus, сводить к реестру придётся больше, чем 32 места.
- 🪤 `5.4` «Мёртвые» статусы не все одинаково мертвы: CONTOURS_EXTRACTED и OCR_COMPLETED — легальные rollback-цели в _STAGE_ORDER (снос из enum сломает откат), а у бусин 'ocr'/'fxml' мертва только ветка AVAILABLE — сами бусины отображаются и доходят до COMPLETED.
- 🪤 `5.6` Серверный канон нельзя строить с нуля «потому что порядка нет вовсе» — rollback.py:26 уже держит серверный порядок статусов, и новый реестр обязан его поглотить, иначе появится третий рассинхронизируемый список.
- 🪤 `5.5` Канон :860-873 живёт внутри таймер-колбэка, обёрнутого в собственный `except Exception: pass` (:883-884) — при сведении к канону не утащить этот глушитель вместе с логикой.


## Этап 6 — 29✔ / 0⏳ / 0✖ / 0❔ (пунктов: 11)

Этап 6 — самый точный из проверенных: все 31 фактическое утверждение по 11 пунктам подтверждены сегодняшним кодом (ветка dev), ни одного STALE или REFUTED; ключевые числа сошлись с точностью до мелкого дрейфа (прологов задач 11, а не 10; к 11 сырым send_task в app/api есть 12-я точка worker/tasks/skeleton.py:768 и chain().apply_async в app/api/segmentation.py:97-100, которые в периметр 6.9 не попали; «9 из ~24» точек диспатча пересчитаны и сошлись ровно). Главная ловушка этапа — адреса-basename без каталогов: skeleton.py/segmentation.py/detection.py в соседних пунктах означают то app/api/, то worker/tasks/ (в 6.10 skeleton.py — оба файла сразу), diagram_workspace.py и error_report_dialog.py живут в ui/widgets/, а адрес «:1947» в 6.7 вообще без файла. Вторая ловушка — «частичная база» safe_dispatch (worker/utils/db_helpers.py:111) в боевом коде не вызывается ни разу, а ключ skeletonizing_simple, которого «не знает» ретрай 6.5, есть в мёртвом retry_operation (app/api/diagrams.py:460) — чинить легко не тот эндпоинт.

**Не-подтверждённое и ловушки:**
- 🪤 `6.1` Формулировка «все 4 ветки» скрывает пятую законную ветку :118 (completed/skipped) — запирать надо фолбэки «пускаем, потому что не поняли», а не всё подряд (Л9 в EXECUTION.md).
- 🪤 `6.2` Адрес :199 указывает на строку фолбэка, а ветка «сейчас True» — это :200-201; и diagram_workspace.py:198 держится за приватную _parse_dt гейта — при правке одного файла второй сломается молча.
- 🪤 `6.4` Адрес :17-19 — это только импорты; исполнитель, открывший лишь эти строки, не увидит ни удаления файлов до commit (:233-259 vs :265), ни точки, где надо ревокать задачи.
- 🪤 `6.5` Ключ skeletonizing_simple ЕСТЬ в другом, мёртвом ретрае — app/api/diagrams.py:460 (retry_operation, из UI не вызывается): исполнитель может «починить» не тот эндпоинт и отчитаться зелёным.
- 🪤 `6.6` Дубль держится на трёх ногах сразу — UI (:1327 + вечный _pipe_confirmed), дыра already_past в validation.py:482-492 и гейт задачи skeleton.py:503, пускающий SKELETONIZING_FINAL; правка только UI-вызова гонку не закроет.
- 🪤 `6.7` Адрес «:1947» не называет файл — в одноимённых точках этапа это то app/api, то worker/tasks; здесь нужен ui/widgets/diagram_workspace.py, а «удалённый» _on_button_click на деле пока жив (:1436).
- 🪤 `6.8` Реальных прологов 11, а не 10 (layout в счёт плана не попал); и «частичная база» safe_dispatch (db_helpers.py:111) в боевом коде не вызывается ни разу — только в тестах, т.е. это не база, а мёртвый четвёртый враппер.
- 🪤 `6.9` «Свести 11 точек» — неполный периметр: 12-я сырая точка worker/tasks/skeleton.py:768 и chain().apply_async в segmentation.py:97-100 в список не попали; сведя только перечисленное, авто-звенья останутся сырыми.
- 🪤 `6.10` skeleton.py фигурирует в одном пункте ДВАЖДЫ как разные файлы (app/api/skeleton.py:19 — мёртвый эндпоинт, worker/tasks/skeleton.py:229/:639 — мёртвые ветки) — снос «по basename» рискует удалить не то; error_report_dialog лежит в ui/widgets/, не в ui/dialogs/.


## Этап 7 — 10✔ / 0⏳ / 2✖ / 1❔ (пунктов: 5)

Этап добротный по адресам (все 20+ адресов точны, сдвиг строк только у sam2_contour.py на 1), но с двумя содержательными проколами. Из 13 клеймов: 10 CONFIRMED, 2 REFUTED, 1 UNVERIFIABLE. Главная ловушка — 7.3: «расходящийся PIN_TOL» никогда не существовал (обе копии 0.5 с момента введения, решением 2026-08-14 заморожены — остаётся лишь дедуп двух копий), и туда же 7.2: «пятый допуск» _axes.ORTHO_TOL=1.0 — мёртвая константа без единого использования, тогда как реальная четвёртая копия 1.5 (_gate.py:27 STRAIGHT_TOL) в плане не упомянута. Счёт 11 IoU и смешение форматов в contours_merge подтверждены пересчётом; заметка исполнителю — pretransform.py:580 не IoU, а предикат наложения.

**Не-подтверждённое и ловушки:**
- 🪤 `7.1` «Редакторский» seat_rect живёт не в ui/editors, а в modules/graph/core/edit_checks.py — то есть внутри modules/; изоляция бит-эталона реально держится на том, что worker/app не импортируют edit_*-модули (проверено, 0), а не только на отсутствии импортов ui.editors
- ✖ `7.2` Сегмент, ортогональный для трёх реализаций, в _axes.py — диагональ — _axes.ORTHO_TOL=1.0 — мёртвая константа: repo-wide grep даёт единственное вхождение — само определение _axes.py:17; файл (105 строк) кластеризует центроиды узлов (cluster_1d/build_axes), сегментов вообще не классифицирует, а axial.py:14 импортирует из него только MODE, TOL, build_axes. Расхождение значений (dev 1.0-1.5 px) реально как числа, но ни один код в _axes.py не назовёт такой сегмент диагональю — конфликт латентный, не действующий
- 🪤 `7.2` Сводить надо не то, что в списке: «пятый допуск» _axes.ORTHO_TOL=1.0 — мёртвый код (0 использований), реального спора судьи с роутером нет, зато есть не учтённая планом четвёртая живая копия 1.5 — _gate.py:27 STRAIGHT_TOL (и там же третий SIDE_EPS)
- ✖ `7.3` edit_avoid.py:556-559 держит 0.5, layout/avoid_router.py:54 — второй, расходящийся PIN_TOL — Вторая копия существует, но НЕ расходится: оба PIN_TOL = 0.5 (edit_avoid.py:72 и avoid_router.py:54, комментарии дословно совпадают). По git оба были 0.5 с самого введения (a6c9340 для avoid_router, 610c782 для edit_avoid) — значения никогда не различались; это дубликат константы, а не развилка значений. То же зафиксировано в MEASUREMENTS.md строка 12.6 (2026-08-14): «Значения равны и согласны — заморожено решением»
- ❔ `7.3` Позиция 1: менять только клиентский, иначе сдвинется база всех замеров; позиция 2: в обоих, иначе сервер выбросит принятое редактором — Позиции проверяющих — не свойство кода, чтением не проверяются. Кодом видно лишь, что предпосылка спора снята: значения уже идентичны (0.5/0.5), и механика расхождения реальна лишь гипотетически (avoid_router.py:362-364 бракует маршрут при отклонении > PIN_TOL); по решению §8 от 2026-08-14 PIN_TOL заморожен
- 🪤 `7.3` Пункт описывает развилку значений, которой в коде никогда не было: оба PIN_TOL равны 0.5 с рождения, решение 2026-08-14 их заморозило — «разводить осознанно» нечего, максимум дедупликация двух копий одной константы; исполнитель, ищущий «второе значение», потратит время впустую
- 🪤 `7.4` pretransform.py:580 — не IoU, а булев предикат наложения в формате центр+w/h: при «сведении к канону» его нельзя механически заменить общей IoU-функцией (изменится семантика подсчёта наложений); и в счёте 11 не учтены 2 mask-IoU в pipe_segmentation/model/metrics.py
- 🪤 `7.5` Самоссылающиеся импорты `pipe_segmentation.*` — это стиль всего пакета (он же обещает CLI `python -m pipe_segmentation`, докстринг __init__.py:4-8): точечная замена на `modules.pipe_segmentation.` в двух файлах сломает пакет — чинить надо либо весь пакет разом, либо относительными импортами


## Этап 8 — 10✔ / 1⏳ / 0✖ / 7❔ (пунктов: 9)

Из 15 утверждений: 9 подтверждены кодом (адресация и механика диспетчера, воронка ресайза, легаси-близнецы трассировки, отсутствие дедупа, обход undo — всё бьётся строка в строку), 5 непроверяемы (все корпусные числа этапа — 3640/4763, 3205/3245, 40.5%/354px, 1518/31.8%, пик яркости 191 — требуют storage/-корпуса и стендов, которых нет; MEASUREMENTS.md:327-328 сам это фиксирует), 1 протух (8.9 снят 2026-08-14). Главная ловушка: пункт 8.9 уже растворён в задаче 1.6 · Р2 · Г1 — исполнение его «как написано» создаст лишнюю правку; вторая по важности — тесты test_connector_radius_split пиняют только радиус-инвариантность и не гарантируют падения при неверной правке Г5б, а при сносе легаси-трассировки (8.7) легко зацепить одноимённую obs-метку 'trace_edges' и живой compute_edge_statistics сразу за близнецами.

**Не-подтверждённое и ловушки:**
- ❔ `8.1` Сломает tests/ui/test_connector_radius_split.py:146-211 — Тесты существуют и пиняют _get_node_bbox (T1 :145-162, T2 :194-220), но T1/T2-свипы пересчитывают baseline ПОСЛЕ правки (инвариантность только к CONNECTOR_DRAW_RADIUS), абсолютные ассерты — лишь :162 (фолбэк equipment = радиус-бокс) и :185-188 (значения 8/4.0). Радиус-независимое ветвление по типу в _get_node_bbox эти тесты может и НЕ сломать — исход зависит от конкретной правки, проверяется только прогоном с ней
- 🪤 `8.1` Защита тестами слабее, чем звучит в плане: свип-тесты сравнивают с baseline после правки, и «неправильная» правка Г5б в примитиве может пройти их зелёными — не полагаться на tests как на единственный детектор, число мест 9 (не 8)
- 🪤 `8.2` Число 1518/31.8% исполнителю не воспроизвести без корпуса; но механизм и 4 теста — живые, диапазон 8.00–11.31 px следует из кода точно
- ❔ `8.3` Замер на полной популяции: канон 3640/4763 и 0 концов мимо формы; connect_* как есть — 4681, но 1562 мимо формы; connect_*+Г5б — 3631; D2 — 3205/3245 и 0 мимо формы — Замер стенда на корпусе storage/diagrams (8 uid, не в git, невоспроизводим). Сам проект это фиксирует: MEASUREMENTS.md:130 и :327-328 — «числа фронтов не перепроверялись… 3205/3245… замеры Этапа 8 (3640/4763…)» требуют стендов, которых нет
- ❔ `8.3` Г5 и Г5б взаимно уничтожаются: весь выигрыш +22 п.п. сидит в паре коннектор↔коннектор и держится на дефекте, который убирает Г5б — Число +22 п.п. — производная того же корпусного замера. Механизм согласуется с кодом (диспетчер contour_editor.py:338-343 сажает conn↔conn-конец на виртуальный r=8 бокс — это и есть «дефектный выигрыш», который Г5б убирает), но количественная часть чтением кода не проверяется
- ❔ `8.3` План мерил популяцию без conn+conn — 32 % рёбер вкладки — Доля conn↔conn-рёбер — свойство корпуса, не кода; согласуется с 31.8% из 8.2 (тот же корпус), но пересчитать без storage/ нельзя
- 🪤 `8.3` Все числа пункта наследуют доказательную базу стенда, которой на этой машине нет — исполнителю принимать D2 придётся на веру в VERIFY_part_front7, либо сначала поднять корпус
- ❔ `8.5` 40.5 % концов сдвинутся >5 px (max 354 px) — Корпусный замер (нужны реальные холсты storage/); MEASUREMENTS.md:328 прямо относит «замеры Этапа 8 (3640/4763, 40.5 %, 354 px)» к непроверенным — требуют стендов. Инструмент tools/reseat_preview_probe.py существует, но данных нет; докстринг base_graph_tab.py:386 даёт другую цифру того же явления (до 14.8% концов на старых холстах)
- 🪤 `8.5` Число 40.5%/354px не пересчитать без корпуса; сам код открыто документирует обход undo как решение заказчика (§8.3.1) — «провести под стек команд» будет пересмотром принятого решения, а не просто починкой
- 🪤 `8.7` Коллизия имён: строка 'trace_edges' живёт как метка obs-шага (builder.py:311, worker/tasks/graph.py:437, tests/observability/, ui/services/progress_model.py:78) — при сносе функций эти вхождения трогать нельзя; нижняя граница удаления — строка 966, compute_edge_statistics (:967) живой
- ❔ `8.8` На 13d1ef5f пик яркости 191 — Нужен растр диаграммы 13d1ef5f из storage/ (не в git). Кода «теста просвета» в репо ещё нет (Grep 'просвет|brightness' по modules/ — только аугментации обучения), порог 255 живёт в тексте плана, не в коде. MEASUREMENTS.md:327-329 относит замеры Этапа 8 к непроверенным
- ❔ `8.8` Случаев второго рода два, а не один — Подсчёт случаев по корпусу диаграмм — нужны данные storage/ и прогон; чтением кода не проверяется (реализации Г2 в коде нет)
- 🪤 `8.8` Пункт целиком висит на растре одной диаграммы, которой в репо нет — прежде чем зашивать порог ≥150, исполнителю нужно восстановить сам замер
- ⏳ `8.9` План не видит mode_handlers/simple_handlers.py:166-169 как второй путь — Кодовый факт жив (строки 166-169 зовут _enter_resize_mode:169), и против плана от 2026-08-11 претензия была верна (VERIFY_2026-08-11_deep_check.md:226). Но 2026-08-14 задача 8.9 СНЯТА: EXECUTION.md:385 и :610 — «у защёлки одно определение (_enter_resize_mode, simple_graph_editor.py:274-278) и четыре двери; гард в воронке закрывает все, simple_handlers.py не трогается». Код подтверждает воронку: все входы (:209, :480, simple_handlers.py:169, advanced:5499) идут через единственный _enter_resize_mode. Работа выполняется внутри 1.6 · Р2 · Г1 (EXECUTION.md:299, карта §10.4 ТЗ)
- 🪤 `8.9` ГЛАВНАЯ ловушка этапа: пункт уже снят 2026-08-14 — отдельная правка в simple_handlers.py будет лишней и разойдётся с принятой архитектурой «один гард в _enter_resize_mode»; исполнять через карту 1.6, не через этот текст


## Этап 9 — 6✔ / 0⏳ / 2✖ / 6❔ (пунктов: 7)

Из 14 фактических утверждений: 6 подтверждено, 6 непроверяемы чтением (все — числа со стенда layout_align/корпуса storage, которых в репо нет: 148/225, 55 %, 1 из 151, 32 %, 10.93→18.93), 2 опровергнуты — обе в ключевом пункте 9.1: сервер УЖЕ пишет концы принятого маршрута в порты (avoid_router.py ends_out :263-267 + apply_routing :441-444, с 2026-07-31, т.е. до написания плана), концы теряет только редакторский _avoid_apply_route. Главная ловушка этапа: исполнитель М1 по тексту плана будет чинить оба пути, тогда как работа чисто клиентская, а на клиенте мало дописать концы — надо повторить серверную связку «пере-посадка + судья сторон + адресный откат». Вторая ловушка — 9.2: пятый аргумент False в ShapeConnectionPin это proportional, а не exclusive, и в редакторе совпавшие концы сливаются в один класс пина кэшем смещений, так что «одним setExclusive» эксклюзивность не делается.

**Проблемные адреса:**
- `9.1`: advanced_graph_editor.py — basename, единственный боевой кандидат ui/editors/advanced_graph_editor.py (копии в .claude/worktrees не в счёт): ok, _avoid_apply_route ровно на :2004-2033. avoid_router.py — единственный modules/graph/core/layout/avoid_router.py: строки живы (mid = pts[1:-1] на :268), но содержимое частично не то: в этих же строках :263-267 уже стоит запись концов в ends_out — противоречит тезису пункта.
- `9.5`: edge_routing.py — basename, единственный боевой кандидат ui/editors/edge_routing.py: :328 def _segments_collinear_overlap — ok; :367 вызов в score_candidate — ok; :394 — строка turns * 100 в блоке return :391-395; сам штраф наложения collinear_overlap * 5 на :391 (адрес указывает в скоринг, но не в строку наложения).

**Не-подтверждённое и ловушки:**
- ✖ `9.1` advanced_graph_editor.py:2004-2033 пишет mid = pts[1:-1], концы не трогает; то же на сервере avoid_router.py:262-271 — Половина про редактор верна: ui/editors/advanced_graph_editor.py:2013-2018 — mid = pts[1:-1], пишутся только waypoints и кэши сторон, source_point/target_point не трогаются (единственный вызов — :1983). Половина про сервер ложна: modules/graph/core/layout/avoid_router.py:263-267 пишет концы в ends_out ('концы — в порты'), apply_routing передаёт ends_new (:427-428) и применяет в e['source_point']/['target_point'] (:441-444). Код таким стал в caffe8f 2026-07-31 — ДО даты плана, т.е. утверждение было неверно уже при написании, это не дрейф. Приёмка :362-364 держит концы маршрута в пинах с PIN_TOL=0.5, так что ends_out ≈ концы принятого маршрута.
- ✖ `9.1` «М1 разблокирует М2» — опровергнуто: оба потребителя берут только pts[1:-1] — «Только pts[1:-1]» верно лишь для редактора (advanced_graph_editor.py:2013). Сервер берёт pts[1:-1] для waypoints (avoid_router.py:268), но ПЛЮС записывает концы через ends_out (:263-267) с применением в apply_routing (:441-444). «Оба потребителя» — неверно: концы теряет один путь из двух, работа М1 чисто клиентская.
- ❔ `9.1` Размер М1 не M, а L: 8 зависимостей — Перечень «8 зависимостей» в коде не воспроизводится — это оценка объёма из плана; из кода видно лишь совместимое число: «конец в пине» держат 2 копии PIN_TOL (edit_avoid.py:72,556-559; avoid_router.py:54,362-364) + сторожа pinned_port/PORT_MATCH_TOL в edit_engine.py:359, edit_smooth.py:179, autofix_chains.py:451-453, ports.py:212/:40 — порядка 8 якорей, но соответствие их списку плана не установить чтением. К тому же серверная половина М1 уже сделана (см. выше), что меняет размер задачи.
- 🪤 `9.1` Исполнитель, следуя тексту буквально, полезет чинить сервер, где запись концов уже работает с 2026-07-31 (ends_out + apply_routing:441-444) — реальный объём М1 только редакторский (_avoid_apply_route), и после записи концов там надо повторить серверную связку «пере-посадка + судья сторон» (:453-468), а не просто присвоить pts[0]/pts[-1].
- ❔ `9.2` Лечит 148 наложений из 225 (97 из них с серверного пути, где слотов нет вовсе) — Числа 148/225 и 97 — замер стенда по корпусу storage/diagrams (данных нет в репо; MEASUREMENTS.md:327-329 сам числит «148/225» среди непроверяемого без стенда). Кодом подтверждается только под-факт «на серверном пути слотов нет»: слоты живут лишь в редакторе (edit_engine.py:96 _slot_seat, вызов :376; SLOT_PITCH ports.py:39), в modules/graph/core/layout/ упоминаний слотов ноль — сервер ставит по свежему пин-классу на конец (avoid_router.py:336-340) без распределения.
- 🪤 `9.2` Ловушка чтения кода: пятый позиционный аргумент False в ShapeConnectionPin(..., False, 0.0, ...) на обоих путях (avoid_router.py:338-340, edit_avoid.py:436-438) — это proportional, а НЕ exclusive; setExclusive в проекте не вызывается нигде (грепом только vendor:4474 и Qt-баттоны), а в редакторе два конца на одной точке сливаются в ОДИН класс пина через кэш ckey=(round(off,4)) (edit_avoid.py:421-426) — «эксклюзивность» надо чинить и там, одним setExclusive не обойтись.
- ❔ `9.3` У 55 % концов грани нет вообще; правило «вдоль грани» даёт 9–11 отказов из 124 (~8 %) — Проценты и отказы — замер стенда по корпусу (не под git, невоспроизводим); MEASUREMENTS.md:327-329 прямо оставляет «замеры 9.3 (55 %)» непроверенными за отсутствием стенда. Кодом можно лишь подтвердить существование концов без грани: ConnDirAll для точек не на грани рамки (edit_avoid.py:147-161, avoid_router._conn_dirs) — но долю не посчитать.
- 🪤 `9.3` Допуски у сторожей разные и не сводимые по имени (0.5 у PIN_TOL, 0.75 у PORT_MATCH_TOL, точное равенство у pinned_port; рядом STRAIGHT_TOL = 1.5 и 3.0 под одним именем в разных файлах) — ослабив допуск только в двух местах М1, исполнитель получит конфликт с остальными четырьмя файлами сторожей.
- ❔ `9.4` Ступенька не исчезает: 10.93 → 18.93 px, просто вышла за порог метрики 12 px; длина маршрутов +1.9 % — Замер стенда _scratch/layout_align (вне области проверки, свой git, корпус не воспроизводим); метрики с порогом 12 px в репо нет (в коде только STEP_MAX=24.0 в edit_smooth.py:66 — порог сглаживания, не метрики). Нужен стенд с корпусом 8 графов.
- 🪤 `9.4` Приёмка эффекта М6 опирается на метрику с порогом 12 px, живущую вне этого репо (стенд layout_align) — в репозитории её не воспроизвести, и «ступенька исчезла» по такой метрике может означать лишь «стала 18.93 px».
- ❔ `9.5` М5: 32 % отказов — hug вдоль своего узла — Статистика отказов требует прогона корпуса на стенде; в коде виден только механизм (приёмка сессии судит hug тем же _fb_seg_bad — advanced_graph_editor.py:2010-2012, edit_avoid.py:35), но долю 32 % чтением не получить.
- 🪤 `9.5` Обе цифры-обоснования приоритетов (1 из 151 для М4, 32 % для М5) непроверяемы без стенда — если исполнитель захочет переиграть приоритеты, ему не на чем пересчитать; в коде же скоринг наложений у лестницы уже есть, и «добавить учёт наложений» (М5) в лоб продублирует :328/:391.
- ❔ `9.6` existsOrthogonalTouchingPaths не детектирует даже эталонное наложение; вердикт глобальный и залипает в True — Глобальность вердикта подтверждается сигнатурой биндинга: метод Router без аргументов, один bool на весь роутер, пер-рёберного варианта нет (vendor/adaptagrams/win/adaptagrams.py:4125-4126). Но «не детектирует эталон» и «залипает в True» — поведение C++ внутри _adaptagrams.pyd: исходников libavoid в репо нет (vendor/adaptagrams/win — только .pyd и SWIG-обёртка), проверить можно лишь живым пробником на стенде. В боевом коде метод сегодня не вызывается нигде (греп по репо — только vendor), так что рекомендация «не использовать как приёмку» уже соблюдена де-факто.
- 🪤 `9.6` Пункт запрещает то, чего в коде и так нет — опасность лишь в том, что ключевое обоснование (пропуск эталонного наложения, залипание) держится на невоспроизводимом из репо пробнике: перепроверить перед спором с пунктом нечем.
- 🪤 `9.7` Счёт «четыре причины» — про блоки проверок, а не про метки: в логах и stats исполнитель увидит девять разных reasons, плюс существует не упомянутый планом адресный откат отдельного ребра (:466-468) — легко спутать «весь прогон откатился» с «откатилось одно ребро».


## Этап 10 — 32✔ / 0⏳ / 1✖ / 2❔ (пунктов: 15)

Этап 10 — самый точный из проверенных: из 31 клейма 27 CONFIRMED (почти все адреса до строки, счётные факты сходятся: 21 хендлер, 22 команды, 44 Dict-метода, 13 общих методов OBE/AGE, 1332 строки окон, все 5 таймеров), 3 UNVERIFIABLE (замеры 10.2 по данным корпуса и поведение WebEngine в 10.15 — рантайм) и 1 REFUTED. Главная ловушка — 10.4: «второй параллельный undo-стек мимо UndoManager» в OcrLayerMixin не существует (_ocr_commit кладёт SnapshotCommand в тот же undo_mgr через push_executed, файл не менялся с 31.07), а настоящий параллельный стек живёт в OcrBindingEditor (ocr_binding_editor.py:305/2421/2440) — исполнитель 10.4 пойдёт чинить не туда, при том что это зона пункта 10.9. Мелкие завышения чисел: в AGE ~31 узловая запись вместо 40 (но ~90 прямых dict-записей всего), у OcrBindingTab 55 методов в классе вместо «~75» (69 — по всему файлу), и в 10.1 реализация ресайза, которую велено «оставить в base_graph_editor», на деле лежит в simple_graph_editor.py.

**Не-подтверждённое и ловушки:**
- 🪤 `10.1` «Оставить реализацию в base_graph_editor» — неточно: реализация ресайза лежит в simple_graph_editor.py (274-330), base_graph_editor.py:1621-1669 — лишь диспетчер событий; и удалять надо весь ResizeNodeHandler (139-197), а не только 149-174
- ❔ `10.2` числа плана из другой системы координат: 2056 узлов и 27.2 % посчитаны на холсте «Ручной правки» с порогом 8 — Сами числа 2056/27.2% — замер на данных корпуса, из кода не пересчитать (нужны graph-артефакты стенда). Кодовая подложка подтверждается: base_graph_editor.py:111 — canvas-режим («Ручная правка», холст 1920×1080) даёт CLICK_THRESHOLD=8.0; :73 — растровый дефолт CLICK_THRESHOLD=20
- ❔ `10.2` в «Проверке схемы» — растровые graph_validated и порог 20: «пространство ручек» 23.3 % вместо 62.3 %, узлов со 100 % захвата — 0 вместо 572 — Проценты 23.3/62.3 и счёт 0/572 требуют прогона по данным диаграмм (storage/ вне области). Кодовая часть верна: SimpleGraphTab работает от graph_validated в растровых координатах (base_graph_tab.py:703 download_artifact 'graph_validated') с CLICK_THRESHOLD=20 (base_graph_editor.py:73)
- 🪤 `10.2` Проценты и счётчики пункта — невоспроизводимые замеры по данным; исполнителю верифицируемы только константы 8/20 и геометрия ручек, пересчёт «под Проверку схемы» придётся делать заново на живых данных
- 🪤 `10.3` tests/test_stage7_graph_flow.py:261-382 тестирует GraphValidationWindow — снос пакета обязан снести/переписать и эти тесты, иначе коллекция pytest сломается
- ✖ `10.4` второй параллельный undo-стек: _ocr_push_snapshot:524/_ocr_commit:531 мимо UndoManager — ocr_layer_mixin.py:533 — _ocr_commit делает self.undo_mgr.push_executed(cmd): SnapshotCommand кладётся в ТОТ ЖЕ UndoManager (undo_manager.py:121-125, revision растёт) — это документированный паттерн SnapshotCommand (undo_manager.py:54-59), а не обходной стек. Файл не менялся с 2026-07-31 (git log), т.е. не STALE — диагноз плана был неверен. Настоящий параллельный undo-стек существует, но в другом классе: OcrBindingEditor._undo_stack (ocr_binding_editor.py:305, _push_undo:2421, _undo:2440) — тот вообще не под BaseGraphEditor
- 🪤 `10.4` Исполнитель пойдёт «чинить» второй undo-стек в OcrLayerMixin, которого там нет — параллельный стек живёт в ocr_binding_editor.py:305/2421/2440; в миксине чинить надо только протечку контракта (_start_rubber_band/_cursor_scene_pos)
- 🪤 `10.6` polygon_handlers.py:134 — ложный пример: там чтение, а мутация вершин идёт правильно через Command; и счёт «40 в AGE» набирается только вместе с edge_data-записями — фронт работ шире узловых записей примерно втрое (~90 прямых записей)
- 🪤 `10.10` «~75 методов у одного класса» завышено: в god-классе 55 методов, ~69 def — это весь файл вместе с воркерами и тулбаром


## Этап 11 — 11✔ / 0⏳ / 1✖ / 4❔ (пунктов: 7)

Из 15 утверждений: 10 подтверждены кодом (все адреса avoid_router/edit_smooth/edit_bench живы и точны до строки — карты снимались 2026-08-15, код с тех пор не менялся; дословность dup-4 и двойного снимка сходится, 356 символов биндинга и отсутствие vpsc-решателя пересчитаны исполнением), 4 непроверяемы чтением кода и 1 опровергнуто. Непроверяемы — ровно все ключевые проценты этапа (57/58, 70/57 %, 60→69, ×6/10.65 с): они сняты аудитом на невоспроизводимом корпусе, а стенд 11.1 (smooth_bench), которым их велено переснимать, не существует — на этих числах при этом стоят приоритеты 11.2 и 11.3. Опровергнуто «используем 6 символов» — по коду 15 (VERIFY И5-2 это уже фиксировал, но карта и пункт тянут старое число). Главная ловушка этапа: мотивировка почти каждой строки опирается на неперепроверяемые до 11.1 замеры — исполнитель обязан начать со стенда и переснять цифры, а не строить на них решения; вторая ловушка — дрейф нотации (С1/С3 против Л1/БД-имен) прямо в заголовках пунктов.

**Не-подтверждённое и ловушки:**
- ❔ `11.2` виновник — угловая амнистия _gate._side_set при SIDE_EPS=1.5; адресный откат спасает 57 маршрутов из 58 — Механизм в коде подтверждён: _gate.py:28 SIDE_EPS=1.5, :79-89 _side_set (докстринг: «На углу конец попадает в обе смежные стороны»), avoid_router.py:392-398 _end_sides строит множества через _gate._side_set, :464-465 side_ok — пересечение множеств, :466-468 — откат. Но число «57 из 58» — прогон роутинга на корпусе storage/diagrams (вне git, помечен невоспроизводимым); стенда нет, источник — VERIFY_part_front4.md:79 (К-10.d); карта 11.2 сама требует замер «до» переснять («ожидание — 57 из 58»)
- 🪤 `11.2` «57/58» — число аудита, на текущем коде не перепроверенное (стенда нет); карта велит замерить «до/после» самому и править только поведение угловой ветки _side_set, не значение SIDE_EPS
- ❔ `11.3` reseat-only = 70 % при нуле колен; route-only = 57 %, хуже headless — Это Qt-прогоны редактора на 20 холстах корпуса: VERIFY_part_front4.md:295-296, :1176 (reseat-only 49/70=70 %, колен 0; route-only 40/70=57 % против headless 42/70=60 %). Чтением кода не пересчитать — нужен offscreen-стенд + корпус; сама карта 11.3 пишет: «Замеры плана (по коду не сверялись — переснять на 11.1)», а стенда 11.1 (smooth_bench) не существует
- ❔ `11.3` прирост 60→69 % даёт reseat_fn, а не ступень «колено» — Причинная атрибуция требует тех же прогонов (VERIFY_part_front4.md:294-299: headless 60 %, боевой 69 %, разведение по клеткам 2×2). Статически видно лишь, что «колено» закрыто условием `if route_fn is not None:` (edit_smooth.py:639) — сам код атрибуцию прироста не решает
- 🪤 `11.3` все цифры пункта сняты аудитом до перестройки редактора и без стенда 11.1 непроверяемы; строка легко читается как «выключить route_fn» — она не об этом (route_fn держит косые: diag 51→12 только с ним)
- 🪤 `11.4` снимок нельзя просто удалить: внутренний restore() работает в середине лестницы — снимок придётся передавать из _try_remedies в _attempt и беречься алиасинга shallow-restore, иначе повторный откат восстановит порченое
- 🪤 `11.6` заголовок пункта «К-3 → К-2/К-0 → С1» расходится с картой этапа («К-3 → К-2/К-0 → Л1»); плюс нотация Х7: С1/С3 фронта 4 — не БД1/БД3 сущностей, в VERIFY_part_front4.md осталась старая — легко перепутать адресат порядка
- ❔ `11.7` «в 45–100 раз дороже libavoid» — на деле медиана ×6, худший ×36–40; худший холст не 5.91 с, а 10.65 с (8d517a35) — третий порог Nielsen — Тайминги корпуса storage/diagrams (вне git, помечен невоспроизводимым); MEASUREMENTS.md §9 прямо относит «×6 медиана» к числам, наследующим базу VERIFY_part_front*.md и не перемеренным (источник: VERIFY_part_front4.md:106-107, И5-6/И5-7). Чтением кода не пересчитать — нужен стенд с таймингом и корпус
- ✖ `11.7` весь Adaptagrams вкомпилирован: 356 символов, используем 6 — «356» подтверждено точно: len([s for s in dir(adaptagrams) if not s.startswith('_')]) = 356 (пересчитано импортом vendored-биндинга). Но «используем 6» неверно: distinct-символов `ag.*` — 15 в avoid_router.py и те же 15 в edit_avoid.py (grep -oE + sort -u: Point, Polygon, Router, ShapeRef, ConnRef, ConnEnd, ShapeConnectionPin, ConnDir{All,Up,Down,Left,Right}, OrthogonalRouting, shapeBufferDistance, idealNudgingDistance); собственный аудит проекта это уже поправил — VERIFY_part_front4.md:102 (И5-2): «Не 6 символов, а 15»
- 🪤 `11.7` пункт, чинящий чужие цифры, сам тянет опровергнутую: «используем 6» давно поправлено аудитом на 15 — исполнитель рискует вписать в доки новую неверную цифру вместо старой


## Этап 12 — 16✔ / 0⏳ / 1✖ / 0❔ (пунктов: 6)

Этап фактически точен: из 15 claims 14 подтверждены (часто с точностью до 2 строк) и лишь 1 опровергнут; ничего не протухло — все шесть файлов не менялись с 2026-07-30, т.е. код совпадает с состоянием на дату плана. Главная ловушка этапа — методика замера «размер = расстояние до следующего def»: для top-level функций она верна, но для замыканий приписывает им хвост объемлющей функции, из-за чего все три «вложенные god-функции» tracing.py (реально 96/40/13 строк вместо 324/190/164 — единственный REFUTED), _sign в spread.py (26 вместо 160) и _pipe_dir в nodes.py (38 вместо 172) выглядят монстрами, которыми не являются; реальные цели декомпозиции — объемлющие trace_edges_v3/trace_single_edge_v3/trace_single_edge, prepare_tracing_data и сама spread(). Вторая мелочь: «4 near-identical try_*» — не копии, откат централизован в try_shift, так что план «свести дубли» превращается в «разрезать try_shift и её пакет запретов».

**Не-подтверждённое и ловушки:**
- 🪤 `12.x2` Числа по внутренностям spread() сняты «до следующего def»: _sign — 26 строк, а не 160; замыканий 9 (+3 lambda), а не 14; и «свести 4 одинаковых try_*» не выйдет буквально — откат уже централизован в try_shift, резать надо саму try_shift и её пакет запретов.
- ✖ `12.x3` 3 вложенных god-функции: get_or_create_node:86 (324 строки), try_rollback_to_fork:445 (190), is_excluded_node:803 (164) — Функции существуют на заявленных строках, но их реальные размеры по AST: get_or_create_node:86→181 = 96 строк (не 324), try_rollback_to_fork:445→484 = 40 (не 190), is_excluded_node:803→815 = 13 (не 164). Заявленные числа — расстояние до следующего def, т.е. в них зачтён хвост ОБЪЕМЛЮЩИХ функций. Настоящие god-функции — сами объемлющие: trace_edges_v3:42→407 (366 строк), trace_single_edge_v3:410→631 (222), trace_single_edge:711→964 (254). Резать нужно их.
- 🪤 `12.x3` Размеры «трёх вложенных god-функций» завышены в 3-12 раз (реально 96/40/13 строк) — исполнитель, режущий именно их, потратит силы на мелкие хелперы; реальные цели декомпозиции — объемлющие trace_edges_v3 (366), trace_single_edge_v3 (222) и легаси trace_single_edge (254).
- 🪤 `12.x4` _pipe_dir — 38-строчное замыкание, а не 172-строчный монстр: третьей отдельной цели декомпозиции нет, крупных функций в файле две (плюс split_merged_nodes:455 — 121 строка, которой в плане нет).
- 🪤 `12.x6` Единственная оговорка: apply_direction_rules (97 строк) стоит вплотную к порогу — при работах рядом не дать эталону деградировать.


## Этап 13 — 37✔ / 0⏳ / 0✖ / 1❔ (пунктов: 17)

Этап 13 — самый точный из проверенных: из 34 фактических утверждений 33 CONFIRMED (числа строк, адреса импортов и цитаты доков бьются с кодом почти дословно), 1 UNVERIFIABLE (прогноз ложной регрессии в 13.x15), чистых REFUTED/STALE нет — но четыре пункта несут существенные оговорки. Главная ловушка этапа — 13.x4: адреса включают pipe_segmentation/{model,data}, которые сегодня под прод-импортом воркера (segmentation.py:246-247 → model.architecture, engine.py:26 → data.tiling), «~3000 строк» бьётся только с чисто офлайн-подмножеством (~2600-3000 из 6200 по адресам), а канонический незащищённый autocast (engine.py:224) лежит в боевом inference, не в офлайн-контуре. Второстепенные ловушки: «трёх доков» с ложью о замке нет (CODING_GUIDE.md:72 о замке молчит — там другой дрейф), OCR при текущем конфиге реально стартует (thermohydraulics.yaml:276 enabled: true — ложь ответа API безусловна только для контуров), а числа «:267/:366» в адресах 13.x5 — размеры файлов, не строки кода.

**Проблемные адреса:**
- `13.x15`: UI_GUIDE.md:250 — ok; AUTO_LAYOUT_E2E_TESTPLAN.md:58 — ok; CODING_GUIDE.md:72 — содержимое не то: там строка «Auto-fix: auto_fix() → auto_fix_graph()...», ни слова о замке (слов «замок/заперт/выключен/lock» в CODING_GUIDE нет вообще; файл не менялся с 2026-08-03)

**Не-подтверждённое и ловушки:**
- 🪤 `13.x2` Есть ЖИВОЙ однофамилец: modules/mask_refinement.py::refine_pipe_mask используется worker/tasks/skeleton.py:570, и своя refine_pipe_mask есть в clean_and_ocr.py:371 — отделяя по basename, легко зацепить боевой код
- 🪤 `13.x4` Адреса включают model/ и data/, которые воркер импортирует напрямую — «вывести из-под прод-импорта» буквально по адресам уронит сегментацию; и фикс autocast (2.10) нельзя считать чисто офлайновым — главный незащищённый вызов в боевом inference/engine.py:224
- 🪤 `13.x5` Числа после двоеточия у binder.py — размеры файлов, а не адреса строк: исполнитель, открыв kks_binding/binder.py:267, увидит хвост геометрической функции и решит, что адрес битый
- 🪤 `13.x7` Комментарии в самом worker.txt объясняют, что torch/sam2/ensemble-boxes НАМЕРЕННО ставятся отдельными слоями Dockerfile (resolution-too-deep, пересборка sam2 с повторной закачкой torch) — «внести torch/sam2 в requirements» в лоб сломает сборку образа
- 🪤 `13.x8` «Сервисов пять» — исполнитель, пересчитав фактический docker-compose.yml, увидит 15 (CVAT-стек в том же файле) и может усомниться в остальных замерах пункта
- 🪤 `13.x10` В проекте два segmentation.py — открыв app/api/segmentation.py:357-380, исполнитель попадёт не туда
- 🪤 `13.x12` В гисте «§6 глоссарий» — на деле §6 файла это «Протокол работы Claude»; глоссарий — отдельный docs/GLOSSARY.md (строка 2 реестра §8)
- 🪤 `13.x14` Не переписывать message как «OCR не стартует» — при текущем конфиге стартует (ocr.enabled: true, что заодно опровергает строку CLAUDE.md «OCR по умолчанию выключен»); безусловно ложна только часть про contours
- ❔ `13.x15` По этой строке объявят ложную регрессию на корректном коде — Прогноз поведения тестировщика чтением кода не проверяется; предпосылка подтверждена — E2E-план шаг 1.6б прямо требует проверить выключенность «Авто-выравнивания», и такой прогон честного кода провалит
- 🪤 `13.x15` «Трёх доков» нет — ложь о замке только в двух (UI_GUIDE:250, E2E:58); CODING_GUIDE.md:72 о замке молчит, но протух иначе: описывает за «Авто-выравниванием» старый auto_fix, тогда как кнопка вызывает smooth_canvas
- 🪤 `13.x16` Не писать доку вкладки заново — UI_GUIDE §7.3 уже есть; дописывать надо этап в ARCHITECTURE/STATUS_MACHINE (статус FRAME_CLEANED, бусина, позиция в откате)
