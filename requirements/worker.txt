# =============================================================================
# Worker requirements - Celery + ML
# =============================================================================

-r base.txt

# Celery
celery[redis]>=5.3.0

# PyTorch - устанавливается отдельно в Dockerfile для Linux
# Для Windows: pip install torch==2.6.0+cu124 torchvision==0.21.0+cu124 --index-url https://download.pytorch.org/whl/cu124

# YOLO
ultralytics>=8.3.0
sahi>=0.11.0

# Ensemble merging (WBF/NMS/Soft-NMS) — ставится отдельным слоем
# в Dockerfile.worker ПОСЛЕ этого файла (см. комментарий там);
# добавление сюда раздувает граф зависимостей до resolution-too-deep

# Segmentation
segmentation-models-pytorch>=0.3.3
albumentations>=2.0.0

# Image processing
opencv-contrib-python-headless>=4.9.0
Pillow>=10.2.0
scikit-image>=0.22.0

# Scientific
numpy>=1.26.0,<2.0.0
scipy>=1.11.0

# Auto-layout (modules/graph/core/layout) — версия закреплена точно:
# в shapely 2.x менялся API STRtree, а строгий арбитр наложений в гейте
# раскладки зависит от его поведения. До этого shapely приезжал только
# транзитивно (worker.lock.txt), то есть версия была неуправляемой.
shapely==2.1.2

# Visualization (для отладки)
matplotlib>=3.8.0

fastapi>=0.109.0

# SAM2 contour extraction — ставится отдельным слоем в Dockerfile.worker
# (RUN pip install --no-build-isolation sam2), здесь НЕ указывать:
# иначе pip пересобирает его с изолированной сборкой и качает torch заново
# sam2
